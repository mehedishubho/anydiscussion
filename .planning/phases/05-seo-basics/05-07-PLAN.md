---
phase: 05-seo-basics
plan: 07
type: execute
wave: 3
depends_on: ["05-05", "05-06"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/app/globals.css
  - src/components/editor/extensions.ts
  - src/components/editor/TiptapEditor.tsx
  - src/actions/posts-schema.ts
  - src/lib/slug/derive.ts
  - src/lib/slug/__tests__/derive.test.ts
  - src/app/(admin)/dashboard/posts/PostForm.tsx
autonomous: true
gap_closure: true
requirements: [SEO-02, SEO-06]
must_haves:
  truths:
    - "/dashboard/posts/new renders the styled Tiptap surface on FIRST load — typography-styled text, no browser-default black focus ring, and a visible placeholder while the body is empty"
    - "Clicking Publish or Save draft with missing/invalid fields NEVER silently no-ops — an error toast names the first problem and focus jumps to that field"
    - "A missing Category reports the message 'Category is required', not Zod 4's default type-error string"
    - "The Slug field auto-fills from the Title (URL-safe Latin + hyphens) while the user has not typed a slug; a user-entered or existing slug is never overwritten"
    - "A fully filled form publishes with visible success toasts ('Post saved' then 'Published') — the UAT re-run R1 flow end-to-end"
  artifacts:
    - 'src/app/globals.css — @plugin "@tailwindcss/typography" (Tailwind v4 CSS-first wiring) + .tiptap.ProseMirror surface rules + placeholder ::before styling'
    - "src/components/editor/extensions.ts — Placeholder extension added to the shared client+server array (decoration-only, same server-safety class as CharacterCount)"
    - "src/components/editor/TiptapEditor.tsx — immediatelyRender:true + wrapper-class fix (dead focus:outline-none removed from the wrapper only)"
    - "src/lib/slug/derive.ts — pure deriveSlugFromTitle helper (strip-to-regex, NO transliteration per D-20) + unit tests in src/lib/slug/__tests__/derive.test.ts"
    - "src/app/(admin)/dashboard/posts/PostForm.tsx — shared onInvalid (toast + focus/scroll) wired on all three handleSubmit call sites + slug auto-derive wiring"
    - "src/actions/posts-schema.ts — categoryId Zod 4 constructor-level error message"
  key_links:
    - "TiptapEditor 'prose prose-sm' classes -> @plugin typography in globals.css -> prose CSS actually present in the served dashboard chunk (the exact link that was dead)"
    - "Placeholder extension (extensions.ts) -> is-empty decoration -> globals.css ::before rule -> placeholder visible on first load without focus"
    - "RHF handleSubmit(onValid, onInvalid) at all three submit paths -> toast.error + element focus/scrollIntoView -> first invalid field (title, slug, categoryId all have HTML ids)"
    - "title watch -> deriveSlugFromTitle -> setValue('slug', { shouldValidate: true }) -> SLUG_REGEX passes without the user typing a slug"
---

<objective>
Close the UAT re-run gap R1 (root cause confirmed by 2 parallel debug agents; hydration-death refuted — the live word-count in the screenshot proves hydration):

Cause A — editor surface unstyled: the rebuilt classic-editor shell (05-05) renders its toolbar and word-count footer, but the writing surface itself has ZERO styles. @tailwindcss/typography was never installed and globals.css has no @plugin, so the `prose prose-sm dark:prose-invert` classes on TiptapEditor.tsx L128 generate no CSS at all (verified against the served chunk). Tiptap v3 ships no CSS of its own, no Placeholder extension exists (empty surface shows nothing), the wrapper's focus:outline-none never applies because focus lands on the child contenteditable (the hard black border is the browser default focus ring), and useEditor lacks immediatelyRender:true (transient null-editor first frame under Next.js). Fix: typography plugin (Tailwind v4 @plugin wiring — this project has NO tailwind.config, globals.css @theme/@utility IS the config), authored .tiptap.ProseMirror + placeholder rules, Placeholder via @tiptap/extensions@3.27.1, and immediatelyRender:true (safe — client-only behind next/dynamic({ssr:false})).

Cause B — Publish no-op on validation failure: posts-schema.ts L30 makes categoryId required, but /dashboard/posts/new defaults it to undefined, so RHF/Zod validation fails before any mutation fires — and all three handleSubmit calls (PostForm.tsx L208 form onSubmit, L358 Publish onClick, L368 Submit-for-review onClick) pass NO onInvalid callback, so the failure is a silent no-op (only small inline captions render mid-form). Zod 4's default message for the missing number is the cryptic type-error string, never the intended "Category is required" (the .positive() message only fires when a number IS provided). Slug is the second silent blocker — a bare manual input with no derivation, failing the regex quietly. Fix: a shared onInvalid (toast + focus + scroll) on all three submit paths, the Zod 4 constructor-level error message, and derive-on-empty slug auto-fill from title (strip-to-regex; NO transliteration — D-20 boundary).

Purpose: make the R1 truth real — the editor looks and behaves like the WordPress-classic surface it was rebuilt to be, and Publish either succeeds with a toast or fails LOUDLY with focus on the offending field.
Output: styled editor surface (PostForm + PageForm both benefit — they share EditorProvider), loud validation, auto-derived slugs, full-suite + build regression gate.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-basics/05-UAT.md
@.planning/phases/05-seo-basics/05-05-SUMMARY.md
@.planning/phases/05-seo-basics/05-06-SUMMARY.md

@src/components/editor/TiptapEditor.tsx
@src/components/editor/extensions.ts
@src/components/editor/EditorProvider.tsx
@src/app/(admin)/dashboard/posts/PostForm.tsx
@src/app/(admin)/dashboard/posts/components/CategoryPicker.tsx
@src/actions/posts-schema.ts
@src/lib/slug/index.ts
@src/app/globals.css
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Styled editor surface — typography plugin, ProseMirror rules, Placeholder, immediatelyRender</name>
  <files>package.json, pnpm-lock.yaml, src/app/globals.css, src/components/editor/extensions.ts, src/components/editor/TiptapEditor.tsx</files>
  <behavior>
    - Test (existing, must stay green): pnpm vitest run src/components/editor — the round-trip test proves the shared client+server extensions array still serializes every node/mark identically after Placeholder joins it; the surface smoke test proves Visual/Text tabs + word count still work.
    - Behavior: with an empty doc, the first paragraph shows placeholder text (decoration class is-empty on the node) without the editor being focused.
    - Behavior: .tiptap.ProseMirror carries outline:none so focusing the surface produces no browser-default black ring.
    - Behavior: useEditor creates the editor synchronously (immediatelyRender:true) — no null-editor first frame; the surface smoke test's immediate .tiptap.ProseMirror query still passes.
  </behavior>
  <action>
    Install (pnpm only, exact pins): `pnpm add --save-exact @tailwindcss/typography@0.5.20 @tiptap/extensions@3.27.1`.
    Package-choice note (planner decision, verified against registry.npmjs.org 2026-08-25): the UAT named @tiptap/extension-placeholder@3, but that package at 3.27.1 lives in tiptap's packages-deprecated folder and peers on @tiptap/extensions@3.27.1 — it is a re-export shim. @tiptap/extensions@3.27.1 (monorepo packages/extension, NOT deprecated) exports Placeholder at the root and peers exactly on @tiptap/core@3.27.1 + @tiptap/pm@3.27.1 — both already installed at exactly those versions. Same version line, one fewer package, no deprecated shim. If the root named export is missing from the typings at implement time, use the documented subpath import @tiptap/extensions/placeholder instead.

    src/app/globals.css:
    - Add `@plugin "@tailwindcss/typography";` immediately after the `@import 'tailwindcss';` line — this is the Tailwind v4 CSS-first plugin wiring (the project has NO tailwind.config.js; @theme/@utility directives in globals.css are the config, and typography 0.5.20 declares Tailwind v4 support via its peer range). This single line is what makes the existing prose classes on TiptapEditor.tsx L128 generate CSS.
    - Append a new "Tiptap editor surface" section near the third-party CSS block (same style as the .flatpickr-* rules):
      1. A rule targeting the ProseMirror contenteditable with `outline: none; min-height: inherit;` — outline:none removes the browser default focus ring (the screenshot's hard black border: focus lands on the child contenteditable, which is why the wrapper's focus:outline-none class never applied); min-height:inherit lets the surface fill the wrapper's min-h-[350px] so the white writing area is tall.
      2. A placeholder rule targeting the decoration Tiptap's Placeholder extension paints on empty nodes (first empty paragraph only): `content: attr(data-placeholder); color: var(--color-gray-400); float: left; height: 0; pointer-events: none;` — the documented Tiptap placeholder CSS pattern, using the project theme token for the gray so dark mode needs no extra rule (the token adapts). Selector shape: the surface element, then the first-child empty paragraph carrying the is-empty class. If the rendered decoration class differs (check devtools), align the selector to what the extension actually paints — the rule is the requirement, not the exact selector string.
    - Add a short comment documenting the scoping judgment: these rules are global but the public (site) renders sanitized static HTML with no contenteditable, so the ProseMirror selector matches zero nodes there; prose utilities only emit CSS where the prose classes are used (TiptapEditor — a dashboard-only dynamic import). Do NOT wrap the rules in a dashboard-only parent class — zero matched nodes on the public side needs no scoping.

    src/components/editor/extensions.ts:
    - Import Placeholder from @tiptap/extensions and append `Placeholder.configure({ placeholder: "Write something…", showOnlyCurrent: false })` to the editorExtensions array with a comment citing this gap closure (UAT re-run R1) and the deprecated-shim package choice. showOnlyCurrent:false so the placeholder is visible on first load WITHOUT the editor being focused; the CSS :first-child restriction keeps it to one placeholder.
    - Server-safety comment (same argument as CharacterCount, already established in this file): Placeholder is a decoration-only extension with NO schema output — server-side generateHTML walks the schema only, so the client+server parity this file exists to protect is unaffected. The round-trip test is the gate that proves it.

    src/components/editor/TiptapEditor.tsx:
    - Add `immediatelyRender: true` to the useEditor options, with a comment: the component is client-only behind EditorProvider's next/dynamic({ssr:false}) (verified), so there is no SSR/hydration surface — this eliminates the transient null-editor first frame under Next.js.
    - On the Visual-surface wrapper div (the one carrying the prose classes): REMOVE the focus:outline-none utility — it is dead (focus lands on the child .ProseMirror contenteditable; the new globals.css rule owns outline removal). KEEP prose prose-sm dark:prose-invert max-w-none min-h-[350px] and the padding utilities — they become live once the plugin is wired. Do NOT touch the Text-tab textarea's own focus:outline-none (that one sits on the focused element and is correct).
    - Update the file header comment block where it describes surface styling, citing 05-07 / UAT R1.

    Atomic commit: `fix(05): style editor surface (typography plugin, placeholder, immediatelyRender)`
  </action>
  <verify>
    <automated>grep -c "tailwindcss/typography" src/app/globals.css && grep -c "ProseMirror" src/app/globals.css && grep -c "Placeholder" src/components/editor/extensions.ts && grep -c "immediatelyRender" src/components/editor/TiptapEditor.tsx && pnpm vitest run src/components/editor && pnpm exec tsc --noEmit</automated>
    Live check (dev server, for the UAT R1 re-run): open /dashboard/posts/new — the body surface is typography-styled (padding, comfortable text), the placeholder text is visible before clicking, and clicking into the surface produces NO black focus border. /dashboard/pages (PageForm) shares EditorProvider and benefits identically — spot-check it too.
  </verify>
  <done>Prose CSS actually generates (plugin wired); surface has outline:none + min-height fill + visible placeholder; editor renders synchronously; round-trip AND surface smoke tests green; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loud validation — onInvalid toast/focus, Zod 4 category message, slug auto-derive</name>
  <files>src/actions/posts-schema.ts, src/lib/slug/derive.ts, src/lib/slug/__tests__/derive.test.ts, src/app/(admin)/dashboard/posts/PostForm.tsx</files>
  <behavior>
    - Test 1 (derive.test.ts): deriveSlugFromTitle("Hello World 2026!") === "hello-world-2026".
    - Test 2: uppercase folds to lowercase; runs of non-[a-z0-9] chars collapse to a single hyphen; leading/trailing hyphens are trimmed.
    - Test 3: a Bangla-only title derives "" (strip, never transliterate — D-20) and every NON-empty derived value satisfies SLUG_REGEX imported from @/actions/posts-schema.
    - Test 4 (schema): postSchema.safeParse({ title: "t", slug: "t" }) — no categoryId — reports the message "Category is required" on categoryId (the Zod 4 constructor error, not the default type-error string).
  </behavior>
  <action>
    src/actions/posts-schema.ts: change the categoryId line to use the Zod 4 constructor-level error parameter — z.number({ error: "Category is required" }) — keeping the existing .int().positive() chain and its message. WHY: .positive()'s message only fires when a number is provided but fails the check; for undefined (the /new default) Zod 4 emits the default invalid-type string from the constructor path, which is what the user never saw because onInvalid didn't exist. Update the adjacent comment to explain both halves (constructor error covers the missing path).

    New file src/lib/slug/derive.ts — a PURE module (no imports beyond none at all): export function deriveSlugFromTitle(title: string): string. Lowercase the input, replace every character outside [a-z0-9] with a hyphen, collapse consecutive hyphens, trim leading/trailing hyphens, return the result ("" when nothing survives). Header comment MUST state the D-20 boundary (mirroring src/lib/slug/index.ts): this is STRIP-to-regex, never transliteration — Bangla-to-Latin transliteration is explicitly out of scope for v1; a fully-Bangla title derives "" and the now-loud slug validation catches it (toast + focus, Task wiring below). Do NOT add this to src/lib/slug/index.ts — that file imports db (server-only); PostForm is a client component.

    New file src/lib/slug/__tests__/derive.test.ts — the <behavior> cases above; import SLUG_REGEX and postSchema from @/actions/posts-schema (a pure schema module, no db) for the regex and message assertions.

    src/app/(admin)/dashboard/posts/PostForm.tsx:
    - Add a shared onInvalid handler (typed for FieldErrors of the form): find the first field key carrying an error; fire toast.error with that field's message (sonner is already imported and the Toaster mounted from 05-06); then focus the field via document.getElementById(String(key)) with ?.focus() and scrollIntoView({ block: "center" }) — the title input (id "title"), slug input (id "slug"), and CategoryPicker's select (id "categoryId") all have HTML ids; degrade gracefully (toast only) when an errored field has no id (SeoPanel fields).
    - Wire it as the SECOND argument at ALL THREE handleSubmit sites — the form onSubmit (currently handleSubmit(onValid)), the Publish button onClick, and the Submit-for-review button onClick. This is the core fix: validation failure was a total silent no-op at exactly these three call sites.
    - Slug auto-derive (planner-scoped v1 — derive-on-empty, never overwrite): keep a slugTouched ref, set to true from a custom onBlur merged into register("slug", { onBlur }) (RHF merges custom handlers with its internal ones). Subscribe to title and slug with the same watch/useWatch pattern already used for featureImage (keep the existing eslint-disable convention for watch()). When the title changes and the current slug is "" and slugTouched is false, call setValue("slug", deriveSlugFromTitle(title), { shouldValidate: true }). NEVER write when the slug is non-empty — that protects user-entered slugs AND existing slugs on /edit (slug is content identity per D-12/D-20; a title edit on a published post must not silently change the URL).
    - Update the header comment block citing 05-07 / UAT R1 (validation no-op + slug derive).
    - Do NOT touch the mutation wiring, role gating, or button rendering from 05-06 — those are correct; only the validation-failure path and slug UX change. No Server Action signature changes; server-side validation (postSchema parse + validateSlug + assertUniqueSlug in savePost) is untouched and remains the authority.

    Atomic commit: `fix(05): surface publish validation failures (onInvalid toast + slug auto-derive)`
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/slug && grep -c "onInvalid" "src/app/(admin)/dashboard/posts/PostForm.tsx" && grep -c "deriveSlugFromTitle" "src/app/(admin)/dashboard/posts/PostForm.tsx" && grep -c "Category is required" src/actions/posts-schema.ts && pnpm exec tsc --noEmit</automated>
    Live check (dev server, for the UAT R1 re-run): on /dashboard/posts/new, type a title — the slug fills itself as you type; leave Category empty and click Publish — an error toast shows "Category is required" and focus jumps to the Category select (page scrolls to it); fill Category + body — Publish fires the "Post saved" then "Published" success toasts.
  </verify>
  <done>All three submit paths are loud on invalid input (toast + focus + scroll); missing category reads "Category is required"; empty slug auto-derives from title without ever overwriting entered slugs; derive tests green; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression gate — suite, typecheck, build, generated-CSS proof</name>
  <files>.next/ (build artifacts only — no source changes)</files>
  <action>
    Run the full gate in order (all pnpm):

    1. `pnpm test` — the full vitest run: the 590-test baseline plus the new derive/schema tests, ZERO failures. Critical non-regressions: the editor round-trip test (src/components/editor/__tests__/round-trip.test.ts — the shared client+server extensions array parity, now including Placeholder) and the surface smoke test must both be green; nothing in the posts/permissions suites may move (no action signatures changed).
    2. `pnpm exec tsc --noEmit` — project-wide typecheck (same gate as prior gap plans).
    3. `rm -rf .next && pnpm build` — the rm -rf is MANDATORY before building (known gotcha: warm builds reuse stale 'use cache' output when globals.css changes; also kill any leftover next-start node PID on Windows first). Build must exit 0.
    4. Prove the CSS actually generated — the exact root cause being closed was "classes generate zero CSS": grep the build output for (a) the authored surface selector fragment "ProseMirror" and (b) the distinctive typography-plugin selector fragment "not-prose" under .next/static/ — both must hit. If (b) misses, the @plugin line did not take effect — check it sits AFTER the tailwindcss import and re-run the build.

    Document the R1 live walkthrough for the end-of-phase UAT re-run (curl-able parts runnable now): with the dev server up, publish a post via the fixed flow, then curl /sitemap.xml (expect the /blog/{slug} entry at 0.8/weekly) and /rss.xml (expect one item per published post) — the downstream R1 checks.

    Atomic commit: `test(05): regression gate for editor surface + publish validation fixes`
  </action>
  <verify>
    <automated>pnpm test && pnpm exec tsc --noEmit && rm -rf .next && pnpm build && grep -rql "ProseMirror" .next/static/ && grep -rql "not-prose" .next/static/</automated>
  </verify>
  <done>Full suite green (590 baseline + new tests); typecheck clean; cold build exits 0; built CSS contains both the ProseMirror surface rules and the typography plugin output — the dead-prose-classes root cause is provably closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard client -> Server Actions | Untrusted form input; actions re-validate — this plan changes NO action signatures and NO server-side validation |
| npm registry -> node_modules | Two new packages enter the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Tampering (supply chain) | pnpm add @tailwindcss/typography + @tiptap/extensions | high | mitigate | Package Legitimacy Audit (planner, registry.npmjs.org, 2026-08-25): @tailwindcss/typography@0.5.20 -> github.com/tailwindlabs/tailwindcss-typography, maintainers adamwathan/reinink/malfaitrobin (Tailwind Labs principals), peers tailwindcss >=3 || >=4 (v4-compatible). @tiptap/extensions@3.27.1 -> github.com/ueberdosis/tiptap packages/extension (NOT the deprecated folder), peers exactly @tiptap/core@3.27.1 + @tiptap/pm@3.27.1 (both already installed at those exact pins), published via trusted-publishing OIDC. Chosen OVER @tiptap/extension-placeholder@3.27.1, which lives in packages-deprecated and merely re-exports it. Both installed with --save-exact. |
| T-05-15 | Tampering | client-side slug derivation feeding savePost | low | accept | Derivation is UX-only. The server contract is unchanged: savePost still runs the same postSchema parse + validateSlug + assertUniqueSlug (SLUG_NOT_UNIQUE on collision). A crafted client could already submit any slug; auto-derive changes nothing server-side. |
| T-05-16 | Information disclosure / CSS scope | global ProseMirror + typography rules in globals.css | low | accept | The public (site) renders sanitized static HTML with no contenteditable — the surface selector matches zero nodes there; prose utilities emit CSS only for classes used by the dashboard-only dynamic TiptapEditor. Cost to the public bundle is negligible CSS bytes with zero behavioral surface. |
</threat_model>

<verification>
- pnpm test: 590-test baseline + new derive/schema tests, all green. Editor round-trip + surface smoke tests explicitly green (the shared extensions array now includes Placeholder — decoration-only, schema-untouched).
- pnpm exec tsc --noEmit clean; rm -rf .next && pnpm build exits 0.
- Built CSS proof: both the ProseMirror surface selector and the typography plugin's not-prose fragment present under .next/static/ — the dead-prose-classes root cause closed at the artifact level, not just in source.
- Grep gates: @plugin + ProseMirror rules in globals.css; Placeholder in extensions.ts; immediatelyRender in TiptapEditor.tsx; onInvalid + deriveSlugFromTitle in PostForm.tsx; constructor-level category message in posts-schema.ts.
- UAT R1 re-run walkthrough (documented in tasks): styled surface + placeholder on first load; empty-Category Publish -> error toast + focus jump; complete form -> "Post saved" + "Published" toasts; post visible in /sitemap.xml and /rss.xml.
</verification>

<success_criteria>
- UAT re-run gap R1 cause A closed: the body editor renders the styled Tiptap surface (typography, no black focus ring, visible placeholder) on first load — for BOTH PostForm and PageForm via the shared EditorProvider.
- UAT re-run gap R1 cause B closed: validation failures are loud (toast + focus + scroll) on all three submit paths; missing Category reads "Category is required"; empty slug auto-derives from title without ever overwriting entered/existing slugs.
- No regressions: round-trip parity intact, 590-test baseline intact, no Server Action or permission-surface changes, editor stays dashboard-only (client JS + dynamic import boundaries untouched).
</success_criteria>

<output>
Create `.planning/phases/05-seo-basics/05-07-SUMMARY.md` when done
</output>
