---
phase: 05-seo-basics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/migrations/0004_*.sql
  - src/lib/storage/seed.ts
  - src/instrumentation.ts
  - src/lib/seo/metadata.ts
  - src/lib/seo/jsonld.ts
  - src/lib/seo/validation.ts
  - src/lib/seo/settings.ts
  - src/lib/seo/__tests__/shared-fixtures.ts
  - src/lib/seo/__tests__/metadata.test.ts
  - src/lib/seo/__tests__/jsonld.test.ts
  - src/lib/seo/__tests__/validation.test.ts
  - src/app/(site)/layout.tsx
  - src/app/(site)/page.tsx
  - src/app/(site)/preview/[token]/page.tsx
autonomous: true
requirements: [SEO-01, SEO-03, SEO-04, SEO-05, SEO-06]

must_haves:
  truths:
    - "Given a fake published post + postSeo + settings snapshot, buildPostMetadata returns a Metadata object whose title, description, alternates.canonical, openGraph.type='article', openGraph.publishedTime, and twitter.card fields are all populated correctly (SEO-01, SEO-04, SEO-05)"
    - "Given postSeo.canonicalUrl is set, buildPostMetadata respects it as the canonical; when it is null, the canonical derives from the post slug (SEO-04 per D-04)"
    - "Given no postSeo.ogImage and no posts.featureImage, buildPostMetadata falls back to settings.defaultOgImage; twitter.card is 'summary_large_image' when an image exists, 'summary' otherwise (SEO-05 per D-09)"
    - "blogPostingJsonLd returns an object with @context='https://schema.org', @type='BlogPosting', headline, datePublished (ISO 8601), author.@type='Person', and mainEntityOfPage.@type='WebPage' (SEO-03 per D-03)"
    - "websiteJsonLd returns @type='WebSite' with a potentialAction SearchAction targeting {base}/search?q={search_term_string}; organizationJsonLd returns @type='Organization' with name/url/logo (D-03)"
    - "A 59-grapheme Bangla meta description passes seoMetaSchema.safeParse; a 250-grapheme Latin description fails the grapheme refine (SEO-06 per D-10)"
    - "The (site)/layout.tsx HTML contains two script tags with type='application/ld+json' — one WebSite, one Organization — rendered server-side via dangerouslySetInnerHTML (D-02, D-03, landmine #3)"
    - "The home page (site)/page.tsx renders a title tag sourced from settings.site.title (via the layout's generateMetadata), and the build succeeds under cacheComponents:true (landmine #4)"
    - "getSeoSettings reads the five settings keys (site.title, site.description, seo.default_og_image, site.canonical_base_url, seo.twitter_handle) with NEXT_PUBLIC_SITE_URL env fallback when a row is absent (D-04, D-11)"
    - "pnpm db:generate produces a migration file whose SQL contains a CREATE TABLE for redirects with old_path, new_path, status_code columns (landmine #1, schema-push gate)"
  artifacts:
    - "src/lib/seo/metadata.ts exporting buildPostMetadata, buildPageMetadata, buildArchiveMetadata, buildSiteMetadata"
    - "src/lib/seo/jsonld.ts exporting blogPostingJsonLd, websiteJsonLd, organizationJsonLd"
    - "src/lib/seo/validation.ts exporting seoMetaSchema, graphemeCount, SeoMetaInput"
    - "src/lib/seo/settings.ts exporting getSeoSettings (with 'use cache' directive + cacheTag('seo-settings'))"
    - "src/lib/seo/__tests__/shared-fixtures.ts exporting fake PostLike, PostSeoLike, SeoSettings, and the empirical 59-grapheme Bangla fixture"
    - "src/lib/seo/__tests__/metadata.test.ts, jsonld.test.ts, validation.test.ts (all green under pnpm test -- src/lib/seo)"
    - "src/db/migrations/0004_*.sql generated by pnpm db:generate, containing the redirects table DDL"
    - "src/db/schema.ts exporting `redirects` (pgTable) with oldPath/newPath/statusCode/createdAt/updatedAt"
    - "src/lib/storage/seed.ts extended with seedSeoSettings inserting the five settings keys"
    - "src/app/(site)/layout.tsx exporting async generateMetadata with 'use cache' + rendering site-wide JSON-LD script tags"
    - "src/app/(site)/page.tsx exporting async generateMetadata calling getSeoSettings + buildSiteMetadata"
    - "src/app/(site)/preview/[token]/page.tsx exporting async generateMetadata preserving robots:{index:false}"
  key_links:
    - "getSeoSettings (lib/seo/settings.ts) → consumers: (site)/layout.tsx generateMetadata, (site)/page.tsx generateMetadata, and (transitively via Plan 02) sitemap.ts/robots.ts/rss.xml/route.ts"
    - "cacheTag('seo-settings') in getSeoSettings ↔ revalidateTag('seo-settings', 'max') in Plan 03's saveSeoSettings — without the tag, settings changes are invisible until container restart (landmine #4, Pitfall 6)"
    - "redirects table export in schema.ts → imported by Plan 03's app/not-found.tsx redirects check (landmine #1, #2)"
    - "seoMetaSchema in validation.ts → reused by Plan 03's post-editor SeoPanel + savePost action (D-08, D-10)"
    - "buildPostMetadata / blogPostingJsonLd in lib/seo/* → consumed by Phase 6's /[slug] route (the P5↔P6 seam, D-01)"
---

<objective>
Plan 01 — SEO Engine Foundation + Schema Push + Site-wide Metadata (Wave 1, unblocks Plans 02 and 03)

This plan delivers the first vertical slice of Phase 5: the `redirects` Drizzle table (the [BLOCKING] schema-push gate), the pure SEO builder library (`src/lib/seo/`), the cached settings reader, the seeded site-wide SEO defaults, and real `generateMetadata` wired into the existing `(site)` routes (layout + home + preview). After this plan, a visitor to `/` or `/preview/[token]` sees correct `<title>`, meta description, OG/Twitter tags, and site-wide WebSite + Organization JSON-LD in the page source, and every pure builder is unit-tested with no DB required.

Purpose: This is the foundation that Plan 02 (standalone routes) and Plan 03 (dashboard SEO surface + redirects check) depend on. `getSeoSettings()` is the single cached settings snapshot consumed by all four metadata-emitting routes; the pure builders are what Phase 6's per-route `generateMetadata` will call as one-liners; the `redirects` table must exist before Plan 03's `not-found.tsx` can query it. Per D-01, no `/[slug]` post route is pulled forward — Phase 6 owns SITE-07.

Output: `src/lib/seo/{metadata,jsonld,validation,settings}.ts`, the `redirects` table + generated migration, seeded SEO settings, modified `(site)` layout/home/preview routes, and the Wave 0 pure-builder test suite.

Decisions implemented: D-01 (existing-routes-only seam), D-02 (metadata builders + `(site)/layout.tsx` placement), D-03 (BlogPosting + WebSite + Organization JSON-LD), D-04 (canonical + metadataBase from settings), D-05 (sitemap builder structure — consumed by Plan 02), D-09 (OG fallback chain), D-10 (Bangla grapheme Zod), D-11 (settings seeds).
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-basics/05-CONTEXT.md
@.planning/phases/05-seo-basics/05-RESEARCH.md
@.planning/phases/05-seo-basics/05-PATTERNS.md

# Key source files to read before editing (executor reads these during execution, not now)
@src/db/schema.ts
@src/lib/storage/seed.ts
@src/app/(site)/layout.tsx
@src/app/(site)/page.tsx
@src/app/(site)/preview/[token]/page.tsx
@src/lib/excerpt/index.ts
@src/lib/permissions/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Schema push — redirects table + SEO settings seeds</name>
  <files>src/db/schema.ts, src/db/migrations/0004_*.sql (generated), src/lib/storage/seed.ts, src/instrumentation.ts</files>

  <read_first>
    - `src/db/schema.ts` lines 128-149 (the `pages` + `settings` table definitions — the sibling shape to mirror for `redirects`)
    - `src/lib/storage/seed.ts` lines 40-59 (the existing `seedStorageSettings` pattern — extend with a sibling `seedSeoSettings`)
    - `src/instrumentation.ts` (the boot-time seed caller — add `seedSeoSettings` after `seedStorageSettings`)
    - `.planning/phases/05-seo-basics/05-PATTERNS.md` section "`src/db/schema.ts` (MODIFY — add `redirects` table)" for the verified column names + the landmine that the table does NOT exist today
    - `.planning/phases/05-seo-basics/05-RESEARCH.md` "Package Legitimacy Audit" (zero installs this phase — confirms no audit needed)
    - `CLAUDE.md` "Database schema" reference row for `redirects` (old_path, new_path, status_code 301/302)
  </read_first>

  <action>
    [BLOCKING SCHEMA-PUSH GATE — per the orchestrator's schema_push_requirement. This task MUST complete and its migration MUST be generated before any other task in Phase 5 can verify.]

    Add a new `redirects` Drizzle table export to `src/db/schema.ts`, placed immediately after the existing `settings` table (line 149). Mirror the `pages` table conventions already in that file (serial PK, varchar with explicit length, timestamp defaults). The table name is `redirects` with these columns per CLAUDE.md "Database schema" + PATTERNS.md landmine #4: `id` serial primary key; `oldPath` as `varchar("old_path", { length: 255 }).notNull().unique()`; `newPath` as `varchar("new_path", { length: 255 }).notNull()`; `statusCode` as `integer("status_code").default(301).notNull()` (values 301 or 302); `createdAt` and `updatedAt` as `timestamp(...).defaultNow().notNull()` matching the existing convention. Export it as `export const redirects = pgTable("redirects", {...})`. Add a brief header comment citing CLAUDE.md "redirects — old_path, new_path, status_code (301/302)" and noting the table ships EMPTY in v1 per D-12 (the redirects-manager UI that fills it is v2 SETT-03).

    Then run `pnpm db:generate` (the project's `drizzle-kit generate` alias — confirmed in package.json). This produces a new migration file under `src/db/migrations/` named `0004_<drizzle-kit-generated-slug>.sql` containing the `CREATE TABLE "redirects"` DDL. Do NOT hand-write SQL — CLAUDE.md hard rule. Do NOT run `drizzle-kit push`.

    Next, extend `src/lib/storage/seed.ts` with a new exported `seedSeoSettings(): Promise<void>` function that inserts the five SEO settings keys per D-11: `site.title` (value "Any Discussion"), `site.description` (value "A fast, SEO-optimized blog from Any Discussion."), `seo.default_og_image` (empty string — operator sets via the settings/seo page in Plan 03), `site.canonical_base_url` (value `process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"`), and `seo.twitter_handle` (empty string). Use the same `db.insert(schema.settings).values([...]).onConflictDoNothing()` idempotent pattern as the existing `seedStorageSettings` (lines 44-58) — re-runs must be safe.

    Finally, wire `seedSeoSettings` into `src/instrumentation.ts` immediately after the existing `seedStorageSettings()` call (mirror the existing call site's try/catch + NEXT_RUNTIME === "nodejs" guard). The seed runs at first boot so the settings keys exist before any `getSeoSettings()` consumer reads them.

    Per landmine #1 and #2: do NOT touch `middleware.ts` and do NOT create any `src/proxy.ts` file — the redirects-check wiring is Plan 03's job and targets `app/not-found.tsx` (Node runtime).
  </action>

  <verify>
    <automated>cd "D:/Devsroom-Work/anydiscussion" && pnpm db:generate && ls src/db/migrations/0004_*.sql && grep -l "redirects" src/db/migrations/0004_*.sql && grep -c "old_path" src/db/migrations/0004_*.sql && pnpm tsc --noEmit</automated>
  </verify>

  <done>
    - `src/db/schema.ts` exports `redirects` with oldPath/newPath/statusCode/createdAt/updatedAt columns
    - `pnpm db:generate` produced a `0004_*.sql` migration whose SQL contains both `CREATE TABLE` and `old_path` and `new_path` and `status_code`
    - `src/lib/storage/seed.ts` exports `seedSeoSettings` inserting the five D-11 keys idempotently
    - `src/instrumentation.ts` calls `seedSeoSettings` after `seedStorageSettings` under the existing nodejs-runtime guard
    - `pnpm tsc --noEmit` passes (no type errors from the schema change)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SEO engine pure builders + Wave 0 tests (metadata, jsonld, validation)</name>
  <files>src/lib/seo/metadata.ts, src/lib/seo/jsonld.ts, src/lib/seo/validation.ts, src/lib/seo/__tests__/shared-fixtures.ts, src/lib/seo/__tests__/metadata.test.ts, src/lib/seo/__tests__/jsonld.test.ts, src/lib/seo/__tests__/validation.test.ts</files>

  <behavior>
    - buildPostMetadata with a post that has no postSeo: title comes from post.title, description from post.excerpt (then settings.siteDescription fallback), canonical is "/" + post.slug, ogImage is post.featureImage then settings.defaultOgImage, openGraph.type is "article", openGraph.publishedTime is post.publishedAt ISO string, twitter.card is "summary_large_image" when an image resolves.
    - buildPostMetadata with postSeo.canonicalUrl set: alternates.canonical equals that override (NOT the slug-derived path).
    - buildPostMetadata with no ogImage anywhere: twitter.card is "summary".
    - buildPageMetadata reads page.metaTitle/metaDescription/canonical with the same override-else-derive logic.
    - buildSiteMetadata returns metadataBase as a new URL(canonicalBaseUrl), title as {default, template} pair, and openGraph.type "website".
    - blogPostingJsonLd: @type is "BlogPosting", headline is the title, datePublished is ISO 8601, author.@type is "Person", mainEntityOfPage.@type is "WebPage", mainEntityOfPage.@id is the canonical URL.
    - websiteJsonLd: @type is "WebSite", potentialAction.@type is "SearchAction", target contains "/search?q={search_term_string}".
    - organizationJsonLd: @type is "Organization", url is canonicalBaseUrl, logo is defaultOgImage.
    - graphemeCount on a 59-grapheme Bangla fixture returns 59 (not 84 UTF-16 units, not 220 bytes).
    - seoMetaSchema.safeParse on a 59-grapheme Bangla metaDescription succeeds.
    - seoMetaSchema.safeParse on a 250-grapheme Latin metaDescription fails the refine.
  </behavior>

  <read_first>
    - `.planning/phases/05-seo-basics/05-RESEARCH.md` Pattern 1 (lines 344-424 — the verified buildPostMetadata body), Pattern 4 (lines 505-594 — JSON-LD builders + injection), Code Example 1 (lines 760-805 — the Bangla grapheme Zod schema), Pitfall 3 (lines 719-732 — the empirical 59-grapheme Bangla verification)
    - `src/lib/excerpt/index.ts` (the closest pure-lib analog — mirror its module header + the safeSlice Bangla-aware reasoning)
    - `src/actions/pages-schema.ts` lines 39-42 (the existing Latin-max SEO Zod pattern that validation.ts replaces with grapheme refines)
    - `src/lib/slug/__tests__/slug.test.ts` (the pure-builder test scaffold — vi.mock pattern for chainable DB, describe/it shape)
    - `.planning/phases/05-seo-basics/05-PATTERNS.md` sections for metadata.ts, jsonld.ts, validation.ts (exact analog assignments + landmine callouts)
  </read_first>

  <action>
    Create the three pure-builder modules in `src/lib/seo/`. These are PURE — no DB access, no fetch, no side effects. They take already-fetched DB rows + a settings snapshot IN and return typed shapes OUT (per RESEARCH.md "Primary recommendation" lines 126-127). This makes them trivially unit-testable with the shared fixtures.

    `src/lib/seo/metadata.ts` — export `buildPostMetadata(post: PostLike, seo: PostSeoLike | null, s: SeoSettings): Metadata`, `buildPageMetadata(page: PageLike, s: SeoSettings): Metadata`, `buildArchiveMetadata(opts: ArchiveMetadataInput, s: SeoSettings): Metadata` (the archive builder is a thin shape consumed by Phase 6 — title/description/canonical only; keep it minimal but present so Phase 6 extends rather than rewrites), and `buildSiteMetadata(s: SeoSettings): Metadata`. Define the `PostLike`, `PostSeoLike`, `PageLike`, `SeoSettings` interfaces at the top of the file (mirror the shapes in RESEARCH Pattern 1 lines 384-389 — fields: id, title, slug, excerpt, featureImage, publishedAt, updatedAt, authorName for PostLike; metaTitle, metaDescription, ogImage, canonicalUrl for PostSeoLike). Implement D-04 canonical resolution (seo.canonicalUrl || "/" + post.slug) and D-09 OG fallback chain (seo.ogImage || post.featureImage || s.defaultOgImage). For openGraph on posts use type "article" with publishedTime/modifiedTime/authors fields (verified available on OpenGraphArticle type). For twitter, card is "summary_large_image" when an image resolves else "summary"; include `site: s.twitterHandle` only when non-empty. Import `type { Metadata } from "next"`. Add NO "use client" directive — these are server-only pure modules.

    `src/lib/seo/jsonld.ts` — export `blogPostingJsonLd(i: BlogPostingInput)`, `websiteJsonLd(s: WebSiteJsonLdInput)`, `organizationJsonLd(s: OrganizationJsonLdInput)`. Return plain objects (NOT stringified — the consumer does JSON.stringify + dangerouslySetInnerHTML). Follow the exact schema.org shapes in RESEARCH Pattern 4 (lines 520-569): @context "https://schema.org"; BlogPosting with headline/description/image/datePublished/dateModified/author(Person)/publisher(Organization with optional logo ImageObject)/mainEntityOfPage(WebPage with @id). websiteJsonLd includes the `potentialAction` SearchAction per D-03. No imports beyond TypeScript types.

    `src/lib/seo/validation.ts` — export `graphemeCount(s: string, locale = "en"): number` using `new Intl.Segmenter(locale, { granularity: "grapheme" })` spread to an array and .length'd (per RESEARCH Code Example 1 + the empirical Node 24 verification in Pitfall 3). Export constants `TITLE_MAX_GRAPHEMES = 80` and `DESC_MAX_GRAPHEMES = 200` (per D-10 discretion — RESEARCH recommends these as defensible). Export `seoMetaSchema = z.object({...})` with metaTitle (z.string().max(255) with a grapheme refine ≤ 80, optional), metaDescription (z.string().max(600) with a grapheme refine ≤ 200, optional), ogImage (z.string().url().optional().or(z.literal(""))), canonicalUrl (same shape as ogImage). The hard .max caps are byte/code-unit ceilings; the refines enforce the grapheme rule. Export `type SeoMetaInput = z.input<typeof seoMetaSchema>`. Import `z` from "zod" (v4.4.3 — confirmed installed). This is the SEO-06 rule (landmine #5: graphemes, not .length, not bytes).

    `src/lib/seo/__tests__/shared-fixtures.ts` — export a fake `PostLike` (a published post with a Bangla-capable title, a slug, an excerpt, a featureImage, publishedAt/updatedAt Dates, authorName), a fake `PostSeoLike` (all four fields populated), a `PostSeoLike | null` variant (null, to exercise the fallback chain), a fake `SeoSettings` (canonicalBaseUrl "https://anydiscussion.com", siteTitle "Any Discussion", siteDescription, defaultOgImage URL, twitterHandle "@anydiscussion"), a fake `PageLike`, and the empirical 59-grapheme Bangla fixture string from RESEARCH Pitfall 3 (the sample that is 84 UTF-16 units and 220 bytes — use it to prove graphemeCount returns 59). Also export a 250-grapheme Latin string for the over-long-fails case.

    The three test files (`metadata.test.ts`, `jsonld.test.ts`, `validation.test.ts`) cover the behaviors listed in the `<behavior>` block. Use the vitest `describe/it/expect` shape from `src/lib/slug/__tests__/slug.test.ts`. No DB mock needed — these are pure functions taking fixtures. Assert on the returned `Metadata` / JSON-LD object fields directly.
  </action>

  <verify>
    <automated>cd "D:/Devsroom-Work/anydiscussion" && pnpm test -- src/lib/seo/__tests__/metadata.test.ts src/lib/seo/__tests__/jsonld.test.ts src/lib/seo/__tests__/validation.test.ts --run</automated>
  </verify>

  <done>
    - `pnpm test -- src/lib/seo --run` exits 0 with all metadata/jsonld/validation cases green
    - buildPostMetadata, buildPageMetadata, buildSiteMetadata, buildArchiveMetadata all exported from metadata.ts
    - blogPostingJsonLd, websiteJsonLd, organizationJsonLd all exported from jsonld.ts
    - seoMetaSchema, graphemeCount exported from validation.ts with TITLE_MAX_GRAPHEMES=80 / DESC_MAX_GRAPHEMES=200
    - The 59-grapheme Bangla fixture passes seoMetaSchema; the 250-grapheme Latin fixture fails it (SEO-06 verified)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cached settings reader + site-wide generateMetadata on (site) layout/home/preview</name>
  <files>src/lib/seo/settings.ts, src/app/(site)/layout.tsx, src/app/(site)/page.tsx, src/app/(site)/preview/[token]/page.tsx</files>

  <read_first>
    - `src/app/(site)/layout.tsx` (current 18-line skeletal layout — the file being modified to add generateMetadata + JSON-LD)
    - `src/app/(site)/page.tsx` lines 4-8 (the current static `export const metadata` being replaced)
    - `src/app/(site)/preview/[token]/page.tsx` lines 31-34 (the current static metadata + lines 57-69 the DB lookup pattern to reuse)
    - `.planning/phases/05-seo-basics/05-RESEARCH.md` Pattern 1 (lines 350-378 — the verified `'use cache'` generateMetadata body), Pattern 4 (lines 572-594 — the JSON-LD injection in the layout body), Pitfall 1 (lines 707-711 — the cacheComponents build-error landmine), Pitfall 6 (lines 746-750 — the cacheTag + revalidateTag requirement)
    - `.planning/phases/05-seo-basics/05-PATTERNS.md` section "`src/lib/seo/settings.ts`" (the greenfield `'use cache'` pattern — no existing analog) + section "`src/app/(site)/layout.tsx`" (the title.template semantic landmine)
    - `src/actions/storage-settings.ts` lines 69-76 (the readSetting helper shape — the closest DB-read analog for the settings reader)
    - `next.config.ts` line 5 (confirms `cacheComponents: true` — the driver of the `'use cache'` requirement)
  </read_first>

  <action>
    Create `src/lib/seo/settings.ts` exporting `getSeoSettings(): Promise<SeoSettings>`. This is the greenfile `'use cache'` + `cacheTag` site (landmine #4 — first in the repo, no existing analog). The function body starts with the `"use cache"` directive (first line inside the function, per RESEARCH Pattern 1 line 364) then calls `cacheTag("seo-settings")` (imported as `import { unstable_cacheTag as cacheTag } from "next/cache"` — verify the exact export name against the installed next@16.2.9 types at execution time; the alias may also be available as `cacheTag` without the unstable_ prefix). Then read the five settings keys from the `settings` table via Drizzle: site.title, site.description, seo.default_og_image, site.canonical_base_url, seo.twitter_handle. Use `NEXT_PUBLIC_SITE_URL` as the fallback for canonical_base_url when the settings row is absent or empty (D-04 + Pitfall 7 — getSeoSettings is the SINGLE source; no consumer reads the env directly). Return a plain `SeoSettings` object (canonicalBaseUrl, siteTitle, siteDescription, defaultOgImage, twitterHandle) matching the interface in metadata.ts. The `SeoSettings` type should be exported from metadata.ts (or re-exported from settings.ts) so consumers import one type.

    Modify `src/app/(site)/layout.tsx` to add `export async function generateMetadata(): Promise<Metadata>` with the `"use cache"` directive (landmine #4 — REQUIRED under cacheComponents:true for an otherwise-prerenderable route that reads DB data; without it the build fails with "metadata accesses uncached data"). Inside, call `await getSeoSettings()` and return `buildSiteMetadata(s)` augmented with `metadataBase: new URL(s.canonicalBaseUrl)` and `title: { default: s.siteTitle, template: "%s | " + s.siteTitle }` and `twitter: { card: "summary_large_image" }`. NOTE the title.template semantic (RESEARCH Anti-Patterns line 687 + PATTERNS.md landmine #7): a template defined in the layout applies only to CHILD segments, not to the layout's own default — so the home page inherits via the layout's `title.default`. Then modify the layout's default export to render the two site-wide JSON-LD script tags (landmine #3 — NOT via metadata.other; real `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(websiteJsonLd(s)) }} />` and the same for `organizationJsonLd(s)`). Call `getSeoSettings()` inside the layout component body (it's a Server Component — async DB read is fine; the cached reader keeps it cheap). Import `websiteJsonLd`, `organizationJsonLd` from `@/lib/seo/jsonld` and `getSeoSettings` from `@/lib/seo/settings` and `buildSiteMetadata` from `@/lib/seo/metadata`.

    Modify `src/app/(site)/page.tsx` to replace the static `export const metadata` (lines 4-8) with `export async function generateMetadata(): Promise<Metadata>` containing `"use cache"` + a `getSeoSettings()` call, returning `buildSiteMetadata(s)` (the home route is settings-driven and near-static — the `'use cache'` directive is the verified resolution per RESEARCH Pitfall 1). The page body is otherwise unchanged.

    Modify `src/app/(site)/preview/[token]/page.tsx` to replace the static metadata (lines 31-34) with `export async function generateMetadata({ params }: { params: Promise<{ token: string }> }): Promise<Metadata>`. Await params (Next 16 async API), look up the post by previewToken (reuse the existing DB query pattern at lines 60-66), and if found return `{ ...buildPostMetadata(post, null, s), robots: { index: false, follow: false } }` (preserve the noindex — preview must never be indexed); if not found return `{ title: "Not Found", robots: { index: false, follow: false } }`. Do NOT add `"use cache"` here — the `params` make this route dynamic by default (RESEARCH Pitfall 1 alternative path). Call `getSeoSettings()` for the settings snapshot.

    Per landmine #5: use 2-arg `revalidateTag(tag, "max")` form anywhere it appears (not in this task, but the saveSeoSettings action in Plan 03 will call `revalidateTag("seo-settings", "max")` to invalidate this cache).
  </action>

  <verify>
    <automated>cd "D:/Devsroom-Work/anydiscussion" && pnpm tsc --noEmit && pnpm test -- src/lib/seo --run && pnpm build</automated>
  </verify>

  <done>
    - `src/lib/seo/settings.ts` exports `getSeoSettings` with the `"use cache"` directive as its first line + `cacheTag("seo-settings")` call
    - `src/app/(site)/layout.tsx` exports async `generateMetadata` with `"use cache"` and renders two `application/ld+json` script tags (WebSite + Organization)
    - `src/app/(site)/page.tsx` exports async `generateMetadata` (no more static `export const metadata`)
    - `src/app/(site)/preview/[token]/page.tsx` exports async `generateMetadata` preserving `robots: { index: false, follow: false }`
    - `pnpm build` succeeds (proves the cacheComponents + `'use cache'` interaction is correct — landmine #4)
    - `pnpm test -- src/lib/seo --run` still green (no test regressions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings table → getSeoSettings → generateMetadata | DB-sourced site-wide metadata crosses into the public HTML response; a compromised settings row could inject into `<title>`/OG/JSON-LD |
| post_seo / pages.metaTitle → buildPostMetadata → `<title>` | Editor-authored strings render into the public `<head>`; must not allow HTML injection |
| JSON-LD builder output → dangerouslySetInnerHTML | Server-built JSON.stringify payload rendered into a `<script>` tag in the layout body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | JSON-LD `dangerouslySetInnerHTML` payload (layout/home) | medium | mitigate | Build JSON-LD only from server-fetched DB rows already gated by the Tiptap/double-sanitize pipeline; JSON.stringify escapes `<` sequences that could close a `<script>` tag. The `application/ld+json` MIME type is non-executing in browsers. |
| T-05-02 | Information Disclosure | settings values rendered into public metadata | low | accept | SEO settings (site title, default OG, canonical base, twitter handle) are non-sensitive by design — they are public marketing metadata, not secrets. No encryption needed (unlike Phase 4 storage creds). |
| T-05-03 | Tampering | `redirects` table data (ships empty in v1) | low | accept | Table is empty in v1 (greenfield DB); no risk surface until SETT-03 v2 UI populates it. When populated, new_path must be validated as internal (`/`-prefixed) — note for v2. |
| T-05-SC | Tampering | npm installs (package legitimacy) | low | accept | Zero packages installed this phase (RESEARCH.md Package Legitimacy Audit confirms — all primitives ship with next@16.2.9). No slopsquat surface. |
</threat_model>

<verification>
- `pnpm db:generate` produces `0004_*.sql` containing the redirects DDL (landmine #1 closed)
- `pnpm test -- src/lib/seo --run` green (SEO-01/03/04/05/06 verified at helper level)
- `pnpm build` succeeds under `cacheComponents:true` (landmine #4 — the `'use cache'` directive is correctly placed)
- Manual smoke (deferred to phase verification): `curl http://localhost:3000/` shows `<title>Any Discussion</title>` + two `application/ld+json` script tags in the HTML source
</verification>

<success_criteria>
- SEO-01 (partial — site/home/preview routes emit generateMetadata; full per-route coverage is Phase 6): buildPostMetadata/buildSiteMetadata return correct Metadata shapes, verified by unit tests + live build
- SEO-03: blogPostingJsonLd + websiteJsonLd + organizationJsonLd builders produce valid schema.org shapes (helper-level; Phase 6 renders blogPostingJsonLd on the post page)
- SEO-04: buildPostMetadata respects postSeo.canonicalUrl override else derives /{slug} (D-04)
- SEO-05: OG fallback chain (postSeo.ogImage → post.featureImage → settings.defaultOgImage) + twitter card logic verified
- SEO-06: Bangla 59-grapheme fixture passes, Latin 250-grapheme fails (grapheme rule, not .length)
- Schema-push gate closed: redirects table exists in a generated migration
</success_criteria>

<output>
Create `.planning/phases/05-seo-basics/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase (Plan 01) produces

**New exports / symbols:**
- `src/db/schema.ts` → `export const redirects` (pgTable: id, oldPath, newPath, statusCode, createdAt, updatedAt)
- `src/lib/seo/metadata.ts` → `buildPostMetadata`, `buildPageMetadata`, `buildArchiveMetadata`, `buildSiteMetadata` + interfaces `PostLike`, `PostSeoLike`, `PageLike`, `SeoSettings`
- `src/lib/seo/jsonld.ts` → `blogPostingJsonLd`, `websiteJsonLd`, `organizationJsonLd` + input interfaces
- `src/lib/seo/validation.ts` → `seoMetaSchema`, `graphemeCount`, `SeoMetaInput`, `TITLE_MAX_GRAPHEMES`, `DESC_MAX_GRAPHEMES`
- `src/lib/seo/settings.ts` → `getSeoSettings` (with `'use cache'` + `cacheTag("seo-settings")`)
- `src/lib/storage/seed.ts` → `seedSeoSettings` (new function)
- Cache tag identifier: `"seo-settings"` (consumed by Plan 03's `revalidateTag("seo-settings", "max")`)

**New/modified files:**
- `src/db/migrations/0004_*.sql` (generated — contains redirects DDL)
- `src/lib/seo/__tests__/{shared-fixtures,metadata,jsonld,validation}.test.ts`
- `src/instrumentation.ts` (calls seedSeoSettings)
- `src/app/(site)/layout.tsx` (generateMetadata + JSON-LD script tags)
- `src/app/(site)/page.tsx` (generateMetadata)
- `src/app/(site)/preview/[token]/page.tsx` (generateMetadata)

**Patterns introduced (first-in-repo):**
- `'use cache'` directive + `cacheTag(...)` — the first Cache Components cached-data-read site in the codebase (no prior analog; verify the import name `unstable_cacheTag` vs `cacheTag` against installed types at execution time)
