---
phase: 05-seo-basics
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/components/editor/extensions.ts
  - src/components/editor/TiptapEditor.tsx
  - src/components/editor/toolbar/Toolbar.tsx
  - src/components/editor/__tests__/round-trip.test.ts
  - src/lib/sanitize/index.ts
  - src/components/editor/__tests__/tiptap-editor-surface.test.tsx
autonomous: true
gap_closure: true
requirements: [SEO-02, SEO-06]
must_haves:
  truths:
    - "Visual/Text tabs switch between the rich editor and an editable HTML source view; content edited in the Text tab round-trips back into the visual editor and the RHF body field"
    - "The toolbar presents controls in the WordPress-classic order: block-type dropdown (Paragraph/H1/H2/H3), Bold, Italic, Bulleted list, Numbered list, Blockquote, Align left/center/right, Insert link, Insert table, More(...) overflow (strike, code, code block, image via MediaPicker, undo/redo)"
    - "The writing area is a large min-height surface and the footer shows a live word count that increments as the user types"
    - "Both the post form and the page form render the new editor surface with ZERO changes to EditorProvider's public API (name + control props)"
    - "Aligned content survives the server pipeline: generateHTML(editorExtensions) emits the text-align style and sanitizeBeforeRender preserves it"
  artifacts:
    - "src/components/editor/extensions.ts — TextAlign (types heading+paragraph) + CharacterCount added to the single shared array"
    - "src/components/editor/TiptapEditor.tsx — tab header, min-height writing area, word-count footer"
    - "src/components/editor/toolbar/Toolbar.tsx — rebuilt in WordPress-classic order"
    - "src/components/editor/__tests__/round-trip.test.ts — text-align serialization + sanitize-preserve cases"
    - "src/components/editor/__tests__/tiptap-editor-surface.test.tsx — jsdom smoke test of tabs/toolbar/footer"
  key_links:
    - "editorExtensions (extensions.ts) <-> generateHTML in lib/post-render.ts — single source of truth; the extended round-trip test must stay green (UAT missing-item hard requirement)"
    - "TiptapEditor onUpdate -> onChange prop -> EditorProvider Controller -> RHF body field — the live word-count footer is the canary proving this pipe works (the 'body box not working' UAT symptom)"
---

<objective>
Close the editor half of UAT gap 1 (Phase 5 UAT test 2): rebuild the body editor surface to the WordPress-classic-editor spec the user asked for verbatim — "the body box must be functional like the screenshot I provided ... so I want same for post and pages or where the body box used". The surface applies EVERYWHERE EditorProvider/TiptapEditor is used (PostForm.tsx + PageForm.tsx), delivered automatically because EditorProvider's public API (name + control) does not change.

Scope per the UAT missing-item spec: Visual/Text tabs (Text = editable HTML source view); toolbar in this exact order — Paragraph/block-type dropdown (Paragraph/H1/H2/H3), Bold, Italic, Bulleted list, Numbered list, Blockquote, Align left/center/right, Insert link, Insert table, More(...) overflow containing strike, code, code block, image via MediaPicker, undo/redo; large white min-height writing area; footer bar with live Word count. extensions.ts stays the single shared client+server source and the round-trip test must still pass. pnpm only; @tiptap/*@3.27.1 line.

Two NEW dependencies (@tiptap/extension-text-align and @tiptap/extension-character-count) are required for alignment and word count. The existing 05-RESEARCH.md audit table is empty (that phase installed zero packages), so the legitimacy audit was performed by the planner directly against registry.npmjs.org on 2026-08-25 — see the threat model SC row for the full audit record. Both packages are official ueberdosis/tiptap monorepo releases, exact-pinned 3.27.1 to match the installed @tiptap/*@3.27.1 line.

Purpose: the user could not author post bodies confidently ("body text box not working properly") and the rebuilt surface plus its live word-count footer gives an always-visible proof that typing registers in form state — which also unblocks publishing (Plan 05-06) and with it the blocked sitemap/RSS verification (UAT test 2).
Output: Rebuilt editor surface (tabs + ordered toolbar + min-height area + word-count footer), two registry-verified pinned deps, extended round-trip + sanitize + component tests.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-basics/05-UAT.md

@src/components/editor/extensions.ts
@src/components/editor/TiptapEditor.tsx
@src/components/editor/EditorProvider.tsx
@src/components/editor/toolbar/Toolbar.tsx
@src/components/editor/__tests__/round-trip.test.ts
@src/lib/sanitize/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Text-align + character-count extensions with round-trip and sanitize tests</name>
  <files>package.json, pnpm-lock.yaml, src/components/editor/extensions.ts, src/components/editor/__tests__/round-trip.test.ts, src/lib/sanitize/index.ts</files>
  <behavior>
    - Test 1 (RED first): generateHTML over a doc containing a paragraph node with attrs textAlign "center" and text "Centered", using editorExtensions, produces HTML containing a paragraph with inline style text-align: center.
    - Test 2: the same aligned HTML passed through sanitizeBeforeRender RETAINS the text-align style (defense-in-depth render gate must not strip alignment).
    - Test 3: a heading node with textAlign "right" serializes the same way (alignment applies to both configured types).
    - Existing round-trip cases (headings, lists, links, tables, images, code blocks, iframe raw-HTML) stay green — the array change must not drop any existing node/mark.
  </behavior>
  <action>
    RED: extend src/components/editor/__tests__/round-trip.test.ts with the three cases above first and run the suite — the alignment cases MUST fail (TextAlign not installed; the attrs are dropped by generateHTML).

    Install (pnpm only, exact pins to match the installed line): pnpm add --save-exact @tiptap/extension-text-align@3.27.1 @tiptap/extension-character-count@3.27.1. Note: the registry description for @tiptap/extension-character-count erroneously reads "font family extension" — that is a known tiptap metadata copy-paste bug; the package IS the official character-count extension (verified: same ueberdosis/tiptap repository, same tiptap.dev maintainers, same 3.27.1 release train as the installed @tiptap/* packages).

    GREEN: in src/components/editor/extensions.ts add to the shared editorExtensions array:
    - TextAlign from @tiptap/extension-text-align, configured with types limited to heading and paragraph (alignment on body-level blocks only, matching the toolbar surface).
    - CharacterCount from @tiptap/extension-character-count (default configuration — its storage powers the footer word count; it contributes no schema output so server-side generateHTML is unaffected).
    Follow the file's established convention: a short comment per extension stating WHY it is there and the config rationale, and update the header config-notes block with the two new entries (UAT 05 gap 1 spec). The file keeps NO "use client" directive — it must stay importable from lib/post-render.ts.

    If Test 2 (sanitize-preserve) fails because DOMPurify strips the style attribute: add "style" to the ADD_ATTR list in src/lib/sanitize/index.ts with a comment justifying it (alignment inline styles from the editor's TextAlign extension; DOMPurify continues to sanitize the attribute value) and note the change in the task commit message. Do NOT pre-add it speculatively — let the test decide.

    Run the full suite: pnpm test --run (all existing suites must stay green — especially sanitize.test.ts same-config drift test and posts tests).
  </action>
  <verify>
    <automated>pnpm test --run src/components/editor/__tests__/round-trip.test.ts && pnpm test --run src/lib/sanitize/__tests__/</automated>
  </verify>
  <done>Alignment serializes through generateHTML and survives sanitizeBeforeRender; both extensions pinned at exact 3.27.1 in package.json; every pre-existing test green.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the toolbar in WordPress-classic order</name>
  <files>src/components/editor/toolbar/Toolbar.tsx</files>
  <action>
    Rebuild src/components/editor/toolbar/Toolbar.tsx so the controls render in EXACTLY this order (UAT verbatim spec):

    1. Block-type dropdown — Paragraph / Heading 1 / Heading 2 / Heading 3. A native select styled to the TailAdmin button scale is acceptable (accessible, zero-JS menu). Current block read via editor.isActive("heading", { level }) checks vs paragraph; commands: setParagraph() and toggleHeading({ level }) via editor.chain().focus().
    2. Bold (toggleBold) — keep existing ToolbarButton pattern.
    3. Italic (toggleItalic).
    4. Bulleted list (toggleBulletList).
    5. Numbered list (toggleOrderedList).
    6. Blockquote (toggleBlockquote).
    7. Align left / center / right — three buttons calling editor.chain().focus().setTextAlign("left" | "center" | "right").run(), active state via editor.isActive({ textAlign: <dir> }); include an unset when the active alignment matches (unsetTextAlign) so the button toggles.
    8. Insert link — keep the existing prompt-based manual link flow UNCHANGED (D-05 locked: manual links, target=_blank + anti-tabnabbing rel defaults).
    9. Insert table — keep the existing insertTable({ rows: 3, cols: 3, withHeaderRow: true }) command.
    10. More("...") overflow — a single button toggling a small dropdown panel containing: Strike (toggleStrike), Code (toggleCode), Code block (toggleCodeBlock), Image (opens the EXISTING MediaPicker modal — keep the current MediaPicker wiring inside Toolbar), Undo (editor.chain().focus().undo().run()), Redo (redo).

    Constraints:
    - Preserve the existing ToolbarButton active/disabled styling pattern and the MediaPicker state handling (useState before the early return — Rules of Hooks).
    - Use useEditorState from @tiptap/react (v3 reactive selector API) to derive toolbar active states in one selector object instead of reading editor.isActive across renders — this is the v3-recommended pattern and keeps the toolbar reactive to selection/transaction changes.
    - Keep the "use client" directive and the file's comment-header convention; update citations to note this rebuild closes the Phase 5 UAT editor-surface gap (WordPress-classic spec).
    - Extract subcomponents (e.g. a BlockTypeSelect or MoreMenu file under toolbar/) only if Toolbar.tsx would otherwise exceed ~250 lines — one file is fine if compact.
    - Do NOT touch extensions.ts here (Task 1 owns it) and do NOT change the Toolbar props contract beyond what the new controls need (editor: Editor | null stays).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Toolbar renders the ten controls in the specified order; align buttons toggle alignment on heading/paragraph; More menu holds strike/code/code-block/image/undo/redo; MediaPicker image flow intact.</done>
</task>

<task type="auto">
  <name>Task 3: Editor shell — Visual/Text tabs, min-height area, word-count footer</name>
  <files>src/components/editor/TiptapEditor.tsx, src/components/editor/__tests__/tiptap-editor-surface.test.tsx</files>
  <action>
    Rebuild the TiptapEditor frame around the existing useEditor/EditorContent core:

    - Tab header: two tabs, Visual and Text (WordPress-classic placement: top of the editor frame). A mode state ("visual" | "text") drives which surface shows.
    - Visual surface: the existing EditorContent inside a LARGE min-height writing area — min-height around 350px (use the Tailwind min-h-* scale), white background in light mode (bg-white; keep the existing dark-mode surface treatment so the dashboard dark theme is not broken), prose typography preserved.
    - Text surface: a controlled textarea (monospace font) holding the HTML source. Initialize from editor.getHTML() when switching Visual -> Text. On every textarea change: update the local html state AND call editor.commands.setContent with the new HTML string and emitUpdate true — ProseMirror parses the HTML against the schema (unknown tags are dropped by the schema, and the existing double-sanitize contract in savePost/renderPostBody is unchanged), and emitUpdate fires onUpdate so onChange(editor.getJSON()) reaches the RHF body field. This keeps form state live even if the user never switches back to Visual.
    - Footer bar: live word count + character count from editor.storage.characterCount (words() and characters()), derived reactively via useEditorState (same selector pattern as the toolbar). Format like "Words: 12" plus a character count. This footer doubles as the pipe canary for the UAT "body box not working" symptom — if typing increments the count, onUpdate -> onChange -> RHF is proven live.
    - EditorProvider is NOT modified: its public API (name + control props) and the dynamic({ ssr: false }) lazy boundary stay exactly as-is, so PostForm.tsx and PageForm.tsx inherit the entire new surface without touching either form (verify with a grep that both still render EditorProvider with just name and control).
    - Keep TiptapEditorProps (value/onChange/editable) unchanged — consumers depend on it.

    New jsdom smoke test — src/components/editor/__tests__/tiptap-editor-surface.test.tsx with the `// @vitest-environment jsdom` pragma (per-file opt-in, vitest.config.ts is node-default; @testing-library/react is already a devDependency). Cover: renders Visual and Text tab labels; clicking Text swaps EditorContent for a textarea; typing into the textarea (fireEvent.change) calls the onChange prop with a JSON doc (assert the callback received an object with type "doc"); the footer word-count text renders. If Tiptap-in-jsdom initialization proves flaky in this environment, reduce to render + tab-swap assertions and note the reduction in the commit message — do not delete the test.

    If, after this rebuild, typing in the visual editor STILL fails to register (the count does not move), capture the precise symptom (cannot type / toolbar dead / content not saved) in the SUMMARY under Issues — that becomes a /gsd-debug input; do not attempt speculative fixes beyond the rebuild.
  </action>
  <verify>
    <automated>pnpm test --run src/components/editor/__tests__/ && rm -rf .next && pnpm build</automated>
  </verify>
  <done>Tabs switch surfaces; textarea edits flow into RHF form state; min-height writing area renders; footer shows live counts; jsdom smoke test green; pnpm build exits 0 with both forms compiling unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor user -> Text tab (raw HTML input) | Free-form HTML source crosses into the ProseMirror doc and later the public render path |
| stored post body -> public render | Existing boundary (generateHTML -> sanitizeBeforeRender) — unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Tampering / XSS | TiptapEditor Text tab | high | mitigate | Three existing layers bound the raw-HTML surface: (1) ProseMirror schema strips non-schema nodes/marks at setContent time, (2) sanitizeBeforeStore runs in savePost before storage, (3) sanitizeBeforeRender runs before dangerouslySetInnerHTML (same shared CONFIG — drift-tested). Task 1's sanitize-preserve test extends this contract to alignment styles specifically. No new sanitization bypass is introduced. |
| T-05-SC | Tampering (supply chain) | pnpm add of @tiptap/extension-text-align + @tiptap/extension-character-count | high | mitigate | Package Legitimacy Audit performed by the planner against registry.npmjs.org (2026-08-25) because the 05-RESEARCH.md audit table is empty for these gap-closure installs: BOTH packages resolve to repository https://github.com/ueberdosis/tiptap (the official monorepo of every installed @tiptap/* package), maintainers are the tiptap.dev staff accounts (arnaugomeztiptap, patrickbaber, timoisik, _bdbch, svenadlung, tiptap-bot), package ages 2020/2021, 342/335 published versions, 3.27.1 exists in both — the same release train as the installed @tiptap/*@3.27.1 line. Peers align with installed @tiptap/core 3.27.1. Verdict: LEGITIMATE, no [SUS]/[SLOP] indicators. Install with --save-exact pins so the lockfile cannot drift. |
</threat_model>

<verification>
- pnpm test --run green (extended round-trip + sanitize + new surface smoke test + all pre-existing suites).
- rm -rf .next && pnpm build exits 0 (stale 'use cache' guard per project memory — extensions.ts feeds the cached renderPostBody path).
- PostForm and PageForm both render the new surface (tabs, ordered toolbar, min-height area, word-count footer) with no source changes to either form.
</verification>

<success_criteria>
- The editor matches the WordPress-classic spec verbatim: tabs, toolbar order, More(...) overflow, large writing area, live word count.
- The round-trip test (single shared extensions array) stays green — server rendering parity is unbroken.
- Aligned content survives storage + render sanitization (proven by test, not assumption).
- EditorProvider consumers (post + page forms) pick up the surface with zero API changes.
</success_criteria>

<output>
Create `.planning/phases/05-seo-basics/05-05-SUMMARY.md` when done
</output>
