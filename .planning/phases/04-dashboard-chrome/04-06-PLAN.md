---
phase: 04-dashboard-chrome
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/dashboard/users/UserDrawer.tsx
  - src/app/(admin)/dashboard/profile/ProfileForm.tsx
autonomous: true
gap_closure: true
requirements: [DASH-04]
requirements_addressed: [DASH-04]

must_haves:
  truths:
    - "The avatar field in UserDrawer.tsx renders <MediaPicker> (the reusable modal from Plan 04-02) instead of a standalone text input"
    - "The avatar field in ProfileForm.tsx renders <MediaPicker> instead of a standalone text input"
    - "Both avatar fields mirror the PostForm feature-image wiring — useState open-state, watch() preview, setValue('avatar', url) on select, thumbnail preview with Replace/Remove, and a Select-image button when empty"
    - "The Rule-3 text-input fallback (CDN-URL placeholder) is removed from both files — the picker is the sole avatar entry path, not a supplement"
    - "pnpm build and pnpm test remain green (243/243 maintained) — no regression to user/profile create/edit flows"
  artifacts:
    - "src/app/(admin)/dashboard/users/UserDrawer.tsx — avatar field swapped to <MediaPicker>"
    - "src/app/(admin)/dashboard/profile/ProfileForm.tsx — avatar field swapped to <MediaPicker>"
  key_links:
    - "MediaPicker onSelect callback → RHF setValue('avatar', url) in both consumers (the same wiring as PostForm's feature-image field)"
    - "watch('avatar') drives the thumbnail preview + empty-state button toggle in both consumers"
---

<objective>
Close the single verification gap from 04-VERIFICATION.md (31/32 must-haves): Plan 04-03 shipped avatar fields as text inputs because <MediaPicker> lived in a parallel-wave worktree. 04-02 has since merged (MediaPicker.tsx exists), so this plan applies the documented one-line-per-file swap in both avatar consumers.

Purpose: Satisfy Plan 04-03's must_have truth row 20 ("Avatar field uses the <MediaPicker> modal from Plan 04-02") and bring DASH-04 to full plan-fidelity (the management capability already worked; this removes the UX downgrade).
Output: Two files edited in place — no new artifacts. The <MediaPicker> component is reused unchanged.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-chrome/04-VERIFICATION.md

# Gap-closure scope is defined ENTIRELY by the gaps: block in 04-VERIFICATION.md frontmatter.
# Do NOT expand scope. The only failed truth is avatar-field MediaPicker integration.

# The component being reused (merged from Plan 04-02 — DO NOT modify it):
@src/components/dashboard/media/MediaPicker.tsx

# The canonical wiring pattern to mirror (feature-image field, lines ~178-225):
@src/app/(admin)/dashboard/posts/PostForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap avatar text input for MediaPicker in UserDrawer.tsx and ProfileForm.tsx</name>
  <files>src/app/(admin)/dashboard/users/UserDrawer.tsx, src/app/(admin)/dashboard/profile/ProfileForm.tsx</files>
  <read_first>
    - src/app/(admin)/dashboard/users/UserDrawer.tsx (the file being edited — current avatar block is the text input at the "Avatar URL" label; the integration target is documented in the file header lines 11-18)
    - src/app/(admin)/dashboard/profile/ProfileForm.tsx (the file being edited — same text-input avatar block under the "Avatar URL" label; integration target documented in header lines 11-16)
    - src/app/(admin)/dashboard/posts/PostForm.tsx (the REFERENCE pattern to mirror verbatim — feature-image field: useState at the top of the component, watch('featureImage') for the preview value, the conditional preview-block vs Select-image-button JSX around lines 178-225, and the trailing &lt;MediaPicker&gt; element with isOpen/onClose/onSelect)
    - src/components/dashboard/media/MediaPicker.tsx (the component being wired — confirm the exported props { isOpen, onClose, onSelect, accept? } and that onSelect receives a URL string; the component is default-exported)
  </read_first>
  <behavior>
    - Avatar field in UserDrawer.tsx: opening the picker (Select-image button) → choosing a media row or pasting an external URL → the avatar value updates via setValue('avatar', url) and the thumbnail preview renders.
    - Avatar field in ProfileForm.tsx: identical behavior.
    - Remove-image button clears the avatar (setValue('avatar', '', { shouldValidate: true })) and returns the field to the Select-image empty state.
    - The standalone CDN-URL text input (Rule-3 fallback) no longer renders as the avatar entry control in either file.
  </behavior>
  <action>
    Apply the SAME edit to BOTH files. The pattern is identical to PostForm's feature-image field — mirror it, do not invent a new shape.

    Per gap fix in 04-VERIFICATION.md (DASH-04 plan-fidelity remediation):

    USERDRAWER.tsx (src/app/(admin)/dashboard/users/UserDrawer.tsx):
    - Imports: add `useState` to the existing `react` import (the file currently imports only `useEffect` from react). Add a default import for the picker component from `@/components/dashboard/media/MediaPicker` (default export, same path PostForm uses). Add `import Image from "next/image";` (needed for the thumbnail preview — project hard rule: content-image previews go through next/image, never raw img).
    - useForm destructure: the file currently destructures `register, handleSubmit, reset, formState` from useForm. ADD `setValue` and `watch` to that destructure (same names PostForm uses).
    - Component body (near the existing createMutation declaration): add `const [avatarPickerOpen, setAvatarPickerOpen] = useState(false);` and `const avatarValue = watch("avatar");`. These mirror PostForm's `mediaPickerOpen` / `featureImageValue` exactly — only the field name differs.
    - Replace the avatar field block (the `&lt;div&gt;` containing the "Avatar URL" label, the comment about the MediaPicker integration target, the `&lt;input {...register("avatar")}&gt;` with its CDN-URL placeholder, and the helper paragraph beneath it) with the PostForm feature-image shape: keep a hidden registered input `<input type="hidden" {...register("avatar")} />` so RHF submits the field; then a conditional — when `avatarValue` is truthy, render a preview row (next/image `Image` with `src={avatarValue}`, `fill`, `sizes`, `object-cover`, the URL text, a Replace button that calls `setAvatarPickerOpen(true)`, and a Remove-image button that calls `setValue("avatar", "", { shouldValidate: true })`); otherwise render a Select-image button that calls `setAvatarPickerOpen(true)`. After the conditional, render the picker element: `<MediaPicker isOpen={avatarPickerOpen} onClose={() => setAvatarPickerOpen(false)} onSelect={(url) => { setValue("avatar", url, { shouldValidate: true }); setAvatarPickerOpen(false); }} />`. Use the `accept="image"` default (omit the prop — image is the default).
    - Remove the now-stale "MediaPicker integration target" header comment block (lines ~11-18) and the inline "Until 04-02 merges, this is a text input (Rule 3 auto-fix)" comment — the fallback is no longer a fallback; 04-02 has merged. Replace with a one-line note that the avatar field reuses the Plan-04-02 picker via setValue('avatar', url), mirroring PostForm's feature-image field.
    - Keep the label text "Avatar" (drop the "URL" suffix — the field is no longer a URL paste box). Retain the existing dark-mode Tailwind classes on the new elements (match PostForm's class strings: `rounded-lg border border-gray-200 p-3 dark:border-gray-800` for the preview row, the brand button classes for Select image, the `text-brand-500`/`text-error-500` link classes for Replace/Remove).

    PROFILEFORM.tsx (src/app/(admin)/dashboard/profile/ProfileForm.tsx):
    - Apply the IDENTICAL set of edits. Imports: add `useState` to the react import (file currently imports nothing from react — it uses useForm etc. from react-hook-form; add `import { useState } from "react";`). Add the default picker import and `import Image from "next/image";`. useForm destructure currently is `register, handleSubmit, formState` — ADD `setValue` and `watch`. Add the two `useState`/`watch` lines. Replace the avatar text-input block (under "Avatar URL" label, with the same integration-target comment) with the hidden registered input + preview-or-button conditional + trailing picker element, all reading `avatarValue`/`avatarPickerOpen`/`setValue("avatar", ...)`. Update the header comment identically. Rename the label to "Avatar".
    - Note: ProfileForm's avatar field is self-service (D-09) but the picker wiring is identical — setValue target 'avatar' is the same; the only contextual difference is that this form omits the role field, which is unrelated to this swap.

    Do NOT modify MediaPicker.tsx (it is reused unchanged). Do NOT extract a shared hook — PostForm inlines this pattern and DRY-across-3-consumers does not justify extraction for a 4-line state block; inlining keeps all three consumers identical and avoids a new module. Do NOT touch any other field in either form (name/email/password/role/bio). Do NOT change the Zod schemas (avatar stays `z.string().optional().or(z.literal(""))` — the picker writes a URL string or empty, same as before).

    Gate hygiene note: the positive greps in acceptance_criteria count occurrences of component/button identifiers that the swap intentionally introduces; they are self-fulfilling once the edit lands.
  </action>
  <verify>
    <automated>
      pnpm build &&
      pnpm test &&
      test "$(grep -c 'MediaPicker' 'src/app/(admin)/dashboard/users/UserDrawer.tsx')" -ge 1 &&
      test "$(grep -c 'MediaPicker' 'src/app/(admin)/dashboard/profile/ProfileForm.tsx')" -ge 1 &&
      test "$(grep -c 'Select image' 'src/app/(admin)/dashboard/users/UserDrawer.tsx')" -ge 1 &&
      test "$(grep -c 'Select image' 'src/app/(admin)/dashboard/profile/ProfileForm.tsx')" -ge 1 &&
      test "$(grep -c 'avatarPickerOpen' 'src/app/(admin)/dashboard/users/UserDrawer.tsx')" -ge 1 &&
      test "$(grep -c 'avatarPickerOpen' 'src/app/(admin)/dashboard/profile/ProfileForm.tsx')" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MediaPicker' src/app/(admin)/dashboard/users/UserDrawer.tsx` returns >= 1 (picker element + import present).
    - `grep -c 'MediaPicker' src/app/(admin)/dashboard/profile/ProfileForm.tsx` returns >= 1.
    - `grep -c 'Select image' src/app/(admin)/dashboard/users/UserDrawer.tsx` returns >= 1 (the open-picker button replaced the text input).
    - `grep -c 'Select image' src/app/(admin)/dashboard/profile/ProfileForm.tsx` returns >= 1.
    - `grep -c 'avatarPickerOpen' src/app/(admin)/dashboard/users/UserDrawer.tsx` returns >= 2 (useState decl + at least one reference).
    - `grep -c 'avatarPickerOpen' src/app/(admin)/dashboard/profile/ProfileForm.tsx` returns >= 2.
    - `grep -c "setValue" src/app/(admin)/dashboard/users/UserDrawer.tsx` returns >= 1 (setValue added to destructure and used in onSelect).
    - `grep -c "setValue" src/app/(admin)/dashboard/profile/ProfileForm.tsx` returns >= 1.
    - `pnpm build` exits 0 (no TypeScript errors from the new imports / next/image usage).
    - `pnpm test` exits 0 with 243/243 passing (no regression to users.test.ts / profile / auth-gate / any existing suite).
    - The CDN-URL placeholder text that advertised the text-input fallback is gone from both avatar fields (replaced by the picker UX, not supplemented).
    - No raw `<img>` tag introduced — the thumbnail preview uses next/image `Image` (project hard rule).
    - The MediaPicker.tsx component file is NOT modified (reuse, not duplication).
  </acceptance_criteria>
  <done>
    Both avatar fields (UserDrawer + ProfileForm) render the reusable <MediaPicker> modal as the sole avatar entry path, mirroring PostForm's feature-image wiring (useState open-state, watch preview, setValue on select, Replace/Remove buttons, Select-image empty state). The Rule-3 text-input fallback is removed. Build is green and the full test suite passes (243/243). Plan 04-03 must_have truth row 20 is now satisfied, closing the only verification gap.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` exits 0 — both edited files compile; next/image import resolves; MediaPicker default import resolves.
- `pnpm test` exits 0, 243/243 — no regression to users.test.ts (6 listUsers/updateUser cases), auth-gate, profile, or any other suite.
- Structural greps confirm the picker + button + state are present in both files (see acceptance_criteria).
- The 04-VERIFICATION.md failed truth (row 20) is now achievable: re-running the verifier's grep on both files would find <MediaPicker> where the text input used to be.
</verification>

<success_criteria>
- Plan 04-03 must_have "Avatar field uses the <MediaPicker> modal from Plan 04-02" is satisfied in BOTH consumers.
- DASH-04 reaches full plan-fidelity (management capability was already delivered; the UX downgrade is removed).
- Zero new files, zero new tests required (this reuses a tested component and mirrors an established, already-tested wiring pattern), zero schema/migration changes.
- The single verification gap is closed without scope creep — no other plan, field, or component is touched.
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-chrome/04-06-SUMMARY.md` when done. Re-run `gsd verify-phase 04` (or the project's verifier) to confirm 32/32 must-haves.
</output>
