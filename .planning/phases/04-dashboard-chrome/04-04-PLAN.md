---
phase: 04-dashboard-chrome
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/actions/pages.ts
  - src/actions/pages-schema.ts
  - src/actions/__tests__/pages.test.ts
  - src/lib/storage/seed.ts
  - src/app/(admin)/dashboard/pages/page.tsx
  - src/app/(admin)/dashboard/pages/[id]/edit/page.tsx
  - src/app/(admin)/dashboard/pages/PageForm.tsx
autonomous: true
requirements: [DASH-05, DASH-06]
user_setup: []

must_haves:
  truths:
    - "GET /dashboard/pages renders a TailAdmin table listing all (non-soft-deleted) pages with their status (draft/published)"
    - "GET /dashboard/pages/[id]/edit renders a slimmed Tiptap editor (title + slug + body + SEO fields + status) — drops post-only fields (category, tags, excerpt, feature-image, schedule, preview token)"
    - "Page status is draft OR published only — NO pending_review (D-20 — legal/contact content does not need editorial review)"
    - "Page body uses the same Tiptap JSON → HTML render pipeline + same lib/sanitize config as posts (CLAUDE.md hard rule — sanitize raw HTML before storage AND render; no exception for trusted admin content)"
    - "T&C + Privacy + Contact pages are seeded at migration (D-17) — title + slug + empty body + draft status. Admin just edits content"
    - "About page is HARD-CODED TSX/MDX (PROJECT.md) — NOT a pages row (D-17)"
    - "Contact page = content-only here (intro text, address, hours). The Contact FORM (SMTP/honeypot/rate-limit) is Phase 6 SITE-10 — NOT pulled forward (D-19)"
    - "Page actions (createPage/updatePage/listPages/softDeletePage) all start with requireCan({ page: [...] }) — Phase 2 Pitfall #1"
    - "Page save uses RHF + Zod + TanStack useMutation (D-26); page save IS optimistic (D-27 — high-frequency small mutation)"
  artifacts:
    - "src/actions/pages.ts (NEW — pages CRUD with permission-check-first)"
    - "src/actions/pages-schema.ts (NEW — Zod v4 schema shared client+server; status enum: draft | published only)"
    - "src/actions/__tests__/pages.test.ts (NEW — Wave 0 stub from VALIDATION.md; covers permission-check-first + draft/published only + soft-delete)"
    - "src/lib/storage/seed.ts (EXTEND — idempotent page seed for T&C + Privacy + Contact, ON CONFLICT DO NOTHING)"
    - "src/app/(admin)/dashboard/pages/page.tsx (NEW — list table)"
    - "src/app/(admin)/dashboard/pages/[id]/edit/page.tsx (NEW — slimmed editor shell)"
    - "src/app/(admin)/dashboard/pages/PageForm.tsx (NEW — slimmed RHF + Zod + Tiptap form)"
  key_links:
    - "PageForm → useMutation wrapping savePage; optimistic per D-27 (page save = high-frequency small mutation)"
    - "Page body render → @/lib/sanitize (shared DOMPurify config from Phase 3) — pages body sanitized before storage AND before render"
    - "PageForm Tiptap editor → reuses the Phase 3 lazy-loaded TiptapEditor + EditorProvider + extensions.ts (single source of truth, slimmed — drops category/tags/excerpt/feature-image/schedule/preview)"
    - "Seed migration runs via instrumentation.ts (existing seedStorageSettings pattern) — idempotent INSERT ... ON CONFLICT DO NOTHING for the three pages"
  prohibitions:
    - "DO NOT add a pending_review status to pages (D-20 — legal/contact content does not need editorial review; draft | published only)"
    - "DO NOT pull the Contact FORM behavior forward from Phase 6 SITE-10 (D-19 — this phase edits Contact CONTENT only)"
    - "DO NOT seed the About page (D-17 / PROJECT.md — About stays hard-coded TSX/MDX, not a pages row)"
    - "DO NOT bypass the shared lib/sanitize config for page body (CLAUDE.md 'sanitize raw HTML before storage AND render — no exception for trusted admin content')"
    - "DO NOT modify the Tiptap editor itself — REUSE the Phase 3 component, slimmed (drop post-only fields in the PageForm wrapper, not in the editor)"
---

<objective>
Ship the Pages management surface for T&C, Privacy, and Contact content using the existing `pages` table (Phase 1 D-06) and the slimmed Phase 3 Tiptap editor (D-18). Add `actions/pages.ts` + `pages-schema.ts` following the permission-check-first pattern, seed the three legal/contact pages at migration (D-17), and wire a TailAdmin-quality list + edit page using the RHF + Zod + TanStack Query pattern (D-26).

Purpose: Phase 1 created the `pages` table with its own SEO columns. Phase 3 validated the Tiptap JSON → HTML → sanitize render pipeline. This plan composes both into a dashboard surface where an editor can manage T&C/Privacy/Contact content without developer intervention — closing DASH-05 and D-17.

Output: `/dashboard/pages` (list) + `/dashboard/pages/[id]/edit` (slimmed editor) + `actions/pages.ts` + `pages-schema.ts` + Wave 0 `pages.test.ts` + idempotent seed for the three pages (D-17/D-29a).
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-chrome/04-CONTEXT.md
@.planning/phases/04-dashboard-chrome/04-RESEARCH.md
@.planning/phases/04-dashboard-chrome/04-PATTERNS.md
@.planning/phases/04-dashboard-chrome/04-01-SUMMARY.md
@.planning/phases/03-content-engine/03-01-SUMMARY.md
@.planning/phases/03-content-engine/03-02-SUMMARY.md

# Authoritative code to read before editing
@src/actions/categories.ts
@src/actions/posts.ts
@src/actions/posts-schema.ts
@src/actions/__tests__/taxonomy.test.ts
@src/db/schema.ts
@src/lib/storage/seed.ts
@src/lib/sanitize/index.ts
@src/components/editor/extensions.ts
@src/components/editor/EditorProvider.tsx
@src/app/(admin)/dashboard/posts/page.tsx
@src/app/(admin)/dashboard/posts/[id]/edit/page.tsx
@src/app/(admin)/dashboard/posts/PostForm.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: actions/pages.ts + pages-schema.ts + Wave 0 pages.test.ts + idempotent seed (D-17, D-20, D-29a)</name>
  <files>
    src/actions/pages.ts,
    src/actions/pages-schema.ts,
    src/actions/__tests__/pages.test.ts,
    src/lib/storage/seed.ts
  </files>
  <read_first>
    - src/actions/categories.ts (the canonical small-CRUD action template — listCategories / createCategory / updateCategory / softDeleteCategory; permission-check-first pattern line 29; soft-delete pattern lines 78-86)
    - src/actions/posts.ts (the larger CRUD template — listPosts / savePost / getPost; Zod-parse pattern line 99-100)
    - src/actions/posts-schema.ts (the schema-module template — NO "use server"/"use client" directive; pure schema module imported by both sides; SLUG_REGEX export; schema/types export)
    - src/actions/__tests__/taxonomy.test.ts (the test scaffold template — vi.hoisted + vi.mock("@/lib/db") + vi.mock("@/lib/permissions") + MUST_NOT_BE_REACHED pattern)
    - src/db/schema.ts (pages table — verify columns: id, title, slug, body, status, metaTitle, metaDescription, canonical, deletedAt; the pageStatusEnum is draft | published only — line 44)
    - src/lib/storage/seed.ts (the existing seedStorageSettings pattern — idempotent INSERT ... ON CONFLICT DO NOTHING; this Task extends with the page seed)
    - 04-CONTEXT.md D-17 (seed T&C + Privacy + Contact), D-18 (slimmed editor — drop post-only fields), D-19 (Contact = content-only here), D-20 (draft/published only), D-29 (seed-only migration)
    - 04-PATTERNS.md "src/actions/pages.ts" assignment (lines 180-225) + "src/actions/pages-schema.ts" assignment (lines 227-254)
    - 04-VALIDATION.md Wave 0 row "pages.test.ts — covers DASH-05/D-17/D-20"
  </read_first>
  <behavior>
    - pageSchema Zod v4 schema enforces: title (1-255 chars), slug (regex), body (Tiptap JSON optional), status (enum: draft | published — NO pending_review per D-20), metaTitle (≤255), metaDescription (≤500), canonical (URL or empty)
    - createPage(input) → requireCan({ page: ["create"] }) FIRST → parse input via pageSchema → db.insert(schema.pages)
    - updatePage(id, input) → requireCan({ page: ["update"] }) FIRST → parse → db.update
    - listPages() → requireCan({ page: ["read"] }) FIRST → db.select where deletedAt IS NULL
    - getPage(id) → requireCan({ page: ["read"] }) FIRST → db.select by id, throw NOT_FOUND if missing
    - softDeletePage(id) → requireCan({ page: ["delete"] }) FIRST → db.update set deletedAt = new Date()
    - Page body goes through lib/sanitize BEFORE storage (sanitizeBeforeStorage) — same shared config as posts
    - The T&C + Privacy + Contact rows exist post-seed (idempotent; safe to re-run)
    - Non-admin calling createPage → throws FORBIDDEN BEFORE db.insert (MUST_NOT_BE_REACHED test)
  </behavior>
  <action>
    **pages-schema.ts:** Create `src/actions/pages-schema.ts` per the 04-PATTERNS.md "pages-schema.ts" assignment lines 227-254. NO "use server" / "use client" directive. Export `SLUG_REGEX` (reuse from posts-schema OR re-declare — pick one and document). Export `pageSchema = z.object({ id: z.number().int().positive().optional(), title: z.string().min(1).max(255), slug: z.string().min(1).regex(SLUG_REGEX), body: z.any().optional() /* Tiptap JSON */, status: z.enum(["draft", "published"]).optional() /* D-20: NO pending_review */, metaTitle: z.string().max(255).optional(), metaDescription: z.string().max(500).optional(), canonical: z.string().url().optional().or(z.literal("")) })`. Export `PageSchemaInput` and `PageSchemaOutput` types.

    **pages.ts:** Create `src/actions/pages.ts` per the 04-PATTERNS.md "pages.ts" assignment lines 180-225. Start with `"use server"`. Import `{ db, schema } from "@/lib/db"`, `{ eq, isNull } from "drizzle-orm"`, `{ log } from "@/lib/log"`, `{ requireCan } from "@/lib/permissions"`, `{ sanitizeBeforeStorage } from "@/lib/sanitize"`, `{ pageSchema, type PageSchemaInput } from "./pages-schema"`. Implement `createPage`, `updatePage(id, input)`, `listPages`, `getPage(id)`, `softDeletePage(id)`. Each mutating action starts with `await requireCan({ page: [...] })` (Phase 2 Pitfall #1). On create/update, run `body` through `sanitizeBeforeStorage` BEFORE db.write (CLAUDE.md hard rule). Mirror the soft-delete pattern from `src/actions/categories.ts` lines 78-86 (set `deletedAt = new Date()`; reads exclude via `where(isNull(schema.pages.deletedAt))`).

    DO NOT add a `transitionPage` / `submitForReview` action (D-20 — pages use draft | published only, no review workflow).

    **pages.test.ts (Wave 0):** Create `src/actions/__tests__/pages.test.ts` per the 04-PATTERNS.md "src/actions/__tests__/pages.test.ts" assignment lines 630-645. Clone the mock scaffold from `src/actions/__tests__/taxonomy.test.ts` (or users.test.ts lines 14-81 — same vi.hoisted + vi.mock("@/lib/db") + vi.mock("@/lib/permissions") + vi.mock("@/lib/log") block). Add describe blocks for each action:
    - createPage: admin → inserts; non-admin → throws FORBIDDEN BEFORE db.insert (MUST_NOT_BE_REACHED pattern from users.test.ts lines 162-174); `requireCan({ page: ["create"] })` called with exact arg.
    - updatePage: same pattern with `["update"]`.
    - listPages: returns rows where deletedAt IS NULL (mock the select chain).
    - getPage: returns row OR throws NOT_FOUND for missing id.
    - softDeletePage: sets deletedAt; non-admin → FORBIDDEN BEFORE db.update.
    - Schema test: pageSchema rejects `status: "pending_review"` (D-20 — only draft | published accepted).

    **Seed (D-17):** Extend `src/lib/storage/seed.ts` with a new `seedPages()` function. Idempotent: `INSERT INTO pages (title, slug, body, status) VALUES (...), (...), (...) ON CONFLICT (slug) DO NOTHING` (drizzle equivalent: `db.insert(schema.pages).values([...]).onConflictDoNothing({ target: schema.pages.slug })`). Three rows: `{ title: "Terms & Conditions", slug: "terms-and-conditions", body: null, status: "draft" }`, `{ title: "Privacy Policy", slug: "privacy-policy", body: null, status: "draft" }`, `{ title: "Contact", slug: "contact", body: null, status: "draft" }`. Call `seedPages()` from the existing `seedStorageSettings` entry point (or whichever function instrumentation.ts calls at boot). Verify `src/lib/storage/seed.ts` is invoked from `src/instrumentation.ts` at boot — if not, wire it. NO drizzle-kit generate migration needed (D-29 seed-only — the pages table already exists from Phase 1; the seed is data, not schema). Confirm by running `pnpm test:migrations` — the clean-room migration test should still pass (no schema.ts change).

    DO NOT seed the About page (D-17 — About stays hard-coded TSX/MDX).
  </action>
  <verify>
    <automated>test -f "src/actions/pages.ts" && test -f "src/actions/pages-schema.ts" && test -f "src/actions/__tests__/pages.test.ts" </automated>
    <automated>grep -q 'export async function createPage' "src/actions/pages.ts" </automated>
    <automated>grep -q 'export async function updatePage' "src/actions/pages.ts" </automated>
    <automated>grep -q 'export async function listPages' "src/actions/pages.ts" </automated>
    <automated>grep -q 'export async function softDeletePage' "src/actions/pages.ts" </automated>
    <automated>grep -q 'pending_review' "src/actions/pages-schema.ts" && echo "FAIL: pending_review present" || echo "OK: no pending_review" </automated>
    <automated>grep -q 'seedPages' "src/lib/storage/seed.ts" </automated>
    <automated>pnpm test src/actions/__tests__/pages.test.ts -x </automated>
    <automated>pnpm test:migrations </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - actions/pages.ts exports createPage / updatePage / listPages / getPage / softDeletePage (all permission-check-first)
    - pages-schema.ts enforces draft | published only (rejects pending_review)
    - pages.test.ts Wave 0 stub complete; covers permission-check-first + draft/published only + soft-delete + NOT_FOUND
    - seedPages() idempotently inserts T&C / Privacy / Contact rows at boot
    - About NOT seeded
    - `pnpm test:migrations` passes (no schema.ts change)
    - `pnpm build` succeeds
  </done>
  <acceptance_criteria>
    - `test -f 'src/actions/pages.ts'` exits 0
    - `test -f 'src/actions/pages-schema.ts'` exits 0
    - `test -f 'src/actions/__tests__/pages.test.ts'` exits 0
    - `grep -c 'export async function createPage' 'src/actions/pages.ts'` returns 1
    - `grep -c 'export async function listPages' 'src/actions/pages.ts'` returns 1
    - `grep -c 'export async function softDeletePage' 'src/actions/pages.ts'` returns 1
    - `grep -cE "requireCan.*page" 'src/actions/pages.ts'` returns ≥4 (one per mutating action + listPages/getPage)
    - `grep -c 'pending_review' 'src/actions/pages-schema.ts'` returns 0 (D-20 honored)
    - `grep -c 'seedPages' 'src/lib/storage/seed.ts'` returns ≥1
    - `pnpm test src/actions/__tests__/pages.test.ts -x` exits 0
    - `pnpm test:migrations` exits 0 (no schema drift)
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pages list table + slimmed editor page (D-18, D-19, D-26)</name>
  <files>
    src/app/(admin)/dashboard/pages/page.tsx,
    src/app/(admin)/dashboard/pages/[id]/edit/page.tsx,
    src/app/(admin)/dashboard/pages/PageForm.tsx
  </files>
  <read_first>
    - src/app/(admin)/dashboard/posts/page.tsx (the list-page shell template)
    - src/app/(admin)/dashboard/posts/[id]/edit/page.tsx (the edit-page shell template — params Promise destructure, getPost try/catch + notFound fallback, render <PostForm initial*={...} />)
    - src/app/(admin)/dashboard/posts/PostForm.tsx (the RHF + Zod + useMutation baseline; the form to slim — drop post-only fields)
    - src/app/(admin)/dashboard/posts/schema-client.ts (the schema-client bridge template — re-export schema + zodResolver)
    - src/components/editor/{TiptapEditor,EditorProvider,extensions,toolbar/Toolbar}.tsx (the Phase 3 editor to REUSE, not rebuild — slim by wrapping, not editing)
    - src/lib/sanitize/index.ts (the shared sanitize config — page body uses the same pipeline)
    - 04-CONTEXT.md D-18 (slimmed editor — drop category/tags/excerpt/feature-image/schedule/preview; keep title/slug/body/SEO/status), D-19 (Contact = content-only), D-26 (RHF + Zod + TanStack Query)
    - 04-PATTERNS.md "src/app/(admin)/dashboard/pages" assignment (lines 368-408)
  </read_first>
  <behavior>
    - GET /dashboard/pages renders a TailAdmin table (title, slug, status badge — Draft/Published only, no Pending Review; updated-at, actions: edit + soft-delete)
    - Click edit → /dashboard/pages/[id]/edit → renders PageForm prefilled with the page's current data
    - PageForm fields: title, slug, body (TiptapEditor — same lazy-loaded Phase 3 editor + extensions single source of truth), status (draft | published select), metaTitle, metaDescription, canonical — NO category/tags/excerpt/feature-image/schedule/preview-token fields
    - Page save uses useMutation wrapping savePage (createPage OR updatePage based on initial data); optimistic per D-27
    - Page body reuses the SAME sanitize-before-storage pipeline as posts (CLAUDE.md hard rule)
    - Page status badge shows Draft or Published ONLY (no Pending Review entry in the badge map)
    - The Contact page is editable like any other page (content-only — the form behavior is Phase 6 SITE-10, not pulled forward per D-19)
  </behavior>
  <action>
    **List page** (`src/app/(admin)/dashboard/pages/page.tsx`): Server Component. Mirror `src/app/(admin)/dashboard/posts/page.tsx` shell. Call `listPages()` in try/catch; pass rows to a client `<PagesTable>` component (inline OR separate file — pick based on size; if < 60 lines inline is fine). Drop the `STATUS_BADGE["pending_review"]` entry from the badge map (D-20). Columns: Title, Slug, Status (Draft/Published), Updated At, Actions (Edit link → `/dashboard/pages/${id}/edit`; soft-delete button calling `softDeletePage` via useMutation optimistic per D-27).

    **Edit page** (`src/app/(admin)/dashboard/pages/[id]/edit/page.tsx`): Server Component. Mirror `src/app/(admin)/dashboard/posts/[id]/edit/page.tsx` lines 31-50. Destructure `params: Promise<{ id: string }>` (Next 16 async params). Call `getPage(id)` in try/catch; on error → `notFound()`. Pass to `<PageForm initial={page} />`. Drop the `SchedulePicker` + `PreviewLink` sidebar (post-only per D-18).

    **PageForm.tsx** (`"use client"`): Slimmed version of PostForm.tsx. RHF + Zod via `zodResolver(pageSchema)` from a new `schema-client.ts` (mirror `src/app/(admin)/dashboard/posts/schema-client.ts` — 12-line bridge re-exporting the schema + zodResolver). Fields:
    - `<input {...register("title")}>` — title
    - `<input {...register("slug")}>` — slug
    - `<EditorProvider initial={initial?.body}>{<TiptapEditor />}</EditorProvider>` — body (lazy-loaded via `next/dynamic({ ssr: false })` — copy the import shape from PostForm.tsx; uses the SAME extensions.ts single source of truth from Phase 3)
    - `<select {...register("status")}>` — status (options: draft, published ONLY — no pending_review)
    - `<input {...register("metaTitle")}>` — metaTitle
    - `<textarea {...register("metaDescription")}>` — metaDescription
    - `<input {...register("canonical")}>` — canonical URL (optional)

    On submit: `useMutation({ mutationFn: (values) => initial ? updatePage(initial.id, values) : createPage(values), onMutate: optimistic, onError: rollback, onSuccess: invalidate ['pages'] })` — D-27 optimistic (page save = high-frequency small mutation). The `savePage` wrapper inside PageForm dispatches create vs update based on `initial` presence. The body field uses `setValue('body', editor.getJSON())` on editor change (mirror PostForm's editor-change handler).

    DO NOT modify the Tiptap editor itself (Phase 3 component). DO NOT add category/tags/excerpt/feature-image/schedule/preview-token fields (D-18 drops them). DO NOT add a Contact-form behavior field (D-19 — content-only). All image inserts inside the body go through the existing Toolbar image button (which Plan 04-02 wired to `<MediaPicker>`).

    Create a small `src/app/(admin)/dashboard/pages/schema-client.ts` if you prefer the schema-bridge pattern (matches posts); alternatively inline the zodResolver import. Pick one and document.
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/dashboard/pages/page.tsx" && test -f "src/app/(admin)/dashboard/pages/[id]/edit/page.tsx" && test -f "src/app/(admin)/dashboard/pages/PageForm.tsx" </automated>
    <automated>grep -q 'useMutation' "src/app/(admin)/dashboard/pages/PageForm.tsx" </automated>
    <automated>grep -q 'TiptapEditor\|EditorProvider' "src/app/(admin)/dashboard/pages/PageForm.tsx" </automated>
    <automated>! grep -q 'pending_review' "src/app/(admin)/dashboard/pages/page.tsx" </automated>
    <automated>! grep -qE 'CategoryPicker|TagPicker|featureImage|SchedulePicker|previewToken' "src/app/(admin)/dashboard/pages/PageForm.tsx" </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - `/dashboard/pages` renders the table over `listPages()` (Draft/Published only, no Pending Review)
    - `/dashboard/pages/[id]/edit` renders the slimmed editor with prefilled data
    - PageForm uses RHF + Zod + useMutation (optimistic per D-27)
    - PageForm reuses the Phase 3 Tiptap editor (extensions.ts single source of truth)
    - PageForm drops post-only fields (no category/tags/excerpt/feature-image/schedule/preview)
    - Page body uses the same lib/sanitize pipeline as posts
    - Contact page is content-only (no form-builder UI — D-19)
    - `pnpm build` succeeds
    - Manual: dark mode renders correctly on /dashboard/pages + /dashboard/pages/[id]/edit (DASH-08)
  </done>
  <acceptance_criteria>
    - `test -f 'src/app/(admin)/dashboard/pages/page.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/pages/[id]/edit/page.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/pages/PageForm.tsx'` exits 0
    - `grep -c 'useMutation' 'src/app/(admin)/dashboard/pages/PageForm.tsx'` returns ≥1
    - `grep -cE 'TiptapEditor|EditorProvider' 'src/app/(admin)/dashboard/pages/PageForm.tsx'` returns ≥1 (Phase 3 editor reused)
    - `grep -c 'pending_review' 'src/app/(admin)/dashboard/pages/page.tsx'` returns 0 (D-20 honored)
    - `grep -cE 'CategoryPicker|TagPicker|featureImage|SchedulePicker|previewToken' 'src/app/(admin)/dashboard/pages/PageForm.tsx'` returns 0 (D-18 slimmed)
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→pages Server Actions | Every mutating action calls requireCan({ page: [...] }) FIRST (Phase 2 Pitfall #1) — UI hiding via sidebar is supplementary |
| Tiptap body → render | Page body sanitized before storage AND before render via the shared lib/sanitize config (CLAUDE.md hard rule — no exception for trusted admin content) |
| Page seed → DB | Idempotent ON CONFLICT DO NOTHING — safe to re-run; cannot overwrite admin-edited content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Elevation of privilege | Non-admin calling createPage / updatePage / softDeletePage | high | mitigate | Each action starts with requireCan({ page: [...] }) FIRST (Phase 2 Pitfall #1); MUST_NOT_BE_REACHED test proves ordering. Sidebar (Plan 04-01) is supplementary |
| T-04-17 | Tampering / XSS | Page body Tiptap JSON → HTML render | high | mitigate | Page body runs through the SAME lib/sanitize config as posts (Phase 3 D-02 — double-sanitize at storage AND render). CLAUDE.md "no exception for trusted admin content" |
| T-04-18 | Tampering | Slug collision / canonical URL | low | mitigate | pageSchema enforces SLUG_REGEX + URL validation; existing slug-validation utilities (Phase 3) reused. ON CONFLICT DO NOTHING on the seed prevents duplicate slugs |
| T-04-19 | Information disclosure | Draft pages visible to non-admins | low | accept | listPages calls requireCan({ page: ["read"] }) — any authenticated team member can see draft legal/contact pages (small team, per CLAUDE.md). Public draft visibility is Phase 6 SITE-15 (preview token) — out of scope here |
| T-04-20 | Repudiation | Page save optimistic UI rollback | low | mitigate | onError rolls back the cache; server is source of truth. If save fails (FORBIDDEN), the form re-appears with the original data + error toast |
</threat_model>

<verification>
**Wave 2 gate (this plan):**
- `pnpm test src/actions/__tests__/pages.test.ts -x` passes (Wave 0 stub complete)
- `pnpm test:migrations` passes (no schema.ts change — D-29 seed-only)
- `pnpm build` succeeds
- Manual: visit /dashboard/pages → confirm T&C, Privacy, Contact rows present (seed worked); edit each → confirm slimmed editor; publish; revert to draft
- Manual: page body with `<img src=x onerror=alert(1)>` is stripped on save AND on render (CLAUDE.md double-sanitize)
- Manual: dark mode renders correctly on /dashboard/pages + /dashboard/pages/[id]/edit (DASH-08)
</verification>

<success_criteria>
- DASH-05: Pages management UI (T&C, Privacy, Contact via slimmed Tiptap editor against the existing pages table) — fully satisfied
- DASH-06: RHF + Zod + TanStack Query applied to PageForm; optimistic per D-27 — extended
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-chrome/04-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New files:**
- `src/actions/pages.ts` — pages CRUD with permission-check-first (createPage / updatePage / listPages / getPage / softDeletePage)
- `src/actions/pages-schema.ts` — Zod v4 schema (shared client+server); status enum draft | published only (NO pending_review per D-20)
- `src/actions/__tests__/pages.test.ts` — Wave 0 test (permission-check-first + draft/published only + soft-delete + NOT_FOUND)
- `src/app/(admin)/dashboard/pages/{page.tsx,[id]/edit/page.tsx,PageForm.tsx,schema-client.ts}` — list table + slimmed editor

**Modified files:**
- `src/lib/storage/seed.ts` — extended with `seedPages()` (idempotent seed of T&C / Privacy / Contact per D-17; ON CONFLICT DO NOTHING on slug)

**No new deps** (Tiptap + RHF + Zod + TanStack Query all already installed in Phase 3 / Plan 04-01).

**No new env vars** in this plan.

**No new SQL migrations** (D-29 seed-only — pages table already exists from Phase 1; the seed is data not schema. `pnpm test:migrations` confirms no schema drift).
</output>
