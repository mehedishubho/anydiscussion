---
phase: 04-dashboard-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/dashboard/posts/page.tsx
  - src/app/(admin)/dashboard/posts/new/page.tsx
  - src/app/(admin)/dashboard/posts/[id]/edit/page.tsx
  - src/app/(admin)/dashboard/posts/PostForm.tsx
  - src/app/(admin)/dashboard/posts/schema-client.ts
  - src/app/(admin)/dashboard/posts/components/
  - src/app/(admin)/dashboard/calendar/page.tsx
  - src/app/(admin)/dashboard/profile/page.tsx
  - src/app/(admin)/dashboard/page.tsx
  - src/app/(admin)/QueryProvider.tsx
  - src/app/(admin)/AdminShell.tsx
  - src/layout/AppSidebar.tsx
  - scripts/test-auth-gate.mjs
autonomous: true
requirements: [DASH-01, DASH-06, DASH-07, DASH-08]
user_setup: []

must_haves:
  truths:
    - "Every admin page is reachable under the /dashboard/* URL prefix (no root-level /posts, /calendar, /profile)"
    - "The (admin)-scoped QueryClientProvider wraps AdminShell's children ONLY — never the root app/layout.tsx, never (site)"
    - "AppSidebar navItems contains ONLY the CMS nav (Posts, Categories, Tags, Media, Pages, Users, Settings, Profile, Calendar) plus a collapsed 'Components' reference group — no Ecommerce/Charts/Forms/Tables demo entries"
    - "Sidebar items filter by viewer role (authors don't see Users/Settings; editors don't see Users/Storage Settings) — UX layer only, server-side checks still authoritative"
    - "The /dashboard overview shows server-rendered real stats (posts-by-status counts, pending-review list, media count, 'New post' CTA) — no charts, no 'Dashboard content will be wired' placeholder"
    - "The (others-pages)/{blank,charts,forms,tables} demo routes AND their now-unused component files are deleted"
    - "The Phase 2 auth-gate test (scripts/test-auth-gate.mjs) passes after the route move AND after the overview placeholder text is replaced — the dashboardMarkers list and HTTP check string are updated in lockstep"
    - "PostForm.tsx wraps savePost in TanStack useMutation (mutation.isPending / mutation.error) — NOT optimistic (D-27)"
    - "Dark mode renders correctly on /dashboard, /dashboard/posts, /dashboard/profile, /dashboard/calendar (ThemeContext already provides it — verify, don't rebuild)"
  artifacts:
    - "src/app/(admin)/dashboard/posts/{page,new,[id]/edit,PostForm,schema-client}.tsx (moved from src/app/(admin)/posts/*)"
    - "src/app/(admin)/dashboard/calendar/page.tsx (moved from src/app/(admin)/(others-pages)/calendar)"
    - "src/app/(admin)/dashboard/profile/page.tsx (moved from src/app/(admin)/(others-pages)/profile)"
    - "src/app/(admin)/QueryProvider.tsx (NEW — TanStack QueryClient scoped to (admin) only)"
    - "DELETED: src/app/(admin)/(others-pages)/{blank,charts,forms,tables}/ AND now-unused demo components they imported"
  key_links:
    - "AdminShell.tsx wraps {children} with <QueryProvider> — QueryProvider is INSIDE AdminShell, never in app/layout.tsx"
    - "AppSidebar CMS nav hrefs all start with /dashboard/* (e.g. /dashboard/posts, /dashboard/categories, /dashboard/tags, /dashboard/media, /dashboard/pages, /dashboard/users, /dashboard/settings/storage, /dashboard/profile, /dashboard/calendar)"
    - "scripts/test-auth-gate.mjs dashboardMarkers array + HTTP check (line ~215) reference the NEW overview marker string, not the old 'Dashboard content will be wired' literal"
  prohibitions:
    - "DO NOT rename middleware.ts → proxy.ts (Pitfall 1 — Turbopack never registers proxy.ts in middleware-manifest.json)"
    - "DO NOT add QueryClientProvider to the root app/layout.tsx (would leak TanStack JS into the (site) public bundle — Pitfall / D-28)"
    - "DO NOT apply optimistic UI to post save mutations (D-27 — high-stakes + revalidation needs server confirmation)"
    - "DO NOT delete the (ui-elements)/ showcase or the calendar/profile features (D-02/D-03 — keep them as 'Components' reference + real tools)"
---

<objective>
Establish the dashboard shell + shared form/mutation pattern foundation for Phase 4. Per D-01, move every `(admin)` sub-page under a real `/dashboard/*` URL segment. Per D-02/D-03/D-05, replace the unmodified TailAdmin sidebar with a focused CMS nav (role-filtered) and delete unused chart/form/table demos. Per D-04, replace the overview placeholder with lean real-stats. Per D-26/D-28, scope a TanStack QueryClient to `(admin)` only and retrofit PostForm with `useMutation` as the dashboard-wide form/mutation baseline.

Purpose: This plan unblocks every later Phase 4 plan — they all add pages under `/dashboard/*` and reuse the RHF+Zod+TanStack Query pattern. Without this plan first, the other plans would create pages at the wrong URL prefix and miss the shared form pattern.

Output: Moved posts routes under `/dashboard/posts/*`; moved calendar/profile under `/dashboard/*`; deleted demo routes; replaced sidebar; lean overview page; `(admin)`-scoped `QueryProvider.tsx`; retrofitted `PostForm.tsx`; updated auth-gate test markers.

User story (Phase 4 goal, MVP-mode framing): **As a** team member (admin/editor/author), **I want to** manage the full content lifecycle through a polished TailAdmin dashboard wired to real data — posts, taxonomy, media, users/roles, dashboard-managed pages, AND the active image storage destination — **so that** I can run the blog end-to-end from the dashboard without touching code.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-chrome/04-CONTEXT.md
@.planning/phases/04-dashboard-chrome/04-RESEARCH.md
@.planning/phases/04-dashboard-chrome/04-PATTERNS.md
@.planning/phases/04-dashboard-chrome/04-VALIDATION.md
@.planning/todos/pending/2026-07-04-dashboard-route-prefix-restructure.md
@.planning/phases/03-content-engine/03-01-SUMMARY.md

# Authoritative code to read before editing
@middleware.ts
@src/layout/AppSidebar.tsx
@src/app/(admin)/AdminShell.tsx
@src/app/(admin)/layout.tsx
@src/app/(admin)/dashboard/page.tsx
@src/app/(admin)/posts/PostForm.tsx
@src/app/(admin)/posts/schema-client.ts
@scripts/test-auth-gate.mjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Route restructure — move posts/profile/calendar under /dashboard/* (D-01)</name>
  <files>
    src/app/(admin)/dashboard/posts/page.tsx,
    src/app/(admin)/dashboard/posts/new/page.tsx,
    src/app/(admin)/dashboard/posts/[id]/edit/page.tsx,
    src/app/(admin)/dashboard/posts/PostForm.tsx,
    src/app/(admin)/dashboard/posts/schema-client.ts,
    src/app/(admin)/dashboard/posts/components/ (move as-is),
    src/app/(admin)/dashboard/calendar/page.tsx,
    src/app/(admin)/dashboard/profile/page.tsx
  </files>
  <read_first>
    - middleware.ts (Pitfall 1 — DO NOT rename to proxy.ts; the matcher ALREADY targets /dashboard/:path*, no edit needed)
    - src/app/(admin)/posts/page.tsx (CURRENT path — to be moved; contains "/posts/new" + "/posts/${post.id}/edit" Link hrefs to update)
    - src/app/(admin)/posts/new/page.tsx
    - src/app/(admin)/posts/[id]/edit/page.tsx
    - src/app/(admin)/posts/PostForm.tsx (lines 60-67 savePost call — Task 3 will wrap in useMutation; for now move as-is)
    - src/app/(admin)/(others-pages)/profile/page.tsx
    - src/app/(admin)/(others-pages)/calendar/page.tsx
    - .planning/todos/pending/2026-07-04-dashboard-route-prefix-restructure.md (exact file list + decision context)
    - scripts/test-auth-gate.mjs (Task 3 will edit markers; read now to understand the coupling)
  </read_first>
  <behavior>
    - After the move, GET /posts returns 404 and GET /dashboard/posts returns the posts list
    - GET /dashboard/posts/new renders the post editor
    - GET /dashboard/posts/123/edit renders the edit page
    - GET /dashboard/profile renders the profile page (subsequent Plan 04-03 wires it to real data)
    - GET /dashboard/calendar renders the calendar page
    - The "+ New Post" button href = "/dashboard/posts/new" (was "/posts/new")
    - Per-row edit link href = "/dashboard/posts/${post.id}/edit" (was "/posts/${post.id}/edit")
    - Any router.push / router.replace calls referencing "/posts/*" or "/profile" / "/calendar" are prefixed with "/dashboard/"
  </behavior>
  <action>
    Per D-01, perform a file-system move of route folders under the existing `(admin)/dashboard/` segment. The `(admin)` route group (parentheses) adds NO URL segment; the inner `dashboard` folder is the real URL segment. Concretely:

    1. Move `src/app/(admin)/posts/` → `src/app/(admin)/dashboard/posts/` (keep `page.tsx`, `new/page.tsx`, `[id]/edit/page.tsx`, `PostForm.tsx`, `schema-client.ts`, `components/` together). Use `git mv` so history is preserved.
    2. Move `src/app/(admin)/(others-pages)/profile/` → `src/app/(admin)/dashboard/profile/` (Phase 4 Plan 04-03 wires it to real data per D-09).
    3. Move `src/app/(admin)/(others-pages)/calendar/` → `src/app/(admin)/dashboard/calendar/`.
    4. In `src/app/(admin)/dashboard/posts/page.tsx`, find every `<Link href="/posts/...">` and `router.push("/posts/...")` and prefix with `/dashboard`. The "+ New Post" button (line ~50) and per-row edit link (line ~91) are the two known anchors — verify by grep.
    5. In `src/app/(admin)/dashboard/posts/new/page.tsx` and `[id]/edit/page.tsx`, update any internal hrefs / breadcrumbs referencing `/posts`.
    6. In `src/app/(admin)/dashboard/posts/PostForm.tsx`, update `router.push("/posts")` (success redirect after save) to `router.push("/dashboard/posts")`.

    DO NOT edit `middleware.ts` — the matcher is already `/dashboard/:path*` per the 18-line in-file comment (Pitfall 1). DO NOT rename `middleware.ts` → `proxy.ts` (Next.js 16.2.9 + Turbopack never registers `proxy.ts` in middleware-manifest.json; the team reverted in Phase 2 plan 02-05). DO NOT touch `(ui-elements)/` showcase per D-02.

    Leave the chart/form/table demos in `(others-pages)/` for Task 2 to delete (cleaner separation).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -Eq "(Compiled successfully|✓|Route \(app\))" && ! grep -q "Failed to compile" </automated>
    <automated>test -f "src/app/(admin)/dashboard/posts/page.tsx" && test -f "src/app/(admin)/dashboard/profile/page.tsx" && test -f "src/app/(admin)/dashboard/calendar/page.tsx" && ! test -d "src/app/(admin)/posts" </automated>
  </verify>
  <done>
    - `(admin)/dashboard/posts/{page,new/page,[id]/edit/page,PostForm,schema-client,components}` all exist at the new path
    - `(admin)/posts/` directory no longer exists
    - `(admin)/dashboard/profile/page.tsx` and `(admin)/dashboard/calendar/page.tsx` exist
    - `pnpm build` succeeds with no broken imports
    - No `href="/posts"` or `href="/profile"` or `href="/calendar"` references remain in the codebase (grep returns zero hits outside middleware matchers and historical comments)
    - `middleware.ts` filename unchanged; matcher unchanged
  </done>
  <acceptance_criteria>
    - `test -f "src/app/(admin)/dashboard/posts/page.tsx"` exits 0
    - `test -f "src/app/(admin)/dashboard/posts/PostForm.tsx"` exits 0
    - `test -f "src/app/(admin)/dashboard/profile/page.tsx"` exits 0
    - `test -f "src/app/(admin)/dashboard/calendar/page.tsx"` exits 0
    - `! test -d "src/app/(admin)/posts"` exits 0
    - `grep -rn 'href="/posts' src/app/\(admin\)/dashboard/` returns zero hits (all internal links reprefixed)
    - `pnpm build` exits 0
    - `middleware.ts` exists at repo root AND `proxy.ts` does NOT exist at repo root (Pitfall 1 honored)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sidebar CMS nav (D-02/D-05) + demo cleanup (D-03)</name>
  <files>
    src/layout/AppSidebar.tsx
  </files>
  <read_first>
    - src/layout/AppSidebar.tsx (CURRENT — unmodified TailAdmin default; navItems lines 29-64 contain "Ecommerce"/"Forms"/"Tables"/demo "Pages" subitems; othersItems lines 66-95 contain chart/blank demo entries)
    - src/app/(admin)/layout.tsx (how AppSidebar is mounted; how the session/role is available — needed for D-05 role filter)
    - src/app/(admin)/AdminShell.tsx (shell composition — AppSidebar is rendered here)
    - src/lib/permissions/index.ts ( getSessionOrThrow / role helpers — for the role-filter wiring)
    - .planning/phases/04-dashboard-chrome/04-PATTERNS.md → "AppSidebar.tsx" assignment (line 473-487)
    - 04-CONTEXT.md D-02 (CMS nav + collapsed Components group), D-03 (delete chart/form/table demos, keep calendar/profile/components), D-05 (role-filter UX layer only)
  </read_first>
  <behavior>
    - AppSidebar renders exactly these top-level items: Posts, Categories, Tags, Media, Pages, Users, Settings, Profile, Calendar (each linking to its /dashboard/* path)
    - A collapsed "Components" reference group preserves the (ui-elements) showcase (alerts, avatars, badges, buttons, images, modals, videos) per D-02 — NOT deleted
    - Role filter: authors do NOT see Users or Settings; editors do NOT see Users or Storage Settings; admins see everything. Filter is UX-only — server-side checks remain authoritative (per D-05 / CLAUDE.md "never rely on UI hiding alone")
    - The demo (others-pages)/{blank,charts,forms,tables} route folders are deleted AND their now-unused component files are removed
    - The (ui-elements)/ routes are PRESERVED (D-02)
    - Calendar + Profile route folders are PRESERVED (already moved by Task 1 to /dashboard/calendar and /dashboard/profile)
  </behavior>
  <action>
    Per D-02, replace the entire `navItems` array (lines 29-64) and `othersItems` array (lines 66-95) with the CMS nav. Use the existing navItems item-shape (`{ name, icon, path, items? }` — match the existing TypeScript interface in the file). The new CMS nav top-level entries:
    - Posts → /dashboard/posts
    - Categories → /dashboard/categories
    - Tags → /dashboard/tags
    - Media → /dashboard/media
    - Pages → /dashboard/pages
    - Users → /dashboard/users (admin only — D-05 filter)
    - Settings → parent with child "Storage" → /dashboard/settings/storage (admin only — D-05 filter; the route ships in Plan 04-05, link can render disabled/hidden until then)
    - Profile → /dashboard/profile
    - Calendar → /dashboard/calendar

    Plus a collapsed "Components" reference group (`othersItems` replacement) listing the (ui-elements) routes prefixed under /dashboard — `alerts`, `avatars`, `badge`, `buttons`, `images`, `modals`, `videos`. (Move the (ui-elements) folder to `(admin)/dashboard/(ui-elements)/` OR keep at current path and prefix the sidebar hrefs — pick whichever is cleaner; document the choice in the file header comment.)

    Per D-05, add a role filter: pass the current user's role from `(admin)/layout.tsx` (where `getSessionOrThrow` runs) into AppSidebar as a prop, OR fetch the session client-side via the existing Better Auth client. Filter `navItems` via `navItems.filter(item => !item.requiredRole || hasRole(role, item.requiredRole))`. Add a clear code comment: "UX ONLY — every mutating Server Action still re-checks permissions server-side (Phase 2 Pitfall #1, CLAUDE.md 'never rely on UI hiding alone')". The role-filtering helper can be a small `hasRole(role, required)` pure function in this file.

    Per D-03, delete the now-unused demo routes. Concretely: delete `src/app/(admin)/(others-pages)/blank/`, `src/app/(admin)/(others-pages)/charts/`, `src/app/(admin)/(others-pages)/forms/`, `src/app/(admin)/(others-pages)/tables/`. Then grep their imports — delete any component file under `src/components/{charts,tables,form}/` that was used ONLY by these deleted demos. Keep `src/components/{form,tables}/` if any real Phase 4 page (Plan 04-02/04-03/04-04) will reuse them (the dashboard pages will). After deletion, run `pnpm build` to confirm no broken imports.

    The `ecommerce/` folder was already removed in Phase 1 — do not search for it.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
    <automated>! test -d "src/app/(admin)/(others-pages)/charts" && ! test -d "src/app/(admin)/(others-pages)/forms" && ! test -d "src/app/(admin)/(others-pages)/tables" && ! test -d "src/app/(admin)/(others-pages)/blank" </automated>
    <automated>grep -c 'Ecommerce\|line-chart\|bar-chart\|basic-tables\|form-elements' src/layout/AppSidebar.tsx | grep -q '^0$' </automated>
  </verify>
  <done>
    - AppSidebar renders only the CMS nav + collapsed Components group (no Ecommerce/Forms/Tables/Charts demo entries)
    - Role filter is wired and documented as UX-only
    - Chart/form/table/blank demo routes are deleted; their now-unused components are removed
    - (ui-elements) showcase is preserved (the "Components" collapsed group)
    - Calendar + Profile routes are preserved (moved by Task 1)
    - `pnpm build` succeeds with zero broken-import errors
  </done>
  <acceptance_criteria>
    - `grep -c 'Ecommerce' src/layout/AppSidebar.tsx` returns 0
    - `grep -c 'basic-tables\|form-elements\|line-chart\|bar-chart' src/layout/AppSidebar.tsx` returns 0
    - `test -d 'src/app/(admin)/(ui-elements)'` exits 0 (preserved per D-02)
    - `! test -d 'src/app/(admin)/(others-pages)/charts'` exits 0 (deleted per D-03)
    - `grep -c 'UX ONLY' src/layout/AppSidebar.tsx` returns ≥1 (D-05 comment present)
    - `grep -cE 'requireRole|hasRole' src/layout/AppSidebar.tsx` returns ≥1 (role filter wired)
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Lean overview (D-04) + (admin)-scoped QueryClient (D-28) + PostForm useMutation retrofit (D-26) + auth-gate test markers (Pitfall 5)</name>
  <files>
    src/app/(admin)/dashboard/page.tsx,
    src/app/(admin)/QueryProvider.tsx,
    src/app/(admin)/AdminShell.tsx,
    src/app/(admin)/dashboard/posts/PostForm.tsx,
    scripts/test-auth-gate.mjs
  </files>
  <read_first>
    - src/app/(admin)/dashboard/page.tsx (CURRENT placeholder — line 19 contains "Dashboard content will be wired to real data in Phase 4."; scripts/test-auth-gate.mjs line 87 + line 215 search for this literal)
    - src/app/(admin)/AdminShell.tsx (50 lines — wrap {children} with <QueryProvider> per D-28)
    - src/app/(admin)/layout.tsx (root for (admin) — already mounts AdminShell; do NOT add QueryProvider here, add INSIDE AdminShell)
    - src/app/(admin)/posts/PostForm.tsx (lines 39, 45, 60-67 — current useState submitError + direct savePost call to retrofit per D-26)
    - scripts/test-auth-gate.mjs (lines 86-91 dashboardMarkers array + line ~215 HTTP check — both search for "Dashboard content will be wired")
    - 04-RESEARCH.md "Pattern 3" (lines 398-425 — QueryProvider implementation) and "Pattern 4" (lines 437-444 — PostForm retrofit shape)
    - 04-VALIDATION.md "Per-Task Verification Map" rows 1 + 9 (auth gate + QueryClient scope)
  </read_first>
  <behavior>
    - GET /dashboard renders server-rendered real stats: posts-by-status counts (draft, pending_review, published), a short list of pending-review drafts needing action (title + author + edit link), media count, and a "New post" CTA linking to /dashboard/posts/new. No charts.
    - QueryProvider.tsx is a new client component that creates ONE QueryClient per mount (useState initializer), wraps children with QueryClientProvider, and renders ReactQueryDevtools ONLY when NODE_ENV === "development"
    - AdminShell.tsx wraps {children} with <QueryProvider> — QueryProvider is INSIDE AdminShell, never in app/layout.tsx
    - PostForm.tsx calls savePost via const mutation = useMutation({ mutationFn: savePost, onSuccess: invalidate ["posts"] }); isSubmitting → mutation.isPending; submitError → mutation.error?.message. NO optimistic UI on post save (D-27 explicit)
    - scripts/test-auth-gate.mjs dashboardMarkers array + HTTP check string both reference the new overview marker (e.g. "Pending review" or whatever real string the lean overview renders) — NOT the old "Dashboard content will be wired" literal
    - `pnpm test:auth-gate` exits 0 after the marker update
  </behavior>
  <action>
    **3a. Lean overview (D-04):** Replace `src/app/(admin)/dashboard/page.tsx` entirely. The page is a Server Component (no `"use client"`). Reuse the list-action-try-catch shell from `src/app/(admin)/dashboard/posts/page.tsx` lines 25-44 (PATTERNS.md "Server Component page + list-action call"). Call `listPosts()` from `@/actions/posts` THREE times filtered by status (or a single `listPosts()` then partition client-side — pick whichever the existing `listPosts` signature supports; read `src/actions/posts.ts` to confirm). Also call a media count — if no `countMedia` action exists, derive from `listMedia()` length (Plan 04-02 may add a dedicated `countMedia` later; for now `listMedia().length` is fine for a small team dashboard). Render: a stat-tile row (3 tiles: Draft / Pending Review / Published counts), a "Pending review needing action" list (max 5 items, each with title + author + edit link to `/dashboard/posts/[id]/edit`), a media-count tile, and a prominent "+ New post" button linking to `/dashboard/posts/new`. NO charts (D-04 explicit). NO client components. Verify the page renders a stable marker string (e.g. an `<h1>` or `<h2>` containing "Pending review" or "Dashboard overview") that Task 3c will reference from the auth-gate test.

    **3b. QueryProvider + AdminShell wiring (D-28):** Create `src/app/(admin)/QueryProvider.tsx` per 04-RESEARCH.md Pattern 3 lines 398-425. Mandatory: `"use client"` directive; `useState(() => new QueryClient({ defaultOptions: { queries: { staleTime: 30_000, refetchOnWindowFocus: false } } }))`; `{process.env.NODE_ENV === "development" && <ReactQueryDevtools />}`. Add `@tanstack/react-query-devtools` as a devDependency via `pnpm add -D @tanstack/react-query-devtools` (Discretionary per D-28/A6 — pick this option to enable devtools; if the install fails, skip devtools entirely with a code comment). In `src/app/(admin)/AdminShell.tsx`, wrap `{children}` with `<QueryProvider>{children}</QueryProvider>`. DO NOT modify `src/app/layout.tsx` — the root layout must stay free of TanStack JS (PERF-02 bundle isolation, audited in Phase 7). DO NOT add the provider inside `(site)`.

    **3c. PostForm useMutation retrofit (D-26):** In `src/app/(admin)/dashboard/posts/PostForm.tsx`, replace the manual try/catch submit handler (lines ~60-67) with a TanStack `useMutation` per 04-RESEARCH.md Pattern 4 lines 437-444: `const mutation = useMutation({ mutationFn: (values: PostSchemaInput) => savePost(values as Parameters<typeof savePost>[0]), onSuccess: () => queryClient.invalidateQueries({ queryKey: ["posts"] }) })`. Replace `isSubmitting` (line ~45) with `mutation.isPending`. Remove the `useState<string | null>(null)` for `submitError` (line ~39) — replace usages with `mutation.error?.message ?? null`. Add a code comment citing D-27: "NOT optimistic on post save — high-stakes mutation with revalidation needs server confirmation." The form fields and validation stay identical — only the submission wrapper changes.

    **3d. Auth-gate test marker sync (Pitfall 5):** In `scripts/test-auth-gate.mjs`, the `dashboardMarkers` array (lines 86-91) currently contains `"Dashboard content will be wired"` as its first entry. Task 3a removed that string from the overview page. Update the array entry to a string that DOES exist in the new lean overview (e.g. `"Pending review"` or whatever `<h1>`/`<h2>` heading the overview renders). Also update the HTTP check on line ~215 which independently searches for the same literal — both must reference the SAME new string. After the edit, run `pnpm test:auth-gate` — it must exit 0. If new `/dashboard/*` paths (e.g. `/dashboard/posts`) gain static prerender shells that the structural check (lines 65-106) doesn't cover, extend the route-keys list there too — but only if the test fails; the structural check is path-agnostic for the /dashboard root.

    The four sub-edits (3a overview, 3b QueryProvider, 3c PostForm retrofit, 3d test markers) are coupled by Pitfall 5 — the test marker update (3d) depends on the overview rewrite (3a). They MUST land in the same commit so the auth-gate test never goes red on main.
  </action>
  <verify>
    <automated>pnpm test:auth-gate </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
    <automated>test -f "src/app/(admin)/QueryProvider.tsx" && grep -q "QueryClientProvider" "src/app/(admin)/QueryProvider.tsx" </automated>
    <automated>grep -q 'useMutation' "src/app/(admin)/dashboard/posts/PostForm.tsx" </automated>
    <automated>! grep -q 'Dashboard content will be wired' "src/app/(admin)/dashboard/page.tsx" </automated>
  </verify>
  <done>
    - `/dashboard` renders the lean real-stats overview (stat tiles + pending-review list + New post CTA)
    - The "Dashboard content will be wired" placeholder string no longer exists anywhere in the codebase
    - QueryProvider.tsx exists, exports QueryClientProvider wrapped component, renders devtools only in dev
    - AdminShell.tsx wraps {children} with QueryProvider
    - PostForm.tsx uses `useMutation` for savePost (no manual try/catch submitError state)
    - scripts/test-auth-gate.mjs references the new overview marker (not the old placeholder)
    - `pnpm test:auth-gate` exits 0
    - `pnpm build` succeeds
    - Manual: dark mode renders correctly on /dashboard, /dashboard/posts, /dashboard/profile, /dashboard/calendar (D-06 verify, don't rebuild — DASH-08)
  </done>
  <acceptance_criteria>
    - `test -f 'src/app/(admin)/QueryProvider.tsx'` exits 0
    - `grep -c 'QueryClientProvider' 'src/app/(admin)/QueryProvider.tsx'` returns ≥1
    - `grep -c 'useState.*QueryClient\|new QueryClient' 'src/app/(admin)/QueryProvider.tsx'` returns ≥1
    - `grep -c 'useMutation' 'src/app/(admin)/dashboard/posts/PostForm.tsx'` returns ≥1
    - `grep -c 'Dashboard content will be wired' 'src/app/(admin)/dashboard/page.tsx'` returns 0
    - `grep -c 'Dashboard content will be wired' scripts/test-auth-gate.mjs` returns 0
    - `pnpm test:auth-gate` exits 0
    - `pnpm build` exits 0
    - `grep -c 'NOT optimistic' 'src/app/(admin)/dashboard/posts/PostForm.tsx'` returns ≥1 (D-27 comment present)
    - Manual: dark mode toggle works on /dashboard, /dashboard/posts/*, /dashboard/profile, /dashboard/calendar (no light-only fragments) — DASH-08 / D-06 verification
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→(admin) Server Component | Unauthenticated visitor hitting `/dashboard/*` is redirected by `middleware.ts` (UX gate) and again by `(admin)/layout.tsx` `getSession()` (authoritative) |
| sidebar→role check | AppSidebar role-filter is UX-only; the authoritative role check remains inside every Server Action (Phase 2 Pitfall #1) |
| QueryClient scope boundary | `(admin)`-only — must NOT leak into `(site)` (PERF-02 bundle isolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | middleware.ts auth gate | high | mitigate | DO NOT rename middleware.ts → proxy.ts (Pitfall 1 — Turbopack defect); matcher already `/dashboard/:path*`; verify with `pnpm test:auth-gate` after route move |
| T-04-02 | Elevation of privilege | AppSidebar role filter (D-05) | medium | mitigate | Role filter is documented "UX ONLY"; every mutating Server Action still calls `requireCan`/`requireRole` server-side (Phase 2 Pitfall #1). Cite D-05 + CLAUDE.md "never rely on UI hiding alone" in code comment |
| T-04-03 | Information disclosure | QueryClient bundle scope (D-28) | medium | mitigate | QueryProvider wraps `(admin)/AdminShell.tsx` ONLY — never root `app/layout.tsx`, never `(site)`. ESLint `no-restricted-imports` enforces. Phase 7 PERF-02 audits |
| T-04-04 | Tampering | PostForm retrofit (D-26) | low | accept | Retrofit replaces manual try/catch with useMutation; behavior identical. D-27 forbids optimistic UI on post save (high-stakes + revalidation) — code comment cites D-27 |
| T-04-SC | Tampering | npm/pnpm installs (react-query-devtools) | high | mitigate | `@tanstack/react-query-devtools` is the official TanStack package (already-installed `@tanstack/react-query` is its peer); legitimacy verified via npm registry in Phase 3 research. No [SUS] flag. Install with `pnpm add -D` |
</threat_model>

<verification>
**Wave 1 gate (this plan):**
- `pnpm build` succeeds with no broken imports after the route move + demo deletion
- `pnpm test:auth-gate` exits 0 (the marker sync in Task 3d keeps this green)
- `pnpm test` exits 0 (no regression in Phase 3 unit tests)
- Manual: dark mode renders on `/dashboard`, `/dashboard/posts`, `/dashboard/profile`, `/dashboard/calendar` (DASH-08/D-06)
- Manual: `/dashboard` shows real stats (not the placeholder)
- Manual: sidebar filters by role (sign in as author → Users/Settings hidden; sign in as editor → Users hidden; sign in as admin → all visible)

**Downstream unblocks:**
- Plan 04-02 (taxonomy + media + picker) — needs `/dashboard/*` prefix + QueryClient + PostForm baseline
- Plan 04-03 (users + profile) — needs `/dashboard/*` prefix + QueryClient
- Plan 04-04 (pages) — needs `/dashboard/*` prefix + QueryClient
</verification>

<success_criteria>
- DASH-01 (partial): posts routes relocated under `/dashboard/posts/*` (D-01) — full DASH-01 satisfaction comes from PostForm retrofit landing here + the OTHER dashboard pages landing in Plans 04-02..04-04
- DASH-06 (baseline): RHF + Zod + TanStack Query pattern established via the PostForm retrofit + `(admin)`-scoped QueryClient — Plans 04-02..04-04 apply it to their forms
- DASH-07: `ecommerce/` already removed in Phase 1; this plan deletes the chart/form/table demo routes + their now-unused components (D-03) and lazy-loads the editor (already lazy via Phase 3 `next/dynamic`)
- DASH-08: dark mode verified on the new pages (D-06 no-op-by-design)
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-chrome/04-01-SUMMARY.md` when done. Use the standard SUMMARY template (frontmatter + provides/affects/key-files/key-decisions/patterns-established/requirements-completed/coverage).
</output>

## Artifacts this phase produces

This plan (04-01) creates/modifies these symbols — list is exhaustive for plan-review-convergence drift-exclusion:

**New files:**
- `src/app/(admin)/QueryProvider.tsx` — TanStack QueryClient scoped to (admin) (D-28)

**Moved files (path change, content largely unchanged):**
- `src/app/(admin)/posts/*` → `src/app/(admin)/dashboard/posts/*` (D-01)
- `src/app/(admin)/(others-pages)/profile/page.tsx` → `src/app/(admin)/dashboard/profile/page.tsx` (D-01 + D-09 wiring lands in Plan 04-03)
- `src/app/(admin)/(others-pages)/calendar/page.tsx` → `src/app/(admin)/dashboard/calendar/page.tsx` (D-01)

**Modified files:**
- `src/layout/AppSidebar.tsx` — replaced navItems/othersItems with CMS nav + Components group + role filter (D-02/D-05)
- `src/app/(admin)/AdminShell.tsx` — wraps children with QueryProvider (D-28)
- `src/app/(admin)/dashboard/page.tsx` — replaced placeholder with lean real-stats overview (D-04)
- `src/app/(admin)/dashboard/posts/PostForm.tsx` — retrofit savePost with useMutation (D-26 baseline; D-27 non-optimistic)
- `src/app/(admin)/dashboard/posts/{page,new/page,[id]/edit/page}.tsx` — internal hrefs reprefixed `/dashboard/*` (D-01)
- `scripts/test-auth-gate.mjs` — dashboardMarkers + HTTP check string synced to new overview (Pitfall 5)

**Deleted files (D-03):**
- `src/app/(admin)/(others-pages)/blank/` directory
- `src/app/(admin)/(others-pages)/charts/` directory
- `src/app/(admin)/(others-pages)/forms/` directory
- `src/app/(admin)/(others-pages)/tables/` directory
- Any component under `src/components/{charts,form,tables}/` used ONLY by the deleted demos (verify by grep before deleting)

**Added deps (package.json):**
- `@tanstack/react-query-devtools` (devDependency, optional — discretion per D-28/A6)

**No new env vars in this plan** (`SETTINGS_ENCRYPTION_KEY` lands in Plan 04-05; cloudinary lands in Plan 04-05).
