---
phase: 04-dashboard-chrome
plan: 05
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/lib/crypto/index.ts
  - src/lib/crypto/__tests__/crypto.test.ts
  - src/lib/storage/types.ts
  - src/lib/storage/registry.ts
  - src/lib/storage/cloudinary.ts
  - src/lib/storage/push-cdn.ts
  - src/lib/storage/__tests__/cloudinary.test.ts
  - src/lib/storage/__tests__/push-cdn.test.ts
  - src/lib/storage/seed.ts
  - src/actions/storage-settings.ts
  - src/actions/storage-settings-schema.ts
  - src/actions/__tests__/storage-settings.test.ts
  - src/actions/media.ts
  - src/actions/__tests__/media.test.ts
  - src/instrumentation.ts
  - src/app/(admin)/dashboard/settings/storage/page.tsx
  - src/app/(admin)/dashboard/settings/storage/StorageSettingsForm.tsx
  - src/layout/AppSidebar.tsx
  - next.config.ts
  - .env.example
  - package.json
autonomous: true
requirements: [DASH-09]
user_setup:
  - service: SETTINGS_ENCRYPTION_KEY
    why: "32-byte base64 key for AES-256-GCM credential encryption (D-25). Without it, saveStorageSettings crashes on encrypt."
    env_vars:
      - name: SETTINGS_ENCRYPTION_KEY
        source: "Generate locally: node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\""
  - service: cloudinary
    why: "Optional — only needed to test the Cloudinary provider probe (D-24). The default local provider keeps uploads working without it."
    env_vars: []
    dashboard_config: []
  - service: push-CDN origin (S3-compatible)
    why: "Optional — only needed to test the push-CDN provider probe (D-24). The default local provider keeps uploads working without it."
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "An admin can open /dashboard/settings/storage and pick the active image destination (local / R2 / Cloudinary / push-CDN) and enter per-provider credentials"
    - "The save action re-checks requireRole('admin') server-side BEFORE any encryption or DB write (D-23 — UI hiding via sidebar is supplementary)"
    - "Provider credentials are AES-256-GCM encrypted at rest in settings.value via lib/crypto (D-25); the envelope is iv:authTag:ciphertext base64"
    - "The settings-read action returns redactCredentials(creds) — secret fields (matching /secret|key|token|password|api[-_]?key/i) come back as empty strings; the Storage Settings form NEVER pre-fills secrets (Pitfall 7)"
    - "Each provider has a 'Test connection' button that runs a no-op probe (list bucket / upload 1px / ping CDN) before Save persists (D-24)"
    - "Cloudinary provider (D-22) implements StorageProvider; upload via cloudinary.v2.uploader.upload_stream; bypasses sharp (Cloudinary owns transforms); getPublicUrl returns cloudinary.url(...) transform URLs"
    - "Push-CDN provider (D-21) implements StorageProvider; S3-compatible origin upload via @aws-sdk/client-s3 (reused) + cdnBaseUrl overlay on getPublicUrl; sharp 3-variant pipeline runs at upload"
    - "The deleteMedia bug (Pitfall 0) is FIXED — deleteMedia routes via getProviderByName(row.provider ?? 'local'), NOT getActiveProvider(). A row stored under provider='r2' is deleted via r2Provider even if active is cloudinary"
    - "next.config.ts images.remotePatterns includes res.cloudinary.com AND the operator-configurable push-CDN hostname (Pitfall 4)"
    - "The Cloudinary + push-CDN providers register at boot via instrumentation.ts → registerStorageProvider (no app restart needed when settings change)"
    - "Missing SETTINGS_ENCRYPTION_KEY → graceful failure (clear error on save, not a crash on app boot)"
  artifacts:
    - "src/lib/crypto/index.ts (NEW — encrypt / decrypt / redactCredentials; AES-256-GCM; key from SETTINGS_ENCRYPTION_KEY)"
    - "src/lib/crypto/__tests__/crypto.test.ts (NEW — Wave 0 from VALIDATION.md: round-trip + tamper detection + redact + missing-key graceful failure)"
    - "src/lib/storage/types.ts (EXTEND — widen name union: local | r2 | cloudinary | push-cdn)"
    - "src/lib/storage/registry.ts (EXTEND — add getProviderByName(name) synchronous lookup; existing registerStorageProvider + getActiveProvider unchanged)"
    - "src/lib/storage/cloudinary.ts (NEW — CloudinaryProvider; D-22)"
    - "src/lib/storage/push-cdn.ts (NEW — PushCdnProvider; D-21)"
    - "src/lib/storage/__tests__/cloudinary.test.ts (NEW — Wave 0: mock SDK, verify upload returns variants:[] + transform URLs)"
    - "src/lib/storage/__tests__/push-cdn.test.ts (NEW — Wave 0: mock S3Client, verify cdnBaseUrl overlay + sharp variants)"
    - "src/lib/storage/seed.ts (EXTEND — seed settings.storage.{cloudinary,push_cdn}_creds empty + storage.encryption_key_version = 1)"
    - "src/actions/storage-settings.ts (NEW — saveStorageSettings admin-gated + getStorageSettings redact-on-read + testStorageConnection probe)"
    - "src/actions/storage-settings-schema.ts (NEW — Zod v4 schema for the form; provider-name union)"
    - "src/actions/__tests__/storage-settings.test.ts (NEW — Wave 0: admin save persists encrypted blob; non-admin FORBIDDEN; redact-on-read)"
    - "src/actions/media.ts (EDIT — Pitfall 0 deleteMedia rewrite: route via getProviderByName)"
    - "src/actions/__tests__/media.test.ts (EXTEND — Pitfall 0 multi-provider delete case)"
    - "src/instrumentation.ts (EXTEND — register cloudinary + push-cdn providers at boot inside the NEXT_RUNTIME === 'nodejs' gate)"
    - "src/app/(admin)/dashboard/settings/storage/{page.tsx,StorageSettingsForm.tsx} (NEW — admin-only Storage Settings UI)"
    - "src/layout/AppSidebar.tsx (EDIT — wire the Storage child link under Settings; the link is in the sidebar from Plan 04-01 but the route was a placeholder until now)"
    - "next.config.ts (EDIT — images.remotePatterns adds res.cloudinary.com + the push-CDN base URL pattern)"
    - ".env.example (EDIT — add SETTINGS_ENCRYPTION_KEY with generation instructions)"
    - "package.json (cloudinary@2.10.0 added)"
  key_links:
    - "saveStorageSettings → requireRole('admin') FIRST → encrypt(credentials) via lib/crypto → db.update settings.storage.<provider>_creds + settings.storage.active_provider"
    - "getStorageSettings → requireRole('admin') FIRST → decrypt(credentials) → redactCredentials(...) → return to client (Pitfall 7)"
    - "testStorageConnection(provider, creds) → requireRole('admin') FIRST → switch on provider → no-op probe (ListObjectsV2Command MaxKeys:1 / cloudinary.v2.api.ping / fs.access local root)"
    - "registry.getActiveProvider → reads settings.storage.active_provider → returns providers[name] ?? providers.local (default-safe)"
    - "registry.getProviderByName(name) → returns providers[name] ?? providers.local (Pitfall 0 fix — used by deleteMedia)"
    - "instrumentation.ts register() → dynamic-import cloudinary + push-cdn providers → registerStorageProvider (inside NEXT_RUNTIME === 'nodejs' gate)"
    - "deleteMedia → getProviderByName(row.provider ?? 'local') (NOT getActiveProvider) → provider.delete(row.providerKey)"
    - "StorageSettingsForm defaultValues for secret fields = '' (empty) — never pre-filled (Pitfall 7)"
  prohibitions:
    - "DO NOT run sharp when Cloudinary is the active provider (D-22 — Cloudinary owns transforms at delivery URL time; sharp would double-transform)"
    - "DO NOT expose decrypted credentials to the client — every settings read that crosses the client boundary runs through redactCredentials (Pitfall 7 / D-25)"
    - "DO NOT apply optimistic UI to Storage Settings save (D-27 — credentials are high-stakes; server confirmation required)"
    - "DO NOT hard-fail app boot on missing SETTINGS_ENCRYPTION_KEY — fail gracefully at the save call (the local provider must keep working without encryption configured)"
    - "DO NOT use a paid Cloudinary feature / paid API (CLAUDE.md 'no paid third-party APIs' — the free tier + the SDK's upload_stream + URL transforms suffice)"
    - "DO NOT skip the deleteMedia rewrite (Pitfall 0) — DASH-09 makes multi-provider real; the active-provider delete bug becomes a live leak without the fix"
    - "DO NOT skip next.config.ts remotePatterns extension (Pitfall 4) — next/image 400s on unknown hostnames; Cloudinary + push-CDN URLs must be allowlisted"
---

<objective>
Ship the most technically nuanced cluster in Phase 4: DASH-09 Storage Settings + two new storage providers (Cloudinary + generic S3-compatible push-CDN) + AES-256-GCM credential encryption + the latent `deleteMedia` multi-provider bug fix. This plan is tightly bound — the providers, the encrypted settings read/write, the redaction, the Storage Settings form, the Pitfall 0 fix, and the next.config remotePatterns must all agree on a credential shape and provider-name union. Splitting them across distant slices would create integration drift, so they ship together.

Purpose: An admin can pick where images go (local / R2 / Cloudinary / push-CDN), enter credentials, and the system actually routes uploads through the chosen provider — without leaking secrets, leaking orphaned objects (Pitfall 0), or breaking next/image (Pitfall 4). Closes DASH-09 + D-21/D-22/D-23/D-24/D-25.

Output: Two new StorageProvider implementations, lib/crypto helper, admin-only Storage Settings page + actions, deleteMedia fix, next.config + instrumentation wiring, cloudinary install, .env.example update, Wave 0 tests for all new code paths.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-chrome/04-CONTEXT.md
@.planning/phases/04-dashboard-chrome/04-RESEARCH.md
@.planning/phases/04-dashboard-chrome/04-PATTERNS.md
@.planning/phases/04-dashboard-chrome/04-02-SUMMARY.md
@.planning/phases/03-content-engine/03-03-SUMMARY.md

# Authoritative code to read before editing
@src/lib/storage/types.ts
@src/lib/storage/registry.ts
@src/lib/storage/local.ts
@src/lib/storage/r2.ts
@src/lib/storage/seed.ts
@src/lib/storage/__tests__/registry.test.ts
@src/lib/r2/index.ts
@src/lib/log/index.ts
@src/lib/permissions/index.ts
@src/lib/db/index.ts
@src/actions/media.ts
@src/actions/settings.ts
@src/actions/__tests__/users.test.ts
@src/actions/__tests__/media.test.ts
@src/instrumentation.ts
@src/app/(admin)/dashboard/posts/PostForm.tsx
@src/app/(admin)/dashboard/posts/schema-client.ts
@src/layout/AppSidebar.tsx
@next.config.ts
@.env.example
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/crypto (D-25) + cloudinary install + .env.example + Wave 0 crypto test</name>
  <files>
    src/lib/crypto/index.ts,
    src/lib/crypto/__tests__/crypto.test.ts,
    .env.example,
    package.json
  </files>
  <read_first>
    - src/lib/log/index.ts (16 lines — the canonical "small isolated server-only lib helper" pattern to mirror for lib/crypto)
    - src/lib/r2/index.ts (lines 18-24 — the env-read-at-module-load pattern; mirror for SETTINGS_ENCRYPTION_KEY)
    - src/lib/storage/__tests__/registry.test.ts (the lib-helper test pattern; vitest.config.ts defaults to Node env — no jsdom pragma needed for crypto tests)
    - 04-RESEARCH.md "Pattern 2" (lines 354-391 — the canonical AES-256-GCM encrypt/decrypt/redactCredentials implementation; the redactCredentials regex /secret|key|token|password|api[-_]?key/i on line 388)
    - 04-RESEARCH.md Pitfall 2 (lines 503-507 — key rotation strategy; v1 simpler approach: document that rotation requires re-entering credentials)
    - 04-RESEARCH.md "Validation Architecture" Wave 0 row "crypto.test.ts"
    - 04-CONTEXT.md D-25 (encrypted at rest; never exposed to client; redact on read)
    - .env.example (current contents — extend, do not rewrite)
    - package.json (verify cloudinary NOT yet installed — this Task installs it)
  </read_first>
  <behavior>
    - lib/crypto/index.ts exports: encrypt(plaintext: string): string, decrypt(envelope: string): string, redactCredentials<T>(creds: T): T
    - encrypt produces envelope "ivB64:authTagB64:ciphertextB64" (base64, colon-separated) — fits settings.value text column with no schema change
    - decrypt reverses encrypt exactly; throws on tampered authTag (AES-256-GCM integrity)
    - redactCredentials zeroes secret fields (matching /secret|key|token|password|api[-_]?key/i — case-insensitive); preserves non-secret fields
    - Key from process.env.SETTINGS_ENCRYPTION_KEY (32-byte base64); missing key → graceful failure (encrypt throws a clear "SETTINGS_ENCRYPTION_KEY missing" error AT CALL TIME, NOT at module load — the local provider must keep working without encryption configured)
    - pnpm test src/lib/crypto/__tests__/crypto.test.ts passes (round-trip + tamper detection + redact + missing-key graceful failure)
    - cloudinary@2.10.0 added to package.json via pnpm add cloudinary@2.10.0
    - .env.example contains SETTINGS_ENCRYPTION_KEY= placeholder + generation instructions
  </behavior>
  <action>
    **Install cloudinary:** `pnpm add cloudinary@2.10.0` (verified per 04-RESEARCH.md Package Legitimacy Audit — official repo, 831K weekly downloads, OK verdict, no [SUS] flag, no checkpoint required).

    **lib/crypto/index.ts:** Create `src/lib/crypto/index.ts` mirroring the lib/log/index.ts module shape (single-file barrel, NO "use client" directive — server-only, one-line purpose comment at top citing D-25). Implement `encrypt`, `decrypt`, `redactCredentials` per 04-RESEARCH.md "Pattern 2" lines 354-391 verbatim. Key handling:
    - Read `process.env.SETTINGS_ENCRYPTION_KEY` lazily INSIDE `encrypt`/`decrypt` (NOT at module load) — so the module loads cleanly even when the env var is missing.
    - If missing OR not 32 bytes: throw a clear Error("SETTINGS_ENCRYPTION_KEY missing or invalid — generate with: node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\"") inside encrypt/decrypt.
    - The redactCredentials helper uses the regex `/secret|key|token|password|api[-_]?key/i` (line 388) — copy verbatim. Add a JSDoc note that this is the canonical secret-field matcher (consistent across storage-settings + future backup settings).

    **Wave 0 crypto test** (`src/lib/crypto/__tests__/crypto.test.ts`): Mirror src/lib/storage/__tests__/registry.test.ts structure. Vitest environment = Node (default — no jsdom pragma). Set a known `SETTINGS_ENCRYPTION_KEY` in beforeAll/beforeEach via `vi.stubEnv("SETTINGS_ENCRYPTION_KEY", knownTestKey)`. Test cases:
    1. Round-trip: `decrypt(encrypt(plaintext)) === plaintext` for several plaintexts including JSON credential blobs.
    2. Tamper detection: flip one byte of the authTag → `decrypt` throws (AES-256-GCM integrity).
    3. redactCredentials zeroes secret fields: input `{ accessKeyId: "AKIA", secretAccessKey: "shh", bucket: "media" }` → output `{ accessKeyId: "AKIA", secretAccessKey: "", bucket: "media" }`. Test multiple field-name variants (apiKey, api_key, token, password) to verify the regex.
    4. Missing-key graceful failure: `vi.stubEnv("SETTINGS_ENCRYPTION_KEY", "")` → `encrypt("x")` throws the clear "missing" error (NOT a generic Node crypto error). Restore the env after.

    **.env.example extension:** Append `SETTINGS_ENCRYPTION_KEY=` placeholder + an inline comment with the generation command. Do NOT commit a real key.

    Verify package legitimacy is already established per 04-RESEARCH.md Package Legitimacy Audit (cloudinary verdict OK).
  </action>
  <verify>
    <automated>pnpm add cloudinary@2.10.0 2>&1 | tail -5 </automated>
    <automated>test -f "src/lib/crypto/index.ts" && test -f "src/lib/crypto/__tests__/crypto.test.ts" </automated>
    <automated>grep -q 'encrypt' "src/lib/crypto/index.ts" && grep -q 'decrypt' "src/lib/crypto/index.ts" && grep -q 'redactCredentials' "src/lib/crypto/index.ts" </automated>
    <automated>grep -q 'aes-256-gcm' "src/lib/crypto/index.ts" </automated>
    <automated>grep -q 'SETTINGS_ENCRYPTION_KEY' ".env.example" </automated>
    <automated>grep -q '"cloudinary"' "package.json" </automated>
    <automated>pnpm test src/lib/crypto/__tests__/crypto.test.ts -x </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - lib/crypto/index.ts exports encrypt/decrypt/redactCredentials
    - AES-256-GCM verified by tamper-detection test
    - redactCredentials zeroes secret fields per the canonical regex
    - Missing-key → graceful failure (clear error at call time, not module load)
    - .env.example updated with SETTINGS_ENCRYPTION_KEY
    - cloudinary@2.10.0 in package.json
    - pnpm test src/lib/crypto passes
    - pnpm build succeeds
  </done>
  <acceptance_criteria>
    - `test -f 'src/lib/crypto/index.ts'` exits 0
    - `test -f 'src/lib/crypto/__tests__/crypto.test.ts'` exits 0
    - `grep -c 'aes-256-gcm' 'src/lib/crypto/index.ts'` returns ≥1
    - `grep -cE 'export function encrypt|export function decrypt|export function redactCredentials' 'src/lib/crypto/index.ts'` returns ≥3
    - `grep -c 'SETTINGS_ENCRYPTION_KEY' '.env.example'` returns ≥1
    - `grep -c '"cloudinary"' 'package.json'` returns ≥1
    - `pnpm test src/lib/crypto/__tests__/crypto.test.ts -x` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cloudinary + push-CDN providers + types/registry extension + Pitfall 0 deleteMedia fix + instrumentation register + next.config remotePatterns + Wave 0 tests (D-21, D-22, Pitfall 0, Pitfall 4)</name>
  <files>
    src/lib/storage/types.ts,
    src/lib/storage/registry.ts,
    src/lib/storage/cloudinary.ts,
    src/lib/storage/push-cdn.ts,
    src/lib/storage/__tests__/cloudinary.test.ts,
    src/lib/storage/__tests__/push-cdn.test.ts,
    src/lib/storage/seed.ts,
    src/actions/media.ts,
    src/actions/__tests__/media.test.ts,
    src/instrumentation.ts,
    next.config.ts
  </files>
  <read_first>
    - src/lib/storage/types.ts (the StorageProvider interface — line 31 has the comment "Phase-4 DASH-09 adds 'cloudinary' | 'push-cdn'"; widen the name union)
    - src/lib/storage/registry.ts (the existing providers map line 32-35 + registerStorageProvider line 48-53 + getActiveProvider line 69-78 — add getProviderByName as a sibling)
    - src/lib/storage/r2.ts (full file — the closest provider analog for both new providers; copy the singleton shape)
    - src/lib/storage/local.ts (full file — the sharp 3-variant pipeline lines 66-101 to reuse in push-cdn)
    - src/lib/r2/index.ts (lines 13-31 — the proven S3Client-with-custom-endpoint pattern for push-cdn)
    - src/lib/storage/seed.ts (extend with new settings keys)
    - src/lib/storage/__tests__/registry.test.ts (286 lines — the test pattern; Wave 0 cloudinary/push-cdn tests clone its mock structure)
    - src/actions/media.ts (CURRENT — Pitfall 0: lines 149-152 document the intended row-provider routing, line 172 incorrectly calls getActiveProvider; this Task rewrites line 172)
    - src/actions/__tests__/media.test.ts (CURRENT — 11 cases from Phase 3 + Plan 04-02's findMediaReferences extension; this Task adds the Pitfall 0 multi-provider delete case)
    - src/instrumentation.ts (lines 25-30 — the existing register() body inside NEXT_RUNTIME === 'nodejs'; extend with dynamic-import register calls)
    - next.config.ts (lines 22-29 — images.remotePatterns currently only cdn.anydiscussion.com + localhost:9000; extend with res.cloudinary.com + push-CDN pattern)
    - 04-RESEARCH.md "Code Examples" Example 1 (Cloudinary shape — lines 542-588), Example 2 (push-CDN shape — lines 591-637)
    - 04-RESEARCH.md Pitfall 0 (deleteMedia bug — lines 490-495), Pitfall 3 (Cloudinary upload_stream buffer handling — lines 509-513), Pitfall 4 (remotePatterns — lines 515-519)
    - 04-PATTERNS.md assignments for cloudinary.ts (lines 57-85), push-cdn.ts (lines 89-118), types.ts (lines 122-131), registry.ts (lines 135-154), media.ts deleteMedia rewrite (lines 523-540)
  </read_first>
  <behavior>
    - types.ts StorageProvider.name union includes "local" | "r2" | "cloudinary" | "push-cdn"
    - registry.ts exports getProviderByName(name) → returns providers[name] ?? providers.local (default-safe)
    - CloudinaryProvider.upload bypasses sharp (returns variants:[]); upload uses cloudinary.v2.uploader.upload_stream with Readable.from(buffer).pipe(stream) per Pitfall 3; getPublicUrl returns cloudinary.url(key, { transformation: [...] })
    - PushCdnProvider.upload runs the same sharp 3-variant pipeline as local/r2 (sm 640 / md 1024 / lg 1920, webp quality 80, fit:inside, withoutEnlargement); getPublicUrl overlays `${cdnBaseUrl}/${key}`
    - Both providers configure from encrypted settings (a configureCloudinary(creds) / configurePushCdn(creds) factory called by registry at boot)
    - deleteMedia routes via getProviderByName(row.provider ?? "local") — Pitfall 0 fixed
    - instrumentation.ts registers cloudinary + push-cdn providers at boot inside NEXT_RUNTIME === "nodejs" gate via dynamic-import
    - next.config.ts images.remotePatterns includes res.cloudinary.com + the push-CDN base URL (or a documented manual-edit step for operator-supplied CDN hostname)
    - Wave 0 cloudinary.test.ts + push-cdn.test.ts pass (mock SDK / S3Client; verify interface conformance + the provider-specific behaviors above)
    - Extended media.test.ts: Pitfall 0 case — row with provider="r2" deleted via r2Provider when active is cloudinary (proves row-routed deletion)
    - seed.ts seeds settings.storage.cloudinary_creds (empty blob), settings.storage.push_cdn_creds (empty blob), settings.storage.encryption_key_version = 1
  </behavior>
  <action>
    **types.ts (D-21/D-22):** Edit `src/lib/storage/types.ts` line 31 — widen `readonly name: "local" | "r2"` to `readonly name: "local" | "r2" | "cloudinary" | "push-cdn"`. Update the comment. No other contract changes — the upload/getPublicUrl/delete signatures stay identical.

    **registry.ts (Pitfall 0 fix):** Edit `src/lib/storage/registry.ts` — add a synchronous `export function getProviderByName(name: string | null | undefined): StorageProvider { return providers[name ?? "local"] ?? providers.local; }`. The providers map (line 32) already exists — this is a one-line lookup helper. Leave getActiveProvider + registerStorageProvider unchanged.

    **cloudinary.ts (D-22):** Create `src/lib/storage/cloudinary.ts` per 04-RESEARCH.md Example 1 lines 542-588 + 04-PATTERNS.md assignment lines 57-85. Export `cloudinaryProvider: StorageProvider` (name: "cloudinary") + a `configureCloudinary({ cloud_name, api_key, api_secret })` factory called by registry/instrumentation at boot. Pitfall 3: use `cloudinary.v2.uploader.upload_stream` with `Readable.from(buffer).pipe(stream)` for upload; return `variants: []` (Cloudinary owns transforms). getPublicUrl returns `cloudinary.url(key, { transformation: [{ fetch_format: "auto", quality: "auto", raw_transformation: variant === "sm" ? "w_640" : variant === "lg" ? "w_1920" : "w_1024" }] })`. delete uses `cloudinary.v2.uploader.destroy(key).catch(() => {})` (idempotent). Verify the exact v2 SDK signatures against the installed cloudinary@2.10.0 readme at implementation time (RESEARCH.md A1/A2 — marked [ASSUMED]; structural shape is correct regardless).

    **push-cdn.ts (D-21):** Create `src/lib/storage/push-cdn.ts` per 04-RESEARCH.md Example 2 lines 591-637 + 04-PATTERNS.md assignment lines 89-118. Export `pushCdnProvider: StorageProvider` (name: "push-cdn") + a `configurePushCdn({ endpoint, region, accessKeyId, secretAccessKey, bucket, cdnBaseUrl, forcePathStyle? })` factory. Reuse `@aws-sdk/client-s3` `S3Client` with custom endpoint (the proven lib/r2/index.ts pattern lines 13-31). The sharp 3-variant pipeline mirrors local.ts lines 66-101 (sm 640 / md 1024 / lg 1920, webp quality 80). getPublicUrl overlays `${cdnBaseUrl}/${key}` (strip trailing slash from cdnBaseUrl at configure time). delete uses `DeleteObjectCommand + .catch(() => {})` idempotent (mirrors r2.ts lines 67-76).

    **Wave 0 cloudinary.test.ts + push-cdn.test.ts:** Create per 04-VALIDATION.md Wave 0. Mirror registry.test.ts mock structure. Mock the cloudinary SDK (`vi.mock("cloudinary", ...)`) and `@aws-sdk/client-s3` (`vi.mock("@aws-sdk/client-s3", ...)`). Assertions:
    - Cloudinary: upload returns `variants: []` + correct `primary.key = public_id`; getPublicUrl produces a `https://res.cloudinary.com/...` URL with transform params; sharp is NEVER called in this provider's path (D-22).
    - Push-CDN: upload returns 3 sharp variants for image mime (sm/md/lg); getPublicUrl overlays `cdnBaseUrl`; non-image mime skips sharp.

    **media.ts deleteMedia rewrite (Pitfall 0):** Edit `src/actions/media.ts` line ~172 — replace `const provider = await getActiveProvider()` with `const provider = getProviderByName(row.provider ?? "local")` (synchronous — no await; the providers map is module-scoped). The intent was always documented in the file's own comment (lines 149-152); the implementation finally matches. Import `getProviderByName` from `@/lib/storage/registry`.

    **media.test.ts extension (Pitfall 0 case):** Add a new test block to `src/actions/__tests__/media.test.ts`: when `row.provider === "r2"` AND `getActiveProvider()` would return cloudinary (mock settings.storage.active_provider = "cloudinary"), `deleteMedia` calls `r2Provider.delete(...)` (NOT cloudinaryProvider.delete). Mock both providers; assert the r2Provider.delete was called AND cloudinaryProvider.delete was NOT called.

    **instrumentation.ts (register new providers):** Edit `src/instrumentation.ts` inside the existing `if (process.env.NEXT_RUNTIME === "nodejs")` block (lines 25-30). After the existing `startScheduler()`, dynamic-import + register the new providers: `const { registerStorageProvider, getActiveProvider } = await import("@/lib/storage/registry"); const { cloudinaryProvider, configureCloudinary } = await import("@/lib/storage/cloudinary"); const { pushCdnProvider, configurePushCdn } = await import("@/lib/storage/push-cdn"); registerStorageProvider("cloudinary", cloudinaryProvider); registerStorageProvider("push-cdn", pushCdnProvider);`. Optionally read encrypted creds from settings + call configureCloudinary/configurePushCdn at boot (best-effort — if creds are missing, the provider stays unconfigured and getActiveProvider falls back to local). The dynamic-import convention (existing in lines 11-13) keeps node-only deps out of the Edge bundle.

    **next.config.ts (Pitfall 4):** Edit `next.config.ts` images.remotePatterns (lines 22-29). Add `{ protocol: "https", hostname: "res.cloudinary.com" }` for Cloudinary delivery. For the push-CDN, the hostname is operator-configured — either (a) document the manual edit step (operator adds their CDN hostname when configuring push-CDN), OR (b) add a wildcard pattern if Next 16 supports it. Pick (a) — document clearly in a comment + in the Storage Settings page help text (Plan 04-05 Task 3 surfaces this). DO NOT add a localhost:9000-style hardcode for an unknown CDN hostname.

    **seed.ts (D-29b):** Extend `src/lib/storage/seed.ts` with the new settings keys: `settings.storage.cloudinary_creds` (empty string — no creds yet), `settings.storage.push_cdn_creds` (empty string), `settings.storage.encryption_key_version` (string "1" — D-25 key-rotation reference). Idempotent ON CONFLICT DO NOTHING pattern. The existing seedStorageSettings is the template.
  </action>
  <verify>
    <automated>test -f "src/lib/storage/cloudinary.ts" && test -f "src/lib/storage/push-cdn.ts" </automated>
    <automated>grep -q '"cloudinary" | "push-cdn"' "src/lib/storage/types.ts" </automated>
    <automated>grep -q 'getProviderByName' "src/lib/storage/registry.ts" </automated>
    <automated>grep -q 'getProviderByName' "src/actions/media.ts" </automated>
    <automated>grep -q 'cloudinary' "src/instrumentation.ts" && grep -q 'push-cdn\|pushCdn' "src/instrumentation.ts" </automated>
    <automated>grep -q 'res.cloudinary.com' "next.config.ts" </automated>
    <automated>pnpm test src/lib/storage/__tests__/cloudinary.test.ts -x </automated>
    <automated>pnpm test src/lib/storage/__tests__/push-cdn.test.ts -x </automated>
    <automated>pnpm test src/lib/storage/__tests__/registry.test.ts -x </automated>
    <automated>pnpm test src/actions/__tests__/media.test.ts -x </automated>
    <automated>pnpm test:migrations </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - types.ts name union includes cloudinary | push-cdn
    - registry.ts exports getProviderByName
    - Cloudinary provider: upload bypasses sharp; uses cloudinary.v2.uploader.upload_stream; getPublicUrl returns transform URLs
    - Push-CDN provider: S3Client + sharp variants + cdnBaseUrl overlay
    - deleteMedia routes by row.provider (Pitfall 0 fixed); proven by extended media.test.ts
    - instrumentation.ts registers both providers at boot
    - next.config.ts allows res.cloudinary.com
    - Wave 0 cloudinary/push-cdn tests pass; existing registry test still passes
    - seed.ts seeds the new settings keys
    - pnpm test:migrations passes (no schema.ts change — only seeds + provider code)
    - pnpm build succeeds
  </done>
  <acceptance_criteria>
    - `test -f 'src/lib/storage/cloudinary.ts'` exits 0
    - `test -f 'src/lib/storage/push-cdn.ts'` exits 0
    - `test -f 'src/lib/storage/__tests__/cloudinary.test.ts'` exits 0
    - `test -f 'src/lib/storage/__tests__/push-cdn.test.ts'` exits 0
    - `grep -c '"cloudinary" | "push-cdn"' 'src/lib/storage/types.ts'` returns ≥1
    - `grep -c 'getProviderByName' 'src/lib/storage/registry.ts'` returns ≥1
    - `grep -c 'getProviderByName' 'src/actions/media.ts'` returns ≥1 (Pitfall 0 fix)
    - `grep -c 'getActiveProvider' 'src/actions/media.ts'` is 0 OR no longer used in deleteMedia (Pitfall 0 — the row-provider routing replaces active-provider routing)
    - `grep -c 'cloudinary' 'src/instrumentation.ts'` returns ≥1
    - `grep -c 'push-cdn\|pushCdn' 'src/instrumentation.ts'` returns ≥1
    - `grep -c 'res.cloudinary.com' 'next.config.ts'` returns ≥1 (Pitfall 4)
    - `pnpm test src/lib/storage/__tests__/cloudinary.test.ts -x` exits 0
    - `pnpm test src/lib/storage/__tests__/push-cdn.test.ts -x` exits 0
    - `pnpm test src/actions/__tests__/media.test.ts -x` exits 0
    - `pnpm test:migrations` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: actions/storage-settings.ts (D-23/D-24/D-25) + Storage Settings page + sidebar Storage link + Wave 0 storage-settings test</name>
  <files>
    src/actions/storage-settings.ts,
    src/actions/storage-settings-schema.ts,
    src/actions/__tests__/storage-settings.test.ts,
    src/app/(admin)/dashboard/settings/storage/page.tsx,
    src/app/(admin)/dashboard/settings/storage/StorageSettingsForm.tsx,
    src/layout/AppSidebar.tsx
  </files>
  <read_first>
    - src/actions/settings.ts (the existing settings action — only getSetting; this Task adds saveStorageSettings + getStorageSettings + testStorageConnection as siblings in a NEW file, not modifying settings.ts)
    - src/actions/users.ts (the permission-check-first template — for requireRole('admin') usage)
    - src/lib/permissions/index.ts (requireRole('admin') signature — D-23 explicit admin re-check)
    - src/lib/crypto/index.ts (Task 1 output — encrypt / decrypt / redactCredentials)
    - src/lib/storage/registry.ts (Task 2 output — getActiveProvider + getProviderByName)
    - src/lib/storage/cloudinary.ts + push-cdn.ts (Task 2 output — the providers whose creds this UI manages)
    - src/app/(admin)/dashboard/posts/PostForm.tsx (RHF + Zod + useMutation baseline; the form pattern to mirror)
    - src/app/(admin)/dashboard/posts/schema-client.ts (the schema-client bridge template)
    - src/layout/AppSidebar.tsx (Plan 04-01 added the "Settings" parent; this Task verifies the "Storage" child link points to /dashboard/settings/storage and renders)
    - 04-RESEARCH.md "Code Examples" Example 3 (Test connection probe — lines 643-659 — the per-provider switch)
    - 04-RESEARCH.md Pitfall 7 (lines 533-537 — credentials never pre-filled)
    - 04-CONTEXT.md D-23 (admin-only), D-24 (Test connection), D-25 (encrypted + never exposed)
  </read_first>
  <behavior>
    - saveStorageSettings(provider, credentials) calls requireRole('admin') FIRST → validates via storageSettingsSchema → encrypt(credentials) → db.update settings.storage.<provider>_creds (encrypted blob) + settings.storage.active_provider (plaintext)
    - getStorageSettings() calls requireRole('admin') FIRST → reads settings.storage.<each_provider>_creds + active_provider → decrypts each → returns { activeProvider, providers: { cloudinary: redactedCreds, push_cdn: redactedCreds, r2: redactedCreds, local: {...} } }
    - testStorageConnection(provider, creds) calls requireRole('admin') FIRST → switch on provider: local → fs.access STORAGE_LOCAL_ROOT; r2/push-cdn → ListObjectsV2Command MaxKeys:1; cloudinary → cloudinary.v2.api.ping. Returns { ok, error? }
    - Non-admin calling any of the three → FORBIDDEN BEFORE any DB or encryption call (proven by MUST_NOT_BE_REACHED test)
    - StorageSettingsForm defaultValues for secret fields = '' (empty string) — NEVER pre-filled (Pitfall 7). Non-secret fields (bucket, cloud_name, region, cdnBaseUrl) ARE pre-filled from getStorageSettings.
    - Each provider section has a "Test connection" button (D-24) that calls testStorageConnection and shows inline ok/error feedback before Save
    - Save uses useMutation (NOT optimistic — D-27 — credentials are high-stakes)
    - The sidebar "Storage" link under "Settings" navigates to /dashboard/settings/storage (Plan 04-01 added the sidebar entry; this Task confirms the route renders)
  </behavior>
  <action>
    **storage-settings-schema.ts:** Create `src/actions/storage-settings-schema.ts`. Pure schema module (NO "use server" / "use client" directive — mirror posts-schema.ts pattern). Export `storageSettingsSchema = z.object({ activeProvider: z.enum(["local", "r2", "cloudinary", "push-cdn"]), cloudinary: z.object({ cloud_name: z.string(), api_key: z.string(), api_secret: z.string() }).optional(), r2: z.object({ endpoint: z.string(), region: z.string(), accessKeyId: z.string(), secretAccessKey: z.string(), bucket: z.string(), forcePathStyle: z.boolean() }).optional(), push_cdn: z.object({ endpoint: z.string(), region: z.string(), accessKeyId: z.string(), secretAccessKey: z.string(), bucket: z.string(), cdnBaseUrl: z.string().url(), forcePathStyle: z.boolean() }).optional() })`. Types exported.

    **storage-settings.ts:** Create `src/actions/storage-settings.ts`. Start with `"use server"`. Imports: `{ db, schema } from "@/lib/db"`, `{ eq } from "drizzle-orm"`, `{ requireRole } from "@/lib/permissions"`, `{ encrypt, decrypt, redactCredentials } from "@/lib/crypto"`, `{ log } from "@/lib/log"`, the provider configure functions from Task 2's cloudinary.ts + push-cdn.ts (for reconfiguration after save), and cloudinary's v2 for the ping probe. Implement three actions:

    `saveStorageSettings(input)`: First line `await requireRole("admin")` (D-23 — explicit admin, not requireCan). Parse via storageSettingsSchema. For each provider with creds provided (non-empty secret fields — empty means "no change"), `encrypt(JSON.stringify(creds))` → `db.update(schema.settings).set({ value: encryptedBlob }).where(eq(schema.settings.key, "storage.<provider>_creds"))` OR insert if missing. Update `settings.storage.active_provider` with the plaintext value. After save, call the relevant `configureCloudinary(configuredCreds)` / `configurePushCdn(configuredCreds)` so the provider picks up new creds without app restart. Log the change. Return `{ ok: true }`.

    `getStorageSettings()`: First line `await requireRole("admin")`. Read all four provider-cred keys + active_provider. For each provider that has creds: `decrypt(blob)` → `JSON.parse` → `redactCredentials(creds)` (zero secret fields). Return the redacted shape + the plaintext activeProvider.

    `testStorageConnection(provider, creds)`: First line `await requireRole("admin")`. Switch on provider (RESEARCH.md Example 3 lines 643-659):
    - "local": `await fs.access(process.env.STORAGE_LOCAL_ROOT ?? "storage/local")` → { ok: true } or { ok: false, error: "..." }
    - "r2" / "push-cdn": `await makeS3Client(creds).send(new ListObjectsV2Command({ Bucket: creds.bucket, MaxKeys: 1 }))` → { ok: true } on success
    - "cloudinary": configure cloudinary.v2.config(creds) temporarily → `await cloudinary.v2.api.ping()` → { ok: true } on success
    Return `{ ok: boolean, error?: string }`. Never throw — catch and return error.

    **Wave 0 storage-settings test** (`src/actions/__tests__/storage-settings.test.ts`): Clone the mock scaffold from `src/actions/__tests__/users.test.ts`. Mock @/lib/db, @/lib/permissions, @/lib/crypto, @/lib/log, @/lib/storage/cloudinary, @/lib/storage/push-cdn. Test cases:
    - saveStorageSettings: admin → encrypts + persists; non-admin → FORBIDDEN BEFORE encrypt or db.update (MUST_NOT_BE_REACHED pattern — assert `requireRole("admin")` was called AND encrypt was NOT called when requireRole throws).
    - getStorageSettings: admin → returns redacted creds (secret fields empty); non-admin → FORBIDDEN.
    - testStorageConnection: admin → returns { ok: true } for a valid local probe; non-admin → FORBIDDEN.
    - Redact-on-read: even if the DB has real creds stored, getStorageSettings returns empty strings for secretAccessKey/api_secret/apiKey fields.

    **Storage Settings page** (`src/app/(admin)/dashboard/settings/storage/page.tsx`): Server Component. Call `getStorageSettings()` in try/catch; pass to `<StorageSettingsForm initial={settings} />`. Mirror the dashboard list-page shell.

    **StorageSettingsForm.tsx** (`"use client"`): RHF + Zod form. Field structure: a "Active provider" radio/select (local / r2 / cloudinary / push-cdn). Conditional per-provider credential field sections (only the selected provider's section is visible, OR all four are visible with the active one highlighted — pick the cleaner UX). Each section's secret fields (api_secret, secretAccessKey, apiKey) have `defaultValue=""` and a placeholder `"•••••••• (enter new value to change)"` — Pitfall 7. Non-secret fields (bucket, cloud_name, region, cdnBaseUrl, endpoint) ARE pre-filled from `initial`. Each section has a "Test connection" button calling `testStorageConnection(provider, getValues(provider))` and rendering inline ok/error feedback. On submit: `useMutation({ mutationFn: saveStorageSettings })` — NON-optimistic (D-27 — credentials high-stakes). Show loading + success/error states.

    Create a small `src/app/(admin)/dashboard/settings/storage/schema-client.ts` bridge if you prefer the schema-bridge pattern (matches posts).

    **Sidebar link check** (`src/layout/AppSidebar.tsx`): Plan 04-01 added a "Settings" parent with a "Storage" child. Verify the child path = `/dashboard/settings/storage` (the route this Task creates). If Plan 04-01 left the child as a placeholder href, fix it now via Edit. Do NOT add new top-level nav items — just verify the Storage child link lands on the new route.
  </action>
  <verify>
    <automated>test -f "src/actions/storage-settings.ts" && test -f "src/actions/storage-settings-schema.ts" && test -f "src/actions/__tests__/storage-settings.test.ts" </automated>
    <automated>test -f "src/app/(admin)/dashboard/settings/storage/page.tsx" && test -f "src/app/(admin)/dashboard/settings/storage/StorageSettingsForm.tsx" </automated>
    <automated>grep -q 'requireRole' "src/actions/storage-settings.ts" </automated>
    <automated>grep -q 'saveStorageSettings\|getStorageSettings\|testStorageConnection' "src/actions/storage-settings.ts" </automated>
    <automated>grep -q 'redactCredentials' "src/actions/storage-settings.ts" </automated>
    <automated>grep -q '/dashboard/settings/storage' "src/layout/AppSidebar.tsx" </automated>
    <automated>pnpm test src/actions/__tests__/storage-settings.test.ts -x </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - saveStorageSettings / getStorageSettings / testStorageConnection exported; all admin-gated (requireRole FIRST)
    - Wave 0 storage-settings test passes (permission-check-first + redact-on-read)
    - /dashboard/settings/storage renders the form (admin-only — sidebar filter + server re-check)
    - Secret fields never pre-filled (Pitfall 7); non-secret fields pre-filled from getStorageSettings
    - Test connection button works for each provider (inline ok/error feedback)
    - Sidebar Storage link lands on the route
    - Save uses non-optimistic useMutation (D-27)
    - pnpm build succeeds
    - Manual: dark mode renders correctly on /dashboard/settings/storage (DASH-08)
  </done>
  <acceptance_criteria>
    - `test -f 'src/actions/storage-settings.ts'` exits 0
    - `test -f 'src/actions/storage-settings-schema.ts'` exits 0
    - `test -f 'src/actions/__tests__/storage-settings.test.ts'` exits 0
    - `test -f 'src/app/(admin)/dashboard/settings/storage/page.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/settings/storage/StorageSettingsForm.tsx'` exits 0
    - `grep -c 'requireRole' 'src/actions/storage-settings.ts'` returns ≥3 (one per action)
    - `grep -c 'redactCredentials' 'src/actions/storage-settings.ts'` returns ≥1
    - `grep -c 'saveStorageSettings' 'src/actions/storage-settings.ts'` returns ≥1
    - `grep -c 'testStorageConnection' 'src/actions/storage-settings.ts'` returns ≥1
    - `grep -c '/dashboard/settings/storage' 'src/layout/AppSidebar.tsx'` returns ≥1 (sidebar link wired)
    - `pnpm test src/actions/__tests__/storage-settings.test.ts -x` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin → /dashboard/settings/storage | Sidebar (Plan 04-01) hides; saveStorageSettings/getStorageSettings/testStorageConnection all call requireRole('admin') FIRST (D-23) |
| admin → credential encryption | SETTINGS_ENCRYPTION_KEY env var (runtime, NOT NEXT_PUBLIC) → lib/crypto AES-256-GCM → settings.value blob |
| settings DB → client (read path) | getStorageSettings decrypts server-side THEN redactCredentials zeroes secret fields before returning to client (Pitfall 7 / D-25) |
| Cloudinary SDK / S3 origin → next/image | next/image validates CDN hostname via next.config.ts remotePatterns (Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | pnpm install (cloudinary@2.10.0) | high | mitigate | Package Legitimacy Audit returned OK (official repo cloudinary/cloudinary_npm, 831K weekly downloads, 9-year v2 lineage, published 2026-04-25, no postinstall script). 04-RESEARCH.md "Package Legitimacy Audit" table cited. No checkpoint:human-verify required — legitimacy pre-verified |
| T-04-26 | Elevation of privilege | Non-admin calling saveStorageSettings | critical | mitigate | requireRole('admin') FIRST — BEFORE encrypt or db.update. MUST_NOT_BE_REACHED test proves ordering. A non-admin attacker cannot influence provider selection or read/modify credentials |
| T-04-27 | Information disclosure | Decrypted credentials crossing to client | critical | mitigate | getStorageSettings decrypts server-side THEN redactCredentials zeroes secret fields (regex /secret\|key\|token\|password\|api[-_]?key/i). Storage Settings form defaultValues for secret fields = '' (Pitfall 7). Network response from getStorageSettings contains no secret values (asserted by storage-settings.test.ts) |
| T-04-28 | Information disclosure | Encryption key leakage into build bundle | high | mitigate | SETTINGS_ENCRYPTION_KEY is a runtime env var (Coolify) NOT NEXT_PUBLIC_*. Next.js never inlines non-public env vars into the client bundle. Phase 7 PERF-02 audits. lib/crypto reads the key lazily inside encrypt/decrypt (not at module load) |
| T-04-29 | Tampering | Encrypted blob tampered at rest | high | mitigate | AES-256-GCM auth tag — decrypt.final() throws on any modification. decrypt NEVER returns partial/garbage plaintext. crypto.test.ts proves tamper detection |
| T-04-30 | Tampering | Cloudinary upload_stream buffer double-encoding (Pitfall 3) | medium | mitigate | Use Readable.from(buffer).pipe(stream) per RESEARCH Pitfall 3 — the canonical Node stream pattern. Verify against the installed cloudinary@2.10.0 readme at implementation time (RESEARCH A1/A2 marked [ASSUMED]) |
| T-04-31 | Tampering | deleteMedia deletes via wrong provider (Pitfall 0) | high | mitigate | Rewrite deleteMedia to use getProviderByName(row.provider ?? "local") — proven by extended media.test.ts. Without this fix, switching active provider would leak orphaned objects in the previous provider's storage |
| T-04-32 | Information disclosure | next/image breaks on Cloudinary / push-CDN URLs (Pitfall 4) | medium | mitigate | next.config.ts images.remotePatterns extended with res.cloudinary.com + documented manual-edit step for operator-configured push-CDN hostname. Without this, next/image 400s on the new hostnames — availability not confidentiality, but blocks the feature |
| T-04-33 | Denial of service | Missing SETTINGS_ENCRYPTION_KEY → app boot crash | medium | mitigate | lib/crypto reads the key lazily inside encrypt/decrypt — module loads cleanly without the key; the local provider keeps working. saveStorageSettings throws a clear "SETTINGS_ENCRYPTION_KEY missing" error AT CALL TIME, not a generic crypto error. crypto.test.ts proves graceful failure |
| T-04-34 | Repudiation | Storage Settings save audit trail | low | accept | log.info on every save records the provider switch (not the credentials). Acceptable for a 2-5 person team. v2 may add a dedicated audit log |
</threat_model>

<verification>
**Wave 3 gate (this plan — also the phase gate since this is the last plan):**
- `pnpm test` (full suite) passes — including all new Wave 0 tests (cloudinary, push-cdn, crypto, storage-settings, pages, extended users, extended media)
- `pnpm test:migrations` passes (no schema.ts change — D-29 seed-only)
- `pnpm test:auth-gate` passes (route restructure from Plan 04-01 + overview marker sync)
- `pnpm build` succeeds
- Manual: admin opens /dashboard/settings/storage → enters Cloudinary creds → clicks "Test connection" → success → Save → uploads a new media item → verify it lands in Cloudinary (or simulate via provider mocks if no live creds)
- Manual: switch active provider from local → r2 → cloudinary → push-cdn → local; verify uploads route correctly each time
- Manual: delete a media item stored under provider='r2' while active is 'cloudinary' → verify the r2 object is deleted (Pitfall 0 fix)
- Manual: verify the Storage Settings form never pre-fills secret fields (Pitfall 7) — switch to each provider section, confirm api_secret / secretAccessKey / apiKey fields show empty placeholder
- Manual: dark mode renders on /dashboard/settings/storage (DASH-08)
- Manual: enter bad creds → "Test connection" shows clear failure → Save is blocked or warns
</verification>

<success_criteria>
- DASH-09: Storage Settings page (admin-only) + provider selection (local/R2/Cloudinary/push-CDN) + per-provider credentials + Cloudinary + push-CDN providers implemented + save action re-checks admin server-side — fully satisfied
- Cross-cutting: deleteMedia multi-provider bug (Pitfall 0) FIXED; next.config remotePatterns (Pitfall 4) extended; encryption (D-25) + redact-on-read (Pitfall 7) implemented
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-chrome/04-05-SUMMARY.md` when done. This is the final Phase 4 plan — also signal phase-readiness for /gsd-verify-work.
</output>

## Artifacts this phase produces

**New files:**
- `src/lib/crypto/index.ts` — AES-256-GCM encrypt/decrypt/redactCredentials (D-25)
- `src/lib/crypto/__tests__/crypto.test.ts` — Wave 0 crypto test
- `src/lib/storage/cloudinary.ts` — CloudinaryProvider (D-22)
- `src/lib/storage/push-cdn.ts` — PushCdnProvider (D-21)
- `src/lib/storage/__tests__/cloudinary.test.ts` — Wave 0 cloudinary test
- `src/lib/storage/__tests__/push-cdn.test.ts` — Wave 0 push-cdn test
- `src/actions/storage-settings.ts` — admin-gated saveStorageSettings/getStorageSettings/testStorageConnection (D-23/D-24)
- `src/actions/storage-settings-schema.ts` — Zod v4 schema
- `src/actions/__tests__/storage-settings.test.ts` — Wave 0 storage-settings test
- `src/app/(admin)/dashboard/settings/storage/{page.tsx,StorageSettingsForm.tsx,schema-client.ts}` — admin-only Storage Settings UI

**Modified files:**
- `src/lib/storage/types.ts` — name union widened (local | r2 | cloudinary | push-cdn)
- `src/lib/storage/registry.ts` — added getProviderByName(name) (Pitfall 0 enabler)
- `src/lib/storage/seed.ts` — seeds settings.storage.cloudinary_creds / push_cdn_creds / encryption_key_version
- `src/actions/media.ts` — Pitfall 0 deleteMedia rewrite (route via getProviderByName)
- `src/actions/__tests__/media.test.ts` — Pitfall 0 multi-provider delete case
- `src/instrumentation.ts` — registers cloudinary + push-cdn providers at boot
- `src/layout/AppSidebar.tsx` — verifies/wires the Storage child link under Settings
- `next.config.ts` — images.remotePatterns adds res.cloudinary.com + push-CDN documented manual edit
- `.env.example` — SETTINGS_ENCRYPTION_KEY placeholder + generation instructions
- `package.json` — cloudinary@2.10.0 added

**New env var:** `SETTINGS_ENCRYPTION_KEY` (32-byte base64) — runtime, NOT NEXT_PUBLIC.

**New dep:** `cloudinary@2.10.0` (legitimacy pre-verified per 04-RESEARCH.md Package Legitimacy Audit — no checkpoint required).

**No new SQL migrations** (D-29 seed-only — settings table already exists from Phase 1; the new keys are data, not schema).
</output>
