---
phase: 04-dashboard-chrome
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/(admin)/dashboard/categories/page.tsx
  - src/app/(admin)/dashboard/categories/CategoriesTable.tsx
  - src/app/(admin)/dashboard/tags/page.tsx
  - src/app/(admin)/dashboard/tags/TagsTable.tsx
  - src/app/(admin)/dashboard/media/page.tsx
  - src/app/(admin)/dashboard/media/MediaGrid.tsx
  - src/app/(admin)/dashboard/media/MediaUploader.tsx
  - src/components/dashboard/media/MediaPicker.tsx
  - src/app/(admin)/dashboard/posts/PostForm.tsx
  - src/components/editor/toolbar/Toolbar.tsx
  - src/actions/media.ts
  - src/actions/media-schema.ts
  - src/actions/__tests__/media.test.ts
autonomous: true
requirements: [DASH-02, DASH-03, DASH-06]
user_setup: []

must_haves:
  truths:
    - "GET /dashboard/categories renders a TailAdmin table listing all (non-soft-deleted) categories with create/edit/soft-delete actions"
    - "GET /dashboard/tags renders a TailAdmin table listing all (non-soft-deleted) tags with create/edit/soft-delete actions"
    - "GET /dashboard/media renders a grid (default) of uploaded media with a list-toggle, drag-drop upload, per-file progress, alt-text prompt, soft-delete with reference warning"
    - "The reusable <MediaPicker> modal is invoked from (a) PostForm feature-image field, (b) editor Toolbar image button (replaces window.prompt), and (c) avatar field (consumed by Plan 04-03)"
    - "MediaPicker includes a 'paste external URL' tab preserving Phase 3 D-10's external-URL option"
    - "Taxonomy CRUD uses optimistic UI (D-27 — high-frequency small mutations); media delete is optimistic; media upload is NOT optimistic (progress indicator communicates state)"
    - "Categories/Tags/Media pages use RHF + Zod + TanStack Query mutations (D-26 pattern from Plan 04-01)"
  artifacts:
    - "src/app/(admin)/dashboard/categories/page.tsx (NEW)"
    - "src/app/(admin)/dashboard/categories/CategoriesTable.tsx (NEW — client component with useMutation)"
    - "src/app/(admin)/dashboard/tags/page.tsx (NEW)"
    - "src/app/(admin)/dashboard/tags/TagsTable.tsx (NEW)"
    - "src/app/(admin)/dashboard/media/page.tsx (NEW — server component shell)"
    - "src/app/(admin)/dashboard/media/MediaGrid.tsx (NEW — client grid + list toggle)"
    - "src/app/(admin)/dashboard/media/MediaUploader.tsx (NEW — react-dropzone, multi-file, progress, alt-text prompt)"
    - "src/components/dashboard/media/MediaPicker.tsx (NEW — reusable modal: browse + upload-in-place + select + external-URL tab)"
    - "src/actions/media.ts (EXTEND — add findMediaReferences for D-15 warn)"
    - "src/actions/__tests__/media.test.ts (EXTEND — findMediaReferences test)"
  key_links:
    - "PostForm feature-image field → setValue('featureImage', url) via <MediaPicker onSelect={url => setValue(...)}> (replaces direct register('featureImage') text input)"
    - "Toolbar.tsx promptImage function → <MediaPicker> opening on image-button click; on select calls editor.chain().focus().setImage({ src: url }).run() (replaces window.prompt)"
    - "CategoriesTable / TagsTable → useMutation wrapping createCategory / updateCategory / softDeleteCategory from @/actions/categories / @/actions/tags"
    - "MediaGrid → useMutation wrapping deleteMedia; onMutate optimistically removes the row; onError rolls back"
    - "Media delete warns (does not block) when findMediaReferences returns post-body or feature-image matches"
  prohibitions:
    - "DO NOT introduce optimistic UI on media UPLOAD (D-27 explicit — the progress indicator already communicates state)"
    - "DO NOT modify the existing deleteMedia implementation in actions/media.ts (Plan 04-05 owns the Pitfall 0 rewrite — only ADD findMediaReferences as a sibling action here)"
    - "DO NOT bypass the 10MB upload cap (Phase 3 D-08) — enforce both client-side (dropzone accept/maxSize) and server-side (existing mediaUploadSchema)"
    - "DO NOT use raw <img> tags anywhere — all image previews go through next/image (CLAUDE.md hard rule)"
---

<objective>
Wire the TailAdmin Categories, Tags, and Media management surfaces to the existing Phase 3 actions, and ship the reusable `<MediaPicker>` modal that the post editor, feature-image field, and avatar field all consume. Per D-26, every form uses RHF + Zod + TanStack Query. Per D-27, optimistic UI is applied selectively (taxonomy CRUD + media delete = optimistic; media upload = NOT optimistic).

Purpose: Plan 04-01 established the `/dashboard/*` prefix, the `(admin)`-scoped QueryClient, and the PostForm `useMutation` baseline. This plan lands three more dashboard surfaces (categories, tags, media) + the reusable picker that closes the Phase 3 UAT gap ("no option to upload the feature image, just URL box").

Output: Categories table, Tags table, Media library browser (grid+list+uploader), reusable `<MediaPicker>` modal wired into PostForm feature-image + Toolbar image button. Closes DASH-02 + DASH-03 + the folded media-picker UAT todo.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-chrome/04-CONTEXT.md
@.planning/phases/04-dashboard-chrome/04-RESEARCH.md
@.planning/phases/04-dashboard-chrome/04-PATTERNS.md
@.planning/phases/04-dashboard-chrome/04-01-SUMMARY.md
@.planning/todos/pending/2026-07-04-media-library-picker-ui.md
@.planning/phases/03-content-engine/03-03-SUMMARY.md

# Authoritative code to read before editing
@src/actions/categories.ts
@src/actions/tags.ts
@src/actions/media.ts
@src/actions/media-schema.ts
@src/app/(admin)/dashboard/posts/page.tsx
@src/app/(admin)/dashboard/posts/PostForm.tsx
@src/components/editor/toolbar/Toolbar.tsx
@src/components/ui/modal/index.tsx
@src/components/ui/table/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Categories + Tags TailAdmin tables with optimistic CRUD (D-16, D-26, D-27)</name>
  <files>
    src/app/(admin)/dashboard/categories/page.tsx,
    src/app/(admin)/dashboard/categories/CategoriesTable.tsx,
    src/app/(admin)/dashboard/tags/page.tsx,
    src/app/(admin)/dashboard/tags/TagsTable.tsx
  </files>
  <read_first>
    - src/app/(admin)/dashboard/posts/page.tsx (the canonical dashboard list-page shell — Server Component + try/catch list action + TailAdmin table chrome + PageBreadcrumb)
    - src/actions/categories.ts (existing — listCategories / createCategory / updateCategory / softDeleteCategory; permission-check-first per Phase 2 Pitfall #1)
    - src/actions/tags.ts (existing — same shape as categories)
    - src/app/(admin)/dashboard/posts/PostForm.tsx (the useMutation retrofit baseline from Plan 04-01)
    - 04-PATTERNS.md "Categories + Tags TailAdmin tables" assignment (lines 353-366)
    - 04-CONTEXT.md D-16 (standalone pages over Phase 3 actions — NO new server actions needed)
    - 04-CONTEXT.md D-26 (RHF + Zod + TanStack Query everywhere)
    - 04-CONTEXT.md D-27 (optimistic on taxonomy CRUD)
  </read_first>
  <behavior>
    - GET /dashboard/categories renders a TailAdmin-quality table of all categories (name, slug, post count if cheap to compute, actions: edit + soft-delete)
    - GET /dashboard/tags renders the equivalent for tags
    - The "+ New Category" / "+ New Tag" button opens an inline Modal or side drawer (NOT a navigation) with an RHF form (name + slug fields)
    - Create/edit/soft-delete use TanStack useMutation wrapping the existing actions; optimistic UI per D-27 (onMutate updates the cache; onError rolls back; onSuccess invalidates ["categories"] / ["tags"])
    - Permission errors (author lacking taxonomy.create) surface as a clear toast / inline error — UI hiding is supplemented, not replaced, by the sidebar role filter (Plan 04-01)
  </behavior>
  <action>
    Per D-16, both pages consume the existing Phase 3 actions — DO NOT add new server actions.

    **Categories page** (`src/app/(admin)/dashboard/categories/page.tsx`): Server Component. Mirror the `src/app/(admin)/dashboard/posts/page.tsx` shell (lines 1-44) — PageBreadcrumb + a rounded-2xl card + try/catch listCategories + loadError fallback. Pass the rows into a client component `<CategoriesTable initialRows={...} />`.

    **CategoriesTable.tsx** (`"use client"`): TailAdmin Table (import from `@/components/ui/table`). Columns: Name, Slug, Actions (edit / delete). The "+ New Category" button opens a Modal (import the Modal shell from `@/components/ui/modal`) with an RHF form. Use `useMutation({ mutationFn: createCategory, onMutate: optimistic-add-to-cache, onError: rollback, onSuccess: invalidate ['categories'] })` — D-27 explicit optimistic. Same shape for update + softDelete. Each mutation references the existing actions from `@/actions/categories`. The form uses RHF + Zod — create a small `categorySchema` in this folder (or import a shared one if Phase 3 already exports one — grep `src/actions/categories.ts` first).

    **Tags page + TagsTable.tsx**: identical shape to Categories, swapping `category*` → `tag*` and `@/actions/categories` → `@/actions/tags`.

    Both tables use `useQuery({ queryKey: ['categories'], queryFn: listCategories, initialData: initialRows })` so the SSR-rendered rows hydrate the client cache (TanStack Query SSR pattern — `initialData` prop avoids a refetch on mount).

    DO NOT modify the existing actions — they already enforce permission-check-first.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
    <automated>test -f "src/app/(admin)/dashboard/categories/page.tsx" && test -f "src/app/(admin)/dashboard/categories/CategoriesTable.tsx" && test -f "src/app/(admin)/dashboard/tags/page.tsx" && test -f "src/app/(admin)/dashboard/tags/TagsTable.tsx" </automated>
    <automated>pnpm test src/actions/__tests__/taxonomy.test.ts -x </automated>
    <automated>grep -q 'useMutation' "src/app/(admin)/dashboard/categories/CategoriesTable.tsx" </automated>
  </verify>
  <done>
    - `/dashboard/categories` and `/dashboard/tags` render TailAdmin tables over the existing Phase 3 actions
    - Create/edit/soft-delete work via RHF + Zod + TanStack useMutation with optimistic UI (D-27)
    - Permission errors from the server surface clearly (UI-hiding is supplemented, not replaced)
    - `pnpm test src/actions/__tests__/taxonomy.test.ts -x` still passes (Phase 3 actions unchanged)
    - `pnpm build` succeeds
  </done>
  <acceptance_criteria>
    - `test -f 'src/app/(admin)/dashboard/categories/page.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/categories/CategoriesTable.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/tags/page.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/tags/TagsTable.tsx'` exits 0
    - `grep -c 'useMutation' 'src/app/(admin)/dashboard/categories/CategoriesTable.tsx'` returns ≥1
    - `grep -c 'useMutation' 'src/app/(admin)/dashboard/tags/TagsTable.tsx'` returns ≥1
    - `grep -cE 'optimistic|onMutate' 'src/app/(admin)/dashboard/categories/CategoriesTable.tsx'` returns ≥1 (D-27)
    - `pnpm test src/actions/__tests__/taxonomy.test.ts -x` exits 0 (Phase 3 actions unchanged)
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Media library browser page — grid + list + uploader + soft-delete warn (D-12, D-14, D-15)</name>
  <files>
    src/app/(admin)/dashboard/media/page.tsx,
    src/app/(admin)/dashboard/media/MediaGrid.tsx,
    src/app/(admin)/dashboard/media/MediaUploader.tsx,
    src/actions/media.ts,
    src/actions/media-schema.ts,
    src/actions/__tests__/media.test.ts
  </files>
  <read_first>
    - src/actions/media.ts (existing — uploadMedia / listMedia / deleteMedia; permission-check-first; MEDIA-02 record fields; soft-delete via deletedAt; Pitfall 0 deleteMedia row-provider bug — Plan 04-05 owns the fix, this Task 2 only ADDS findMediaReferences as a sibling)
    - src/actions/media-schema.ts (existing — mediaUploadSchema + MEDIA_MAX_SIZE_BYTES = 10MB cap from Phase 3 D-08)
    - src/actions/__tests__/media.test.ts (existing — 11 cases from Phase 3; this Task extends with findMediaReferences)
    - src/app/(admin)/dashboard/posts/page.tsx (list-page shell template)
    - package.json (react-dropzone@14.3.8 already installed — no new dep)
    - 04-CONTEXT.md D-12 (grid + list toggle, details drawer), D-14 (drag-drop + multi-file + progress + alt prompt + 10MB cap), D-15 (soft-delete + warn don't block)
    - 04-PATTERNS.md "Media library (D-12)" assignment (line 38 + lines 353-366)
    - 04-RESEARCH.md "Validation Architecture" Wave 0 row "extend media.test.ts for Pitfall 0 multi-provider delete" — Plan 04-05 owns Pitfall 0; THIS task only adds findMediaReferences coverage
  </read_first>
  <behavior>
    - GET /dashboard/media renders a server-component shell (PageBreadcrumb + card) that hydrates a client `<MediaGrid initialMedia={...} />`
    - MediaGrid renders a thumbnail grid (default) with a list-toggle (TwoColumnImageGrid / ThreeColumnImageGrid analogs or a Table)
    - Click a thumbnail → details drawer (alt text, dimensions, provider, used-by where known via findMediaReferences)
    - MediaUploader (separate client component) provides a react-dropzone drag-drop zone with multi-file, per-file progress, alt-text prompt on upload, 10MB client-side cap (matches server-side mediaUploadSchema)
    - Soft-delete (D-15): if findMediaReferences returns post-body or feature-image matches, show a warning ("This image appears in N posts — deleting may orphan the CDN URL") but DO NOT block the delete
    - Media delete = optimistic (D-27 — high-frequency); media upload = NOT optimistic (progress indicator communicates state)
  </behavior>
  <action>
    **Page shell** (`src/app/(admin)/dashboard/media/page.tsx`): Server Component. Mirror the dashboard list-page shell. Call `listMedia()` in try/catch; pass rows to `<MediaGrid initialMedia={rows} />`. Use `next/image` for ALL image previews (CLAUDE.md hard rule — never raw `<img>`). The loader resolves to the active provider's URL via the existing `cdnImageLoader` (Phase 1).

    **MediaGrid.tsx** (`"use client"`): TailAdmin `TwoColumnImageGrid` / `ThreeColumnImageGrid` analog from `src/components/ui/images/` (or fall back to a CSS grid if those components are demo-only). State: `view: 'grid' | 'list'`, `selected: MediaRow | null`. Grid: each cell is a `<button>` opening the details drawer. List view: TailAdmin Table with columns (thumbnail, alt text, provider, dimensions, uploaded-at, actions). The delete button calls `useMutation({ mutationFn: deleteMedia, onMutate: optimistic-remove, onError: rollback, onSuccess: invalidate ['media'] })` — D-27 optimistic. Before deleting, call `findMediaReferences(id)` and if results.length > 0, render a confirm dialog with the warning text — DO NOT block.

    **MediaUploader.tsx** (`"use client"`): uses `react-dropzone` (already installed — `pnpm add` NOT needed). `useDropzone({ accept: {'image/*': []}, maxSize: MEDIA_MAX_SIZE_BYTES, multiple: true, onDrop })`. Per-file upload state via `useMutation` per file (NOT optimistic — D-27). Show a per-file progress bar (XHR progress or simply pending/success/error states if the Server Action doesn't expose progress events; the simplest faithful implementation is per-file `useMutation` tracking `isPending`/`isSuccess`/`isError`). Alt-text prompt: an inline input on each file row before the upload commit (or a modal prompt after upload — pick whichever is cleaner; document the choice). Reuse `uploadMedia` from `@/actions/media` (which already enforces the 10MB cap server-side per Phase 3 D-08).

    **findMediaReferences action (D-15):** Add `findMediaReferences(id: number)` as a sibling to `deleteMedia` in `src/actions/media.ts`. Permission: `await requireCan({ media: ["read"] })`. Implementation: fetch the media row → derive its `getPublicUrl` string → query `posts` where `body::text ILIKE '%<url>%'` OR `feature_image = url` (Claude's discretion per D-15 — a simple substring search on the body JSON + an exact match on feature_image is sufficient; reference-count tracking is v2). Return `{ posts: Array<{ id, title }>, featureImageMatches: number }`. The warn-UI in MediaGrid consumes this.

    **Test extension** (`src/actions/__tests__/media.test.ts`): add a new describe block for `findMediaReferences` — extend the existing 11-case file. Mock `db` and `requireCan`. Assert: (a) permission-check-first (`requireCan({ media: ["read"] })` called BEFORE any db query — MUST_NOT_BE_REACHED pattern from `src/actions/__tests__/users.test.ts` lines 162-174); (b) returns matched posts; (c) returns empty array for an unreferenced media row.
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/dashboard/media/page.tsx" && test -f "src/app/(admin)/dashboard/media/MediaGrid.tsx" && test -f "src/app/(admin)/dashboard/media/MediaUploader.tsx" </automated>
    <automated>pnpm test src/actions/__tests__/media.test.ts -x </automated>
    <automated>grep -q 'findMediaReferences' "src/actions/media.ts" </automated>
    <automated>grep -q 'useDropzone' "src/app/(admin)/dashboard/media/MediaUploader.tsx" </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - `/dashboard/media` renders a server-shell + client MediaGrid with grid/list toggle
    - Drag-drop multi-file upload with per-file state and alt-text prompt works
    - 10MB client-side cap enforced (matches server-side mediaUploadSchema)
    - Soft-delete with reference warning works (warn, don't block — D-15)
    - findMediaReferences action added with permission-check-first; tests green
    - All image previews use `next/image` (no raw `<img>`)
    - Media delete = optimistic; media upload = NOT optimistic (D-27)
  </done>
  <acceptance_criteria>
    - `test -f 'src/app/(admin)/dashboard/media/page.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/media/MediaGrid.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/media/MediaUploader.tsx'` exits 0
    - `grep -c 'findMediaReferences' 'src/actions/media.ts'` returns ≥1
    - `grep -c 'useDropzone' 'src/app/(admin)/dashboard/media/MediaUploader.tsx'` returns ≥1
    - `grep -cE 'MEDIA_MAX_SIZE_BYTES|maxSize' 'src/app/(admin)/dashboard/media/MediaUploader.tsx'` returns ≥1 (D-08 cap honored)
    - `grep -cE '<img ' 'src/app/(admin)/dashboard/media/' ` returns 0 (no raw img — CLAUDE.md rule)
    - `pnpm test src/actions/__tests__/media.test.ts -x` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reusable <MediaPicker> modal + wire into PostForm feature-image + Toolbar image button (D-13)</name>
  <files>
    src/components/dashboard/media/MediaPicker.tsx,
    src/app/(admin)/dashboard/posts/PostForm.tsx,
    src/components/editor/toolbar/Toolbar.tsx
  </files>
  <read_first>
    - src/components/ui/modal/index.tsx (the Modal shell — isOpen / onClose / children / showCloseButton / isFullscreen API + Escape-key + body-overflow lock effects lines 22-49 — copy verbatim into MediaPicker's outer modal)
    - src/app/(admin)/dashboard/posts/PostForm.tsx (CURRENT — after Plan 04-01's useMutation retrofit; lines 126-136 contain the feature-image field to replace)
    - src/components/editor/toolbar/Toolbar.tsx (CURRENT — lines 12-14 + 134 + 50-55 document the v1 window.prompt URL input to replace)
    - src/app/(admin)/dashboard/media/MediaGrid.tsx (Task 2 output — reuse the grid + uploader composition inside the picker)
    - src/app/(admin)/dashboard/media/MediaUploader.tsx (Task 2 output — reuse inside the picker's "upload" tab)
    - .planning/todos/pending/2026-07-04-media-library-picker-ui.md (exact wiring points + decision context)
    - 04-CONTEXT.md D-13 (one reusable picker, 3 consumers, "paste external URL" tab)
    - 04-PATTERNS.md "MediaPicker" assignment (lines 412-438)
  </read_first>
  <behavior>
    - <MediaPicker isOpen={bool} onClose={() => void} onSelect={(url: string) => void} /> is a single reusable modal component
    - The modal has tabs: "Library" (browse existing media via listMedia, click to select), "Upload" (drag-drop new media via the Task 2 MediaUploader, then select), "External URL" (paste any URL — preserves Phase 3 D-10)
    - PostForm feature-image field uses <MediaPicker> to populate setValue('featureImage', url) — replaces the direct register('featureImage') text input
    - Toolbar image button opens <MediaPicker> on click — on select, calls editor.chain().focus().setImage({ src: url }).run() (replaces window.prompt)
    - Plan 04-03 (users drawer) will consume the same <MediaPicker> for the avatar field — no changes needed here, just ensure the component is exported and self-contained
  </behavior>
  <action>
    **MediaPicker.tsx** (`"use client"`): New file at `src/components/dashboard/media/MediaPicker.tsx`. Props: `{ isOpen: boolean; onClose: () => void; onSelect: (url: string) => void; accept?: "image" | "any" }`. Outer shell: copy the Modal API + Escape-key + body-overflow lock from `src/components/ui/modal/index.tsx` lines 4-49 verbatim (or import Modal directly if the API is compatible — preferred to avoid duplication). Tabs:
    1. **Library** — call `listMedia()` (or accept prefetched rows as a prop), render a compact grid (reuse MediaGrid's grid rendering or a simpler thumbnail list — picker can be smaller than the full library page). Click a thumbnail → `onSelect(media.publicUrl)` + `onClose()`.
    2. **Upload** — embed `<MediaUploader onUploaded={(media) => onSelect(media.publicUrl)} />` from Task 2. After upload, auto-select.
    3. **External URL** — a single text input + "Use this URL" button. Validates with a basic URL regex (no fetch — server validates on use). On submit → `onSelect(url)` + `onClose()`.

    All image previews use `next/image`. State: `tab: 'library' | 'upload' | 'external'`.

    **PostForm feature-image wiring:** In `src/app/(admin)/dashboard/posts/PostForm.tsx`, replace the current feature-image URL text input (lines ~126-136) with a "Select image" button that opens `<MediaPicker isOpen={...} onSelect={(url) => setValue('featureImage', url)} onClose={...} />`. Keep the form's `featureImage` field registration (RHF still owns the value) — the picker calls `setValue('featureImage', url, { shouldValidate: true })`. Also render a small "Remove image" link that calls `setValue('featureImage', '')` and a thumbnail preview (`next/image` with `src={watch('featureImage')}`) when the field is populated.

    **Toolbar image-button wiring:** In `src/components/editor/toolbar/Toolbar.tsx`, replace the `promptImage` function (lines ~50-55 — currently `window.prompt`) with state-driven `<MediaPicker>` mounting: a `const [pickerOpen, setPickerOpen] = useState(false)` + `<MediaPicker isOpen={pickerOpen} onClose={() => setPickerOpen(false)} onSelect={(url) => { editor.chain().focus().setImage({ src: url }).run(); setPickerOpen(false); }} />`. The image button's `onClick` becomes `() => setPickerOpen(true)`. Update the button title (currently line 134: "Image (external URL — Phase 4 DASH-03 wires the library picker)") to "Insert image" or similar. Update the file header comment (lines 12-14) to reflect that the picker is now wired.

    DO NOT remove the "External URL" tab from the picker — Phase 3 D-10 explicitly allows library-OR-external; the picker preserves both paths.
  </action>
  <verify>
    <automated>test -f "src/components/dashboard/media/MediaPicker.tsx" </automated>
    <automated>grep -q 'MediaPicker' "src/app/(admin)/dashboard/posts/PostForm.tsx" </automated>
    <automated>grep -q 'MediaPicker' "src/components/editor/toolbar/Toolbar.tsx" </automated>
    <automated>! grep -q 'window.prompt' "src/components/editor/toolbar/Toolbar.tsx" </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - `<MediaPicker>` exists with three tabs (Library / Upload / External URL) and is exported from `src/components/dashboard/media/MediaPicker.tsx`
    - PostForm feature-image uses the picker (`setValue('featureImage', url)`), no longer a plain text input
    - Toolbar image button opens the picker (no `window.prompt` anywhere in Toolbar.tsx)
    - All image previews in the picker use `next/image` (no raw `<img>`)
    - Plan 04-03 can consume `<MediaPicker>` for the avatar field without modification
    - `pnpm build` succeeds
    - Manual (per VALIDATION.md Manual-Only): open the picker in (a) PostForm feature-image, (b) editor image button; confirm browse + upload + paste-URL all land a working URL
  </done>
  <acceptance_criteria>
    - `test -f 'src/components/dashboard/media/MediaPicker.tsx'` exits 0
    - `grep -c 'MediaPicker' 'src/app/(admin)/dashboard/posts/PostForm.tsx'` returns ≥1
    - `grep -c 'MediaPicker' 'src/components/editor/toolbar/Toolbar.tsx'` returns ≥1
    - `grep -c 'window.prompt' 'src/components/editor/toolbar/Toolbar.tsx'` returns 0 (replaced)
    - `grep -cE 'library|upload|external' 'src/components/dashboard/media/MediaPicker.tsx'` returns ≥3 (three tabs present)
    - `grep -cE '<img ' 'src/components/dashboard/media/MediaPicker.tsx'` returns 0 (no raw img)
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Server Action (taxonomy/media) | Existing Phase 3 actions already enforce `requireCan` first (Pitfall #1) — UI hiding via sidebar (Plan 04-01) is supplementary |
| MediaPicker→server upload | All uploads go through `uploadMedia` server action (no client-direct presigned URLs — Phase 3 D-06) |
| External URL in picker | "Paste external URL" tab accepts arbitrary URL — server validates use (e.g. next/image remotePatterns allows the hostname OR the URL is consumed as-is in the editor body) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Elevation of privilege | CategoriesTable / TagsTable / MediaGrid mutations | high | mitigate | All mutations call existing Phase 3 actions that begin with `requireCan(...)` (Pitfall #1 — proven by 03 Phase tests). UI hiding via sidebar (Plan 04-01) is supplementary |
| T-04-06 | Tampering | Media upload 10MB cap (D-08) | medium | mitigate | Enforce BOTH client-side (dropzone `maxSize: MEDIA_MAX_SIZE_BYTES`) AND server-side (existing `mediaUploadSchema` in `actions/media.ts`). Defense in depth — neither layer alone is sufficient |
| T-04-07 | Tampering | MediaPicker "External URL" tab | low | accept | URL is stored as-is in the post body / feature-image field; render path uses next/image (which validates via remotePatterns). No HTML/JS injection — URLs are URL-validated by RHF + used as `src`, not as innerHTML |
| T-04-08 | Information disclosure | findMediaReferences leaks post titles to non-admin | low | mitigate | `findMediaReferences` calls `requireCan({ media: ["read"] })` first; editors/authors with media.read can legitimately see which posts reference a media item they manage |
| T-04-09 | Repudiation | Optimistic UI rollback on taxonomy/media delete failure | medium | mitigate | D-27 — optimistic UI onError rolls back the cache; the server action is the source of truth. If the server rejects (FORBIDDEN), the row re-appears with an error toast |
</threat_model>

<verification>
**Wave 2 gate (this plan):**
- `pnpm build` succeeds
- `pnpm test src/actions/__tests__/taxonomy.test.ts -x` passes (Phase 3 actions unchanged)
- `pnpm test src/actions/__tests__/media.test.ts -x` passes (extended with findMediaReferences)
- Manual: drag-drop 3 files via the MediaUploader; observe per-file state + alt-text prompt
- Manual: open `<MediaPicker>` from (a) PostForm feature-image, (b) editor image button; paste external URL; confirm selection lands
- Manual: delete a media item referenced by a post; observe warning + non-blocking confirm
- Manual: dark mode renders correctly on the three new pages (DASH-08)
</verification>

<success_criteria>
- DASH-02: Categories + Tags management UI (TailAdmin tables over Phase 3 actions, no new server actions) — fully satisfied
- DASH-03: Media library browser UI + reusable `<MediaPicker>` — fully satisfied (closes Phase 3 UAT gap)
- DASH-06: RHF + Zod + TanStack Query applied to all new forms; optimistic UI per D-27 — extended
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-chrome/04-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New files:**
- `src/app/(admin)/dashboard/categories/{page.tsx,CategoriesTable.tsx}` — TailAdmin category management
- `src/app/(admin)/dashboard/tags/{page.tsx,TagsTable.tsx}` — TailAdmin tag management
- `src/app/(admin)/dashboard/media/{page.tsx,MediaGrid.tsx,MediaUploader.tsx}` — media library browser
- `src/components/dashboard/media/MediaPicker.tsx` — the reusable modal (D-13), consumed by PostForm + Toolbar + Plan 04-03's avatar field

**Modified files:**
- `src/app/(admin)/dashboard/posts/PostForm.tsx` — feature-image field now uses MediaPicker (replaces direct text input)
- `src/components/editor/toolbar/Toolbar.tsx` — image button now opens MediaPicker (replaces window.prompt)
- `src/actions/media.ts` — ADD `findMediaReferences` action (D-15 warn; do NOT modify deleteMedia — Plan 04-05 owns the Pitfall 0 rewrite)
- `src/actions/__tests__/media.test.ts` — extended with findMediaReferences coverage

**No new deps** (react-dropzone + next/image + tanstack/react-query already installed in prior phases).
</output>
