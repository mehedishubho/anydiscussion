---
phase: 04-dashboard-chrome
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/actions/users.ts
  - src/actions/__tests__/users.test.ts
  - src/app/(admin)/dashboard/users/page.tsx
  - src/app/(admin)/dashboard/users/UsersTable.tsx
  - src/app/(admin)/dashboard/users/UserDrawer.tsx
  - src/app/(admin)/dashboard/profile/page.tsx
  - src/app/(admin)/dashboard/profile/ProfileForm.tsx
autonomous: true
requirements: [DASH-04, DASH-06]
user_setup: []

must_haves:
  truths:
    - "GET /dashboard/users renders a TailAdmin table of users (admin-only — sidebar hides for non-admins per Plan 04-01, server-side requireCan re-checks)"
    - "An admin can create / edit (credentials, role, bio, avatar) / disable (ban via Phase 2 D-16 banUser) / revoke sessions (Phase 2 D-17) — all via side drawer, without leaving /dashboard/users"
    - "An admin CANNOT destructively delete a user (D-08 — disable-only preserves post authorship integrity)"
    - "Role assignment (admin/editor/author) happens in the create/edit drawer via dropdown; updateUser re-checks requireCan({ user: ['update'] }) server-side (D-11)"
    - "Any role can self-service edit their own name/bio/avatar at /dashboard/profile; admins can edit anyone via the users drawer"
    - "Avatar field uses the <MediaPicker> modal from Plan 04-02 (component reuse — no duplicate picker implementation)"
    - "listUsers + updateUser server actions added to src/actions/users.ts following the permission-check-first pattern (Phase 2 Pitfall #1)"
    - "Ban/role-change use optimistic UI (D-27 — high-frequency small mutations); profile save is NOT optimistic (single-source user record, server confirmation preferred)"
  artifacts:
    - "src/actions/users.ts (EXTEND — add listUsers + updateUser + profile self-edit path)"
    - "src/actions/__tests__/users.test.ts (EXTEND — listUsers/updateUser + non-admin FORBIDDEN + self-edit allowed)"
    - "src/app/(admin)/dashboard/users/page.tsx (NEW — admin-only users table)"
    - "src/app/(admin)/dashboard/users/UsersTable.tsx (NEW — client component with useMutation for ban/role-change)"
    - "src/app/(admin)/dashboard/users/UserDrawer.tsx (NEW — side drawer for create/edit + revoke sessions)"
    - "src/app/(admin)/dashboard/profile/ProfileForm.tsx (NEW — self-service form for any role)"
  key_links:
    - "UsersTable → useMutation wrapping createUser / updateUser / banUser / unbanUser / revokeSessions from @/actions/users"
    - "UserDrawer avatar field → <MediaPicker onSelect={url => setValue('avatar', url)}> (component from Plan 04-02)"
    - "ProfileForm self-edit path → updateUser(ownId, {...}) WITHOUT requireCan({ user: ['update'] }) (the action allows self-edit when userId === session.user.id)"
  prohibitions:
    - "DO NOT add a destructive deleteUser action (D-08 — disable-only via banUser)"
    - "DO NOT expose per-device session list / IP / last-active UI (D-10 — v2 only; v1 ships revoke-sessions only)"
    - "DO NOT require admin permission for self-service profile edits (D-09 — any role can edit own name/bio/avatar)"
    - "DO NOT surface ban-reason or ban-expiry in the drawer form (Open Question #3 in 04-RESEARCH.md — ship a simple ban toggle first; the primitive supports adding them later)"
    - "DO NOT rely on the sidebar role filter (Plan 04-01) as the authoritative gate — every users action re-checks requireCan server-side"
---

<objective>
Surface the Phase 2 user-management primitives (createUser / banUser / unbanUser / revokeSessions) in a TailAdmin users table + drawer, and ship self-service profile editing for any role. Add the two missing actions (`listUsers`, `updateUser`) following the permission-check-first pattern. Per D-26/D-27, the drawer uses RHF + Zod + TanStack Query with selective optimistic UI on ban/role-change.

Purpose: Phase 2 built the RBAC primitives but exposed them only as Server Actions. This plan surfaces them in the dashboard UI so an admin can actually manage the team (create/disable/role-assign) without touching code — closing DASH-04.

Output: `/dashboard/users` (admin-only table + drawer) + `/dashboard/profile` (self-service form for any role) + `listUsers` / `updateUser` actions + extended users tests.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-chrome/04-CONTEXT.md
@.planning/phases/04-dashboard-chrome/04-RESEARCH.md
@.planning/phases/04-dashboard-chrome/04-PATTERNS.md
@.planning/phases/04-dashboard-chrome/04-01-SUMMARY.md
@.planning/phases/04-dashboard-chrome/04-02-SUMMARY.md
@.planning/phases/02-auth-rbac/02-03-SUMMARY.md

# Authoritative code to read before editing
@src/actions/users.ts
@src/actions/__tests__/users.test.ts
@src/lib/permissions/index.ts
@src/lib/auth/index.ts
@src/app/(admin)/dashboard/posts/page.tsx
@src/app/(admin)/dashboard/profile/page.tsx
@src/components/ui/modal/index.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listUsers + updateUser actions (extend src/actions/users.ts) + extend users.test.ts (D-07, D-09, D-11)</name>
  <files>
    src/actions/users.ts,
    src/actions/__tests__/users.test.ts
  </files>
  <read_first>
    - src/actions/users.ts (CURRENT — has createFirstAdmin / createUser / banUser / unbanUser / revokeSessions; permission-check-first pattern at lines 86-96; no listUsers, no updateUser — both required for this Task)
    - src/actions/__tests__/users.test.ts (CURRENT — Phase 2 tests; the vi.hoisted + vi.mock("@/lib/db") + vi.mock("@/lib/permissions") + vi.mock("@/lib/log") block at lines 14-81 is the mock scaffold to clone for new test blocks; the MUST_NOT_BE_REACHED pattern at lines 162-174 is the permission-check-first assertion)
    - src/lib/permissions/index.ts (requireCan / requireRole / getSessionOrThrow / assertOwnsPost — the helpers to call)
    - src/lib/auth/index.ts (auth.api.createUser / auth.api.updateUser / auth.api.listUsers — verify which methods Better Auth exposes; if listUsers is not exposed, query db directly with requireCan({ user: ["read"] }) first)
    - src/actions/categories.ts lines 28-29 + 54 + 78-86 (the canonical action templates: listX / createX / softDeleteX)
    - src/db/schema.ts (user table — role, bio, avatar columns from AUTH-08)
    - 04-CONTEXT.md D-07 (drawer UX), D-09 (self-service profile), D-11 (role assignment via dropdown + requireCan re-check)
    - 04-PATTERNS.md "src/actions/users.ts" assignment (lines 258-278)
    - 04-RESEARCH.md Open Question #4 (RESOLVED — add listUsers + updateUser; updateUser must re-check requireCan for the role-change path)
  </read_first>
  <behavior>
    - listUsers() calls requireCan({ user: ["read"] }) FIRST (Phase 2 Pitfall #1), then returns all users with their role/bio/avatar/email/name
    - updateUser(userId, { name?, bio?, avatar?, role? }) calls requireCan({ user: ["update"] }) FIRST — EXCEPT when userId === session.user.id (self-edit allowed for any role per D-09). For self-edit, role field is IGNORED (a user cannot self-promote)
    - Non-admin calling updateUser on another user → throws FORBIDDEN BEFORE any DB write (proven by MUST_NOT_BE_REACHED test mock)
    - Non-admin calling listUsers → throws FORBIDDEN BEFORE any DB query
    - Role change persists via auth.api.updateUser (Better Auth) OR direct db.update(schema.user).set({ role }) — whichever Better Auth requires
    - Bio + avatar persist via db.update(schema.user) (extended fields from AUTH-08)
  </behavior>
  <action>
    Per 04-RESEARCH.md Open Question #4 (RESOLVED), extend `src/actions/users.ts` with two new actions following the existing file template:

    **listUsers():** Add `export async function listUsers()`. First line: `await requireCan({ user: ["read"] })`. Then `await db.select({ id, name, email, role, bio, avatar, banned, banReason, banExpiresAt }).from(schema.user)` (omit sensitive columns like passwordHash / no need for `emailVerified` in the UI list). Return the rows; let the client component shape them for the table. Mirror `listPosts` from `src/actions/posts.ts` lines 173-179 for the select shape.

    **updateUser(userId, input):** Add `export async function updateUser(userId: string, input: { name?: string; bio?: string; avatar?: string; role?: "admin" | "editor" | "author" })`. Steps:
    1. `const session = await getSessionOrThrow()` — fail closed if not signed in.
    2. Self-edit check: `const isSelf = session.user.id === userId`. If `!isSelf`, `await requireCan({ user: ["update"] })` (admin-only). If `isSelf`, allow BUT strip `role` from the input (a user cannot self-promote) — `const { role: _ignored, ...safeInput } = input`.
    3. Persist credentials (name) via `auth.api.updateUser({ body: { userId, name: safeInput.name } })` IF Better Auth owns that field; otherwise `db.update(schema.user).set({ name }).where(eq(schema.user.id, userId))`. Read `src/lib/auth/index.ts` to confirm which path Better Auth expects.
    4. Persist extended fields (bio, avatar) via `db.update(schema.user).set({ bio, avatar }).where(eq(schema.user.id, userId))` — these are AUTH-08 fields on the Drizzle `user` table, NOT Better Auth internals.
    5. Persist role (only if NOT self-edit AND role is provided): `db.update(schema.user).set({ role }).where(eq(schema.user.id, userId))` OR `auth.api.updateUser({ body: { userId, role, banned } })` — Better Auth's admin plugin may handle role via a specific API; verify by reading `src/lib/auth/index.ts` and the Phase 2 SUMMARY.
    6. Log + return `{ id: userId }`.

    **Test extension** (`src/actions/__tests__/users.test.ts`): clone the existing `vi.hoisted` + mock scaffold (lines 14-81) and add new describe blocks:
    - `listUsers`: (a) admin → returns rows; (b) non-admin → throws FORBIDDEN BEFORE any db.select (MUST_NOT_BE_REACHED pattern from lines 162-174); (c) `requireCan({ user: ["read"] })` called with exact argument.
    - `updateUser`: (a) admin updates another user's role → persists; (b) non-admin updating another user → throws FORBIDDEN BEFORE db.update (MUST_NOT_BE_REACHED); (c) self-edit (any role) → allowed, role field stripped; (d) self-edit attempting role change → role is ignored, no error thrown (graceful).

    DO NOT add a destructive `deleteUser` action (D-08 — disable-only via the existing `banUser`). DO NOT add ban-reason/ban-expiry to the updateUser signature (Open Question #3 — simple toggle first).
  </action>
  <verify>
    <automated>pnpm test src/actions/__tests__/users.test.ts -x </automated>
    <automated>grep -q 'export async function listUsers' "src/actions/users.ts" </automated>
    <automated>grep -q 'export async function updateUser' "src/actions/users.ts" </automated>
    <automated>grep -qE 'requireCan.*user.*read' "src/actions/users.ts" </automated>
    <automated>grep -qE 'requireCan.*user.*update' "src/actions/users.ts" </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - `listUsers()` exported, permission-check-first verified by test
    - `updateUser(userId, input)` exported with self-edit path; non-admin cross-user → FORBIDDEN
    - Self-edit strips `role` (no self-promotion)
    - No destructive `deleteUser` added
    - users.test.ts extended; full file green
    - `pnpm build` succeeds
  </done>
  <acceptance_criteria>
    - `grep -c 'export async function listUsers' 'src/actions/users.ts'` returns 1
    - `grep -c 'export async function updateUser' 'src/actions/users.ts'` returns 1
    - `grep -cE "requireCan.*user.*read|requireCan.*user.*update" 'src/actions/users.ts'` returns ≥2
    - `grep -c 'isSelf' 'src/actions/users.ts'` returns ≥1 (self-edit path)
    - `grep -c 'deleteUser' 'src/actions/users.ts'` returns 0 (no destructive delete per D-08)
    - `pnpm test src/actions/__tests__/users.test.ts -x` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Users table + drawer (D-07, D-08, D-10, D-11) + self-service profile form (D-09)</name>
  <files>
    src/app/(admin)/dashboard/users/page.tsx,
    src/app/(admin)/dashboard/users/UsersTable.tsx,
    src/app/(admin)/dashboard/users/UserDrawer.tsx,
    src/app/(admin)/dashboard/profile/page.tsx,
    src/app/(admin)/dashboard/profile/ProfileForm.tsx
  </files>
  <read_first>
    - src/app/(admin)/dashboard/profile/page.tsx (CURRENT — moved by Plan 04-01 from (others-pages)/profile; the TailAdmin demo profile page; this Task wires it to real data per D-09)
    - src/app/(admin)/dashboard/posts/page.tsx (the dashboard list-page shell template)
    - src/app/(admin)/dashboard/posts/PostForm.tsx (the RHF + Zod + useMutation baseline from Plan 04-01)
    - src/components/dashboard/media/MediaPicker.tsx (from Plan 04-02 — reuse for the avatar field)
    - src/components/ui/modal/index.tsx (the drawer/modal shell — reuse for UserDrawer)
    - src/components/ui/table/index.ts (TailAdmin Table)
    - 04-CONTEXT.md D-07 (table + drawer), D-08 (disable-only), D-09 (self-service profile), D-10 (revoke only), D-11 (role dropdown)
    - 04-PATTERNS.md "src/app/(admin)/dashboard/users" + "profile" assignments
    - 02-03-SUMMARY.md (Phase 2 banUser / unbanUser / revokeSessions primitives — surfaced in the UI here)
  </read_first>
  <behavior>
    - GET /dashboard/users renders a TailAdmin table (name, email, role, status active/banned, actions: edit + ban/unban + revoke-sessions)
    - "+ New User" button opens UserDrawer (NOT navigation) with RHF form (name, email, password, role dropdown, bio, avatar)
    - Edit action opens UserDrawer prefilled with the user's current data (avatar via MediaPicker, role via dropdown)
    - Ban/Unban = a one-click action with confirm; calls banUser/unbanUser (Phase 2 D-16 primitive)
    - Revoke sessions = a one-click action with confirm; calls revokeSessions (Phase 2 D-17 primitive)
    - Ban/role-change use optimistic UI (D-27); createUser/updateUser use plain useMutation (not optimistic — server confirms)
    - GET /dashboard/profile renders ProfileForm prefilled with the current user's data; any role can edit their own name/bio/avatar (NOT role — D-09 explicit); admins editing their own profile land here too
    - UserDrawer avatar field uses <MediaPicker> from Plan 04-02 — onSelect={(url) => setValue('avatar', url)}
    - ProfileForm avatar field also uses <MediaPicker> (same component reuse)
  </behavior>
  <action>
    **Users page** (`src/app/(admin)/dashboard/users/page.tsx`): Server Component. Mirror `src/app/(admin)/dashboard/posts/page.tsx` shell. Call `listUsers()` in try/catch; pass rows to `<UsersTable initialUsers={rows} />`. Note: `listUsers` already enforces `requireCan({ user: ["read"] })` server-side — a non-admin hitting this route via direct URL gets FORBIDDEN at the action layer (the (admin) layout's AuthGate + sidebar role-filter (Plan 04-01) are supplementary).

    **UsersTable.tsx** (`"use client"`): TailAdmin Table. Columns: Name (with avatar thumbnail via `next/image`), Email, Role (badge), Status (Active/Banned), Actions. The Actions cell has: Edit (opens UserDrawer), Ban/Unban toggle (confirm + optimistic), Revoke sessions (confirm). Use `useMutation({ mutationFn: banUser, onMutate: optimistic-toggle-banned, onError: rollback, onSuccess: invalidate ['users'] })` for ban, and similar for unban + revokeSessions. The "+ New User" button at the top opens UserDrawer in create mode.

    **UserDrawer.tsx** (`"use client"`): A side drawer (reuse Modal shell from `src/components/ui/modal/index.tsx` with isFullscreen or slide-in styling). RHF + Zod form with: name (text), email (email), password (password, create-only — hidden on edit), role (select: admin/editor/author), bio (textarea), avatar (MediaPicker button — opens `<MediaPicker isOpen={...} onSelect={(url) => setValue('avatar', url)} />` from Plan 04-02). On submit: create mode → `useMutation({ mutationFn: createUser })`; edit mode → `useMutation({ mutationFn: updateUser })`. Neither is optimistic (D-27 — server confirmation preferred for create/edit).

    **Profile page** (`src/app/(admin)/dashboard/profile/page.tsx`): Server Component. Fetch the current user via `getSessionOrThrow()` + a `getUser(id)` helper (or extend `listUsers` to accept an id filter). Pass to `<ProfileForm initialUser={user} />`. Replace the TailAdmin demo content entirely.

    **ProfileForm.tsx** (`"use client"`): RHF + Zod form. Fields: name, bio, avatar (MediaPicker). NO role field (D-09 — self-edit cannot change own role). Submit calls `updateUser(session.user.id, { name, bio, avatar })` — the self-edit path strips role server-side anyway (defense in depth). `useMutation` non-optimistic (single-source user record).

    All avatar previews use `next/image` (CLAUDE.md hard rule — never raw `<img>`).

    DO NOT add per-device session list / IP / last-active UI (D-10 — v2 only). DO NOT add ban-reason / ban-expiry fields (Open Question #3 — simple toggle first).
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/dashboard/users/page.tsx" && test -f "src/app/(admin)/dashboard/users/UsersTable.tsx" && test -f "src/app/(admin)/dashboard/users/UserDrawer.tsx" </automated>
    <automated>test -f "src/app/(admin)/dashboard/profile/ProfileForm.tsx" </automated>
    <automated>grep -q 'MediaPicker' "src/app/(admin)/dashboard/users/UserDrawer.tsx" </automated>
    <automated>grep -q 'MediaPicker' "src/app/(admin)/dashboard/profile/ProfileForm.tsx" </automated>
    <automated>grep -q 'useMutation' "src/app/(admin)/dashboard/users/UsersTable.tsx" </automated>
    <automated>pnpm build 2>&1 | tee /dev/stderr | grep -q "Compiled successfully" </automated>
  </verify>
  <done>
    - `/dashboard/users` renders the users table (admin-only — sidebar hides for non-admins; server re-checks)
    - UserDrawer supports create/edit with RHF + Zod + useMutation; avatar field uses MediaPicker
    - Ban/Unban/Revoke-sessions work (Phase 2 primitives surfaced)
    - `/dashboard/profile` renders ProfileForm prefilled with current user; any role can self-edit name/bio/avatar (not role)
    - All image previews use `next/image`
    - Ban/role-change = optimistic; create/edit/profile-save = non-optimistic (D-27)
    - `pnpm build` succeeds
    - Manual: dark mode renders correctly on /dashboard/users + /dashboard/profile (DASH-08)
  </done>
  <acceptance_criteria>
    - `test -f 'src/app/(admin)/dashboard/users/page.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/users/UsersTable.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/users/UserDrawer.tsx'` exits 0
    - `test -f 'src/app/(admin)/dashboard/profile/ProfileForm.tsx'` exits 0
    - `grep -c 'MediaPicker' 'src/app/(admin)/dashboard/users/UserDrawer.tsx'` returns ≥1 (component reuse from Plan 04-02)
    - `grep -c 'MediaPicker' 'src/app/(admin)/dashboard/profile/ProfileForm.tsx'` returns ≥1
    - `grep -c 'useMutation' 'src/app/(admin)/dashboard/users/UsersTable.tsx'` returns ≥1
    - `grep -cE 'optimistic|onMutate' 'src/app/(admin)/dashboard/users/UsersTable.tsx'` returns ≥1 (D-27)
    - `grep -cE '<img ' 'src/app/(admin)/dashboard/users/'` returns 0 (no raw img)
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin → /dashboard/users route | Sidebar (Plan 04-01) hides the link; (admin)/layout.tsx AuthGate checks session; listUsers() re-checks requireCan({ user: ["read"] }) server-side (Pitfall #1) |
| self → /dashboard/profile | Any role may self-edit name/bio/avatar; role field stripped server-side (no self-promotion) |
| admin → user role change | updateUser re-checks requireCan({ user: ["update"] }); banUser/revokeSessions re-check per Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Elevation of privilege | Non-admin hitting /dashboard/users via direct URL | high | mitigate | listUsers() calls requireCan({ user: ["read"] }) FIRST; non-admin → FORBIDDEN (Phase 2 Pitfall #1 — proven by MUST_NOT_BE_REACHED test). Sidebar (Plan 04-01) is supplementary |
| T-04-11 | Elevation of privilege | Self-edit attempting role promotion | high | mitigate | updateUser self-edit path strips `role` from input server-side (isSelf check); defense in depth — UI hides role field in ProfileForm AND server strips it |
| T-04-12 | Elevation of privilege | Non-admin calling updateUser on another user | high | mitigate | requireCan({ user: ["update"] }) checked BEFORE any db.update when !isSelf — MUST_NOT_BE_REACHED test proves ordering |
| T-04-13 | Tampering | Avatar URL field | low | accept | URL stored as-is in user.avatar; rendered via next/image (which validates via remotePatterns). No HTML/JS injection — URLs used as `src`, not innerHTML. Same posture as PostForm feature-image (Plan 04-02) |
| T-04-14 | Repudiation | Ban/role-change optimistic UI rollback | medium | mitigate | onError rolls back the cache; server is source of truth. If ban fails (FORBIDDEN), the row re-appears Active with an error toast |
| T-04-15 | Information disclosure | listUsers exposes user emails | medium | accept | Admin-only surface (requireCan gate); the small editorial team (2-5 per CLAUDE.md) legitimately needs to see member emails. No external/PII leak — admin is trusted |
</threat_model>

<verification>
**Wave 2 gate (this plan):**
- `pnpm test src/actions/__tests__/users.test.ts -x` passes (extended with listUsers/updateUser coverage)
- `pnpm build` succeeds
- Manual: sign in as admin → create a new editor; ban them; revoke their sessions; unban; assign admin role
- Manual: sign in as the new editor → /dashboard/profile renders; self-edit name/bio/avatar succeeds; role field absent
- Manual: sign in as author → /dashboard/users NOT in sidebar; hitting the URL directly → FORBIDDEN error
- Manual: dark mode renders on /dashboard/users + /dashboard/profile (DASH-08)
</verification>

<success_criteria>
- DASH-04: Users + roles management UI (admin-only, drawer-based create/edit/disable/revoke/role-assign + self-service profile) — fully satisfied
- DASH-06: RHF + Zod + TanStack Query applied to all new forms; optimistic UI per D-27 — extended
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-chrome/04-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New files:**
- `src/app/(admin)/dashboard/users/{page.tsx,UsersTable.tsx,UserDrawer.tsx}` — admin-only users management surface
- `src/app/(admin)/dashboard/profile/ProfileForm.tsx` — self-service form (any role)

**Modified files:**
- `src/actions/users.ts` — extended with `listUsers` + `updateUser` (self-edit path; no destructive delete per D-08)
- `src/actions/__tests__/users.test.ts` — extended with listUsers/updateUser coverage (permission-check-first + self-edit semantics)
- `src/app/(admin)/dashboard/profile/page.tsx` — wired to real data (was TailAdmin demo, moved by Plan 04-01)

**No new deps** (RHF + Zod + TanStack Query + MediaPicker all already available from prior phases/plans).
</output>
