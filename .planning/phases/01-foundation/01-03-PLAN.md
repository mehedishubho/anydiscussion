---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/r2/index.ts
  - scripts/setup.mjs
  - scripts/verify.mjs
autonomous: true
requirements:
  - FOUND-05
  - FOUND-01
  - FOUND-04
  - FOUND-06
user_setup:
  - service: docker
    why: "Docker Compose provides Postgres 16 + MinIO for local dev and the throwaway Postgres for the clean-room migration test"
    env_vars: []
    dashboard_config:
      - task: "Ensure Docker Desktop is running before pnpm setup"
        location: "Docker Desktop (system tray) — start the Docker engine"

must_haves:
  truths:
    # Success Criterion 5 (FOUND-05)
    - "src/lib/r2/index.ts exports an s3Client (S3Client) and uploadImageVariants(buffer, baseKey) function that produces 3 sharp-derived WebP variants (640/1024/1920 sm/md/lg)"
    - "A test upload through lib/r2 writes 3 objects to MinIO (S3-compatible storage) with ContentType image/webp — the pnpm verify R2 smoke step confirms this end-to-end"
    - "src/lib/r2 uses env-driven forcePathStyle (true for MinIO, false for R2) per D-12 — no hardcoded storage backend"
    # Dev infra CONSUMED (not created) — docker-compose.yml + .env.example shipped in Plan 01 Task 1c; this plan only consumes them
    - "scripts/setup.mjs runs: pnpm install → pnpm approve-builds (sharp) → docker compose up -d (against the Plan 01 compose file) → run migrations → confirm MinIO bucket exists"
    # Verification (D-15)
    - "pnpm verify machine-checks each success criterion 1:1: validates next.config (cacheComponents/standalone/image-loader), runs drift gate (generate + git diff), runs clean-room migration test, runs ESLint isolation against a planted cross-group import (must fail), runs next build (succeeds), runs R2 upload smoke against MinIO"
  artifacts:
    - src/lib/r2/index.ts
    - scripts/setup.mjs
    - scripts/verify.mjs
  key_links:
    - "src/lib/r2/index.ts S3_FORCE_PATH_STYLE env → MinIO (true) vs R2 (false) toggle (D-12)"
    - "docker-compose.yml minio service (Plan 01 Task 1c) → src/lib/r2 smoke test (the storage backend for FOUND-05)"
    - "docker-compose.yml postgres-test service (Plan 01 Task 1c) → scripts/test-migrations.mjs (Plan 02's clean-room test target)"
    - ".env.example (Plan 01 Task 1c) → src/lib/r2 env vars (S3_* defaults) — the single committed env contract"
    - "scripts/verify.mjs → orchestrates all 5 success criteria checks (the single pnpm verify gate, D-15)"
---

<objective>
R2/sharp media pipeline + the pnpm setup / pnpm verify orchestrators.

Purpose: Prove the media pipeline end-to-end (file → sharp variants → object in S3-compatible storage) and provide the single-command developer onboarding (pnpm setup) and single-command phase gate (pnpm verify) that machine-checks all 5 success criteria. This completes the Walking Skeleton: a developer runs pnpm setup && pnpm verify and the entire backbone is proven working. The Docker Compose stack + .env.example are already shipped in Plan 01 Task 1c (Wave 1) — this plan only consumes them.

Output: A minimal server-side lib/r2 upload helper (D-14 — presigned URLs and media UI are Phase 3), the pnpm setup onboarding script, and the pnpm verify orchestrator (all 5 success criteria, including the R2 smoke run via `node --experimental-strip-types scripts/r2-smoke.ts`).
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@D:/Devsroom-Work/anydiscussion/.planning/PROJECT.md
@D:/Devsroom-Work/anydiscussion/.planning/ROADMAP.md
@D:/Devsroom-Work/anydiscussion/CLAUDE.md
@D:/Devsroom-Work/anydiscussion/.claude/CLAUDE.md
@D:/Devsroom-Work/anydiscussion/.planning/phases/01-foundation/01-CONTEXT.md
@D:/Devsroom-Work/anydiscussion/.planning/phases/01-foundation/01-RESEARCH.md
@D:/Devsroom-Work/anydiscussion/.planning/phases/01-foundation/01-PATTERNS.md
@D:/Devsroom-Work/anydiscussion/.planning/phases/01-foundation/01-01-SUMMARY.md
@D:/Devsroom-Work/anydiscussion/.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimal R2/sharp upload helper (lib/r2)</name>
  <files>src/lib/r2/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: R2/S3 Client + Sharp Upload Helper — lines 572-644, verified @aws-sdk/client-s3 3.1077.0 + sharp 0.35.2 pipeline; Pitfall 3: MinIO forcePathStyle — lines 748-755; Anti-Patterns: forcePathStyle:false for MinIO — lines 704-706)
    - .planning/phases/01-foundation/01-PATTERNS.md (src/lib/r2/index.ts assignment — lines 254-265; Env-Driven Config convention — lines 371-376)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12 CDN URL, D-14 minimal helper)
    - .env.example (shipped in Plan 01 Task 1c — confirm the S3_* var names match before reading them here)
  </read_first>
  <action>
    Create the minimal server-side R2/sharp upload helper. The Docker Compose stack and .env.example are already shipped in Plan 01 Task 1c (Wave 1) — this task only builds lib/r2 against those committed env vars. This implements D-12 (env-driven CDN/storage — the S3_* defaults come from .env.example) and D-14 (minimal helper only — no presigned URLs, no media UI).

    1. CREATE src/lib/r2/index.ts — the minimal server-side upload helper per RESEARCH.md Pattern 4 lines 579-644 (verified @aws-sdk/client-s3 3.1077.0 + sharp 0.35.2). Structure:
    - Import S3Client, PutObjectCommand from @aws-sdk/client-s3; sharp from sharp.
    - Read env vars at module top with MinIO defaults (matching .env.example shipped in Plan 01): endpoint (S3_ENDPOINT || "http://localhost:9000"), region (S3_REGION || "us-east-1"), accessKeyId (S3_ACCESS_KEY_ID || "minioadmin"), secretAccessKey (S3_SECRET_ACCESS_KEY || "minioadmin"), bucket (S3_BUCKET || "anydiscussion-media"), forcePathStyle (process.env.S3_FORCE_PATH_STYLE === "true" — boolean from string, Pitfall 3).
    - Export const s3Client = new S3Client({ region, endpoint, credentials: { accessKeyId, secretAccessKey }, forcePathStyle }).
    - Export interface UploadedVariant { key: string; width: number; height: number; format: string; sizeBytes: number }.
    - Export async function uploadImageVariants(buffer: Buffer, baseKey: string): Promise<UploadedVariant[]>. Inside: define sizes array [{width:640,suffix:"sm"},{width:1024,suffix:"md"},{width:1920,suffix:"lg"}]. For each size: run sharp(buffer).resize(size.width, undefined, { fit: "inside", withoutEnlargement: true }).webp({ quality: 80 }).toBuffer({ resolveWithObject: true }) → destruct { data, info }. Build key as `${baseKey}-${size.suffix}.webp`. Send PutObjectCommand({ Bucket: bucket, Key: key, Body: data, ContentType: "image/webp" }). Push { key, width: info.width, height: info.height, format: "webp", sizeBytes: info.size } to variants array. Return variants.
    - NO "use client" — server-only. NO presigned URL logic (Phase 3 MEDIA-01 per D-14).
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/anydiscussion" &amp;&amp; test -f src/lib/r2/index.ts &amp;&amp; grep -q 'S3Client' src/lib/r2/index.ts &amp;&amp; grep -q 'uploadImageVariants' src/lib/r2/index.ts &amp;&amp; grep -q 'forcePathStyle' src/lib/r2/index.ts &amp;&amp; grep -q 'sharp' src/lib/r2/index.ts &amp;&amp; echo 'r2-ok'; ! grep -q 'presigner\|PresignedUrl' src/lib/r2/index.ts &amp;&amp; echo 'no-presigned'</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/r2/index.ts exports s3Client (S3Client instance) and uploadImageVariants (async function)
    - src/lib/r2/index.ts reads S3_FORCE_PATH_STYLE === "true" for the forcePathStyle toggle (boolean from string env — Pitfall 3 mitigation)
    - src/lib/r2/index.ts uploadImageVariants produces exactly 3 WebP variants at widths 640, 1024, 1920
    - src/lib/r2/index.ts contains NO presigned-URL code (no @aws-sdk/s3-request-presigner import — Phase 3 scope per D-14)
    - src/lib/r2/index.ts has NO "use client" directive (server-only)
    - A manual upload smoke (a 1x1 PNG buffer through uploadImageVariants against a running MinIO) writes 3 objects — full smoke automated in Task 2's pnpm verify
  </acceptance_criteria>
  <done>
    lib/r2 writes 3 sharp-derived WebP variants to S3-compatible storage with env-driven forcePathStyle; presigned-URL logic correctly absent (Phase 3 scope). Docker Compose + .env.example are already committed by Plan 01 — no infra created here.
  </done>
</task>

<task type="auto">
  <name>Task 2: pnpm setup onboarding + pnpm verify orchestrator (all 5 success criteria)</name>
  <files>scripts/setup.mjs, scripts/verify.mjs</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples: Drift Gate — lines 833-840; Clean-Room Migration Test Script — lines 783-829; Validation Architecture Wave 0 Gaps — lines 932-943)
    - .planning/phases/01-foundation/01-PATTERNS.md (scripts/setup.mjs + verify.mjs assignment — lines 325-337; Migration Hygiene convention — lines 385-390)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 setup script, D-09 clean-room test, D-15 verify script)
    - .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture — Phase Requirements → Test Map, lines 912-924)
    - package.json (from Plan 01 — confirms the setup/verify/test:migrations script entries exist)
    - scripts/test-migrations.mjs (from Plan 02 — verify.mjs calls it as a subprocess or imports its logic)
  </read_first>
  <action>
    Build the two cross-platform .mjs scripts that complete the developer loop. Implements D-04 (setup), D-15 (verify). Both scripts MUST be Windows-safe: shell out via node:child_process execFileSync (NOT bash-isms, NOT execa — primary dev OS is Windows per D-04). Use process.exitCode = 1 on failure (NOT process.exit(1) — allow finally blocks). Use ✓/✗ console markers.

    1. CREATE scripts/setup.mjs — the clone-to-running onboarding script per D-04. Sequence (each step logs before/after with ✓/✗):
    - Step 1: run pnpm install (execFileSync("pnpm", ["install"], { stdio: "inherit", shell: true })). The shell:true is needed on Windows for pnpm to resolve.
    - Step 2: run pnpm approve-builds (non-interactive where possible — if pnpm approve-builds is interactive and blocks, instead manually ensure package.json has pnpm.onlyBuiltDependencies containing "sharp" — Plan 01 Task 1 persists it; this step verifies it exists and re-runs pnpm install if needed to trigger the sharp postinstall).
    - Step 3: run docker compose up -d (starts postgres, postgres-test, minio). Wait for postgres healthcheck (poll docker inspect or sleep 5s).
    - Step 4: apply migrations — run pnpm db:generate is NOT needed (migrations already committed); instead run a migration-apply step. Import the migrate runner inline OR shell out to a small inline node -e that calls drizzle-orm/node-postgres/migrator against DATABASE_URL. Alternatively, document that the user runs pnpm test:migrations which applies migrations to the test DB; for the dev DB, run node -e with the migrator against DATABASE_URL (port 5432).
    - Step 5: confirm MinIO bucket exists — use @aws-sdk/client-s3 HeadBucketCommand or CreateBucketCommand against the configured S3_ENDPOINT/bucket. If bucket does not exist, create it. Log the result.
    - Log a final "Setup complete — run pnpm dev to start" message.

    2. CREATE scripts/verify.mjs — the single pnpm verify gate per D-15. It machine-checks each of the 5 success criteria 1:1. Structure as a sequence of named check functions, each returning a boolean or throwing, with ✓/✗ logging. Run all checks; if any fails, set process.exitCode = 1 at the end (after printing a summary).

    Check 1 (FOUND-01 — next.config validation): read next.config.ts as text. Assert it contains "cacheComponents", "standalone", "image-loader" (the loaderFile reference). Log ✓ or ✗ with the missing tokens.

    Check 2 (FOUND-03/FOUND-06 — drift gate): run pnpm drizzle-kit generate --name ci-drift-check via execFileSync. Then run git diff --exit-code src/db/migrations/ (execFileSync("git", ["diff","--exit-code","src/db/migrations/"])). Exit code 0 = no drift (pass). Non-zero = drift detected (fail). Per RESEARCH.md Pitfall 1 — this is the REAL drift gate (NOT drizzle-kit check alone).

    Check 3 (FOUND-06 — clean-room migration test): ensure postgres-test container is up (docker compose up -d postgres-test), then run node scripts/test-migrations.mjs as a subprocess. It must exit 0 with all 8 tables present.

    Check 4 (FOUND-04 — ESLint isolation): create a TEMPORARY planted cross-group import file in a temp directory (NOT under src/app/ — to avoid breaking the real build; use a path like .eslint-planted-test/site-imports-admin.ts). The file contains an import from "@/app/(admin)/anything". Run eslint on that file: execFileSync("npx", ["eslint", ".eslint-planted-test/site-imports-admin.ts"]). The eslint command MUST exit non-zero (the rule fires). If it exits 0, the isolation rule is broken — fail. Then clean up the temp file. Also verify the inverse: a file importing from @/lib/db does NOT trigger the error. CRITICAL: the planted file path must NOT match src/app/ — keep it in .eslint-planted-test/ which the Next build ignores but eslint can still lint (add it to the eslint files glob, or run eslint with an explicit --no-ignore flag on it).

    Check 5 (FOUND-01 — next build succeeds): run pnpm build via execFileSync with stdio inherit. Must exit 0.

    Check 6 (FOUND-05 — R2 upload smoke against MinIO): ensure minio container is up. The smoke must import the TypeScript module src/lib/r2/index.ts directly. Use Node's built-in TypeScript stripping as the runner — the env has Node 24.15.0 (confirmed in RESEARCH.md Environment Availability), which supports `node --experimental-strip-types`. Concretely: shell out via execFileSync("node", ["--experimental-strip-types", "scripts/r2-smoke.ts"]). Create scripts/r2-smoke.ts alongside verify.mjs (NOT in files_modified for the plan since it is a transient verify-time helper — but commit it so CI can run it). The smoke script imports { uploadImageVariants } from "../src/lib/r2/index.ts", creates a 1x1 PNG buffer via sharp({create:{width:1,height:1,channels:3,background:{r:255,g:0,b:0}}}).png().toBuffer(), calls uploadImageVariants(buffer, "smoke-test/test"), asserts exactly 3 variants returned with keys smoke-test/test-sm.webp, smoke-test/test-md.webp, smoke-test/test-lg.webp, then optionally cleans up (DeleteObjects via the s3Client). Log ✓ on success, ✗ and set process.exitCode = 1 on any failure.

    Print a summary table of all 6 checks (PASS/FAIL). Exit 0 if all pass, 1 otherwise.

    Print a summary table of all 6 checks (PASS/FAIL). Exit 0 if all pass, 1 otherwise.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/anydiscussion" &amp;&amp; test -f scripts/setup.mjs &amp;&amp; test -f scripts/verify.mjs &amp;&amp; echo 'scripts-exist'; docker compose up -d 2>/dev/null; sleep 5; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - scripts/setup.mjs exists and runs pnpm install, docker compose up -d, applies migrations to the dev DB, and confirms the MinIO bucket — exits 0 on a clean clone after .env.example is copied to .env.local
    - scripts/verify.mjs exists and runs all 6 named checks (next.config validation, drift gate, clean-room migration test, ESLint isolation planted-import, next build, R2 upload smoke)
    - scripts/verify.mjs drift gate uses `drizzle-kit generate` + `git diff --exit-code src/db/migrations/` (NOT drizzle-kit check alone — RESEARCH.md Pitfall 1)
    - scripts/verify.mjs ESLint check creates a planted cross-group import file, runs eslint on it, and asserts the command FAILS (non-zero exit = rule fired = pass)
    - pnpm verify exits 0 when all infrastructure is up (docker compose up -d) and the codebase is in a passing state
    - pnpm verify exits 1 (with a clear summary) when any check fails — e.g., if you temporarily remove cacheComponents from next.config.ts, check 1 fails
    - Both scripts use execFileSync or spawnSync (NOT bash-isms) — Windows-safe per D-04
    - Both scripts set process.exitCode = 1 on failure (NOT process.exit(1)) — allows cleanup/finally to run
  </acceptance_criteria>
  <done>
    pnpm setup takes a clean clone to a running dev environment (deps + sharp builds + Postgres + MinIO + migrations applied + bucket confirmed). pnpm verify machine-checks all 5 success criteria and exits 0 only when the entire backbone is proven working end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → S3-compatible storage | The lib/r2 helper writes objects to MinIO/R2 — the boundary is the s3Client singleton; keys are server-generated (baseKey parameter), not from untrusted user input in Phase 1 |
| Dev machine → Docker services | Docker Compose exposes Postgres (5432/5433) and MinIO (9000/9001) on localhost — acceptable for local dev, must not be exposed externally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | Path traversal in R2 keys | medium | mitigate | The baseKey parameter in uploadImageVariants is server-generated (e.g., "posts/2026/07/my-image"). No user-supplied path component in Phase 1. Phase 3 must sanitize user-uploaded filenames before they reach baseKey. |
| T-01-10 | Denial of Service | Image bomb (decompression attack) via sharp | medium | mitigate | sharp has built-in pixel/byte limits. For Phase 1's minimal helper, the input is a developer test buffer. Phase 3 (MEDIA-01) must add input validation (max file size, max dimensions, magic-byte format check) before calling sharp. |
| T-01-11 | Information Disclosure | S3 credentials in env | high | mitigate | S3_ACCESS_KEY_ID and S3_SECRET_ACCESS_KEY live only in gitignored .env.local for real secrets. .env.example ships MinIO defaults (minioadmin/minioadmin) which are safe for local dev only. Never prefixed NEXT_PUBLIC_. Coolify injects real R2 keys at deploy. |
| T-01-12 | Tampering | Docker ports exposed externally | low | accept | Docker Compose binds to localhost:5432/5433/9000/9001 by default on the dev machine. Not deployed in Phase 1. Acceptable risk for local dev; Phase 7 deployment hardening covers production network config. |
| T-01-SC | Tampering | No new package installs in this plan | low | accept | All packages (drizzle-orm, pg, @aws-sdk/client-s3, sharp) were installed and audited in Plan 01. No additional installs here. |
</threat_model>

<verification>
- docker compose config validates the compose file (shipped in Plan 01 Task 1c — this plan only consumes it)
- docker compose up -d starts all 3 services (postgres, postgres-test, minio) without errors
- pnpm setup completes end-to-end on a clean clone (deps → sharp builds → Compose → migrations → bucket)
- pnpm verify runs all 6 checks and exits 0 when the backbone is healthy
- pnpm verify exits 1 with a clear summary if any check fails (e.g., temporarily breaking next.config.ts, or planting an un-migrated schema change)
- The R2 smoke (node --experimental-strip-types scripts/r2-smoke.ts) writes 3 WebP objects to MinIO (verifiable via the MinIO console at localhost:9001)
</verification>

<success_criteria>
- FOUND-05: lib/r2 upload writes objects to S3-compatible storage with sharp-derived variants (3 WebP at 640/1024/1920) — proven by the verify R2 smoke
- FOUND-01 (cross-checked): pnpm verify validates the next.config block (cacheComponents/standalone/image-loader) AND next build succeeds
- FOUND-04 (cross-checked): pnpm verify's planted-import ESLint check proves the isolation rule fires
- FOUND-06 (cross-checked): pnpm verify runs the drift gate (generate + git diff) AND the clean-room migration test
- D-04: pnpm setup provides clone-to-running onboarding
- D-15: pnpm verify machine-checks all 5 success criteria 1:1
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Config keys / files:**
- (docker-compose.yml + .env.example shipped in Plan 01 Task 1c — this plan consumes them, does not create them)

**Env vars (consumed from .env.example — Plan 01):**
- `S3_ENDPOINT`, `S3_REGION`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `S3_BUCKET`, `S3_FORCE_PATH_STYLE` (read by src/lib/r2)

**Functions / exports:**
- `s3Client` (named export, src/lib/r2/index.ts) — S3Client singleton
- `uploadImageVariants(buffer, baseKey)` (named export, src/lib/r2/index.ts) — async, returns UploadedVariant[]
- `UploadedVariant` (interface, src/lib/r2/index.ts) — { key, width, height, format, sizeBytes }

**New file paths:**
- `src/lib/r2/index.ts`
- `scripts/setup.mjs`
- `scripts/verify.mjs`
- `scripts/r2-smoke.ts` (transient verify-time helper, committed so CI can run the R2 smoke via `node --experimental-strip-types`)

**pnpm script names (implemented here):**
- `setup` (clone-to-running onboarding — D-04)
- `verify` (the single phase gate, machine-checks all 5 success criteria — D-15)

**R2/sharp variant contract (consumed by Phase 3 MEDIA-01):**
- 3 WebP variants per upload: `{baseKey}-sm.webp` (640w), `{baseKey}-md.webp` (1024w), `{baseKey}-lg.webp` (1920w)
- Each variant: `image/webp`, quality 80, `fit: "inside"`, `withoutEnlargement: true`
