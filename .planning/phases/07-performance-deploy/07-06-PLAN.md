---
phase: 07-performance-deploy
plan: 06
type: execute
wave: 6
depends_on:
  - 07-01
  - 07-02
  - 07-03
  - 07-04
gap_closure: true
files_modified:
  - src/lib/auth/index.ts
  - src/lib/rate-limit/index.ts
  - src/lib/rate-limit/__tests__/client-ip.test.ts
  - src/lib/rate-limit/upstash-ioredis-adapter.ts
  - src/lib/rate-limit/__tests__/rate-limit.test.ts
  - src/actions/contact.ts
  - src/actions/newsletter.ts
  - src/actions/__tests__/newsletter.test.ts
  - src/actions/__tests__/taxonomy.test.ts
  - src/actions/__tests__/pages.test.ts
  - src/actions/__tests__/users.test.ts
  - src/actions/pages-schema.ts
  - src/actions/pages.ts
  - scripts/test-auth-ratelimit.mjs
  - .env.example
  - .planning/phases/07-performance-deploy/07-VERIFICATION.md
autonomous: true
requirements:
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-06
user_setup: []

must_haves:
  truths:
    - "Better Auth keys auth rate limits per client IP behind an appending proxy: advanced.ipAddress.trustedProxies in src/lib/auth/index.ts is populated from the TRUSTED_PROXY_CIDR env var (comma-separated CIDRs, comma-split + trimmed + empty-filtered); unset/empty keeps the existing single-value XFF behavior so local dev and the existing integration harness are unchanged (CR-01 leg 1, 07-VERIFICATION gap #2)"
    - "src/actions/contact.ts and src/actions/newsletter.ts key their limiters on the LAST X-Forwarded-For hop via one shared helper getClientIpFromXff exported from src/lib/rate-limit/index.ts — never the client-spoofable first hop; the extraction style exists in exactly one place (CR-01 leg 2)"
    - "A Redis outage degrades the public forms to the defined RATE_LIMITED contract instead of a raw error: submitContact catches a contactFormLimiter rejection and throws Error(\"RATE_LIMITED\") (fail-closed, consistent with the auth limiter's documented T-07-02-06 policy); subscribeNewsletter catches a newsletterLimiter rejection and returns { status: \"error\", message: \"RATE_LIMITED\" } (WR-01)"
    - "The rate-limit unit tests tell the truth: resetEphemeralCache clears the real ephemeral cache (the exported contactFormEphemeralCache Map consumed by the Ratelimit config), and a rejection test proves a Redis failure propagates out of limit() — documenting contact.ts's required catch (WR-02/WR-03)"
    - "Regressions in the Phase-07 revalidation wiring fail green: taxonomy.test.ts, pages.test.ts, and users.test.ts assert concrete revalidatePath literals and 2-arg revalidateTag(\"posts-list\", \"max\") / revalidateTag(`category-${id}`, \"max\") calls per mutating action (WR-04)"
    - "updatePage accepts true partial input: pageUpdateSchema (pageSchema.partial() + required id) parses updatePage(1, { status: \"published\" }) without a ZodError, and the JSDoc Partial<PageInput> promise holds (WR-05)"
    - "scripts/test-auth-ratelimit.mjs reliably reruns: POSIX cleanup kills the process group (detached spawn on non-win32 + negative-PID kill with a LOGGED failure path), the structural check requires the trustedProxies token, TEST_IP is randomized per run inside RFC 5737 space so stale Redis buckets cannot poison attempt counts, and the manual-run instructions include the through-the-proxy XFF verification (WR-06 + CR-01 leg 3)"
    - "07-VERIFICATION.md frontmatter records both fc3286d overrides verbatim from its own Override Suggestions section with accepted_by \"owner (mehedishubho)\" and accepted_at \"2026-08-26\", overrides_applied: 2, truths 4/13 flipped to override-accepted, gaps #3/#4 closed as accepted overrides"
    - "The 429 behavior leg is exercised: docker compose up -d redis + a fresh build + pnpm test:auth-ratelimit runs end-to-end and the HTTP result (PASS, or documented environment SKIP) is recorded in the SUMMARY — attempts 1-3 non-429, attempt 4 HTTP 429 with X-Retry-After/Retry-After"
    - "The full vitest suite stays green after every fix (621+ tests, exit 0), and pnpm build completes (type-check gate)"
  artifacts:
    - "src/lib/rate-limit/index.ts exporting getClientIpFromXff alongside the existing limiter re-exports"
    - "src/lib/rate-limit/__tests__/client-ip.test.ts (new) — pure-function tests for the last-hop extraction"
    - "src/lib/rate-limit/upstash-ioredis-adapter.ts exporting contactFormEphemeralCache (a Map passed as the Ratelimit ephemeralCache config — the supported reset surface)"
    - "src/actions/pages-schema.ts exporting pageUpdateSchema"
    - ".env.example carrying the TRUSTED_PROXY_CIDR documented block (empty default) next to the existing Redis section"
    - "scripts/test-auth-ratelimit.mjs with the trustedProxies structural token, POSIX-safe cleanup, per-run random TEST_IP, and the through-the-proxy manual verification instructions"
    - ".planning/phases/07-performance-deploy/07-VERIFICATION.md updated with the applied overrides block"
  key_links:
    - ".env.example TRUSTED_PROXY_CIDR -> src/lib/auth/index.ts advanced.ipAddress.trustedProxies (the deployment-topology trust anchor — env-driven, never hardcoded, per the escalation-gate decision)"
    - "src/actions/contact.ts + src/actions/newsletter.ts -> getClientIpFromXff (src/lib/rate-limit/index.ts) — one extraction style for both public-form consumers, per the newsletter.ts 'do not invent a second style' contract"
    - "src/lib/rate-limit/__tests__/rate-limit.test.ts -> contactFormEphemeralCache (src/lib/rate-limit/upstash-ioredis-adapter.ts) — the test reset path and the limiter config share one exported Map"
    - "src/actions/pages.ts updatePage -> pageUpdateSchema (src/actions/pages-schema.ts) — the Partial<PageInput> signature promise and the parse behavior agree"
    - "07-VERIFICATION.md overrides block -> truths 4/13 rows + former gaps #3/#4 — a re-verification reads the fc3286d deviations as accepted overrides, not open gaps"
---

## Phase Goal (gap closure)

Close the repo-fixable items from 07-VERIFICATION.md (status: gaps_found, 8/16): the CR-01 rate-limit IP-trust blocker, the six WR-01..06 advisory fixes from 07-REVIEW.md, formal recording of the two owner-accepted fc3286d overrides, and the first end-to-end execution of the auth rate-limit 429 harness. Live-stack legs (deploy, Lighthouse, publish-visible, Umami/DNS) remain owner-deferred and are documented as such below — this plan does not reopen them.

<objective>
Gap-closure slice over 07-VERIFICATION.md:

1. CR-01 (verification gap #2, truths 9/10): fix the client-IP trust model — configure Better Auth `advanced.ipAddress.trustedProxies` from a TRUSTED_PROXY_CIDR env var in src/lib/auth/index.ts; switch contact.ts + newsletter.ts limiter keying from the spoofable first XFF hop to the last hop via one shared helper; add the through-the-proxy curl verification to the harness's manual-run instructions.
2. WR-01..06 (07-REVIEW.md / verification Anti-Patterns table): Redis-outage graceful degradation on the public forms; make resetEphemeralCache real and the failure-path test actually simulate failure; add revalidation call assertions to taxonomy/pages/users tests; fix the broken Partial<PageInput> contract in updatePage; make the harness's POSIX kill path and reruns reliable.
3. Record the two fc3286d overrides in 07-VERIFICATION.md frontmatter exactly as the report's Override Suggestions section specifies (accepted_by "owner (mehedishubho)", accepted_at "2026-08-26"), and update truths 4/13 + gaps #3/#4 so re-verification reads them as accepted.
4. Execute the auth rate-limit integration harness locally (docker-compose redis, local-dev only) to close truth 9's behavior_unverified item, recording the outcome.

Purpose: CR-01 inverts the brute-force protection in exactly the production-like conditions PERF-04 demands (behind an appending proxy, all auth traffic shares one 3/15min bucket — a trivial unauthenticated auth DoS) and must be fixed before any deploy; the WR fixes replace false test assurance with real regression pins; the override recording converts operator-approved deviations (2026-07-29 fc3286d, re-accepted at the escalation gate 2026-08-26) into formally recorded state so the next verification pass scores them correctly.

Output: corrected IP-trust model (auth + both public forms), truthful rate-limit tests, assertion-covered revalidation wiring, fixed updatePage partial contract, reliable + structurally-pinning harness with a recorded live 429 run, and an updated 07-VERIFICATION.md whose open gaps reduce to the four owner-deferred live-stack items.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-deploy/07-CONTEXT.md
@.planning/phases/07-performance-deploy/07-VERIFICATION.md
@.planning/phases/07-performance-deploy/07-REVIEW.md
@.planning/phases/07-performance-deploy/07-04-SUMMARY.md

@src/lib/auth/index.ts
@src/lib/rate-limit/index.ts
@src/actions/contact.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — proxy IP-trust model (trustedProxies + last-hop keying) and WR-01 Redis-outage graceful degradation</name>
  <files>src/lib/auth/index.ts, src/lib/rate-limit/index.ts, src/lib/rate-limit/__tests__/client-ip.test.ts, src/actions/contact.ts, src/actions/newsletter.ts, src/actions/__tests__/newsletter.test.ts, .env.example</files>
  <read_first>
- node_modules/.pnpm/@better-auth+core@1.6.23_@b_5b27005f42a6b8a46b0797e8da4409c3/node_modules/@better-auth/core/dist/utils/ip.mjs — the INSTALLED truth: getIPFromHeader strips the XFF chain from the right when trustedProxies entries match, returns null for multi-value XFF without them (the exact line the verifier confirmed), and drops invalid CIDR entries via findInvalidTrustedProxies. Verify the trustedProxies wiring against this dist, not docs.
- src/lib/auth/index.ts — current rateLimit block + the advanced.ipAddress block with its T-07-02-03 comment (the false "proxy overwrites XFF" claim being replaced)
- src/lib/rate-limit/index.ts — re-export module where the shared helper lands (currently pure re-exports; header documents the limiter history)
- src/actions/contact.ts — step 3 of submitContact: the first-hop XFF extraction at line 77 and the unguarded contactFormLimiter.limit call at lines 78-81
- src/actions/newsletter.ts — step 3 of subscribeNewsletter: the identical first-hop extraction at line 187 and the unguarded newsletterLimiter.limit call; note the docblock contract at lines 147-148 ("contact.ts's extraction, do not invent a second style")
- src/actions/__tests__/newsletter.test.ts — the headers mock at line ~277 (x-forwarded-for returns "203.0.113.7") and the limiter assertion at line ~304 (limit called with "203.0.113.7") that must stay green
- src/components/site/ContactForm.tsx (line ~99) and src/components/site/NewsletterForm.tsx (line ~96) — both already map the RATE_LIMITED sentinel to friendly copy; the server-side fix plugs into an existing contract
- .planning/phases/07-performance-deploy/07-VERIFICATION.md — gap #2 (truth 10) and the Anti-Patterns rows for CR-01
- .planning/phases/07-performance-deploy/07-REVIEW.md — CR-01 full analysis + WR-01 (the verified @upstash/ratelimit@2.0.8 safeEval rethrow behavior)
  </read_first>
  <behavior>
    - Test 1 (new client-ip.test.ts): getClientIpFromXff("203.0.113.42") returns "203.0.113.42" (single value — local dev + existing harness unchanged)
    - Test 2: getClientIpFromXff("1.2.3.4, 203.0.113.9") returns "203.0.113.9" (client-injected first hop ignored; proxy-appended last hop wins)
    - Test 3: getClientIpFromXff("1.2.3.4 , 5.6.7.8 ") returns "5.6.7.8" (whitespace after comma and trailing, both trimmed)
    - Test 4: getClientIpFromXff(null), getClientIpFromXff(""), and getClientIpFromXff("   ") each return "unknown"
    - Test 5 (newsletter.test.ts, action-level): with the headers mock returning a multi-value x-forwarded-for "9.9.9.9, 203.0.113.7", subscribeNewsletter keys the limiter on "203.0.113.7" — expect the limiter mock toHaveBeenCalledWith("203.0.113.7"), NOT the injected "9.9.9.9"
    - Test 6 (newsletter.test.ts): a newsletterLimiter rejection (mock limit rejects with ECONNREFUSED) resolves to { status: "error", message: "RATE_LIMITED" } — never an unhandled rejection
  </behavior>
  <action>
Leg 1 — Better Auth trustedProxies (src/lib/auth/index.ts):
- Extend the existing advanced.ipAddress block with trustedProxies sourced from process.env.TRUSTED_PROXY_CIDR: comma-split, per-entry trim, drop empty entries (same env-parsing idiom as trustedOrigins at the top of this file). Leave ipAddressHeaders as ["x-forwarded-for"] unchanged. Unset/empty env yields an empty array, which by the installed ip.mjs keeps today's single-value XFF behavior — local dev and the existing harness are untouched.
- Rewrite the T-07-02-03 comment block above it: delete the disproven claim that the production proxy overwrites X-Forwarded-For and that an attacker cannot set it. State the verified behavior instead: with only ipAddressHeaders set, @better-auth/core 1.6.23 getIPFromHeader returns null for any multi-value XFF, collapsing all auth traffic into one NO_TRUSTED_IP_KEY 3/15min bucket behind an appending proxy (CR-01, 07-VERIFICATION gap #2); with trustedProxies set to the proxy network CIDR, the chain is stripped from the right and the first untrusted hop (the real client) is used; an over-broad CIDR that matches every hop returns null → shared bucket → fail-closed (over-limiting), never spoofable. Cite the escalation-gate decision that the CIDR must stay env-driven because deployment topology varies and the owner's manual deploy is unreviewed.

Leg 2 — .env.example (trust anchor documentation):
- Append a new block immediately after the existing Redis section: a short comment header naming it the rate-limit IP-trust anchor (CR-01 / PERF-04), explaining that entries are the CIDR ranges of reverse proxies trusted to append X-Forwarded-For, that it stays EMPTY for local dev (direct connection / single-value XFF), and that production sets it to the proxy network (Docker-network example value 172.16.0.0/12). Then the variable itself, TRUSTED_PROXY_CIDR=, with an empty default. Multi-entry form documented as comma-separated.
- PERMISSION NOTE: .env.example may be read-denied in this environment. If the Read tool is denied, view current content via git show HEAD:.env.example and apply the change as a targeted append (bash printf appended after the Redis block) — never rewrite or reformat the rest of the file.

Leg 3 — shared last-hop helper (src/lib/rate-limit/index.ts):
- Export a new pure function getClientIpFromXff(forwardedFor: string | null): string that returns the LAST comma-separated entry of the XFF chain, trimmed, falling back to "unknown" for null/empty/blank results. Document in its docblock: the last hop is the entry our own appending proxy added (not client-controllable); under a single-proxy topology it is the real client IP; under multi-proxy topologies it over-limits (adjacent proxy IP shares one bucket) — fail-closed, never a bypass; the first hop is client-supplied and must never key a limiter (CR-01 leg 2). Keep the module's existing re-exports untouched.

Leg 4 — consumers (src/actions/contact.ts, src/actions/newsletter.ts):
- In contact.ts step 3: replace the inline first-hop split-and-trim extraction with a call to getClientIpFromXff(forwardedFor) imported from @/lib/rate-limit (same module the limiter already comes from). Update the step-3 comment and the file-header CITED line to describe last-hop trust instead of "Coolify's Caddy/Traefik sets it".
- In newsletter.ts step 3: same replacement (the docblock's "first value" wording at lines 147-148 and the step-3 comment at lines 180-184 updated to the shared helper + last-hop trust — preserving the "do not invent a second style" contract by making the style THE shared helper).
- WR-01 (contact.ts): wrap the contactFormLimiter.limit(ip) await in a try/catch; on catch, log via the existing log module if trivially available, then throw Error("RATE_LIMITED") — the documented public contract, consistent with the auth limiter's fail-closed policy (T-07-02-06). The !success branch keeps its existing RATE_LIMITED throw. Update the submitContact docblock: add "Rate-limit backend unreachable → RATE_LIMITED (fail-closed)" to the throws list, and correct any claim that the limiter never rejects.
- WR-01 sibling (newsletter.ts): wrap the newsletterLimiter.limit(ip) await in a try/catch returning { status: "error", message: "RATE_LIMITED" } — matching subscribeNewsletter's own returned-state (never thrown) resilience contract documented in its docblock.
- Correct the false "limiter never throws" claims where they appear in this task's files (the full rate-limit/index.ts + test-file header corrections land in Task 2 with WR-02/03 — do not duplicate that work here beyond what the two action files claim).

Leg 5 — regression pins (src/actions/__tests__/newsletter.test.ts):
- Add the two behavior tests above (multi-hop header → last-hop key; limiter rejection → RATE_LIMITED state). Follow the file's existing vi.mock headers pattern (the headers mock's get returns the test value; the newsletterLimiter mock is the vi.fn already wired). The existing single-value assertion (limit called with "203.0.113.7") must remain green — single-value headers behave identically under last-hop extraction.

No policy changes anywhere: D-01/D-02/D-03 limiter policies (3/900s auth, 5/1h contact, 5/1h newsletter) are untouched — this task only fixes WHO the buckets key on and how outages degrade.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/rate-limit/__tests__/client-ip.test.ts src/actions/__tests__/newsletter.test.ts</automated>
  </verify>
  <acceptance_criteria>
- src/lib/auth/index.ts contains advanced.ipAddress.trustedProxies sourced from TRUSTED_PROXY_CIDR with comma-split/trim/filter-Boolean parsing (grep finds both trustedProxies and TRUSTED_PROXY_CIDR in the file)
- src/lib/auth/index.ts no longer contains the disproven overwrite claim: a case-insensitive grep for "overwrites" and for "cannot set this header" returns 0 matches in that file
- .env.example contains the line TRUSTED_PROXY_CIDR= (empty default) inside a new commented block placed after the Redis section (git diff shows an append-only change — no other lines modified)
- src/lib/rate-limit/index.ts exports getClientIpFromXff; src/actions/contact.ts and src/actions/newsletter.ts both import and call it; a fixed-string grep for the old first-hop extraction pattern split(",")[0] across both action files returns 0 matches
- contact.ts wraps the limiter call in try/catch and throws Error("RATE_LIMITED") on rejection; newsletter.ts wraps its limiter call in try/catch returning status error / message RATE_LIMITED on rejection
- pnpm exec vitest run src/lib/rate-limit/__tests__/client-ip.test.ts src/actions/__tests__/newsletter.test.ts exits 0 with all new tests passing (including the multi-hop keying test and the limiter-rejection state test)
- Full suite unaffected so far: pnpm exec vitest run exits 0
  </acceptance_criteria>
  <done>Auth rate limits key per-client-IP behind an appending proxy when TRUSTED_PROXY_CIDR is set, and behave exactly as before when it is not; both public forms key their limiters on the proxy-appended last hop via one shared helper; a Redis outage surfaces as the defined RATE_LIMITED contract on both forms; the new pure-function and action-level tests pin the behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02..05 — truthful limiter tests, revalidation call assertions, updatePage partial contract</name>
  <files>src/lib/rate-limit/upstash-ioredis-adapter.ts, src/lib/rate-limit/__tests__/rate-limit.test.ts, src/actions/__tests__/taxonomy.test.ts, src/actions/__tests__/pages.test.ts, src/actions/__tests__/users.test.ts, src/actions/pages-schema.ts, src/actions/pages.ts</files>
  <read_first>
- src/lib/rate-limit/upstash-ioredis-adapter.ts — both Ratelimit instances (contactFormLimiter config at lines ~98-104); the ephemeralCache config option is the supported reset surface
- node_modules/.pnpm/@upstash+ratelimit@2.0.8_@upstash+redis@1.38.0/node_modules/@upstash/ratelimit/dist/index.mjs — verify (lines ~757-761) the constructor wraps config.ephemeralCache when it is a Map instance, and (lines ~147-156) safeEval rethrows non-NOSCRIPT errors — the two installed behaviors WR-02/03 rely on
- src/lib/rate-limit/__tests__/rate-limit.test.ts — the vi.mock("@/lib/redis") factory, the no-op resetEphemeralCache (lines ~113-118), and the false "does NOT throw" test (lines ~156-164) being replaced
- src/actions/categories.ts, src/actions/tags.ts, src/actions/pages.ts, src/actions/users.ts — the ACTUAL revalidation literals the assertions must pin (categories.ts lines ~51-57/106-115/138-144; tags.ts lines ~46-51/105-113/134-139; pages.ts lines ~123-127/174-179/240-242; users.ts lines ~343-365 including the no-username else branch and the role-only no-op)
- src/actions/__tests__/taxonomy.test.ts — mock seeding contract: insertMock resolves [{ id: 1, slug: "news" }], selectMock resolves [{ slug: "existing-slug" }]
- src/actions/__tests__/pages.test.ts — mock seeding: insertMock resolves [{ id: 1 }], selectMock resolves rows whose slug is "terms-and-conditions"
- src/actions/__tests__/users.test.ts — the updateUser describe block: countResult is seeded [{ username: "target-user" }] for the username fetch (lines ~405-411)
- src/actions/pages-schema.ts — pageSchema shape (required title/slug at lines ~28-32) that breaks the Partial contract today
- .planning/phases/07-performance-deploy/07-REVIEW.md — WR-02, WR-03, WR-04, WR-05 sections (each contains the verified root cause against the installed libs)
  </read_first>
  <behavior>
    - rate-limit.test.ts — reset reality (WR-02): after blocking an IP (6th call returns success false), calling the reset helper and re-running the same IP against a FRESH mockStore hits Redis again rather than being short-circuited by the stale ephemeral cache; the reset goes through the exported contactFormEphemeralCache Map (clear() on the export), not a private-field probe
    - rate-limit.test.ts — failure propagation (WR-03): with a hoisted failure flag wired into the mocked evalsha, setting it to reject with Error("ECONNREFUSED") makes contactFormLimiter.limit on a FRESH IP (use a documentation-range IP unused by the other tests, e.g. 198.51.100.77 — a blocked cached identifier would short-circuit before Redis) reject with ECONNREFUSED; the old "does NOT throw" test is REPLACED by this one, and its name/comment documents that contact.ts must catch (per Task 1's WR-01 fix)
    - taxonomy.test.ts — createCategory({ name: "News", slug: "news" }) asserts revalidatePath called with "/category/news", "/blog", "/", "/archive", "/sitemap.xml" AND revalidateTag called with ("category-1", "max") and ("posts-list", "max")
    - taxonomy.test.ts — softDeleteCategory(7) asserts revalidatePath("/category/existing-slug") (the pre-delete fetched slug) AND revalidateTag("category-7", "max") and ("posts-list", "max"); updateCategory with a renamed slug asserts BOTH the old-slug and new-slug paths fire
    - taxonomy.test.ts — createTag asserts revalidatePath("/tag/news") and revalidateTag("posts-list", "max") with NO per-entity category tag; softDeleteTag(9) asserts revalidatePath("/tag/existing-slug") and ("posts-list", "max")
    - pages.test.ts — createPage({ title: "T", slug: "t-and-c" }) asserts revalidatePath("/t-and-c") and ("/sitemap.xml") and revalidateTag("posts-list", "max")
    - pages.test.ts — softDeletePage(5) asserts revalidatePath("/terms-and-conditions") and ("/sitemap.xml") and ("posts-list", "max")
    - pages.test.ts — WR-05 proof: updatePage(1, { status: "published" }) (title and slug ABSENT) resolves without throwing, fires the db update chain, and the happy-path updatePage(1, { title: "T2" }) asserts revalidatePath("/terms-and-conditions") (the pre-update fetched slug) + ("/sitemap.xml") + revalidateTag("posts-list", "max")
    - users.test.ts — self-edit updateUser("self-1", { name: "Me" }) asserts revalidatePath("/author/target-user") and ("/sitemap.xml") and revalidateTag("posts-list", "max"); the admin role-only cross-user updateUser("target-1", { role: "editor" }) asserts revalidatePathMock was NOT called (role changes have no public surface)
  </behavior>
  <action>
WR-02 (real ephemeral-cache reset):
- In src/lib/rate-limit/upstash-ioredis-adapter.ts: create and export contactFormEphemeralCache as a new Map, and pass it as the ephemeralCache option on the contactFormLimiter Ratelimit config ONLY (the tests exercise contactFormLimiter; leave newsletterLimiter as-is and note why in a one-line comment). Document on the export: this Map is the supported reset surface for tests — the library wraps it in its Cache inside ctx (installed dist lines ~757-761), so clearing this Map is the documented way to reset memoized blocks between tests.
- In rate-limit.test.ts: replace the resetEphemeralCache body so it clears the imported contactFormEphemeralCache export directly (no private-field probing, no instanceof Map guard against the wrong object). Update the file-header paragraph that currently describes the broken l.cache approach so it describes the export-based reset instead.
- Add the reset-reality behavior test: exhaust an IP's budget (6th call success false), reset mockStore to a fresh Map, call the reset helper, then assert the SAME IP's next limit call consults Redis again (e.g. returns to success true on a fresh store) — proving the reset actually clears the memoized block.

WR-03 (real failure simulation):
- Wire a mutable failure flag into the vi.mock("@/lib/redis") factory using vi.hoisted state (the file already uses vi.hoisted idioms elsewhere in the repo's test suites): when set, evalsha (and eval) reject with the held Error; reset to null in beforeEach.
- Replace the existing test whose subject claims Redis failures never surface with the propagation test described in the behavior block, and fix the file-header lines that assert the false never-throws claim so they state the opposite truth: @upstash/ratelimit 2.0.8 slidingWindow has no catch around safeEval, Redis errors propagate, and callers (contact.ts) must catch — citing Task 1's WR-01 fix and 07-REVIEW WR-01/WR-03.

WR-04 (revalidation call assertions — taxonomy.test.ts, pages.test.ts, users.test.ts):
- Add one focused assertion block per mutating action, using the EXACT literals the source emits (read_first source files are the ground truth; the mock seedings listed in read_first determine the concrete values — /category/news, /category/existing-slug, category-1, category-7, /tag/news, /tag/existing-slug, /t-and-c, /terms-and-conditions, /author/target-user). Assert the 2-arg form explicitly: toHaveBeenCalledWith("posts-list", "max") and toHaveBeenCalledWith("category-7", "max") — a dropped "max" argument or a wrong literal path now fails the suite.
- In users.test.ts, also pin the negative space: the role-only update fires NO revalidatePath (the users.ts conditional at lines ~343-365).
- In taxonomy.test.ts, add the updateCategory rename case: seed selectMock with the existing slug, pass a different new slug, assert revalidatePath fired for BOTH the old and the new /category/... paths.
- Update the stale comment at taxonomy.test.ts line ~103 ("assertions on call patterns where useful") to reflect that the assertions now exist.

WR-05 (updatePage partial contract):
- In src/actions/pages-schema.ts: export pageUpdateSchema built as pageSchema.partial() extended with a REQUIRED id (z.number().int().positive()), with a docblock noting it backs updatePage's Partial<PageInput> contract (07-REVIEW WR-05) and that createPage keeps the strict full pageSchema. Export the input type alongside if the existing style warrants it (mirror the PageSchemaInput pattern).
- In src/actions/pages.ts updatePage: parse via pageUpdateSchema.parse({ ...input, id }) instead of pageSchema.parse; adjust the cast type so downstream conditional spreads (data.title !== undefined ? ...) type-check against the partial output. createPage is NOT touched.
- In pages.test.ts: add the partial-update proof test from the behavior block (updatePage with ONLY status) — this test FAILS against the pre-fix code (ZodError on missing title/slug) and passes after, which is the WR-05 regression pin. The existing FORBIDDEN-ordering tests are unaffected (requireCan throws before any parse).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/rate-limit/__tests__/rate-limit.test.ts src/actions/__tests__/taxonomy.test.ts src/actions/__tests__/pages.test.ts src/actions/__tests__/users.test.ts</automated>
  </verify>
  <acceptance_criteria>
- src/lib/rate-limit/upstash-ioredis-adapter.ts exports contactFormEphemeralCache and passes it as ephemeralCache on the contactFormLimiter config (grep -c contactFormEphemeralCache in the file returns at least 2: export + config use)
- rate-limit.test.ts imports contactFormEphemeralCache (grep finds the import) and no longer probes a private cache field (grep for "ctx" in that file returns no cache-probing cast); the file contains a rejects-toThrow propagation test whose subject mentions Redis failure propagation (the old test name claiming the limiter never throws on the Redis failure surface is gone)
- taxonomy.test.ts, pages.test.ts, and users.test.ts each contain at least one toHaveBeenCalledWith assertion against revalidatePathMock AND at least one against revalidateTagMock; the 2-arg literal "posts-list", "max" appears as a called-with assertion in all three files
- taxonomy.test.ts asserts the per-entity tag form with "max" (e.g. "category-7", "max") and both old+new paths on rename; users.test.ts asserts the role-only update fires no revalidatePath
- src/actions/pages-schema.ts exports pageUpdateSchema; src/actions/pages.ts updatePage parses via pageUpdateSchema (grep finds pageUpdateSchema in both files); createPage still parses via pageSchema
- pages.test.ts contains a partial-update test calling updatePage with a payload lacking title and slug that expects resolution (not a ZodError throw)
- pnpm exec vitest run src/lib/rate-limit/__tests__/rate-limit.test.ts src/actions/__tests__/taxonomy.test.ts src/actions/__tests__/pages.test.ts src/actions/__tests__/users.test.ts exits 0
- Full suite: pnpm exec vitest run exits 0 (0 failed; total at or above the prior 621 — new tests added by this task and Task 1 grow the count)
  </acceptance_criteria>
  <done>The rate-limit unit tests exercise the real reset surface and a real Redis failure; every Phase-07 revalidation call in categories/tags/pages/users is pinned by concrete-literal assertions including the 2-arg max form (dropped tags, wrong slugs, or missing old-URL revalidation now fail green); updatePage honors the Partial contract its signature and JSDoc promise, proven by a partial-payload test that failed before the fix.</done>
</task>

<task type="auto">
  <name>Task 3: WR-06 harness reliability + live 429 verification run + record the fc3286d overrides in 07-VERIFICATION.md</name>
  <files>scripts/test-auth-ratelimit.mjs, .planning/phases/07-performance-deploy/07-VERIFICATION.md</files>
  <read_first>
- scripts/test-auth-ratelimit.mjs — the full script: the header comment carrying the disproven proxy-overwrite claim (lines ~34-37), the fixed TEST_IP constant (line ~37), structuralCheck's required-token list (lines ~69-77), the spawn options (line ~151, detached false), the POSIX kill branch + silent catch (lines ~211-221), and the SKIP manual-run instructions (lines ~246-256)
- .planning/phases/07-performance-deploy/07-VERIFICATION.md — frontmatter (status/score/overrides_applied/gaps/behavior_unverified), the truths table rows 4, 9, 13, the "Override Suggestions (NOT applied)" section with the EXACT override YAML to copy, and the Gaps Summary
- .planning/phases/07-performance-deploy/07-REVIEW.md — WR-06 (ESRCH root cause: detached false means no process group) and the CR-01 fix item 3 (the through-the-proxy curl verification)
- .planning/phases/07-performance-deploy/07-04-SUMMARY.md — the fc3286d decision records the overrides formalize
- package.json — the test:auth-ratelimit script + build script
- docker-compose.yml — the redis dev service (name/ports) and postgres service the live run needs
- .env.example — the REDIS_URL dev default + the new TRUSTED_PROXY_CIDR block from Task 1 (view via git show if read-denied)
- Project memory note: Docker is LOCAL-DEV only (deploy is manual) — the live run uses the dev compose stack and nothing more
  </read_first>
  <action>
WR-06 — make the harness rerunnable (scripts/test-auth-ratelimit.mjs):
- Spawn fix: set the spawn option detached to process.platform !== "win32" so the POSIX child becomes a process-group leader (making the existing negative-PID group kill valid) while Windows keeps its current taskkill /f /t tree-kill path unchanged (detached on Windows would flash a new console). Keep the command and stdio options as they are.
- Kill fix: keep process.kill with the negative PID on POSIX but make the catch LOG the failure (tag + error code) instead of silently swallowing — an orphaned server is exactly the failure this fix exists to surface. Add a short comment citing WR-06 (07-REVIEW): a bare ESRCH from a non-leader child was previously invisible.
- Rerun-poisoning fix: derive TEST_IP per run from the RFC 5737 documentation range with a randomized final octet (e.g. 203.0.113.<1-254>) so a stale Redis bucket left by a previous run can never consume this run's 3-attempt budget. Keep every place that references TEST_IP (the POST header, the printed instructions) using the derived value; update the header comment to say the synthetic IP is randomized per run.
- Structural pin: add the token trustedProxies to structuralCheck's required-token list so the deterministic gate fails if the CR-01 fix is ever removed from src/lib/auth/index.ts. Add a brief comment that this token is the CR-01 leg-1 pin (07-VERIFICATION gap #2).
- Comment corrections: replace the header comment's claim that Better Auth trusts the header because the production proxy overwrites it (lines ~34-37) with the corrected trust model from Task 1 (single-value XFF trusted when TRUSTED_PROXY_CIDR is unset; chain stripped from the right when set). Note in the header that WR-07 (npx vs pnpm exec) is a known advisory finding deliberately left untouched by this closure's owner-approved scope.
- CR-01 leg 3 — manual instructions: extend the SKIP branch's "To run manually" steps and the header with the through-the-proxy verification: after deploy, curl the sign-in endpoint THROUGH the real proxy with a header injecting a fake XFF value (documentation-range IP), and confirm from logs/behavior that the resolved client IP is the proxy-derived value, not the injected one — i.e. rate buckets stay per-client. Present it as the per-environment trust-model check (proxy behavior is environment-specific — 07-VERIFICATION "Human Verification Required" item 4).

Live 429 run (truth 9 — behavior_unverified closure, local dev only):
- Step 1: docker compose up -d redis (and the postgres service if not already running — check docker compose ps first).
- Step 2: Fresh build so the tested binary contains the Task 1 auth change: stop anything listening on the harness port, remove the .next directory, then pnpm build (project memory: warm builds can reuse stale cache output; also kill any leftover next-start process on Windows before running).
- Step 3: Optionally clear stale limiter keys in the dev Redis (flush the ratelimit keyspace) — the randomized TEST_IP already makes this unnecessary, but it keeps the run deterministic.
- Step 4: run pnpm test:auth-ratelimit. Expected: Structural PASS (now including the trustedProxies token) and HTTP CHECK PASSED with attempts 1-3 non-429 and attempt 4 HTTP 429 carrying X-Retry-After or Retry-After. Any HTTP FAIL is a real defect — investigate, do not record around it.
- Step 5: Record the exact outcome (PASS with the retry-after value, or SKIP with the environment reason — e.g. Docker daemon unavailable) in the SUMMARY under a clearly headed section; the run must be evidenced, never assumed.
- Note: redis is local-dev tooling per the owner's deploy decision — this run executes entirely on the dev machine and does not touch or plan any production deployment.

Override recording (.planning/phases/07-performance-deploy/07-VERIFICATION.md) — the owner accepted both fc3286d deviations at the escalation gate on 2026-08-26 (formalizing the 2026-07-29 fc3286d approval):
- Frontmatter: change overrides_applied from 0 to 2; replace the NOTE comment about the overrides NOT being applied with a one-line note that both were accepted by the owner at the escalation gate on 2026-08-26; add the overrides list by copying the two entries VERBATIM from the report's own Override Suggestions section (both must_have strings and both reason strings unchanged), filling accepted_by with owner (mehedishubho) and accepted_at with 2026-08-26 in each.
- Frontmatter gaps list: remove the bundle-budget gap entry and the Dockerfile-ARG gap entry (the two former gaps #3 and #4 — identified by their truth texts about the bundle-budget check and the ARG/ENV secret literal) from the gaps list; they are closed as accepted overrides recorded above. Leave the remaining gaps (deploy, IP-trust, Lighthouse live, publish-visible live, Umami/DNS) — except the IP-trust gap, which this plan's Tasks 1-3 close: remove that gap entry too and note its closure in the Gaps Summary prose with a one-line pointer to the 07-06 SUMMARY.
- Truths table: set truth 4 and truth 13 status to VERIFIED (override accepted 2026-08-26) with evidence pointing at the frontmatter overrides block; set truth 10 to VERIFIED with evidence citing the trustedProxies + last-hop fixes and the structural pin (07-06). Conditionally, if and only if the live run in this task reported HTTP CHECK PASSED: set truth 9 to VERIFIED with the run evidence, change behavior_unverified to 0, and remove the behavior_unverified_items entry. If the run SKIPped, leave truth 9 and behavior_unverified exactly as they are.
- Frontmatter score: update the score line to reflect the new verified count (10/16 with the two overrides; 11/16 if the live run also passed) and keep status gaps_found (the four live-stack gaps remain, owner-deferred). Update the "Override Suggestions (NOT applied...)" section heading to record that both overrides were applied on 2026-08-26 per the escalation gate.
- Keep every other row and prose section untouched — this is a scoped bookkeeping edit, not a rewrite of the verifier's report.
  </action>
  <verify>
    <automated>node -c "require('fs')" 2>/dev/null; node --check scripts/test-auth-ratelimit.mjs && pnpm test:auth-ratelimit</automated>
  </verify>
  <acceptance_criteria>
- scripts/test-auth-ratelimit.mjs passes node --check (syntax valid)
- The script spawns with detached true only on non-Windows platforms (grep finds the platform-conditional detached option), still performs the negative-PID group kill on POSIX (grep for the process.kill negative-pid call), and its cleanup catch now logs instead of silently swallowing (grep for the bare empty catch pattern in the finally block returns 0)
- TEST_IP is derived per run with a randomized final octet inside 203.0.113.0/24 (grep shows the IP constructed from a random component, not a fixed literal)
- structuralCheck's required-token list includes trustedProxies (grep finds it in the required array); the script's header no longer claims the production proxy overwrites X-Forwarded-For (case-insensitive grep for "overwrites" in the script returns 0)
- The script's manual-run instructions include a through-the-proxy XFF verification step (grep for X-Forwarded-For in the instructions block finds a curl step against the deployed proxy)
- pnpm test:auth-ratelimit exits 0 with Structural PASS; its HTTP result is PASSED, or SKIPPED with an explicit environment reason — either way the outcome is recorded in the SUMMARY (a FAILED HTTP result blocks task completion)
- .planning/phases/07-performance-deploy/07-VERIFICATION.md frontmatter: grep -c 'accepted_by: "owner (mehedishubho)"' returns 2 and grep -c 'accepted_at: "2026-08-26"' returns 2; overrides_applied is 2; the frontmatter gaps list no longer contains the bundle-budget, Dockerfile-ARG, or IP-trust gap entries; truths 4, 13, and 10 read as VERIFIED (truth 9 additionally so iff the live run passed)
- The two override entries' must_have and reason strings match the report's own Override Suggestions section verbatim (only accepted_by/accepted_at differ from the placeholders)
  </acceptance_criteria>
  <done>The harness kills its server reliably on every platform and cannot be poisoned by prior runs; the deterministic structural gate pins the CR-01 fix; the 429 behavior has actually been executed at least once with the recorded outcome; 07-VERIFICATION.md formally carries both owner-accepted overrides and no longer lists the three repo-side gaps this plan closes, leaving only the four owner-deferred live-stack gaps.</done>
</task>

</tasks>

## Explicit Owner Deferrals (do NOT plan, do NOT silently drop)

These items are OUT OF SCOPE by binding owner decision at the escalation gate (2026-08-26) and prior owner decision (2026-07-29, project memory deploy-approach-manual-no-docker-prod). Re-verification must read them as accepted deferrals, not new gaps:

1. Production deploy of any kind — owner verbatim: "for deployment, do not make any docker system now, milestone 1 does not need this currently." No new docker systems, no Dockerfile changes, no Coolify work. (The WR-06/WR-09/IN-04 runbook-revision work rides the owner's post-app-completion deploy review.)
2. Gap #1 — live deploy + runbook revision (docs/operations/coolify-deploy.md et al.) for the manual VPS flow.
3. Gap #5 — live Lighthouse/CWV run (needs a production URL; config + tooling already verified in-repo).
4. Gap #6 — live publish-to-visible run (instrument verified; PROD_URL leg awaits deploy).
5. Gap #7 — Umami deployment + DKIM/SPF/DMARC DNS publication (rides the deploy review; substantive real-inbox deliverability already proven by 02-UAT 2026-08-24).

Advisory findings WR-07..WR-13 and IN-01..IN-04 from 07-REVIEW.md are likewise outside this closure's owner-approved scope and remain recorded in 07-REVIEW.md as open advisories — not addressed here, not silently dropped.

## Artifacts this plan produces

New symbols / config keys / env vars (exclude from drift verification as newly-created):

- getClientIpFromXff(forwardedFor: string | null): string — exported from src/lib/rate-limit/index.ts
- contactFormEphemeralCache (exported Map) — src/lib/rate-limit/upstash-ioredis-adapter.ts
- pageUpdateSchema (+ its input type export if added) — src/actions/pages-schema.ts
- Env var TRUSTED_PROXY_CIDR (comma-separated CIDR list; empty default in .env.example; consumed by src/lib/auth/index.ts)
- Config key advanced.ipAddress.trustedProxies — src/lib/auth/index.ts (populated from the env var)
- New test file src/lib/rate-limit/__tests__/client-ip.test.ts
- New test blocks: multi-hop + limiter-rejection tests in newsletter.test.ts; reset-reality + propagation tests in rate-limit.test.ts; revalidation assertion blocks in taxonomy.test.ts / pages.test.ts / users.test.ts; partial-update test in pages.test.ts
- scripts/test-auth-ratelimit.mjs changes: trustedProxies structural token, platform-conditional detached spawn, logged kill-catch, per-run randomized TEST_IP, through-the-proxy manual instructions
- 07-VERIFICATION.md frontmatter: overrides list (2 entries), overrides_applied: 2, updated score/truths/gaps

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → reverse proxy → app | The X-Forwarded-For chain crosses here: client-supplied hops are attacker-controlled until the proxy appends its own observed IP; the rate-limit keying must derive from the proxy-appended portion |
| public form (unauthenticated) → Server Action → Redis | Untrusted input reaches the contact/newsletter actions and their shared Redis limiter; a Redis outage must not leak raw internal errors to the public surface |
| environment config → auth config | TRUSTED_PROXY_CIDR is operator-supplied deployment topology; misconfiguration changes who the limiter trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06-01 | Spoofing / DoS (Elevation) | Better Auth rate limiter keying (src/lib/auth/index.ts) | high | mitigate | trustedProxies from TRUSTED_PROXY_CIDR strips the XFF chain from the right (verified against installed @better-auth/core 1.6.23 dist/utils/ip.mjs) so multi-value XFF no longer collapses ALL auth traffic into one NO_TRUSTED_IP_KEY 3/15min bucket; structural token in test-auth-ratelimit.mjs pins the config's presence |
| T-07-06-02 | Spoofing | contact.ts / newsletter.ts limiter keying | high | mitigate | last-hop extraction via the shared getClientIpFromXff helper (proxy-appended, not client-controllable) replaces the spoofable first hop; pure-function + action-level tests pin it; multi-proxy topologies over-limit (fail-closed), never under-limit |
| T-07-06-03 | DoS / Information disclosure | public forms during Redis outage | medium | mitigate | WR-01: contact fails closed to the documented RATE_LIMITED contract (consistent with the auth limiter's T-07-02-06 policy); newsletter returns the RATE_LIMITED error state — no raw internal errors reach the public surface; WR-03's propagation test documents exactly why the catch is required |
| T-07-06-04 | Tampering (config) | TRUSTED_PROXY_CIDR value | medium | mitigate | env-driven (never hardcoded) with both failure modes documented in .env.example: unset → shared-bucket fail-closed behind an appending proxy; over-broad CIDR matching every hop → null → shared bucket (fail-closed, never spoofable); the through-the-proxy curl instruction in the harness makes the trust model re-verifiable per environment at deploy review |
| T-07-06-05 | Tampering (test integrity) | rate-limit + action test files | low | mitigate | WR-02/03/04 replace false assurance (no-op reset, never-simulated failure, zero revalidation assertions) with real regression pins, so future weakening of the trust/revalidation wiring fails green |
| T-07-06-SC | Tampering (supply chain) | package installs | low | accept | This plan installs NO new packages — all work is against already-installed, already-vetted dependencies (@better-auth/core 1.6.23, @upstash/ratelimit 2.0.8, ioredis); no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
Phase-level gap-closure checks (run after all three tasks):

1. Full suite: pnpm exec vitest run exits 0 — zero failures across all 62+ test files (count at or above the prior 621; Tasks 1-2 add tests).
2. Type + build gate: pnpm build completes (project-wide type-check included; the fresh build is also the live run's precondition).
3. IP-trust negative gates (comment-filtered): src/lib/auth/index.ts and scripts/test-auth-ratelimit.mjs contain no case-insensitive "overwrites" claim; the old first-hop extraction literal is absent from both action files.
4. Override recording: 07-VERIFICATION.md frontmatter carries exactly two accepted_by "owner (mehedishubho)" / accepted_at "2026-08-26" override entries copied verbatim from its own Override Suggestions section; overrides_applied: 2; the repo-side gaps (bundle budget, Dockerfile ARG, IP-trust) are gone from the gaps list.
5. Live-run evidence: the SUMMARY records the pnpm test:auth-ratelimit outcome — HTTP CHECK PASSED (attempts 1-3 non-429; 4th 429 + retry-after), or SKIP with the specific environment reason. FAIL blocks phase close.
6. Deferred legs intact: gaps #1/#5/#6/#7 remain documented as owner deferrals (see the Explicit Owner Deferrals section) — nothing in this plan deploys, publishes DNS, or provisions services.
</verification>

<success_criteria>
- CR-01 closed: with TRUSTED_PROXY_CIDR set to a proxy CIDR, multi-value XFF keys auth rate limits per real client IP (chain stripped from the right per the installed dist); with it unset, existing single-value behavior is byte-for-byte preserved (local dev + harness unaffected).
- Both public form limitters key on the proxy-appended last hop through one shared helper; Redis outages degrade to RATE_LIMITED, never raw errors.
- The rate-limit tests exercise the real cache-reset surface and a real Redis-failure propagation; a dropped "max" tag, wrong revalidation literal, or missing old-URL revalidation in categories/tags/pages/users fails the suite.
- updatePage accepts true partial payloads via pageUpdateSchema (proven by a partial-payload test that fails pre-fix).
- The harness reruns cleanly (group-kill on POSIX, logged kill failures, randomized per-run test IP) and its structural gate fails if trustedProxies disappears.
- The auth 429 behavior has one recorded end-to-end execution (pass or documented environment skip).
- 07-VERIFICATION.md formally records both fc3286d overrides (owner, 2026-08-26) and the only remaining gaps are the four owner-deferred live-stack items.
- Full vitest suite green; pnpm build green.
</success_criteria>

<output>
Create `.planning/phases/07-performance-deploy/07-06-SUMMARY.md` when done. The SUMMARY MUST include: (a) the recorded pnpm test:auth-ratelimit outcome verbatim (structural + HTTP result lines), (b) confirmation of each WR-01..06 fix with its file, (c) the override-recording confirmation (2 entries, owner, 2026-08-26), (d) any environment SKIP reasons, so the next verification pass can score truth 9 correctly.
</output>
