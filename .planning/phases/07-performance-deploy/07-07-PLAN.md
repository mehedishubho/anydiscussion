---
phase: 07-performance-deploy
plan: 07
type: execute
wave: 7
depends_on:
  - 07-02
  - 07-04
  - 07-06
gap_closure: true
files_modified:
  - src/actions/contact.ts
  - src/components/site/ContactForm.tsx
  - src/actions/users.ts
  - src/actions/users-schema.ts
  - src/actions/taxonomy-schema.ts
  - src/actions/categories.ts
  - src/actions/tags.ts
  - src/app/(admin)/dashboard/users/UsersTable.tsx
  - src/actions/__tests__/contact.test.ts
  - src/actions/__tests__/users.test.ts
  - src/actions/__tests__/taxonomy.test.ts
  - src/lib/rate-limit/index.ts
  - src/lib/rate-limit/__tests__/client-ip.test.ts
  - src/lib/rate-limit/__tests__/rate-limit.test.ts
  - src/lib/redis/index.ts
  - scripts/test-auth-ratelimit.mjs
  - .env.example
  - docs/operations/coolify-deploy.md
  - docs/operations/dns-email-deliverability.md
  - Dockerfile
autonomous: true
requirements:
  - PERF-04
  - PERF-06
user_setup: []

must_haves:
  truths:
    - "CR-02 (07-VERIFICATION gap #6): submitContact returns the discriminated union { ok: true } | { ok: false; error: \"RATE_LIMITED\" | \"INVALID_INPUT\" } — the RATE_LIMITED public contract reaches the client in PRODUCTION builds (React flight's production serializer forwards digests/returned values, never thrown error .message — verified in the installed dist by 07-REVIEW CR-02). ContactForm branches on the RETURNED state; the error-message equality mapping is gone. Mirrors subscribeNewsletter (src/actions/newsletter.ts:198-205), the repo's reference returned-state shape."
    - "CR-02 users leg: deleteUser's five guard failures (self-delete, user-not-found, last-admin, has-posts, removeUser-rejection) each attach a stable digest to the thrown error; UsersTable maps err.digest to the friendly copy via constants exported from a pure sibling module (users-schema.ts — a 'use server' file may only export async functions). No consumer touched by this plan branches on err.message."
    - "The contact returned-state contract is pinned by tests: src/actions/__tests__/contact.test.ts covers schema-invalid → INVALID_INPUT, honeypot → silent ok, limiter rejection (Redis outage) → RATE_LIMITED fail-closed, limiter success:false → RATE_LIMITED, happy path → ok + sendEmail fired."
    - "WR-06: getClientIpFromXff honors TRUSTED_XFF_HOP_COUNT = the number of trusted appending proxies (default 1 = today's last-hop behavior, byte-identical for every existing pinned test); hop count 2 selects the client IP appended by the outermost trusted proxy (Cloudflare+Coolify chain); invalid/≤0 values fall back to 1; chains shorter than the count fall back to the last hop (fail-closed over-limit direction, never the spoofable prefix). Unit tests cover all of these in client-ip.test.ts; the env var is documented in .env.example next to TRUSTED_PROXY_CIDR and in coolify-deploy.md section 5."
    - "WR-01: the Redis singleton's error listener logs unconditionally through the structured logger (log.error with message context) — a production Redis outage is visible in container logs; the NODE_ENV-guarded console.warn is gone, and coolify-deploy.md V5 describes what production actually emits."
    - "WR-04: scripts/test-auth-ratelimit.mjs sets the failure exit code UNCONDITIONALLY when the HTTP check result is \"failed\" (exit 0 only for \"passed\"/\"skipped\"), and the cleanup kill is guarded against an undefined server.pid."
    - "WR-05: categories/tags actions parse input through categorySchema/tagSchema (name min 1 max 120, description max 1000 optional) and partial variants before any DB write; updateUser parses userUpdateSchema (name min 1 max 255, bio max 2000, avatar via the shared imageUrlSchema — URL-or-empty, role constrained to the three-role enum); partial semantics use !== undefined consistently (empty-string name is REJECTED, never a silent no-op); taxonomy and users tests pin the bounds."
    - "CR-01 (07-VERIFICATION gap #5): coolify-deploy.md section 5 runtime env table contains TRUSTED_PROXY_CIDR (required-for-production, unset/mis-set failure modes, through-the-proxy curl verification) AND TRUSTED_XFF_HOP_COUNT; the 'Auth fails closed' troubleshooting entry covers the shared-bucket lockout symptom, not just Redis."
    - "WR-07: coolify-deploy.md opens with a SUPERSEDED status banner recording the owner decision 2026-07-29 (production deploys are MANUAL; the Docker/Coolify pipeline is local-dev dry-run material; sections 5-6 retained as the production env-var reference) — banner-only, header and V3 reconciled by pointer, no body rewrite."
    - "WR-03: Decision B records 'resolved — 1000 KB applied' matching Dockerfile:95 + package.json:18; the stale Dockerfile:16 header comment says 1000; the V1 dry-run command carries the Decision-A build-time DATABASE_URL build-arg (with the REDIS_URL/no-ARG note)."
    - "WR-02: dns-email-deliverability.md page-route references all read /forgot-password (three occurrences fixed); the one-time API-endpoint distinction note is present."
    - "Full vitest suite green (621+ tests) and pnpm build (type-check) clean after all fixes."
  artifacts:
    - "src/actions/users-schema.ts (NEW) — deleteUser digest constants + digest→message map + userUpdateSchema (WR-05)"
    - "src/actions/taxonomy-schema.ts (NEW) — categorySchema/tagSchema + partial update variants"
    - "src/actions/__tests__/contact.test.ts (NEW) — the returned-state contract suite"
    - "src/actions/contact.ts with the returned-state signature; src/components/site/ContactForm.tsx switched to returned-state branching"
    - "src/lib/rate-limit/index.ts getClientIpFromXff with TRUSTED_XFF_HOP_COUNT support + corrected-selection docblock"
    - "src/lib/redis/index.ts unconditional structured error logging"
    - "scripts/test-auth-ratelimit.mjs unconditional failure exit + pid-guarded cleanup"
    - "docs/operations/coolify-deploy.md — SUPERSEDED banner, two new section-5 env rows, shared-bucket troubleshooting, resolved Decision B, extended V1 dry-run, V5 rewording"
    - "docs/operations/dns-email-deliverability.md — /forgot-password corrections"
    - "Dockerfile — header comment threshold corrected to 1000"
    - ".env.example — TRUSTED_XFF_HOP_COUNT documented block beside TRUSTED_PROXY_CIDR"
  key_links:
    - "contact.ts returned state -> ContactForm.tsx branch (the public contract survives React flight production redaction — the exact break CR-02 verified in the installed dist)"
    - "users.ts guard throws (digest) -> users-schema.ts digest constants -> UsersTable.tsx error-alert copy map (digest IS forwarded by production flight serialization; message is not)"
    - "TRUSTED_XFF_HOP_COUNT (.env.example + coolify-deploy.md section 5) -> getClientIpFromXff selection index in src/lib/rate-limit/index.ts -> contactFormLimiter/newsletterLimiter bucket keys"
    - "TRUSTED_PROXY_CIDR (coolify-deploy.md section 5, NEW row) -> src/lib/auth/index.ts advanced.ipAddress.trustedProxies (the deploy-time activation leg of 07-06's code fix — closes the runbook omission that made a by-the-runbook deploy ship the shared-bucket lockout)"
---

<objective>
Round-2 gap closure for Phase 7: fix the two repo-fixable criticals from 07-REVIEW.md (structured as 07-VERIFICATION gaps #5 and #6) and the seven repo-fixable warning legs (WR-01..WR-07), each landing as a vertical slice — code + tests + docs together — so the phase's deploy-time contract works end-to-end in repo-verifiable form.

Purpose:
- CR-01/gap #5: a by-the-runbook deploy must activate the 07-06 IP-trust mitigation — the runbook's runtime env table currently omits TRUSTED_PROXY_CIDR (grep docs/ returns zero matches), so an operator following it verbatim ships the site-wide shared 3/15-min auth bucket.
- CR-02/gap #6: the defined RATE_LIMITED public contract and the five deleteUser guard messages are dead code in production — React's flight serializer emits digest-only error chunks (verified in the installed dist), so err.message never reaches the client in prod builds.
- WR-01..WR-07: production-invisible Redis outage logging, a 404ing runbook URL, stale/contradictory runbook decisions, a false-green harness exit branch, missing Zod validation on taxonomy/profile actions, the single-proxy XFF assumption (Cloudflare+Coolify collapses all visitors into shared 5/hour form buckets), and the runbook premise contradicting the owner's manual-deploy decision.

Requirement mapping (honest): CR-02 + WR-01 + WR-04 + WR-06 code legs ride PERF-04 (rate-limit soundness — the contact contract and per-client bucket keying are the limiter's client-facing half); CR-01 + WR-02 + WR-03 + WR-07 + the WR-06 runbook leg ride PERF-06 (deploy-preparation material — the runbook/Dockerfile contract an operator follows). WR-05 rides PERF-04-adjacent input hardening on the same surfaces.

Output: 4 tasks — (1) CR-02 returned-state + digest contracts with tests, (2) rate-limit infra soundness (WR-06 hop count + WR-01 logging + WR-04 harness), (3) WR-05 Zod validation with tests, (4) all runbook/Dockerfile documentation fixes.

Mode note: ROADMAP marks this phase mvp, but as recorded in 07-VERIFICATION.md the goal is not in user-story format, so — per the precedent of phases 01-06, 08, and 07-06 itself — standard goal-backward verification applies. Vertical-slice structure is honored at the fix level: every task lands its code, its tests, and its docs in the same task.

Execution note (scope mitigation for the 19-file footprint): the over-threshold file count is intentional and mitigated structurally — Task 4 is docs/comment-only (docs/operations/coolify-deploy.md, docs/operations/dns-email-deliverability.md, and a Dockerfile header comment) and shares zero files_modified with Tasks 1-3. Escape hatch: if executor context tightens after Tasks 1-3 complete, park Task 4 and finish it in a separate session; Tasks 1-3 leave the repo green on their own. Task 4's only cross-task touch is read-only (its runbook rows must agree with Task 2's .env.example TRUSTED_XFF_HOP_COUNT block), so deferring it breaks nothing.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-deploy/07-VERIFICATION.md
@.planning/phases/07-performance-deploy/07-REVIEW.md

# Reference shapes (read before editing — these define the target patterns):
@src/actions/newsletter.ts
@src/actions/contact-schema.ts
@src/lib/validation/image-url.ts

# Project rules that bind every task: pnpm only; Zod schemas in pure sibling
# modules (a "use server" file may only export async functions); permission
# checks stay FIRST in every mutating action; never branch client-side on
# thrown Server Action error text.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-02 — convert submitContact to returned states and deleteUser guards to stable digests (code + tests)</name>
  <files>src/actions/contact.ts, src/components/site/ContactForm.tsx, src/actions/users-schema.ts, src/actions/users.ts, src/app/(admin)/dashboard/users/UsersTable.tsx, src/actions/__tests__/contact.test.ts, src/actions/__tests__/users.test.ts, src/lib/rate-limit/__tests__/rate-limit.test.ts</files>
  <read_first>
  - .planning/phases/07-performance-deploy/07-REVIEW.md (CR-02 full finding — the flight-redaction mechanism and the exact fix shape)
  - .planning/phases/07-performance-deploy/07-VERIFICATION.md (gap #6 — the structured gap this task closes)
  - src/actions/contact.ts (current thrown-contract implementation, docblock lines 37-59)
  - src/actions/newsletter.ts lines 150-226 (subscribeNewsletter — THE reference returned-state shape, cited by the review as "the correct shape")
  - src/components/site/ContactForm.tsx (onValid startTransition block, lines 81-105 — the dead err.message branch)
  - src/actions/users.ts lines 389-489 (deleteUser — the five readable guard throws)
  - src/app/(admin)/dashboard/users/UsersTable.tsx lines 169-201, 344-349 (deleteMutation + the shared error alert rendering deleteMutation.error?.message)
  - src/actions/__tests__/newsletter.test.ts (mock structure to mirror for contact.test.ts: vi.mock of @/lib/rate-limit, next/headers, @/actions/settings, @/lib/email)
  - src/actions/__tests__/users.test.ts lines 630-720 (the five deleteUser guard tests whose assertions this task extends)
  - src/lib/rate-limit/__tests__/rate-limit.test.ts lines 179-195 (the two comments documenting contact.ts's contract — text update only)
  </read_first>
  <behavior>
  - Test (new contact.test.ts): submitContact with schema-invalid input (e.g. missing message) resolves to { ok: false, error: "INVALID_INPUT" }; contactFormLimiter.limit and sendEmail never called.
  - Test: honeypot field non-empty resolves to { ok: true }; limiter and sendEmail never called (silent bot success preserved).
  - Test: contactFormLimiter.limit mocked to resolve { success: false } resolves to { ok: false, error: "RATE_LIMITED" }; sendEmail never called.
  - Test: contactFormLimiter.limit mocked to REJECT (Redis outage, Error "ECONNREFUSED") resolves to { ok: false, error: "RATE_LIMITED" } — fail-closed, action never throws.
  - Test: limiter success + getSetting returning "inbox@example.com" resolves { ok: true }; sendEmail called once with to "inbox@example.com".
  - Test: getSetting returning "" falls back to the FALLBACK_RECIPIENT constant (admin@anydiscussion.com).
  - Test (users.test.ts, extend the five existing guard tests): deleteUser self-delete rejection carries digest "SELF_DELETE"; target-not-found carries "USER_NOT_FOUND"; last-admin carries "LAST_ADMIN"; has-posts carries "USER_HAS_POSTS"; removeUser-rejection carries "DELETE_FAILED" (assert via rejects.toMatchObject({ digest: ... }) alongside the existing message assertions — the message still exists on the thrown instance; the digest is what survives production flight serialization).
  </behavior>
  <action>
  CR-02 leg 1 — contact.ts (source: 07-REVIEW CR-02; gap #6):
  - Change the submitContact signature to return the discriminated union of a successful submission or an error object whose error field is the literal "RATE_LIMITED" or "INVALID_INPUT" — the exact shape subscribeNewsletter's catch blocks produce at newsletter.ts:198-205 (the review names it the correct reference).
  - Replace the throwing Zod parse (contactSchema.parse) with contactSchema.safeParse; on failure return the INVALID_INPUT error state. Order stays: parse, honeypot, rate-limit, recipient, send.
  - Convert both rate-limit failure paths (the catch around contactFormLimiter.limit and the falsy success branch) from thrown errors to returned RATE_LIMITED error states — fail-closed semantics unchanged, only the transport changes.
  - Rewrite the docblock's error-contract section (contact.ts lines ~46-55): describe the returned error states instead of thrown errors, and record WHY in one comment line: React's production flight serializer emits digest-only error chunks (react-server-dom-webpack emitErrorChunk in the installed dist), so thrown Server Action messages never reach the client in production builds — returned states do (07-REVIEW CR-02 / 07-VERIFICATION gap #6).
  - Honeypot, recipient fallback, fire-and-forget sendEmail, and the no-DB-footprint design are untouched.

  CR-02 leg 1 — ContactForm.tsx:
  - In onValid's startTransition, capture the action's return value; on ok render the success state and reset; on the RATE_LIMITED error value render exactly "Too many messages — please try again later."; on the INVALID_INPUT error value render "Please check the form and try again.".
  - Delete the branch that compares the caught error's message text against the RATE_LIMITED sentinel — that mapping is unreachable in production (the review-verified redaction). Keep a try/catch around the await for unexpected transport failures mapping to the existing generic "Something went wrong. Please try again." message — with NO inspection of the error's message text.
  - Update the surrounding comments that describe the thrown contract.

  CR-02 leg 2 — users.ts + users-schema.ts + UsersTable.tsx:
  - Create src/actions/users-schema.ts as a pure sibling module (the "use server" export rule forbids non-function exports from users.ts; precedent: contact-schema.ts, pages-schema.ts). Export a const record of five stable digest tokens — SELF_DELETE, USER_NOT_FOUND, LAST_ADMIN, USER_HAS_POSTS, DELETE_FAILED (values identical to their keys) — and a digest-to-friendly-message lookup covering the five existing guard texts verbatim (keep the current sentence for each: the own-account sentence, the not-found sentence, the last-admin sentence with the promote-first instruction, the has-posts sentence with the reassign instruction, and the generic delete-failure sentence).
  - In deleteUser (users.ts), attach the matching digest property to each of the five thrown guard errors (assign the digest field on the Error instance before throwing; keep the readable message — it still serves dev flights and server logs). Guard order, MUST_NOT_BE_REACHED semantics, and the log-after-resolution discipline are untouched.
  - In UsersTable.tsx's shared error alert, map the delete mutation's error through the digest lookup (narrow type guard reading the digest field from the caught error) with the existing fallback chain retained for digest-less errors. Import the lookup from users-schema.ts (pure module — safe for a client component to import, same as contact-schema.ts is by ContactForm).
  - updateUser's guard throws are the shared UNAUTHORIZED/FORBIDDEN sentinels from the session/permission helpers used by every action — no readable per-action contract to convert; leave them (note this in the SUMMARY so the reviewer sees the boundary was deliberate).

  Tests:
  - Create src/actions/__tests__/contact.test.ts mirroring newsletter.test.ts's mock scaffold (mock @/lib/rate-limit's contactFormLimiter, next/headers, @/actions/settings' getSetting, @/lib/email's sendEmail) with the behaviors listed above.
  - Extend the five deleteUser guard tests in users.test.ts with the digest assertions listed in behavior.
  - Update the two comments in rate-limit.test.ts (near lines 34 and 184-186) that document contact.ts as mapping limiter rejections to a thrown RATE_LIMITED error — reword to the returned-state contract. No test bodies change (they test the limiter, not the action).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/actions/__tests__/contact.test.ts src/actions/__tests__/users.test.ts src/lib/rate-limit/__tests__/rate-limit.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - Behavior: all three test files pass, exit 0; contact.test.ts contains at least 6 tests including the limiter-rejection (Redis outage) fail-closed returned-state case.
  - Source assertion: contact.ts contains zero occurrences of the statement that throws a new Error whose message is the RATE_LIMITED sentinel (grep for that throw statement in src/actions/contact.ts returns 0).
  - Source assertion: the submitContact return type is the ok/error discriminated union (grep for 'INVALID_INPUT' in src/actions/contact.ts returns at least 2 matches — the type and the return site).
  - Source assertion: ContactForm.tsx no longer contains the error-message equality comparison against the RATE_LIMITED sentinel (grep for 'err.message' in src/components/site/ContactForm.tsx returns 0 matches).
  - Source assertion: grep for 'digest' in src/actions/users.ts returns at least 5 matches; grep for 'SELF_DELETE' in src/actions/users-schema.ts returns at least 1.
  - Source assertion: grep for '@/actions/users-schema' in src/app/(admin)/dashboard/users/UsersTable.tsx returns at least 1 match — the digest-lookup import itself must be wired into the file; this criterion cannot pass on a stray token appearing anywhere else.
  - Source assertion: grep for 'toThrow' in src/actions/__tests__/users.test.ts still matches the five guard tests AND grep for 'digest' in the same file returns at least 5 matches.
  </acceptance_criteria>
  <done>CR-02 closed in repo form: the contact form's rate-limit and invalid-input feedback reaches real users in production builds (returned states, not thrown messages), deleteUser guard failures surface their friendly copy via forwarded digests, and the contracts are pinned by the new/extended tests. 07-VERIFICATION gap #6 becomes repo-fixable-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-06 + WR-01 + WR-04 — configurable XFF hop count, visible Redis outage logging, honest harness exit codes</name>
  <files>src/lib/rate-limit/index.ts, src/lib/rate-limit/__tests__/client-ip.test.ts, src/lib/redis/index.ts, scripts/test-auth-ratelimit.mjs, .env.example</files>
  <read_first>
  - .planning/phases/07-performance-deploy/07-REVIEW.md (WR-06 full finding incl. the Cloudflare+Coolify chain analysis; WR-01; WR-04)
  - src/lib/rate-limit/index.ts (getClientIpFromXff + its full docblock — the trust rationale this task extends)
  - src/lib/rate-limit/__tests__/client-ip.test.ts (the four pinned tests that MUST keep passing unchanged at the default)
  - src/lib/redis/index.ts (the NODE_ENV-guarded error listener at lines 47-54)
  - scripts/test-auth-ratelimit.mjs lines 173-186 and 242-267 (the spawn/cleanup block) and lines 287-331 (main()'s result handling with the false-green branch)
  - src/lib/log (the structured logger module — the import surface used by actions)
  - .env.example (the existing TRUSTED_PROXY_CIDR documented block — the new var sits beside it)
  </read_first>
  <behavior>
  - Test (existing, must stay green with NO edits to the four current cases): default env — "203.0.113.42" returns "203.0.113.42"; "1.2.3.4, 203.0.113.9" returns "203.0.113.9" (last hop); whitespace trimmed; null/empty/blank return "unknown".
  - Test: with TRUSTED_XFF_HOP_COUNT=2, the three-hop chain "9.9.9.9, 198.51.100.7, 10.0.0.5" returns "198.51.100.7" (the client IP appended by the outermost trusted proxy — the last entry is the shared Coolify-proxy-appended edge IP).
  - Test: with TRUSTED_XFF_HOP_COUNT=2, a single-value header "203.0.113.42" still returns "203.0.113.42" (chain shorter than the count falls back to the last hop — graceful, fail-closed direction).
  - Test: TRUSTED_XFF_HOP_COUNT set to "0", a negative number, or a non-numeric string behaves identically to unset (treated as 1) — invalid configuration never widens trust toward the spoofable prefix.
  </behavior>
  <action>
  WR-06 — src/lib/rate-limit/index.ts:
  - Extend getClientIpFromXff to read TRUSTED_XFF_HOP_COUNT: the number of trusted appending proxies whose appended entries sit at the tail of the chain; the limiter keys on the entry at position (entry count minus hop count) — i.e. the client IP as observed by the outermost trusted proxy. Default 1 (this project's single-Coolify-proxy shape), which selects the last entry and is therefore byte-identical to today's behavior.
  - SEMANTICS CORRECTION vs the review sketch (record this in the code comment AND the SUMMARY): 07-REVIEW WR-06's sample expression indexes one position further left than intended — applied with its stated default it would select the client-spoofable first hop on two-entry chains, regressing exactly the anti-spoofing property CR-01 leg 2 established and breaking the pinned multi-hop test. The shipped shape must satisfy ALL of: default equals current last-hop behavior; hop count 2 recovers the Cloudflare-appended client IP on three-hop chains; a chain shorter than the count falls back to the LAST entry (over-limit/shared-bucket direction — never the spoofable prefix); non-numeric or sub-1 values coerce to 1 (guard the Number() parse; NaN and <=1 both land on 1).
  - Rewrite the docblock's multi-proxy paragraph: replace "Server Actions reading the raw header cannot [strip the chain]" with the configurable-count reality; document both misconfiguration failure modes — set BELOW the real proxy count ⇒ adjacent-proxy shared buckets (forms over-limited site-wide); set ABOVE the real count ⇒ selection moves into the client-supplied prefix (spoofable — treat as misconfiguration). Note the residual limitation honestly: without CIDR validation (the Better Auth trustedProxies mechanism, deliberately not duplicated here per the review's minimal fix) a hop count higher than the real chain is trusted on faith — hence the runbook's two-client verification (Task 4).

  WR-01 — src/lib/redis/index.ts:
  - Replace the NODE_ENV-guarded console-warning listener body with an unconditional structured log: log.error with a "redis connection error" message and the underlying error's message as context, importing the structured logger used across src/actions (lib/log). Keep the listener's crash-prevention purpose comment; add one line noting the logging is unconditional because a production outage must be visible (07-REVIEW WR-01 — the runbook's V5 diagnostic previously watched for output that was structurally impossible in production).
  - Do not change any connection options (lazyConnect, maxRetriesPerRequest, enableReadyCheck stay).

  WR-04 — scripts/test-auth-ratelimit.mjs:
  - In main()'s HTTP-result else branch ("failed"): set the process exit code to the failure value UNCONDITIONALLY. The structural-passed condition may still print its informational NOTE line, but it no longer influences the exit code. Exit 0 remains only for "passed" and "skipped".
  - Guard the finally-block cleanup kill with a server.pid existence check (spawn failure leaves pid undefined; the current code would attempt a kill on an undefined pid and log predictable noise on both the Windows taskkill path and the POSIX negative-pid path). Keep the existing logged-failure catch.
  - The summary Result line already derives from the exit code — after this change it prints FAIL consistently with the FAIL detail line above it.

  .env.example:
  - Add a TRUSTED_XFF_HOP_COUNT documented block immediately after the existing TRUSTED_PROXY_CIDR block, same prose style: empty/absent default 1 (single appending proxy); set 2 when Cloudflare (orange-cloud) or any second appending proxy fronts the Coolify proxy; both failure modes from the docblock above; pointer to the deploy-runbook section 5 row (added in Task 4).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/rate-limit/__tests__/client-ip.test.ts && node --check scripts/test-auth-ratelimit.mjs</automated>
  </verify>
  <acceptance_criteria>
  - Test command exits 0; client-ip.test.ts contains at least 8 tests (the 4 existing untouched + at least 4 new hop-count cases from behavior).
  - Source assertion: grep for 'TRUSTED_XFF_HOP_COUNT' in src/lib/rate-limit/index.ts returns at least 2 matches (parse + docblock) and in .env.example at least 1.
  - Source assertion: the four pre-existing client-ip test cases are textually unchanged in their expect values (the two-value chain case still expects the last hop).
  - Source assertion: grep for 'console.warn' in src/lib/redis/index.ts returns 0 matches; grep for 'log.error' in src/lib/redis/index.ts returns at least 1 match.
  - Source assertion: grep for 'NODE_ENV' in src/lib/redis/index.ts returns 0 matches.
  - Source assertion: grep for 'if (server.pid)' in scripts/test-auth-ratelimit.mjs returns exactly 1 match; in the HTTP-failed branch the exit-code assignment is no longer nested under the structural-passed condition (source assertion: between the 'FAIL:HTTP CHECK FAILED' log line and the Summary section, an unconditional exit-code failure assignment exists).
  - Behavior: node --check passes on the harness (syntax) — full harness execution requires docker/build and stays a live-run artifact, not this task's gate.
  </acceptance_criteria>
  <done>WR-06 closed: the documented Cloudflare+Coolify topology can key public-form buckets per real client (set the hop count to 2) while the default preserves the pinned single-proxy behavior byte-for-byte; WR-01 closed: production Redis outages emit structured error logs; WR-04 closed: a failed HTTP check can never certify PASS via exit code, and cleanup cannot fire on an undefined pid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-05 — Zod validation for taxonomy actions and updateUser profile fields</name>
  <files>src/actions/taxonomy-schema.ts, src/actions/users-schema.ts, src/actions/categories.ts, src/actions/tags.ts, src/actions/users.ts, src/actions/__tests__/taxonomy.test.ts, src/actions/__tests__/users.test.ts</files>
  <read_first>
  - .planning/phases/07-performance-deploy/07-REVIEW.md (WR-05 full finding — the exact bounds and the truthfulness/undefined inconsistency)
  - src/actions/categories.ts (createCategory/updateCategory — the unvalidated name/description and the truthiness name spread at line 96)
  - src/actions/tags.ts (createTag/updateTag — same shape at line 99)
  - src/actions/users.ts lines 294-334 (updateUser — the unvalidated name/bio/avatar patch build)
  - src/actions/pages-schema.ts + its consumption in src/actions/pages.ts line 149 (the established schema-sibling + safeParse-at-call-site convention this task follows)
  - src/lib/validation/image-url.ts (imageUrlSchema — REUSE for avatar: accepts empty string, absolute http(s), root-relative; rejects everything else)
  - src/actions/users-schema.ts (created in Task 1 — userUpdateSchema is added to it here)
  - src/actions/__tests__/taxonomy.test.ts and src/actions/__tests__/users.test.ts (existing fixtures; note users.test.ts line ~455 passes an avatar fixture without a URL scheme that the new schema will reject — update it)
  </read_first>
  <behavior>
  - Test (taxonomy): createCategory with an empty-string name throws INVALID_INPUT and the db insert is unreachable; name of 121 characters rejected; description of 1001 characters rejected — each before any DB write.
  - Test (taxonomy): updateCategory with an empty-string name throws INVALID_INPUT (no silent no-op) and the db update is unreachable; a valid partial update of only a name still reaches the update.
  - Test (taxonomy): the mirror set for createTag/updateTag.
  - Test (users): updateUser with bio of 2001 characters throws INVALID_INPUT and the db update is unreachable; name of 256 characters rejected; avatar value that is neither empty nor a valid URL nor root-relative rejected.
  - Test (users): updateUser with avatar "" (empty string) is accepted (imageUrlSchema empty-ok); partial { name: "X" } alone is accepted (partial semantics preserved).
  </behavior>
  <action>
  New pure sibling module src/actions/taxonomy-schema.ts (same convention as pages-schema.ts / contact-schema.ts — schemas cannot live in the "use server" files):
  - categorySchema: name constrained to a minimum of 1 and maximum of 120 characters; description optional with a maximum of 1000 characters (exact bounds from 07-REVIEW WR-05).
  - tagSchema: same two-field shape (tags carry no description today; the optional field is harmless and makes the two schemas symmetrical).
  - Export partial variants for updates (categorySchema.partial() / tagSchema.partial() — a present-but-empty name is REJECTED by the retained min-1 constraint; absence is allowed).
  - Slug is deliberately NOT in these schemas: validateSlug + assertUniqueSlug remain the authoritative slug path; the actions parse for name/description bounds and continue using their typed input for the write after validation passes (Zod's default strip of unlisted keys is fine — the parsed output is used as a gate, not as the write payload).

  categories.ts:
  - In createCategory, AFTER the requireCan permission check (guard order is non-negotiable) and BEFORE the slug validation, safeParse the input through categorySchema; on failure throw new Error("INVALID_INPUT") — the same sentinel style as the existing INVALID_SLUG throws in these files.
  - In updateCategory, same placement: requireCan, then safeParse through the partial variant, then the existing slug path.
  - Replace the truthiness name spread in the update's .set() patch with an explicit !== undefined check (matching the description line's existing style). With Zod now rejecting empty names up front, the undefined check is both safe and consistent.

  tags.ts: identical wiring with tagSchema / its partial variant, plus the same truthiness-to-undefined fix.

  users.ts updateUser + users-schema.ts:
  - Add userUpdateSchema to src/actions/users-schema.ts (file created in Task 1): name optional with min 1 / max 255; bio optional with max 2000; avatar optional reusing imageUrlSchema from @/lib/validation/image-url (URL-or-empty per the review); role optional constrained to the three-role enum (admin/editor/author — runtime-validates the cross-user role write that currently trusts the TypeScript type alone).
  - In updateUser, after the session/requireCan guard block and BEFORE building the patch object, safeParse the role-stripped input through userUpdateSchema; on failure throw new Error("INVALID_INPUT").
  - The patch build's existing !== undefined semantics stay exactly as they are (they already follow the convention WR-05 asks for).

  Tests:
  - Add the behavior cases above to taxonomy.test.ts and users.test.ts using the existing mock scaffolds (countResult/MUST_NOT_BE_REACHED idiom for proving the DB write is unreachable).
  - Update the existing updateUser self-edit fixture that passes a scheme-less avatar string — change it to a schema-valid URL (e.g. the same value with an https scheme) so the suite stays green under the new validation.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/actions/__tests__/taxonomy.test.ts src/actions/__tests__/users.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - Test command exits 0 with the new rejection cases present (grep for 'INVALID_INPUT' in taxonomy.test.ts returns at least 4 matches; in users.test.ts at least 3).
  - Source assertion: grep for 'max(120)' in src/actions/taxonomy-schema.ts returns at least 2 matches (category + tag name bounds); grep for 'max(1000)' returns at least 1; grep for 'max(2000)' in src/actions/users-schema.ts returns at least 1.
  - Source assertion: grep for 'imageUrlSchema' in src/actions/users-schema.ts returns at least 1 match (avatar reuses the shared validator — no new URL rule invented).
  - Source assertion: grep for 'INVALID_INPUT' in src/actions/categories.ts returns at least 2 matches, in src/actions/tags.ts at least 2, in src/actions/users.ts at least 1.
  - Source assertion: the truthiness name spread is gone — grep for 'input.name ?' in src/actions/categories.ts and src/actions/tags.ts returns 0 matches, while the !== undefined form is present in both.
  - Source assertion: permission checks remain FIRST — in each edited action the requireCan call still precedes the safeParse call (source-order assertion).
  </acceptance_criteria>
  <done>WR-05 closed: taxonomy and profile mutation inputs are bounded and validated server-side per the project's Zod-reuse convention; the empty-string-name silent no-op is gone (rejected loudly); an authenticated author can no longer persist unbounded name/bio strings or a non-URL avatar; all existing tests stay green.</done>
</task>

<task type="auto">
  <name>Task 4: Documentation — CR-01 runbook env rows + troubleshooting, WR-07 SUPERSEDED banner, WR-03 resolved decisions, WR-02 URL fixes, WR-01/WR-06 runbook legs</name>
  <files>docs/operations/coolify-deploy.md, docs/operations/dns-email-deliverability.md, Dockerfile</files>
  <read_first>
  - .planning/phases/07-performance-deploy/07-REVIEW.md (CR-01 fix text with the suggested table row; WR-02; WR-03; WR-07 banner wording)
  - .planning/phases/07-performance-deploy/07-VERIFICATION.md (gap #5 — the structured gap; the overrides block for the 1000KB context)
  - docs/operations/coolify-deploy.md (full file — sections 3-5, V1/V3/V5, Troubleshooting)
  - docs/operations/dns-email-deliverability.md lines 110-130, 185-200, 240-250 (the three wrong page-route references)
  - Dockerfile lines 10-20 (the stale GATE 2 header comment) and line 95 (the actual 1000 invocation)
  - scripts/test-auth-ratelimit.mjs lines 291-315 (the SKIP instructions the TRUSTED_PROXY_CIDR row points operators at)
  - .env.example TRUSTED_PROXY_CIDR block (added TRUSTED_XFF_HOP_COUNT block from Task 2 — the runbook row must agree with it)
  </read_first>
  <action>
  coolify-deploy.md — CR-01 (gap #5):
  - Add a TRUSTED_PROXY_CIDR row to the section 5 runtime env table (after the REDIS_URL row): value column — the Coolify proxy's internal-network CIDR (e.g. the docker-network range 172.16.0.0/12), comma-separate multiple ranges; notes column — REQUIRED for production behind the appending proxy. Unset means every multi-value XFF request resolves to a null client IP, collapsing ALL auth traffic into one shared 3-per-15-minutes bucket (a trivial unauthenticated site-wide sign-in/password-reset lockout). An over-broad CIDR matching every hop produces the same collapse. Verify after deploy via the through-the-proxy curl procedure in scripts/test-auth-ratelimit.mjs's SKIP instructions ("AFTER DEPLOY - per-environment IP-trust verification").
  - Add a TRUSTED_XFF_HOP_COUNT row beside it (WR-06 doc leg): default 1 (single appending proxy); set 2 when Cloudflare orange-cloud or a second appending proxy fronts the Coolify proxy — keys the public contact/newsletter limiters on the client IP as observed by the outermost trusted proxy. Too low a value shares form buckets site-wide; too high a value keys on client-supplied spoofable entries. Post-deploy check: two different clients must land in different form buckets.
  - Extend the "Auth fails closed (no sign-in possible)" troubleshooting entry: keep the Redis-unreachable cause, add the shared-bucket lockout symptom — sign-in/reset rate-limited site-wide after 3 attempts for everyone ⇒ TRUSTED_PROXY_CIDR unset or over-broad; see the section 5 row.

  coolify-deploy.md — WR-07 (banner-only, do NOT rewrite the body):
  - Insert a prominent status banner at the very top of the file (immediately after the title): SUPERSEDED per owner decision 2026-07-29 — production deploys are MANUAL; the Docker/Coolify git-push pipeline described here is local-dev dry-run material; sections 5 (runtime env vars) and 6 (Redis service) are retained as the production env-var reference; the full revision for the manual flow rides the owner's deploy review.
  - Reconcile the header paragraph (the "push to main builds and deploys directly to production" premise at lines ~13-18) and verification step V3's "Push to main" instruction with a one-line pointer to the banner each. No other body changes.

  coolify-deploy.md + Dockerfile — WR-03:
  - Decision B section: add a resolution note at the top — RESOLVED, 1000 KB applied: the Dockerfile GATE 2 RUN line and the package.json check-bundle script both already run with the 1000 threshold, baseline ~749 KB gzipped (this matches the owner-accepted override recorded in 07-VERIFICATION.md). Update the section's sample command and the GATE-2 troubleshooting entry to speak of the 1000 default rather than presenting the threshold as unresolved.
  - Dockerfile header comment (line 16 region): correct the GATE 2 comment so the flag value reads 1000 and the adjacent "exceeds" phrase reads 1000 KB — comment-only change; the RUN line at 95 already says 1000.
  - V1 dry-run: extend the docker build command with a DATABASE_URL build arg (the Dockerfile declares ARG DATABASE_URL in the builder stage — Decision A path (a) made it required for the prerendering build) and add an explicit note that REDIS_URL has no Dockerfile ARG: the ioredis lazyConnect makes the build survive without it, and an operator who wants it at build time supplies it via the Coolify build-time environment per Decision A, not via a docker build flag.

  dns-email-deliverability.md — WR-02:
  - Replace the three page-route occurrences of the misspelled password-reset path (lines 119, 191, 245 — the variant missing a letter, per 07-REVIEW WR-02) with the correct /forgot-password page route.
  - At the troubleshooting occurrence (line ~245), add the one-time distinction note: the rate-limited Better Auth API endpoint behind the page is /api/auth/forget-password — the page route and the API endpoint differ by that letter pair; the runbook previously conflated them.

  coolify-deploy.md — WR-01 doc leg:
  - Reword V5 to what production actually emits after Task 2: check the Next.js container logs for the structured "redis connection error" entries (error level via the app logger); a healthy deploy shows none; during a Redis outage they repeat and auth fails closed. Remove the implication that the warnings are dev-only console output.
  </action>
  <verify>
    <automated>grep -rn "TRUSTED_PROXY_CIDR" docs/ | wc -l && grep -c "SUPERSEDED" docs/operations/coolify-deploy.md && grep -c "forgot-password" docs/operations/dns-email-deliverability.md && grep -c "max-gz-kb=1000" Dockerfile</automated>
  </verify>
  <acceptance_criteria>
  - CLI: grep for TRUSTED_PROXY_CIDR under docs/ returns at least 2 matches (env-table row + troubleshooting entry) — was 0 before (the gap #5 defect).
  - CLI: grep for TRUSTED_XFF_HOP_COUNT in docs/operations/coolify-deploy.md returns at least 1 match (section 5 row).
  - CLI: grep for SUPERSEDED in docs/operations/coolify-deploy.md returns at least 1 match (the banner); the banner text contains the 2026-07-29 date and the word MANUAL.
  - CLI: grep for the misspelled page path (the wrong password-reset URL as it appears today at dns-email-deliverability.md lines 119/191/245 — see 07-REVIEW WR-02 for the exact string) under docs/ returns 0 matches; grep for "forgot-password" in dns-email-deliverability.md returns at least 3 matches.
  - CLI: grep for the 1000 threshold in the Dockerfile header comment region returns a match on the comment line (line ~16), consistent with the RUN line at 95; grep for "max-gz-kb=1000" in Dockerfile returns 2 matches (comment + RUN).
  - Source assertion: the V1 dry-run docker build command in coolify-deploy.md contains a DATABASE_URL build-arg and the REDIS_URL note.
  - Source assertion: the Decision B section contains a resolution statement recording the applied 1000 KB threshold; the section no longer presents the threshold as an unresolved open decision.
  - Source assertion: the "Auth fails closed" troubleshooting entry mentions the shared auth bucket / TRUSTED_PROXY_CIDR cause in addition to Redis unreachability.
  - Source assertion: V5 references the structured redis connection error log lines (not dev-only console warnings).
  </acceptance_criteria>
  <done>CR-01 closed in repo form (gap #5): a by-the-runbook deploy now sets TRUSTED_PROXY_CIDR (and knows when to set TRUSTED_XFF_HOP_COUNT), recognizes the shared-bucket lockout symptom, and is told up front that the pipeline is local-dev material per the owner's manual-deploy decision. The three runbooks and the Dockerfile header no longer contradict the repo or the owner's recorded decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → (site) Server Actions | Unauthenticated input crosses into submitContact (public contact form) |
| browser → XFF header chain | Client-supplied XFF prefix crosses into the limiter key derivation (getClientIpFromXff) behind trusted appending proxies |
| authenticated dashboard user → (admin) Server Actions | Any author/editor crosses into taxonomy + updateUser input handling |
| runbook reader → production env | The operator's env-var checklist determines whether the 07-06 IP-trust mitigation activates at deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07-01 | Information Disclosure / Spoofing of contract | contact.ts + ContactForm.tsx thrown-error contract | high | mitigate | CR-02 leg 1: returned states replace thrown messages (production flight redaction made the mapping dead code and the generic retry message invited limiter hammering); new test suite pins the contract |
| T-07-07-02 | Spoofing | getClientIpFromXff under multi-proxy chains | high | mitigate | WR-06 with corrected selection semantics: default preserves the pinned last-hop anti-spoof behavior (the review sketch's off-by-one would have keyed on the spoofable first hop — corrected + documented); invalid values coerce to 1; short chains fall back to the last hop |
| T-07-07-03 | Denial of Service | auth endpoints via unset TRUSTED_PROXY_CIDR at deploy | high | mitigate | CR-01: runbook section 5 row (required-for-production, both failure modes) + shared-bucket troubleshooting entry + post-deploy through-the-proxy curl verification |
| T-07-07-04 | Tampering / DoS (DB bloat) | categories/tags/updateUser unbounded inputs | medium | mitigate | WR-05: Zod bounds (name 120/255, description 1000, bio 2000, avatar URL-or-empty, role enum) parsed after the permission gate, before any DB write |
| T-07-07-05 | Information Disclosure | digest tokens crossing to the client | low | mitigate | Digest values are fixed non-sensitive sentinels (SELF_DELETE etc.); they carry no internal detail — the friendly copy lives client-side in a pure module |
| T-07-07-06 | Repudiation / Detection | Redis outage invisible in production logs | medium | mitigate | WR-01: unconditional structured log.error in the singleton's error listener; V5 runbook wording aligned to real production output |
| T-07-07-07 | Tampering (false certification) | harness exit code | medium | mitigate | WR-04: HTTP "failed" sets the failure exit unconditionally; automation can no longer certify a broken limiter |
| T-07-07-SC | Tampering | package installs | low | accept | Zero new dependencies in this plan (zod, ioredis, existing validators all pinned already) — no install tasks, no supply-chain surface added |
</threat_model>

<verification>
- pnpm exec vitest run — full suite green (621+ tests, exit 0) after all four tasks.
- pnpm build — type-check gate clean with the changed signatures (submitContact union, users-schema/taxonomy-schema modules, UsersTable digest typing).
- pnpm exec eslint --max-warnings 0 on the touched files — GATE 1 parity (no cross-group imports introduced; note users-schema.ts is a pure module imported by both an action and a client component, same as contact-schema.ts — this is the established legal pattern).
- Docs greps per Task 4 acceptance criteria: TRUSTED_PROXY_CIDR present under docs/ (was zero — gap #5), SUPERSEDED banner present, misspelled password-reset path eliminated, Dockerfile header consistent with the RUN line.
- 07-VERIFICATION re-check mapping: gap #5 (runbook omission) and gap #6 (dead thrown contracts) are repo-fixable-closed by Tasks 4 and 1 respectively; the four owner-deferred live-stack gaps remain open by owner decision and are NOT claimed.
</verification>

<success_criteria>
- 07-VERIFICATION gaps #5 and #6 close on re-verification: the runbook env table carries TRUSTED_PROXY_CIDR with failure modes and verification; grep for it under docs/ is non-zero; contact.ts and users.ts carry no production-unreachable client contracts; ContactForm/UsersTable branch on returned states/digests.
- All seven warning legs (WR-01..WR-07) land their repo-fixable halves: unconditional Redis error logging + V5 rewording; /forgot-password corrections; resolved Decision B + Dockerfile header + V1 build-arg; unconditional harness failure exit + pid guard; Zod bounds on taxonomy/profile; TRUSTED_XFF_HOP_COUNT code+tests+env+runbook; SUPERSEDED banner.
- Full test suite and build stay green; no behavior regressions in the pinned 07-06 tests (client-ip default cases textually unchanged, revalidation assertions untouched, newsletter tests untouched).
- Nothing in the owner's out-of-scope fence is touched: no docker/deploy-system work, no live-stack verifications, no IN-01..IN-05 items.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New symbols/modules (exclude from drift verification as newly-created):
- src/actions/users-schema.ts — USER_DELETE digest constants (SELF_DELETE, USER_NOT_FOUND, LAST_ADMIN, USER_HAS_POSTS, DELETE_FAILED), the digest→friendly-message lookup, and userUpdateSchema (name/bio/avatar/role bounds)
- src/actions/taxonomy-schema.ts — categorySchema, tagSchema, and their .partial() update variants
- src/actions/__tests__/contact.test.ts — the submitContact returned-state suite
- submitContact's new return type: the ok-union with error values RATE_LIMITED and INVALID_INPUT
- deleteUser's five guard digests (attached to existing throws — new client-visible surface)
- TRUSTED_XFF_HOP_COUNT env var (consumed in src/lib/rate-limit/index.ts; documented in .env.example + coolify-deploy.md section 5)
- New client-ip.test.ts describe block: hop-count behaviors (count 2 selection, short-chain fallback, invalid-value coercion)

New/changed doc sections:
- coolify-deploy.md: SUPERSEDED banner (owner decision 2026-07-29); TRUSTED_PROXY_CIDR + TRUSTED_XFF_HOP_COUNT section 5 rows; shared-bucket extension of the "Auth fails closed" entry; Decision B resolution note; V1 dry-run DATABASE_URL build-arg + REDIS_URL note; V5 structured-log wording; header/V3 banner pointers
- dns-email-deliverability.md: /forgot-password page-route corrections (3) + the API-endpoint distinction note
- Dockerfile: header comment threshold corrected to 1000 (comment-only)

Changed contracts (intentional, review-mandated):
- submitContact: thrown errors → returned states (breaking for its single consumer ContactForm.tsx, updated in the same task)
- UsersTable error alert: message rendering → digest-mapped copy with fallback
- categories/tags update patches: truthiness → !== undefined for name
- test-auth-ratelimit.mjs: exit semantics (failed ⇒ exit 1 unconditionally) + server.pid cleanup guard
</artifacts_produced>

<deferred_and_out_of_scope>
## Deferred / Out of Scope (binding this round)

Owner-deferred live-stack gaps (07-VERIFICATION frontmatter — await the owner's deploy review; NOT planned):
- Deploy on Coolify (SC#5 / PERF-06 live leg) — owner decision 2026-07-29: "for deployment, do not make any docker system now, milestone 1 does not need this currently"
- Live Lighthouse/CWV run (SC#1 live leg)
- Live publish→visible run (SC#3 live leg)
- Umami deploy + DKIM/SPF/DMARC publication (07-04 Tasks 2-3)

Excluded by the owner this round (07-REVIEW info items — do NOT plan):
- IN-01 (recursive bundle scan), IN-02 (blank-subject fallback), IN-03 (createFirstAdmin TOCTOU), IN-04 (ungated read exports), IN-05 (harness port preflight)

Explicit non-goals: no Docker/deploy-system construction, no runbook rewrite for the manual deploy flow (banner-only per WR-07 — the full revision rides the owner's deploy review), no live-stack verification runs, no changes to plans 07-01..07-06.
</deferred_and_out_of_scope>

<output>
Create `.planning/phases/07-performance-deploy/07-07-SUMMARY.md` when done.

In the SUMMARY, explicitly record:
1. The WR-06 semantics correction vs the 07-REVIEW sample formula (the off-by-one that would have selected the spoofable first hop on two-entry chains) — the reviewer should see the deviation and its rationale.
2. The updateUser boundary decision: its guard throws are shared-helper sentinels (UNAUTHORIZED/FORBIDDEN), deliberately left as-is — no readable per-action contract exists to convert.
3. The task→gap mapping for the verifier: Task 1 → gap #6, Task 4 → gap #5, Tasks 2-3 → WR legs.
</output>
