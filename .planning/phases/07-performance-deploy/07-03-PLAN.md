---
phase: 07-performance-deploy
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
files_modified:
  - .planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md
  - src/actions/categories.ts
  - src/actions/tags.ts
  - src/actions/pages.ts
  - src/actions/users.ts
  - scripts/test-publish-visible.mjs
  - package.json
autonomous: true
requirements:
  - PERF-03
user_setup: []

must_haves:
  truths:
    - "Every mutating Server Action in src/actions/ is classified in 07-REVALIDATION-AUDIT.md as HAS (verified revalidation), MISSING (real gap with documented fix applied), or N/A (correctly-not-cached with justification) — zero rows blank (D-16/D-18/D-19)"
    - "Categories, tags, pages, and users actions that the audit identifies as MISSING now call revalidatePath/revalidateTag for every public route they affect, using the SAME invalidation mechanism the route uses for caching (Pitfall #7 — revalidatePath for path-cached routes; revalidateTag for cacheTag-cached routes)"
    - "Every added revalidation call uses CONCRETE LITERAL paths (e.g. `/category/bangla-news`) and the 2-arg revalidateTag form `(tag, \"max\")` (Phase 3 D-25 pattern from src/actions/posts.ts:351-368)"
    - "Revalidation calls are appended AFTER the DB write and AFTER the existing permission gate (preserve the requireRole/requireCan convention)"
    - "scripts/test-publish-visible.mjs polls the deployed PROD_URL after a publish action and confirms the new content appears at the public URL within 30 seconds (D-17)"
    - "The audit finds posts.ts:publishPost and settings.ts:saveSeoSettings already correct (HAS) — these are the templates the fixes copy"
  artifacts:
    - ".planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md — markdown table mapping every mutating action to its revalidation calls with Status classification"
    - "src/actions/categories.ts — createCategory / updateCategory / softDeleteCategory now revalidate /category/{slug}, /blog, /, /sitemap.xml (and the matching cacheTags) after the DB write"
    - "src/actions/tags.ts — createTag / updateTag / softDeleteTag now revalidate /tag/{slug} and the matching cacheTags"
    - "src/actions/pages.ts — createPage / updatePage / softDeletePage now revalidate the public T&C/Privacy/Contact routes"
    - "src/actions/users.ts — updateUser (profile fields) now revalidates /author/{username}; create/disable/ban actions classified N/A (no public surface yet)"
    - "scripts/test-publish-visible.mjs — Node script that publishes a draft post and polls the public URL until content appears (30s ceiling)"
    - "package.json — add `test:publish-visible` script"
  key_links:
    - "src/actions/categories.ts / tags.ts / pages.ts / users.ts revalidation calls follow the template at src/actions/posts.ts:351-368 (concrete literals + 2-arg revalidateTag)"
    - "The audit table's cache-strategy column cross-references the route's `cacheTag` value (Pitfall #7 — invalidation mechanism must match caching mechanism)"
    - "scripts/test-publish-visible.mjs PROD_URL env var -> Plan 07-04's deployed production URL (the script is run after Coolify deploy to verify Pitfall #3 end-to-end on the real stack)"
---

## Phase Goal

**As a** reader, **I want to** see published/updated content appear on the public site immediately, **so that** the publish→visible loop works reliably end-to-end on the real Coolify + Cloudflare stack (closes Pitfall #3).

<objective>
Revalidation audit + fixes slice (PERF-03): a systematic action-by-action audit of every mutating Server Action in `src/actions/` documented as a markdown table with HAS/MISSING/N/A classification, followed by adding the missing revalidation calls to categories, tags, pages, and users actions, plus a publish→visible verification script.

Purpose: RESEARCH.md confirmed via grep that ONLY `posts.ts:publishPost` and `settings.ts:saveSeoSettings` currently call revalidation. Categories, tags, pages, and users CRUD have NONE — these are real gaps because their data surfaces on public routes. The audit must classify each (D-19) and fix the real gaps (D-16/D-18). Pitfall #7 is the load-bearing constraint: the fix must use the SAME mechanism the route uses for caching (path-cached → `revalidatePath`; cacheTag-cached → `revalidateTag`).

Output: 07-REVALIDATION-AUDIT.md, four action files with revalidation calls added, publish→visible test script.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-deploy/07-CONTEXT.md
@.planning/phases/07-performance-deploy/07-RESEARCH.md
@.planning/phases/07-performance-deploy/07-PATTERNS.md
@.planning/phases/07-performance-deploy/07-VALIDATION.md

@src/actions/posts.ts
@src/actions/settings.ts
@src/actions/categories.ts
@src/actions/tags.ts
@src/actions/pages.ts
@src/actions/users.ts
@src/actions/media.ts
@src/actions/storage-settings.ts
@src/actions/contact.ts
@src/lib/queries/posts.ts
@src/lib/queries/taxonomy.ts
@src/lib/queries/users.ts
@src/lib/queries/pages.ts
@package.json
@scripts/test-auth-gate.mjs
</context>

## Artifacts this phase produces

- `.planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md` (NEW) — markdown audit deliverable per D-19.
- `src/actions/categories.ts` (MODIFY) — add revalidation calls after the DB write in create/update/softDelete.
- `src/actions/tags.ts` (MODIFY) — add revalidation calls.
- `src/actions/pages.ts` (MODIFY) — add revalidation calls for T&C/Privacy/Contact public routes.
- `src/actions/users.ts` (MODIFY) — add revalidation calls for /author/{username} when profile fields change.
- `scripts/test-publish-visible.mjs` (NEW) — Node script for end-to-end publish→visible verification.
- `package.json` (MODIFY) — add `test:publish-visible` script.

<tasks>

<task type="auto">
  <name>Task 1: Author the revalidation audit deliverable (read every mutating action, classify HAS / MISSING / N/A)</name>
  <files>.planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md</files>
  <read_first>
    - src/actions/posts.ts (full file — find publishPost at lines 325-375; identify the revalidation block at lines 351-368 as the template; identify savePost/submitForReview/setSchedule/autosavePost and check each for revalidation)
    - src/actions/settings.ts (full file — find saveSeoSettings; identify its revalidateTag(\"seo-settings\", \"max\") + revalidatePath(\"/\", \"layout\") block)
    - src/actions/categories.ts (full file ~87 lines — verify NO next/cache import, NO revalidation calls)
    - src/actions/tags.ts (full file — verify NO revalidation)
    - src/actions/pages.ts (full file — verify NO revalidation)
    - src/actions/users.ts (full file — verify NO revalidation; identify updateUser vs createUser/banUser)
    - src/actions/media.ts (full file — verify NO revalidation; reason about whether media URLs need revalidation)
    - src/actions/storage-settings.ts (full file — verify NO revalidation; reason about whether dashboard-only settings need it)
    - src/actions/contact.ts (full file — verify NO revalidation; reason: email-only, no DB write per D-08)
    - src/lib/queries/posts.ts (identify cacheTag values used by getPostForPublic, listPublished, listFeatured — these are the tags revalidateTag must invalidate)
    - src/lib/queries/taxonomy.ts (identify cacheTag values for category/tag routes)
    - src/lib/queries/users.ts (identify cacheTag values for author pages)
    - src/lib/queries/pages.ts (identify cacheTag values for T&C/Privacy/Contact public routes)
    - .planning/phases/07-performance-deploy/07-RESEARCH.md Pattern 4 (lines 449-474) — the audit table template
    - .planning/phases/07-performance-deploy/07-RESEARCH.md Pitfall 7 (lines 536-540) — cache-strategy matching requirement
  </read_first>
  <action>
    Create `.planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md` as a markdown table per RESEARCH.md Pattern 4. Columns: `| Action file | Function | revalidatePath calls | revalidateTag calls | Route cache strategy | Status |`. The "Route cache strategy" column records whether the affected public route(s) use `revalidate` (time-based ISR), `cacheTag(...)` (tag-based invalidation), or are dynamically rendered — this column is the load-bearing input to Pitfall #7 (the fix must match the caching mechanism).

    Populate one row per mutating Server Action across ALL of `src/actions/`: posts.ts (publishPost, savePost, submitForReview, setSchedule, autosavePost), settings.ts (saveSeoSettings, saveStorageSettings, saveGeneralSettings if exists), categories.ts (createCategory, updateCategory, softDeleteCategory), tags.ts (createTag, updateTag, softDeleteTag), pages.ts (createPage, updatePage, softDeletePage), users.ts (createUser, updateUser, banUser, unbanUser, revokeSessions), media.ts (uploadMedia, deleteMedia), storage-settings.ts (saveStorageSettings), contact.ts (submitContact).

    For each row, mark Status as:
    - **HAS** — the action already calls revalidation correctly (e.g. posts.ts:publishPost at lines 351-368, settings.ts:saveSeoSettings). Document the exact paths/tags.
    - **MISSING** — the action mutates data that surfaces on a public cached route AND has no revalidation. Include the specific fix (which paths/tags to add). Examples per RESEARCH.md Pattern 4: categories.ts (affects /category/{slug}, /blog, /, /sitemap.xml), tags.ts (/tag/{slug}), pages.ts (T&C/Privacy/Contact public routes), users.ts:updateUser (profile fields affect /author/{username}).
    - **N/A** — the action's mutation does NOT surface on any public cached route, OR the surface is not cached. Include a one-line justification. Examples: contact.ts:submitContact (no DB write per D-08), storage-settings.ts:saveStorageSettings (dashboard-only setting), users.ts:createUser/banUser (no public author page yet — author pages only render for users with published posts), media.ts (media URLs handled by next/image cache, not page revalidation), posts.ts:savePost (draft save — drafts don't surface publicly), posts.ts:autosavePost (draft only).

    For each MISSING row, the fix cell MUST specify BOTH the revalidation call AND match the route's cache strategy. Example: if `/category/[slug]` is rendered with `cacheTag(\"category-\" + categoryId)`, the fix uses `revalidateTag(\"category-\" + categoryId, \"max\")`, NOT `revalidatePath(\"/category/\" + slug)`. If the route uses path-based ISR (`export const revalidate = 60`), the fix uses `revalidatePath(\"/category/\" + slug)`.

    The table must have ZERO blank Status cells. Add a "Verification Checklist" section at the bottom with checkboxes for each MISSING fix to be applied in Task 2.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && grep -c "^| " .planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md | grep -qE "^[0-9]{2,}" && grep -cE "HAS|MISSING|N/A" .planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md | grep -qE "[0-9]+" && ! grep -E "^\\| .* \\| *\\| *$" .planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md && echo "PLAN-07-03-AUDIT-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md` exists
    - The file contains a markdown table with the column headers `Action file | Function | revalidatePath calls | revalidateTag calls | Route cache strategy | Status`
    - The table contains at least 15 rows covering every mutating Server Action across posts.ts, settings.ts, categories.ts, tags.ts, pages.ts, users.ts, media.ts, storage-settings.ts, contact.ts
    - Every row's Status cell is one of `HAS`, `MISSING` (with fix), or `N/A` (with justification) — NO blank Status cells (verify: `grep -c "| *|" file` returns 0)
    - `posts.ts:publishPost` is marked HAS with the exact revalidation calls documented (lines 351-368 of src/actions/posts.ts)
    - `settings.ts:saveSeoSettings` is marked HAS
    - `categories.ts`, `tags.ts`, `pages.ts`, `users.ts:updateUser` are marked MISSING with specific fixes
    - `contact.ts:submitContact` is marked N/A with the "no DB write per D-08" justification
    - Each MISSING row's fix cell references BOTH the revalidation call AND the route's cache strategy (Pitfall #7)
    - A "Verification Checklist" section at the bottom lists every MISSING fix to be applied in Task 2
  </acceptance_criteria>
  <done>07-REVALIDATION-AUDIT.md exists, covers every mutating action, every row has a non-blank Status classification, MISSING rows include specific fixes that match the route's cache strategy (Pitfall #7 honored), the document is the authoritative reference for Task 2's fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply revalidation fixes to categories, tags, pages, users actions</name>
  <files>src/actions/categories.ts, src/actions/tags.ts, src/actions/pages.ts, src/actions/users.ts</files>
  <read_first>
    - .planning/phases/07-performance-deploy/07-REVALIDATION-AUDIT.md (the audit deliverable from Task 1 — the authoritative fix list)
    - src/actions/posts.ts (lines 320-370 — the canonical revalidation template: import statement, concrete literal paths, 2-arg revalidateTag, AFTER permission gate and DB write)
    - src/actions/categories.ts (full file — find createCategory/updateCategory/softDeleteCategory; identify the existing requireCan permission gate and DB write; insert revalidation AFTER both)
    - src/actions/tags.ts (full file — same pattern)
    - src/actions/pages.ts (full file — same pattern)
    - src/actions/users.ts (full file — find updateUser specifically; identify profile fields like bio, avatar, username that affect /author/[username])
    - src/lib/queries/taxonomy.ts (confirm the cacheTag values used by /category/[slug] and /tag/[slug] routes — the fix must invalidate these exact tags)
    - src/lib/queries/users.ts (confirm the cacheTag value used by /author/[username])
    - src/lib/queries/pages.ts (confirm cacheTag values for T&C/Privacy/Contact public routes)
  </read_first>
  <action>
    For each MISSING row in 07-REVALIDATION-AUDIT.md, add the specified revalidation calls. Apply the template from src/actions/posts.ts:320-370 verbatim in structure:

    1. Add the import at the top of the file (if not already present): `import { revalidatePath, revalidateTag } from "next/cache";`
    2. After the existing DB write (and AFTER the existing `requireCan`/`requireRole` permission gate — never before), add the revalidation calls.
    3. Use CONCRETE LITERAL paths constructed from the just-written row's data (e.g. `revalidatePath(`/category/${category.slug}`)` — backtick template with a real slug variable, NOT a Next.js route-pattern like `/category/[slug]`).
    4. Use the 2-arg `revalidateTag(tag, "max")` form (single-arg is DEPRECATED in Next 16.2.9 — Phase 3 D-25).
    5. Match the route's cache strategy per Pitfall #7: if the route uses `cacheTag("category-X")`, call `revalidateTag("category-X", "max")`; if the route uses path-based ISR, call `revalidatePath`.

    Specific actions to modify (per the audit deliverable):
    - `src/actions/categories.ts` createCategory/updateCategory/softDeleteCategory: revalidate `/category/${slug}`, `/blog`, `/`, `/sitemap.xml`, plus the cacheTags the /category/[slug] route uses (likely `category-${id}` and the `posts-list` tag).
    - `src/actions/tags.ts` createTag/updateTag/softDeleteTag: revalidate `/tag/${slug}` plus its cacheTag (likely `tag-${id}`).
    - `src/actions/pages.ts` createPage/updatePage/softDeletePage: revalidate the public route for the page type (T&C, Privacy, Contact — likely `/terms`, `/privacy`, `/contact` or similar — confirm via src/lib/queries/pages.ts) plus the page's cacheTag.
    - `src/actions/users.ts` updateUser: if bio/avatar/username changed, revalidate `/author/${username}` plus its cacheTag (likely `author-${id}`). For createUser/banUser/unbanUser/revokeSessions, leave the N/A classification as-is (no public surface) but add a comment noting "N/A per audit — no public author page yet for this user."

    Do NOT touch the existing permission gates. Do NOT touch posts.ts (already HAS), settings.ts (already HAS), media.ts (N/A), storage-settings.ts (N/A), contact.ts (N/A). The audit deliverable is the authoritative list.

    After applying each fix, update the corresponding row in 07-REVALIDATION-AUDIT.md from MISSING to HAS (with the applied calls documented). This keeps the audit document in sync with the code.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && for f in src/actions/categories.ts src/actions/tags.ts src/actions/pages.ts src/actions/users.ts; do echo "== $f =="; grep -c "revalidate\\(Path\\|Tag\\)" "$f" || echo "0 matches in $f"; done && grep -c "from \"next/cache\"" src/actions/categories.ts | grep -q 1 && grep -c "from \"next/cache\"" src/actions/tags.ts | grep -q 1 && grep -c "from \"next/cache\"" src/actions/pages.ts | grep -q 1 && grep -c "from \"next/cache\"" src/actions/users.ts | grep -q 1 && pnpm test -- --run && pnpm lint && echo "PLAN-07-03-FIXES-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/actions/categories.ts` imports `revalidatePath, revalidateTag` from `next/cache` (verify: `grep -c "from \"next/cache\"" src/actions/categories.ts` returns 1)
    - `src/actions/categories.ts` contains at least 3 revalidation call sites (one per mutating function: createCategory, updateCategory, softDeleteCategory) (verify: `grep -c "revalidate\\(Path\\|Tag\\)" src/actions/categories.ts` >= 3)
    - `src/actions/tags.ts` imports from next/cache and contains >= 3 revalidation call sites
    - `src/actions/pages.ts` imports from next/cache and contains >= 3 revalidation call sites
    - `src/actions/users.ts` imports from next/cache; `updateUser` contains revalidation for /author/{username}; create/ban/unban/revoke contain an "N/A per audit" comment
    - All revalidateTag calls use the 2-arg form with `"max"` (verify: `grep -E "revalidateTag\\([^,]+\\)" src/actions/{categories,tags,pages,users}.ts | wc -l` returns 0 — no single-arg calls)
    - All revalidatePath calls use concrete template literals (e.g. `` `/category/${slug}` ``), NOT route patterns (verify: `grep -E "revalidatePath\\(\"/\\[" src/actions/*.ts | wc -l` returns 0 — no `/[slug]` route-pattern strings)
    - The existing `requireCan` / `requireRole` permission gates are preserved in every modified file (manual review or `grep -c "requireCan\\|requireRole" src/actions/{categories,tags,pages,users}.ts` returns >= 1 per file)
    - 07-REVALIDATION-AUDIT.md rows for categories/tags/pages/users updated from MISSING to HAS with the applied calls documented
    - `pnpm test -- --run` exits 0 (no existing tests broken)
    - `pnpm lint` exits 0 (no lint errors introduced)
  </acceptance_criteria>
  <done>Categories, tags, pages, and users actions all revalidate the public routes they affect, using the SAME mechanism each route uses for caching (Pitfall #7 honored). Every revalidateTag uses the 2-arg form; every revalidatePath uses concrete literals; permission gates are preserved. 07-REVALIDATION-AUDIT.md is updated to reflect the applied fixes. pnpm test and pnpm lint both pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create the publish→visible verification script</name>
  <files>scripts/test-publish-visible.mjs, package.json</files>
  <read_first>
    - scripts/test-auth-gate.mjs (full file — copy `waitForServer` poll loop lines 139-159, fetch-with-redirect-manual pattern lines 187-205, graceful SKIP when env missing lines 180-184, exit-code handling line 287, header banner lines 241-243)
    - scripts/test-migrations.mjs (smaller analog for header banner + exit code)
    - src/actions/posts.ts publishPost signature (identify how to invoke it — Server Action via authenticated fetch, OR direct DB seed; RESEARCH.md Example 3 lines 629-672 sketches the options)
    - .planning/phases/07-performance-deploy/07-RESEARCH.md Example 3 (lines 629-672) — the canonical script sketch
  </read_first>
  <action>
    Create `scripts/test-publish-visible.mjs` per RESEARCH.md Example 3 following the scripts/test-auth-gate.mjs pattern. The script publishes a test post via the dashboard Server Action (or a test-only admin API route / direct DB seed — choose the cleanest gated option), then polls the public URL `${PROD_URL}/blog/${TEST_SLUG}` with `fetch(url, { redirect: "manual" })` until either the response HTML contains the test title (PASS) or the 30-second deadline expires (FAIL).

    Specifics:
    - `PROD_URL = process.env.PROD_URL ?? "https://anydiscussion.com"` — default to production (D-32 — no staging).
    - `TEST_SLUG = publish-visible-test-${Date.now()}` — unique per run to avoid cache hits from prior runs.
    - `TEST_TITLE = Publish-Visible Test ${new Date().toISOString()}` — embedded in the post content, used as the visibility assertion token.
    - Deadline: `start + 30_000` (30s ceiling per RESEARCH.md Validation Architecture).
    - Poll interval: 1000ms.
    - On PASS: print `PASS: publish→visible in ${elapsed}s` and exit 0.
    - On FAIL: print `FAIL: post not visible at ${PROD_URL}/blog/${TEST_SLUG} after ${elapsed}s` to stderr and set `process.exitCode = 1` (NOT process.exit(1) — matches scripts/verify.mjs:68 convention).
    - Cleanup step: delete the test post (admin action) so the test does not leave orphan content on production.
    - Graceful SKIP (status: "skipped", exit 0) when `PROD_URL` is unreachable OR when the publish step requires auth that the script cannot provide in the current environment — use the same SKIP-not-FAIL convention as scripts/test-auth-gate.mjs:180-184.
    - Header banner with `[CITED: scripts/test-auth-gate.mjs pattern + RESEARCH.md Example 3]`.

    The publish-step implementation choice (authenticated fetch to the Server Action vs test-only admin API route vs direct DB seed) is at the executor's discretion based on what is cleanest and most naturally gated behind admin permission. RESEARCH.md Example 3 note recommends option (a) — a test-only admin API route — because it mirrors how E2E tests typically work and is naturally gated. If the executor chooses a direct DB seed, ensure the seed publishes the post via the same code path as `publishPost` (so the revalidation calls actually fire).

    Modify `package.json` to add `"test:publish-visible": "node scripts/test-publish-visible.mjs"` to the scripts block. Preserve all existing scripts (`dev`, `build`, `start`, `lint`, `db:generate`, `test`, `test:migrations`, `test:auth-gate`, `setup`, `verify`, `check-bundle` from Plan 07-01, `test:auth-ratelimit` from Plan 07-02).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && grep -c "PROD_URL" scripts/test-publish-visible.mjs | grep -q 1 && grep -c "process.exitCode" scripts/test-publish-visible.mjs | grep -q 1 && grep -c "test:publish-visible" package.json | grep -q 1 && node --check scripts/test-publish-visible.mjs && echo "PLAN-07-03-SCRIPT-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-publish-visible.mjs` exists and parses with `node --check` (no syntax errors)
    - The script reads `PROD_URL` from process.env with default `https://anydiscussion.com` (verify: `grep -c "PROD_URL" scripts/test-publish-visible.mjs` >= 1)
    - The script uses `process.exitCode = 1` on failure (NOT `process.exit(1)`) — verify: `grep -c "process.exit(1)" scripts/test-publish-visible.mjs` returns 0; `grep -c "process.exitCode" scripts/test-publish-visible.mjs` >= 1
    - The script has a 30-second deadline constant (verify: `grep -E "30[_]?000|30e3" scripts/test-publish-visible.mjs | wc -l` >= 1)
    - The script implements a graceful SKIP path when PROD_URL is unreachable (verify: `grep -ci "skip" scripts/test-publish-visible.mjs` >= 1)
    - The script cleans up the test post after the assertion (verify: `grep -ci "cleanup\\|delete\\|teardown" scripts/test-publish-visible.mjs` >= 1)
    - `package.json` scripts block contains `"test:publish-visible": "node scripts/test-publish-visible.mjs"` AND preserves `check-bundle` (Plan 07-01) and `test:auth-ratelimit` (Plan 07-02)
  </acceptance_criteria>
  <done>publish→visible script exists, parses cleanly, defaults PROD_URL to https://anydiscussion.com, has 30s deadline, uses process.exitCode (not process.exit), cleans up the test post, gracefully skips when env is missing. package.json exposes `test:publish-visible`. Plan 07-04 will run this script against the deployed Coolify production URL to verify Pitfall #3 end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutating Server Action → Next.js cache | Every DB write that surfaces on a public cached route must cross this boundary via a revalidation call. A missing call means stale content (Pitfall #3). |
| Cache strategy → Invalidation mechanism | The revalidation call must match the route's caching mechanism (path ISR → revalidatePath; cacheTag → revalidateTag). Mismatch = silent no-op (Pitfall #7). |

## STRIDE Threat Register (ASVS Level 1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Tampering | Stale content after category/tag/page/author update | medium | mitigate | Every MISSING row in 07-REVALIDATION-AUDIT.md gets a revalidation fix in Task 2 that matches the route's cache strategy (Pitfall #7). Stale content is a trust/correctness issue (editors see one thing, readers see another). |
| T-07-03-02 | Repudiation | Missing audit trail for which actions revalidate what | low | mitigate | The 07-REVALIDATION-AUDIT.md deliverable itself is the audit trail. Every row is classified, every fix is documented in the table. |
| T-07-03-03 | Tampering | Permission gate accidentally bypassed during revalidation fix | high | mitigate | All revalidation calls are appended AFTER the existing `requireCan`/`requireRole` gate (per src/actions/posts.ts template). Acceptance criterion: `grep -c "requireCan\\|requireRole"` returns >= 1 per modified file. No gate is moved or removed. |
</threat_model>

<verification>
Phase-level checks for this plan:

1. **Audit completeness:** 07-REVALIDATION-AUDIT.md covers every mutating action; zero blank Status rows.
2. **Fix correctness:** categories/tags/pages/users all import from next/cache and call revalidation in every mutating function.
3. **Pitfall #7 honored:** each fix uses the SAME mechanism the route uses for caching (audit table records this; manual review during execution).
4. **No regression:** pnpm test and pnpm lint both exit 0.
5. **Script readiness:** scripts/test-publish-visible.mjs parses with node --check, exposes PROD_URL env var.
6. **End-to-end verification (deferred to Plan 07-04):** after Coolify deploy, run `PROD_URL=https://anydiscussion.com pnpm test:publish-visible` to confirm Pitfall #3 is closed on the real stack.
</verification>

<success_criteria>
- 07-REVALIDATION-AUDIT.md exists, covers every mutating action, every row has a non-blind Status classification.
- Categories, tags, pages, users actions have correct revalidation calls (matching cache strategies per Pitfall #7).
- All revalidateTag calls use the 2-arg form; all revalidatePath calls use concrete literals.
- Existing permission gates preserved.
- publish→visible script exists and parses cleanly.
- pnpm test and pnpm lint stay green.
- Plan 07-04 can run `pnpm test:publish-visible` against the deployed Coolify production URL to close Pitfall #3 end-to-end.
</success_criteria>

<output>
Create `.planning/phases/07-performance-deploy/07-03-SUMMARY.md` when done.
</output>
