---
phase: 07-performance-deploy
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - docs/operations/umami-deploy.md
  - docs/operations/coolify-deploy.md
  - docs/operations/dns-email-deliverability.md
autonomous: false
requirements:
  - PERF-06
  - ANAL-02
user_setup:
  - service: coolify
    why: "Production deployment (D-32) + Umami service deployment (D-24)"
    dashboard_config:
      - task: "Configure Coolify project: git-push to main triggers production deploy with managed SSL via Let's Encrypt for anydiscussion.com + www (D-23)"
        location: "Coolify dashboard → Projects → New Project"
      - task: "Inject runtime env vars (DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, BETTER_AUTH_TRUSTED_ORIGINS, RESEND_API_KEY, EMAIL_FROM, S3_*/AWS_* credentials, SETTINGS_ENCRYPTION_KEY, REDIS_URL) — NEVER as Dockerfile ARGs (D-21)"
        location: "Coolify dashboard → Project → Environment Variables"
      - task: "Add Redis as a Coolify managed service (redis:7-alpine, 256MB maxmemory, allkeys-lru, no persistence) bound to internal network only"
        location: "Coolify dashboard → Services → Add Service → Redis"
      - task: "Add Umami as a Coolify managed service with managed SSL for analytics.anydiscussion.com (D-24/D-27)"
        location: "Coolify dashboard → Services → Add Service → Umami (docker.umami.is/umami-software/umami:postgresql-latest)"
  - service: cloudflare-dns
    why: "DKIM/SPF/DMARC DNS records for Resend email deliverability (D-33) + existing cdn.anydiscussion.com + analytics.anydiscussion.com"
    dashboard_config:
      - task: "Publish DKIM CNAME record (Resend auto-generates) for the mail-sending domain"
        location: "Cloudflare dashboard → DNS → Records → Add CNAME"
      - task: "Publish SPF TXT record including amazonses.com (Resend auto-generates)"
        location: "Cloudflare dashboard → DNS → Records → Add TXT"
      - task: "Author + publish DMARC TXT record at _dmarc.<domain> (start with v=DMARC1; p=none; for monitoring, tighten to p=quarantine; after the inbox test passes — Pitfall 6)"
        location: "Cloudflare dashboard → DNS → Records → Add TXT at _dmarc.<domain>"
  - service: resend
    why: "Source of DKIM + SPF records (auto-generated) + SMTP relay for the real-inbox verification"
    dashboard_config:
      - task: "Retrieve DKIM + SPF records for the mail-sending domain"
        location: "Resend dashboard → Domains → Select domain → DNS Records"
      - task: "Confirm domain reaches Verified state after DNS propagation"
        location: "Resend dashboard → Domains → Status column"
  - service: docker-local
    why: "Local Docker build dry-run to validate the Dockerfile from Plan 07-01 before pushing to Coolify"
    env_vars:
      - name: NEXT_PUBLIC_SITE_URL
        source: "Production URL (https://anydiscussion.com) — bake at build time"
      - name: NEXT_PUBLIC_CDN_URL
        source: "CDN URL (https://cdn.anydiscussion.com) — bake at build time"

must_haves:
  truths:
    - "The app deploys to production on Coolify via git-push to main with Coolify-managed SSL (Let's Encrypt) on the production domain anydiscussion.com + www — reframed from SC#5/PERF-06 'staging deployment' per D-32 (no staging environment exists)"
    - "Runtime secrets (DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, RESEND_API_KEY, EMAIL_FROM, S3_*/AWS_* credentials, SETTINGS_ENCRYPTION_KEY, REDIS_URL) are injected by Coolify as runtime env vars — verified via `docker run --rm <image> env | grep -E '(DATABASE_URL|RESEND_API_KEY|SECRET)'` returning empty (only NEXT_PUBLIC_* baked at build time per D-21)"
    - "Umami runs as a Coolify-managed Docker service (docker.umami.is/umami-software/umami:postgresql-latest) at analytics.anydiscussion.com with its own database in the shared Postgres instance (D-24/D-25/D-27)"
    - "The default Umami admin/umami password is changed immediately after first boot (Pitfall 5) and recorded in the operator's password manager"
    - "DKIM, SPF, and DMARC DNS records are published on the mail-sending domain for Resend SMTP (D-33) — DMARC starts at p=none; (monitoring) and is tightened to p=quarantine; only after the inbox test passes (Pitfall 6)"
    - "A real-inbox test confirms password-reset AND email-verification emails land in the primary inbox (not spam) at a major provider (Gmail/Outlook)"
    - "The publish→visible script (Plan 07-03) run against the deployed production URL reports publish→visible latency under 30 seconds (Pitfall #3 closed end-to-end on the real Coolify + Cloudflare stack)"
    - "The Umami script URL is configured in /dashboard/settings/seo so the Phase 6 analytics injection mechanism (D-26) actually loads Umami on the public site"
  artifacts:
    - "docs/operations/umami-deploy.md — Umami Coolify service runbook (image, DATABASE_URL pointing to umami database, port, post-deploy password change, script URL retrieval, website registration)"
    - "docs/operations/coolify-deploy.md — Coolify project runbook (git-push trigger, runtime env var injection list, build-vs-runtime separation, Redis service config, health checks, restart policy)"
    - "docs/operations/dns-email-deliverability.md — DKIM/SPF/DMARC record templates + real-inbox verification procedure"
    - "Production deployment on Coolify (the deployed runtime itself)"
    - "Umami deployment on Coolify (the running analytics service)"
  key_links:
    - "Plan 07-01 Dockerfile → Coolify build stage (the two build-step gates run on every push to main)"
    - "Plan 07-02 docker-compose Redis service definition → Coolify Redis managed service (dev parity; prod binds to internal network only)"
    - "Plan 07-03 scripts/test-publish-visible.mjs → deployed production URL (PROD_URL env var; runs as the final phase-gate check)"
    - "Phase 6 D-17 analytics injection mechanism → Umami script URL stored in settings table (D-26 — no new code; just configure in /dashboard/settings/seo)"
    - "Resend DNS records → Cloudflare DNS dashboard (DKIM CNAME + SPF TXT + DMARC TXT at _dmarc.<domain>)"
---

## Phase Goal

**As a** founder, **I want to** deploy the blog to production on Coolify with analytics, email deliverability, and the publish→visible loop all verified on the real stack, **so that** the site is ready for readers at first launch.

<objective>
Ops + deploy slice (PERF-06 production deploy + ANAL-02 Umami + D-33 email deliverability): operator runbooks + Coolify production deploy + Umami service deploy + DKIM/SPF/DMARC DNS records + real-inbox verification + publish→visible end-to-end check. Reframes SC#5/PERF-06 "staging deployment" to "Coolify git-push deploy with managed SSL to production" per D-32 (no staging environment).

Purpose: This is the slice where the build pipeline (Plan 07-01), rate limiting (Plan 07-02), and revalidation fixes (Plan 07-03) all meet the real Coolify + Cloudflare + Postgres + Redis stack. Because there is no staging (D-32), each verification step is load-bearing — the publish→visible script and the inbox test are the only proofs that the production loop actually works.

Output: Three operations runbooks (autonomous Task 1), then operator-gated deploy + Umami + DNS/inbox checkpoints (Tasks 2 and 3).
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-deploy/07-CONTEXT.md
@.planning/phases/07-performance-deploy/07-RESEARCH.md
@.planning/phases/07-performance-deploy/07-PATTERNS.md

@Dockerfile
@docker-compose.yml
@.env.example
@next.config.ts
@src/lib/email/index.ts
@src/lib/redis/index.ts
@.planning/phases/02-auth-rbac/02-UAT.md
</context>

## Artifacts this phase produces

- `docs/operations/umami-deploy.md` (NEW) — Umami service deployment + configuration runbook.
- `docs/operations/coolify-deploy.md` (NEW) — Coolify production deployment runbook.
- `docs/operations/dns-email-deliverability.md` (NEW) — DKIM/SPF/DMARC DNS record templates + real-inbox verification procedure.
- Production deployment (operator task, not a repo file).
- Umami deployment (operator task).
- DNS records published (operator task at Cloudflare).
- Umami script URL configured in the settings table (operator task via /dashboard/settings/seo).

<tasks>

<task type="auto">
  <name>Task 1: Author the operator runbooks for Umami deploy, Coolify deploy, and DNS/email deliverability</name>
  <files>docs/operations/umami-deploy.md, docs/operations/coolify-deploy.md, docs/operations/dns-email-deliverability.md</files>
  <read_first>
    - Dockerfile (from Plan 07-01 — the multi-stage build the Coolify runbook references)
    - docker-compose.yml (the Redis service definition from Plan 07-02 — the Coolify Redis managed service mirrors this)
    - .env.example (the canonical runtime-env-var list — every var here is a Coolify env injection, NOT a Dockerfile ARG per D-21)
    - src/lib/email/index.ts (the Resend wrapper — confirms SMTP provider for D-33)
    - src/lib/redis/index.ts (the singleton — confirms REDIS_URL is consumed at runtime)
    - next.config.ts (confirms output: "standalone" + images.remotePatterns for cdn.anydiscussion.com)
    - .planning/phases/07-performance-deploy/07-RESEARCH.md Example 5 (lines 722-736) — Umami Docker service config
    - .planning/phases/07-performance-deploy/07-RESEARCH.md Pitfall 5 (lines 524-528) — Umami default password change requirement
    - .planning/phases/07-performance-deploy/07-RESEARCH.md Pitfall 6 (lines 530-534) — DMARC record authoring requirement
    - .planning/phases/07-performance-deploy/07-RESEARCH.md § Security Domain (lines 870-900) — Redis external exposure mitigation + email spoofing mitigation
  </read_first>
  <action>
    Create the `docs/operations/` directory and three runbooks inside it.

    **docs/operations/coolify-deploy.md:** Document the production deploy flow. Sections: (1) Project setup — Coolify pulls from git on every push to main (D-32), builds via the repo-root Dockerfile (Plan 07-01), exposes port 3000; Coolify proxy (Caddy/Traefik) terminates SSL via Let's Encrypt for anydiscussion.com + www (D-23). (2) Runtime env injection — list EVERY runtime env var from .env.example (DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, BETTER_AUTH_TRUSTED_ORIGINS, RESEND_API_KEY, EMAIL_FROM, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION, S3_BUCKET, S3_ENDPOINT, S3_PUBLIC_URL, CDN_URL, SETTINGS_ENCRYPTION_KEY, REDIS_URL) and instruct the operator to enter them in Coolify Project → Environment Variables. Explicitly call out: these MUST NOT be Dockerfile ARGs (D-21). (3) Build args — only NEXT_PUBLIC_SITE_URL and NEXT_PUBLIC_CDN_URL are bake-time; configure them as Coolify build args. (4) Redis managed service — add redis:7-alpine as a Coolify managed service with maxmemory 256mb, maxmemory-policy allkeys-lru, no persistence (mirror the docker-compose definition from Plan 07-02); bind to internal network only (NOT public). (5) Health check + restart policy — Coolify default health check against `/` (200 OK); restart policy `unless-stopped`. (6) Secret non-leakage verification — the operator runs `docker run --rm <image> env | grep -E "(DATABASE_URL|RESEND_API_KEY|SECRET)"` against the built image and confirms the output is empty (only NEXT_PUBLIC_* should appear). (7) Build-step gate — note that both gates (pnpm lint --max-warnings 0 + check-bundle-size.mjs) run in the Docker builder stage; a fail aborts the deploy with a clear log message (D-31/D-32 load-bearing).

    **docs/operations/umami-deploy.md:** Document the Umami Coolify service deploy per RESEARCH.md Example 5. Sections: (1) Service creation — image `docker.umami.is/umami-software/umami:postgresql-latest`, port 3001 (Coolify routes analytics.anydiscussion.com via its proxy), DATABASE_URL pointing to a `umami` database in the shared Postgres instance (D-25 — separate database, same server). (2) First-boot — Umami auto-migrates its schema on first boot; no manual migration step. (3) Default credentials — admin/umami; the operator MUST change the password immediately on first login (Pitfall 5) and store it in the password manager. (4) Website registration — log in to analytics.anydiscussion.com, add a website with URL `https://anydiscussion.com`, retrieve the script tag Umami generates (format: `<script async src="https://analytics.anydiscussion.com/script.js" data-website-id="..."></script>`). (5) Wire the script URL into the blog — navigate to `/dashboard/settings/seo` in the blog admin and paste the script tag into the analytics injection field (Phase 6 D-17 already wired the mechanism — this is configuration only, D-26). (6) SSL — Coolify auto-provisions SSL for analytics.anydiscussion.com (D-27). (7) Backup — note that Umami's database should be included in the Phase 8 backup scope.

    **docs/operations/dns-email-deliverability.md:** Document the DKIM/SPF/DMARC publishing procedure per D-33 + RESEARCH.md Pitfall 6. Sections: (1) Retrieve DKIM + SPF records from the Resend dashboard (Resend auto-generates these for the mail-sending domain — Domains → Select domain → DNS Records). (2) Publish the DKIM CNAME record at Cloudflare DNS. (3) Publish the SPF TXT record (includes `amazonses.com` because Resend sends via AWS SES) at the domain apex. (4) Author and publish the DMARC TXT record at `_dmarc.<domain>` — start with `v=DMARC1; p=none; rua=mailto:<operator-email>;` (monitoring mode); tighten to `p=quarantine;` only after the inbox test passes (Pitfall 6). (5) Wait for DNS propagation + Resend domain verification (Resend dashboard shows Verified state). (6) Real-inbox verification — trigger a password-reset email (via `/forget-password`) AND an email-verification email (via the dashboard user-create flow), confirm both land in the primary inbox (not spam) at Gmail and/or Outlook. (7) Tighten DMARC to `p=quarantine;` once the inbox test passes; document the final record value. (8) Document which email the operator used for the test (so the result is reproducible).

    Each runbook includes a `Prerequisites` section, a numbered `Steps` section, a `Verification` section (how to confirm success), and a `Rollback / Troubleshooting` section.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && for f in docs/operations/umami-deploy.md docs/operations/coolify-deploy.md docs/operations/dns-email-deliverability.md; do echo "== $f =="; grep -cE "^## (Prerequisites|Steps|Verification|Rollback)" "$f" || echo "0 matches in $f"; done && grep -ci "DKIM" docs/operations/dns-email-deliverability.md | grep -q 1 && grep -ci "DMARC" docs/operations/dns-email-deliverability.md | grep -q 1 && grep -ci "admin/umami" docs/operations/umami-deploy.md | grep -q 1 && grep -ci "NEXT_PUBLIC" docs/operations/coolify-deploy.md | grep -q 1 && echo "PLAN-07-04-RUNBOOKS-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operations/coolify-deploy.md` exists with sections for Prerequisites, Steps, Verification, Rollback (verify: `grep -cE "^## (Prerequisites|Steps|Verification|Rollback)" docs/operations/coolify-deploy.md` >= 4)
    - `docs/operations/coolify-deploy.md` enumerates every runtime env var from .env.example (DATABASE_URL, BETTER_AUTH_SECRET, RESEND_API_KEY, REDIS_URL, SETTINGS_ENCRYPTION_KEY — verify: `grep -cE "DATABASE_URL|REDIS_URL|BETTER_AUTH_SECRET|RESEND_API_KEY|SETTINGS_ENCRYPTION_KEY" docs/operations/coolify-deploy.md` >= 5)
    - `docs/operations/coolify-deploy.md` explicitly states runtime secrets MUST NOT be Dockerfile ARGs (D-21) (verify: `grep -ci "must not be.*dockerfile\\|never.*arg\\|runtime only" docs/operations/coolify-deploy.md` >= 1)
    - `docs/operations/coolify-deploy.md` documents the build-step gate (pnpm lint --max-warnings 0 + check-bundle-size.mjs) and notes it as the sole pre-production safety net (D-31/D-32) (verify: `grep -ci "build.*gate\\|safety net\\|aborts the deploy" docs/operations/coolify-deploy.md` >= 1)
    - `docs/operations/umami-deploy.md` exists with the four required sections (Prerequisites, Steps, Verification, Rollback)
    - `docs/operations/umami-deploy.md` documents the mandatory default-password change (Pitfall 5) (verify: `grep -ci "admin/umami\\|default.*password\\|change.*password" docs/operations/umami-deploy.md` >= 1)
    - `docs/operations/umami-deploy.md` documents wiring the Umami script URL into /dashboard/settings/seo (D-26) (verify: `grep -ci "settings/seo\\|analytics injection\\|script.js" docs/operations/umami-deploy.md` >= 1)
    - `docs/operations/umami-deploy.md` references the docker.umami.is image (D-24) and the separate umami database in shared Postgres (D-25) (verify: `grep -ci "docker.umami.is\\|separate database\\|umami database" docs/operations/umami-deploy.md` >= 2)
    - `docs/operations/dns-email-deliverability.md` exists with the four required sections
    - `docs/operations/dns-email-deliverability.md` documents all THREE DNS records: DKIM CNAME, SPF TXT, DMARC TXT (verify: `grep -ci "DKIM\\|SPF\\|DMARC" docs/operations/dns-email-deliverability.md` >= 3 with each of the three literals appearing at least once)
    - `docs/operations/dns-email-deliverability.md` documents the DMARC p=none → p=quarantine progression (Pitfall 6) (verify: `grep -cE "p=none|p=quarantine" docs/operations/dns-email-deliverability.md` >= 2)
    - `docs/operations/dns-email-deliverability.md` documents the real-inbox test for both password-reset AND email-verification emails (verify: `grep -ci "inbox\\|spam\\|password.reset\\|email.verification" docs/operations/dns-email-deliverability.md` >= 2)
  </acceptance_criteria>
  <done>Three operations runbooks exist with all required sections, all D-21/D-24/D-25/D-26/D-27/D-31/D-32/D-33 details captured, Pitfall 5 (Umami default password) and Pitfall 6 (DMARC manual authoring) called out explicitly. The operator can execute each runbook step-by-step without further documentation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator executes the Coolify production deploy + Umami deploy + secret non-leakage verification</name>
  <files>(no repo files modified — operator executes via Coolify dashboard + local Docker)</files>
  <read_first>
    - docs/operations/coolify-deploy.md (the runbook from Task 1)
    - docs/operations/umami-deploy.md (the runbook from Task 1)
    - Dockerfile (the build pipeline Coolify invokes)
    - .env.example (the runtime env var checklist)
  </read_first>
  <action>
    Execute the production deploy end-to-end: (1) local `docker build` dry-run to validate the Dockerfile from Plan 07-01 with both gates passing; (2) verify secret non-leakage via `docker run --rm <image> env | grep -E "(DATABASE_URL|RESEND_API_KEY|BETTER_AUTH_SECRET|SETTINGS_ENCRYPTION_KEY|REDIS_URL)"` returning EMPTY (D-21); (3) `git push origin main` to trigger the Coolify build, confirm both gates pass in the builder stage and SSL provisions for anydiscussion.com + www; (4) inject all runtime env vars in Coolify Project → Environment Variables (per docs/operations/coolify-deploy.md); (5) add the Redis managed service (redis:7-alpine, 256mb, allkeys-lru, no persistence, internal network only); (6) smoke-test https://anydiscussion.com (homepage + single post + dark mode + next/image); (7) run `PROD_URL=https://anydiscussion.com pnpm test:publish-visible` to close Pitfall #3 (must report under 30s); (8) deploy Umami via Coolify (docker.umami.is/umami-software/umami:postgresql-latest, DATABASE_URL pointing to umami database in shared Postgres); (9) log into Umami with admin/umami and IMMEDIATELY change the default password (Pitfall 5); (10) register the website in Umami and wire the script URL into /dashboard/settings/seo; (11) attempt 4 invalid sign-ins at /signin within 15 min to confirm the rate-limit response fires.
  </action>
  <what-built>
    The Dockerfile (Plan 07-01), Redis singleton + docker-compose Redis + Better Auth rateLimit (Plan 07-02), and revalidation fixes + publish→visible script (Plan 07-03) are all committed and pushed. The operator now deploys to production on Coolify (D-32) and deploys Umami as a Coolify managed service (D-24/D-27). Because there is no staging, the build-step gate (D-12/D-13) is the only pre-production safety net.
  </what-built>
  <how-to-verify>
    Follow docs/operations/coolify-deploy.md and docs/operations/umami-deploy.md step-by-step:

    1. **Local Docker build dry-run** (recommended before pushing to Coolify): `docker build -t anydiscussion-test --build-arg NEXT_PUBLIC_SITE_URL=https://anydiscussion.com --build-arg NEXT_PUBLIC_CDN_URL=https://cdn.anydiscussion.com .` — confirm both gates pass (lint + bundle-size) and the build succeeds end-to-end.

    2. **Secret non-leakage verification** (local): `docker run --rm anydiscussion-test env | grep -E "(DATABASE_URL|RESEND_API_KEY|BETTER_AUTH_SECRET|SETTINGS_ENCRYPTION_KEY|REDIS_URL)"` — confirm the output is EMPTY (only NEXT_PUBLIC_* vars should appear; if any runtime secret appears, the Dockerfile is wrong — abort and revisit Plan 07-01).

    3. **Push to main → Coolify build**: `git push origin main`. Watch the Coolify build log; confirm both gates run inside the builder stage and pass. Confirm the runner stage starts and the Coolify proxy provisions SSL (Let's Encrypt) for anydiscussion.com + www.

    4. **Runtime env injection**: in Coolify Project → Environment Variables, enter every runtime env var listed in docs/operations/coolify-deploy.md (DATABASE_URL pointing to the production Postgres, BETTER_AUTH_SECRET, RESEND_API_KEY, S3_*/AWS_* credentials, SETTINGS_ENCRYPTION_KEY, REDIS_URL pointing to the Coolify Redis managed service internal URL). Restart the service to pick up the new env.

    5. **Redis managed service**: in Coolify Services, add redis:7-alpine with maxmemory 256mb / allkeys-lru / no persistence; bind to internal network only (NO public port mapping in production). Confirm the Next.js runtime can reach Redis (check logs for the ioredis connection succeeding).

    6. **Smoke test the production URL**: visit https://anydiscussion.com — confirm the homepage loads, a blog post loads, dark mode toggles, an image renders via next/image (cdn.anydiscussion.com).

    7. **Publish→visible end-to-end check** (Pitfall #3 closure): with the production URL live, run `PROD_URL=https://anydiscussion.com pnpm test:publish-visible` from a dev machine. Confirm PASS within 30 seconds. If FAIL, the revalidation fixes from Plan 07-03 need debugging — the audit deliverable is the diagnostic reference.

    8. **Deploy Umami**: in Coolify Services, add the Umami image (docker.umami.is/umami-software/umami:postgresql-latest) with DATABASE_URL pointing to a new `umami` database in the shared Postgres (create the database first: `CREATE DATABASE umami;`). Confirm analytics.anydiscussion.com loads the Umami login screen.

    9. **Umami first-boot + password change** (Pitfall 5): log in with admin/umami, IMMEDIATELY change the password, store the new password in the operator's password manager.

    10. **Umami website registration + script wiring** (D-26): in Umami, add a website (URL: https://anydiscussion.com), copy the generated `<script ...>` tag. In the blog admin at /dashboard/settings/seo, paste the script into the analytics injection field (Phase 6 D-17 mechanism). Save. Visit https://anydiscussion.com and confirm the network tab shows the Umami script.js loading.

    11. **Rate-limit smoke test**: attempt 4 invalid sign-ins at /signin within 15 minutes; confirm the 4th returns the rate-limit error (HTTP 429 or the Better Auth default rate-limit response).
  </how-to-verify>
  <acceptance_criteria>
    - Local Docker build succeeds with both gates passing
    - `docker run --rm <image> env | grep -E "(DATABASE_URL|RESEND_API_KEY|SECRET)"` returns EMPTY output (D-21 verified)
    - Coolify production build succeeds on push to main; SSL provisions for anydiscussion.com + www
    - https://anydiscussion.com homepage loads without error
    - `PROD_URL=https://anydiscussion.com pnpm test:publish-visible` exits 0 within 30 seconds (Pitfall #3 closed on real stack)
    - Redis managed service is reachable from the Next.js runtime (no ioredis connection errors in logs)
    - analytics.anydiscussion.com loads the Umami login screen
    - Umami default password is changed (operator confirms)
    - Umami script.js loads on https://anydiscussion.com (network tab confirmation)
    - 4th invalid sign-in within 15 min triggers the rate-limit response
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && docker build -t anydiscussion-test --build-arg NEXT_PUBLIC_SITE_URL=https://anydiscussion.com --build-arg NEXT_PUBLIC_CDN_URL=https://cdn.anydiscussion.com . && echo "local build OK" && docker run --rm anydiscussion-test env | grep -E "(DATABASE_URL|RESEND_API_KEY|BETTER_AUTH_SECRET|SETTINGS_ENCRYPTION_KEY|REDIS_URL)" | wc -l | grep -qE "^0$" && echo "secret non-leakage OK (D-21)" && PROD_URL=https://anydiscussion.com node scripts/test-publish-visible.mjs && echo "PLAN-07-04-DEPLOY-OK"</automated>
  </verify>
  <done>Local Docker build succeeds with both gates passing; docker image env grep for runtime secrets returns empty (D-21 verified); Coolify production build succeeds on push to main with SSL provisioned; https://anydiscussion.com loads; publish→visible script reports under 30s (Pitfall #3 closed on real stack); Redis managed service reachable; Umami deployed with default password changed and script wired into the public site; 4th invalid sign-in triggers the rate-limit response. The checkpoint was approved by the operator.</done>
  <resume-signal>Type "approved" once all 11 verification steps pass, or describe which step failed and what was rolled back</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator publishes DKIM/SPF/DMARC DNS records + runs the real-inbox email deliverability test</name>
  <files>(no repo files modified — operator publishes DNS records at Cloudflare + triggers real emails)</files>
  <read_first>
    - docs/operations/dns-email-deliverability.md (the runbook from Task 1)
    - .planning/phases/07-performance-deploy/07-RESEARCH.md Pitfall 6 (lines 530-534) — DMARC manual authoring
    - .planning/phases/02-auth-rbac/02-UAT.md (UAT-02-01 — the prior-phase verification debt this task closes per D-33)
  </read_first>
  <action>
    Publish the three DNS records and run the real-inbox verification: (1) retrieve DKIM CNAME + SPF TXT from the Resend dashboard (Resend auto-generates these for the mail-sending domain); (2) publish both at Cloudflare DNS; (3) author the DMARC TXT record at _dmarc.&lt;domain&gt; starting with `v=DMARC1; p=none; rua=mailto:<operator-email>;` (monitoring mode — Pitfall 6: Resend does NOT auto-generate DMARC); (4) wait for DNS propagation + Resend domain Verified state; (5) visit https://anydiscussion.com/forget-password and trigger a password-reset email to a real Gmail/Outlook inbox; (6) trigger an email-verification email via the dashboard user-create flow; (7) confirm BOTH land in the primary inbox (not spam); (8) tighten DMARC from p=none to p=quarantine after both inbox tests pass; (9) update STATE.md AUTH-06/07 verification-debt note to mark D-33 closed.
  </action>
  <what-built>
    The production deployment is live (Task 2). Auth emails (password-reset, email-verification) are sent via Resend SMTP, but their deliverability depends on DKIM/SPF/DMARC DNS records being published on the mail-sending domain (D-33). This task closes the AUTH-06/07 verification debt that has been parked since Phase 2 (STATE.md blocker + 02-UAT.md UAT-02-01).
  </what-built>
  <how-to-verify>
    Follow docs/operations/dns-email-deliverability.md step-by-step:

    1. **Retrieve DKIM + SPF records** from the Resend dashboard (Resend auto-generates these for the mail-sending domain).

    2. **Publish DKIM CNAME** at Cloudflare DNS.

    3. **Publish SPF TXT** at the domain apex (the record Resend provides, including `amazonses.com`).

    4. **Author + publish DMARC TXT** at `_dmarc.<domain>` — start with `v=DMARC1; p=none; rua=mailto:<operator-email>;` (monitoring mode). Note: Resend does NOT auto-generate DMARC; the operator authors this manually (Pitfall 6).

    5. **Wait for DNS propagation** (typically 5-30 minutes; use `dig _dmarc.<domain> TXT` or a public DNS checker to confirm). The Resend dashboard should show the domain as Verified.

    6. **Real-inbox test — password reset**: visit https://anydiscussion.com/forget-password, enter a real email address you control (Gmail or Outlook primary inbox). Wait up to 2 minutes. Confirm the email arrives in the PRIMARY inbox (not spam, not promotions tab). If it lands in spam, do not proceed — diagnose (typically DMARC/SPF misalignment).

    7. **Real-inbox test — email verification**: via the dashboard user-create flow, trigger an email-verification email to the same inbox. Confirm it arrives in the primary inbox.

    8. **Tighten DMARC**: only after BOTH inbox tests pass, update the DMARC TXT record from `p=none;` to `p=quarantine;` (Pitfall 6). Document the final record value.

    9. **Document the test outcome**: which email provider was used, which emails were tested, screenshots or text logs of the inbox placement. Update the AUTH-06/07 verification-debt note in STATE.md to mark D-33 as closed.
  </how-to-verify>
  <acceptance_criteria>
    - DKIM CNAME record is published at Cloudflare (dig lookup returns the Resend-provided value)
    - SPF TXT record is published at the domain apex (dig lookup returns the record including the amazonses.com mechanism)
    - DMARC TXT record is published at _dmarc.<domain> (dig lookup returns the record; starts at p=none for monitoring)
    - Resend dashboard shows the mail-sending domain as Verified
    - Password-reset email lands in the primary inbox (not spam) at the test email provider — screenshot or text confirmation captured
    - Email-verification email lands in the primary inbox (not spam) — screenshot or text confirmation captured
    - DMARC record tightened to p=quarantine; after both inbox tests pass (Pitfall 6 closed)
    - STATE.md AUTH-06/07 verification-debt note updated to mark D-33 closed (operator confirms)
  </acceptance_criteria>
  <verify>
    <automated>echo "DNS record publication is operator-only via Cloudflare dashboard; run 'dig _dmarc.<domain> TXT +short' to confirm the DMARC record is live (must contain 'v=DMARC1'). Real-inbox verification is operator-only — confirm password-reset AND email-verification emails land in primary inbox at Gmail/Outlook."</automated>
  </verify>
  <done>DKIM CNAME, SPF TXT, and DMARC TXT records published at Cloudflare; Resend dashboard shows domain Verified; password-reset email lands in primary inbox (not spam); email-verification email lands in primary inbox; DMARC tightened to p=quarantine after inbox tests pass (Pitfall 6 closed); STATE.md AUTH-06/07 note updated to reflect D-33 closure. The checkpoint was approved by the operator.</done>
  <resume-signal>Type "approved" once both inbox tests pass and DMARC is tightened to p=quarantine, or describe which step failed and whether rollback is needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git main → Coolify build | A push to main crosses this boundary and triggers an automated production build. The Dockerfile build-step gates (Plan 07-01) are the only safety net. |
| Coolify env vars → Runtime container | Runtime secrets are injected here, after the image is built. They must never appear in image layers (D-21). |
| Public internet → analytics.anydiscussion.com | The Umami service is exposed here. Default credentials are the primary threat (Pitfall 5). |
| Mail-sending domain → Recipient inbox | Emails cross this boundary; without DKIM/SPF/DMARC they may be spoofed or spam-filtered (D-33). |

## STRIDE Threat Register (ASVS Level 1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-SC | Tampering | Docker image pulls (Umami + Redis official images) | high | mitigate | Use official images only: `docker.umami.is/umami-software/umami:postgresql-latest` (Umami official), `redis:7-alpine` (Redis Inc. official). The coolify-deploy runbook pins these image sources; operator verifies image digest before deploy. |
| T-07-04-01 | Spoofing | Email spoofing of password-reset links | high | mitigate | DKIM + SPF + DMARC DNS records published on the mail-sending domain (D-33). DKIM signs outgoing mail; SPF authorizes Resend/AWS SES to send; DMARC instructs receivers on alignment policy. The real-inbox test confirms deliverability + non-spam placement. |
| T-07-04-02 | Elevation of Privilege | Default Umami credentials (admin/umami) | high | mitigate | The umami-deploy runbook (Task 1) mandates an IMMEDIATE password change on first boot (Pitfall 5). Task 2 acceptance criterion: operator confirms the password is changed and stored in the password manager. ASVS V2 default-deny. |
| T-07-04-03 | Information Disclosure | Runtime secrets baked into Docker image | high | mitigate | Plan 07-01 Dockerfile has zero ARG/ENV lines for runtime secrets (D-21). Task 2 verification step 2: `docker run --rm <image> env \| grep -E "(DATABASE_URL\|RESEND_API_KEY\|SECRET)"` returns empty. The coolify-deploy runbook explicitly documents runtime-vs-build env separation. |
| T-07-04-04 | Elevation of Privilege | Redis exposed externally | high | mitigate | The coolify-deploy runbook specifies Redis binds to the Coolify internal network only in production (no public port). The docker-compose dev port-mapping (6379:6379) is dev-only. The Redis service is documented as internal-only. |
| T-07-04-05 | Denial of Service | Coolify single-instance outage takes down the whole site | medium | accept | The single-instance deploy model is a deliberate small-team trade-off (D-32). The ISR scaling ADR (Plan 07-05) documents the multi-replica v2 path. Restart policy `unless-stopped` recovers from container crashes. |
</threat_model>

<verification>
Phase-level checks for this plan:

1. **Runbook completeness:** three runbooks exist with all required sections (Task 1 acceptance criterion).
2. **Build pipeline production-ready:** local Docker build succeeds with both gates passing (Task 2 step 1).
3. **Secret non-leakage (D-21):** `docker run --rm <image> env | grep SECRET` returns empty (Task 2 step 2).
4. **Production deploy live:** https://anydiscussion.com loads (Task 2 step 6).
5. **Pitfall #3 closed on real stack:** publish→visible script reports under 30s latency (Task 2 step 7).
6. **Umami deployed + script wired:** analytics.anydiscussion.com loads, Umami script.js loads on the public site (Task 2 steps 8-10).
7. **Rate-limit enforcement on real stack:** 4th invalid sign-in triggers the rate-limit response (Task 2 step 11).
8. **Email deliverability (D-33):** password-reset + email-verification emails land in primary inbox (Task 3 steps 6-7).
9. **DMARC tightened:** p=quarantine after inbox tests pass (Task 3 step 8).
10. **Verification debt closed:** STATE.md AUTH-06/07 note updated to reflect D-33 closure (Task 3 step 9).
</verification>

<success_criteria>
- Three operator runbooks exist in docs/operations/ and are executable step-by-step.
- Production is live on Coolify at https://anydiscussion.com with managed SSL.
- Build-vs-runtime secret separation verified (D-21).
- Umami is deployed, default password changed, script wired into the public site (ANAL-02 closed).
- Redis managed service is internal-only (no public exposure).
- DKIM/SPF/DMARC DNS records published; real-inbox test passes for both auth email types (D-33 closed; AUTH-06/07 verification debt cleared).
- DMARC tightened to p=quarantine.
- publish→visible end-to-end on the real stack reports under 30s (Pitfall #3 closed).
- Plan 07-05 can run the Lighthouse audit against the production URL.
</success_criteria>

<output>
Create `.planning/phases/07-performance-deploy/07-04-SUMMARY.md` when done.
</output>
