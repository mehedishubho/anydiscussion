---
phase: 06-public-frontend
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/(site)/layout.tsx
  - src/components/site/SiteHeader.tsx
  - src/components/site/SiteFooter.tsx
  - src/components/site/ThemeToggle.tsx
  - src/components/site/Analytics.tsx
autonomous: true
requirements:
  - SITE-16
  - ANAL-01
  - ANAL-02
user_setup: []

must_haves:
  truths:
    - "The public site dark mode uses a SEPARATE localStorage key ('site-theme') from the dashboard ('theme') — the two route groups are never coupled per D-13"
    - "A no-flash inline script in the head sets the dark class on documentElement BEFORE first paint (reads 'site-theme' key, falls back to prefers-color-scheme)"
    - "ThemeToggle is a standalone client component with its own hook — it does NOT import ThemeContext from src/context (D-13 / Pitfall 5)"
    - "The analytics script injection reads a URL + site ID from settings and validates the https scheme before emitting a script tag — never injects arbitrary inline HTML from settings"
    - "SiteHeader nav is hard-coded for v1 (Home, Blog, Categories dropdown, About, Contact) — the menu builder is v2 SETT-01"
    - "The Categories dropdown in SiteHeader uses a cached server-component fetch (listCategoriesWithCounts from lib/queries/taxonomy)"
  artifacts:
    - "src/components/site/SiteHeader.tsx (server component)"
    - "src/components/site/SiteFooter.tsx (server component)"
    - "src/components/site/ThemeToggle.tsx (client component, route-isolated)"
    - "src/components/site/Analytics.tsx (server component, reads settings)"
    - "src/app/(site)/layout.tsx extended with header/footer/no-flash script/analytics"
  key_links:
    - "ThemeToggle reads/writes localStorage key 'site-theme' (NOT 'theme' which is the dashboard key)"
    - "The no-flash Script id='site-no-flash' reads 'site-theme' and applies 'dark' class before first paint"
    - "Analytics component calls getSetting('analytics.script') and getSetting('analytics.umami_id') from the settings table"
---

<objective>
Public site chrome — SiteHeader (logo + nav + categories dropdown + search icon + dark-mode toggle) + SiteFooter (blurb + legal links + quick links + socials) + route-isolated dark mode (system preference + header toggle + no-flash head script) + analytics script injection (ANAL-01/02, injection-only per D-17).

Purpose: Every public route is wrapped by this chrome via the (site)/layout.tsx extension. The header/footer establish the brand shell and navigation. Dark mode (SITE-16) must be route-isolated from the dashboard per D-13 (do NOT share ThemeContext — Pitfall 5). Analytics injection (ANAL-01/02) reads from settings and emits a validated script tag (the Umami instance itself deploys in Phase 7 per D-17).

Output: 4 new components + layout extension.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-public-frontend/06-CONTEXT.md
@.planning/phases/06-public-frontend/06-RESEARCH.md
@.planning/phases/06-public-frontend/06-PATTERNS.md
@.planning/phases/06-public-frontend/06-01-SUMMARY.md

@src/app/(site)/layout.tsx
@src/context/ThemeContext.tsx
@src/lib/seo/settings.ts
@src/lib/db/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SiteHeader + SiteFooter + ThemeToggle + no-flash script + layout extension</name>
  <files>src/app/(site)/layout.tsx, src/components/site/SiteHeader.tsx, src/components/site/SiteFooter.tsx, src/components/site/ThemeToggle.tsx</files>
  <read_first>
    - src/app/(site)/layout.tsx (current skeletal form — lines 44-85: the div/main/JSON-LD structure to extend with header/footer; the generateMetadata 'use cache' pattern stays unchanged)
    - src/context/ThemeContext.tsx (lines 1-58: the class-strategy pattern to MIRROR but NOT import — D-13/Pitfall 5; note it uses localStorage key "theme")
    - src/layout/AppHeader.tsx (the dashboard header — DO NOT import; reference for structural ideas only)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (Pattern 4 lines 599-624: the no-flash head script shape)
    - .planning/phases/06-public-frontend/06-PATTERNS.md (ThemeToggle section lines 606-625: MODEL but DO NOT IMPORT; layout section lines 628-646)
    - eslint.config.mjs (lines 17-54: the no-restricted-imports rule — confirm src/context/ is NOT caught, making the D-13 isolation a manual discipline)
  </read_first>
  <action>
src/components/site/ThemeToggle.tsx — "use client" component. A minimal dark-mode toggle with its OWN hook (NOT imported from src/context/ThemeContext — D-13/Pitfall 5). Uses localStorage key "site-theme" (NOT "theme" which is the dashboard key). On mount, reads localStorage: if "dark" → add "dark" class to documentElement; if "light" → remove; if null → defer to system prefers-color-scheme. On click, toggles the class and writes the new value to localStorage "site-theme". A sun/moon icon button. Keep it under 40 lines of logic.

src/components/site/SiteHeader.tsx — server component (no "use client"). Reads getSeoSettings() for the site title/logo. Renders: logo or site title (links to /), hard-coded nav per D-10 (Home, Blog, About, Contact — as plain Link components), a Categories dropdown (calls listCategoriesWithCounts from @/lib/queries/taxonomy — cached via 'use cache' inside the query function; the dropdown is a small nav with each category linking to /category/{slug}), a search icon linking to /search, and the ThemeToggle client component. Nav is hard-coded (menu builder is v2 SETT-01 per D-10). Use next/image for the logo if a logo URL exists in settings.

src/components/site/SiteFooter.tsx — server component. Reads getSeoSettings() for the site blurb. Renders: short site description, legal links (Terms and Conditions → /terms-and-conditions, Privacy Policy → /privacy-policy), quick links (Home, Blog, About, Contact), optional social links from settings (footer.social_twitter, footer.social_facebook, footer.social_linkedin — only render if non-empty). Use next/image or plain anchor tags for socials.

src/app/(site)/layout.tsx — EXTEND the existing layout. Keep the existing generateMetadata (lines 38-42) and JSON-LD scripts (lines 58-81) unchanged. Wrap the existing main with SiteHeader above and SiteFooter below. Add a no-flash inline Script in the head via next/script: id="site-no-flash", strategy="beforeInteractive", body reads localStorage "site-theme" and system prefers-color-scheme, applies the "dark" class to documentElement before first paint. The script content: an IIFE that tries to read "site-theme" from localStorage; if "dark" OR (null AND matchMedia dark) then add "dark" class. This MUST run before React hydration (beforeInteractive strategy). Keep the suppressHydrationWarning on html.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c "site-theme" src/components/site/ThemeToggle.tsx | grep -v '^0'</automated>
  </verify>
  <acceptance_criteria>
    - src/components/site/ThemeToggle.tsx contains "use client" and "site-theme" (the isolated localStorage key)
    - grep -r "from.*context/ThemeContext" src/app/\(site\)/ src/components/site/ returns nothing (D-13 — no dashboard theme coupling)
    - src/app/(site)/layout.tsx contains SiteHeader and SiteFooter
    - src/app/(site)/layout.tsx contains id="site-no-flash" and strategy="beforeInteractive"
    - src/components/site/SiteHeader.tsx contains listCategoriesWithCounts (the cached categories dropdown)
    - src/components/site/SiteHeader.tsx contains href="/search" (the search icon link)
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>SiteHeader + SiteFooter + route-isolated ThemeToggle + no-flash script wired into (site)/layout.tsx; no ThemeContext import from any (site) or components/site file.</done>
</task>

<task type="auto">
  <name>Task 2: Analytics script injection (ANAL-01/02, D-17)</name>
  <files>src/components/site/Analytics.tsx</files>
  <read_first>
    - src/lib/seo/settings.ts (the getSeoSettings / cached settings pattern)
    - src/actions/settings.ts (getSetting function for reading individual settings keys)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (Security Domain lines 938-940: "Analytics injection specific guidance" — URL + ID, NOT freeform HTML; validate https scheme)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (D-17: injection-only; Umami instance deploys Phase 7)
  </read_first>
  <action>
src/components/site/Analytics.tsx — async server component. Reads two settings: analytics.script (the script URL, e.g. "https://umami.example.com/script.js") and analytics.umami_id (the website ID). If analytics.script is empty or null, render nothing (default — Umami deploys Phase 7 per D-17). If non-empty, validate the URL: parse with new URL(), check protocol === "https:". If the scheme is NOT https, do NOT inject (XSS vector — an admin with a compromised account could inject malicious script per the RESEARCH Security Domain guidance). If valid https, render `<script async src={url} data-website-id={id} />`. NEVER inject arbitrary inline HTML from settings — only the two attributes (src + data-website-id). GA4/Plausible are swappable by changing the settings values (decided at deploy per ANAL-02). Wire this component into (site)/layout.tsx (render it after the footer or in the head — beforeInteractive is NOT needed for analytics, use default strategy).

Update src/app/(site)/layout.tsx to render <Analytics /> alongside the header/footer.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/site/Analytics.tsx reads analytics.script and analytics.umami_id from settings
    - src/components/site/Analytics.tsx validates URL protocol === "https:" before rendering
    - src/components/site/Analytics.tsx does NOT use dangerouslySetInnerHTML (only a script tag with src + data-website-id attributes)
    - src/components/site/Analytics.tsx renders nothing when the script URL is empty
    - src/app/(site)/layout.tsx contains Analytics
  </acceptance_criteria>
  <done>Analytics component injects a validated https-only script tag from settings; empty by default (Umami deploys Phase 7); GA4/Plausible swappable via settings change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings DB → Analytics component | Admin-set analytics URL/ID crosses into a public script tag |
| Client browser → ThemeToggle | User toggles dark mode (writes to localStorage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | Analytics script injection | high | mitigate | Validate URL protocol === "https:" before injecting; emit ONLY src + data-website-id attributes; NEVER inject arbitrary inline HTML from settings (prevents stored XSS via compromised admin account) |
| T-06-06 | Information Disclosure | ThemeToggle → dashboard coupling | medium | mitigate | ThemeToggle uses localStorage key "site-theme" (separate from dashboard "theme"); does NOT import ThemeContext (D-13/Pitfall 5); prevents dashboard client JS from leaking into the public bundle |
</threat_model>

<verification>
- pnpm tsc --noEmit passes
- grep -r "from.*context/ThemeContext" src/app/\(site\)/ src/components/site/ returns nothing
- src/components/site/ThemeToggle.tsx uses "site-theme" localStorage key
- src/components/site/Analytics.tsx validates https scheme
- pnpm build succeeds (layout renders header/footer/analytics without errors)
</verification>

<success_criteria>
- (site)/layout.tsx extended with SiteHeader + SiteFooter + no-flash dark-mode script + Analytics component
- Dark mode is route-isolated (separate localStorage key, no ThemeContext import) per D-13
- Analytics injection is https-validated, URL+ID only (no arbitrary HTML) per RESEARCH Security guidance
- Header nav includes the Categories dropdown (cached listCategoriesWithCounts) + search icon per D-10
- Footer includes legal links + quick links + optional social links from settings
</success_criteria>

<output>
Create `.planning/phases/06-public-frontend/06-02-SUMMARY.md` when done
</output>
