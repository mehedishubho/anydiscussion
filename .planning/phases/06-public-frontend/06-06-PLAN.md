---
phase: 06-public-frontend
plan: 06
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/(site)/about/page.tsx
  - src/app/(site)/terms-and-conditions/page.tsx
  - src/app/(site)/privacy-policy/page.tsx
  - src/app/not-found.tsx
  - src/app/(site)/preview/[token]/page.tsx
autonomous: true
requirements:
  - SITE-09
  - SITE-11
  - SITE-12
  - SITE-15
user_setup: []

must_haves:
  truths:
    - "About is a hard-coded TSX page (no DB queries, no CMS content) per SITE-09"
    - "T&C and Privacy render dashboard-managed pages rows via getPublishedPage + renderPostBody (the body content was seeded in Phase 4 D-17)"
    - "Every dangerouslySetInnerHTML on a pages-row body flows through renderPostBody (Pitfall 8 — the security gate)"
    - "The 404 page has TWO separate Suspense boundaries: the existing RedirectChecker + a new SuggestedPosts slot (Pitfall 6 — no DB read outside Suspense)"
    - "The preview route is verified/polished, NOT rebuilt (Phase 3 D-19, SITE-15 — the route already exists and works)"
    - "A 404 on an unpublished or missing pages row calls notFound() (defense-in-depth)"
  artifacts:
    - "src/app/(site)/about/page.tsx (hard-coded TSX)"
    - "src/app/(site)/terms-and-conditions/page.tsx (renders published pages row)",
    - "src/app/(site)/privacy-policy/page.tsx (renders published pages row)",
    - "src/app/not-found.tsx EXTENDED with a second Suspense for suggested posts + styled 404"
  key_links:
    - "T&C/Privacy pages call getPublishedPage from @/lib/queries/pages (Plan 06-01)"
    - "T&C/Privacy bodies rendered via renderPostBody from @/lib/post-render (Pitfall 8 gate)"
    - "404 SuggestedPosts calls listPublished from @/lib/queries/posts (Plan 06-01) inside Suspense"
---

<objective>
Marketing and legal pages: About (hard-coded TSX, SITE-09), Terms & Conditions + Privacy Policy (render dashboard-managed pages rows via renderPostBody, SITE-11), 404 page styling with suggested posts (SITE-12), and preview route verification/polish (SITE-15 — already built in Phase 3, verify only).

Purpose: Completes the non-post public surfaces. T&C/Privacy consume the pages rows seeded in Phase 4 D-17. The 404 styling adds a SuggestedPosts Suspense slot alongside the existing RedirectChecker (Pitfall 6 — two boundaries, not one combined). Preview is verify-only per Phase 3 D-19.

Output: 3 new route pages + 1 extended 404 + 1 polished preview.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-public-frontend/06-CONTEXT.md
@.planning/phases/06-public-frontend/06-RESEARCH.md
@.planning/phases/06-public-frontend/06-PATTERNS.md
@.planning/phases/06-public-frontend/06-01-SUMMARY.md

@src/app/not-found.tsx
@src/app/(site)/preview/[token]/page.tsx
@src/lib/post-render.ts
@src/lib/seo/metadata.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: About page (SITE-09, hard-coded TSX)</name>
  <files>src/app/(site)/about/page.tsx</files>
  <read_first>
    - src/app/(site)/page.tsx (the placeholder being replaced by Plan 06-04 — reference for the generateMetadata pattern; About uses a similar static metadata approach)
    - src/lib/seo/metadata.ts (buildPageMetadata — for the About page metadata)
    - src/lib/seo/settings.ts (getSeoSettings — cached settings read for the metadata base URL)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (SITE-09: hard-coded TSX/MDX, founder-authored at build time)
  </read_first>
  <action>
src/app/(site)/about/page.tsx — a plain server component with hard-coded TSX content (SITE-09). NO database queries (this is a marketing page, not CMS-managed). generateMetadata via 'use cache' + getSeoSettings + buildPageMetadata with a static title "About Us" and description. The page body: a founder-authored about section using prose classes (className="prose prose-lg max-w-none dark:prose-invert"). Include: site mission statement, team/editorial description, and contact CTA linking to /contact. Use next/image for any brand images (NEVER raw img). The exact copy is a CONTEXT.md discretion item ("About page content — marketing, founder-authored at build time") — use placeholder copy that the founder replaces before launch.

NOTE: About does NOT use renderPostBody (no stored body to render — it's literal TSX). This is the one page where dangerouslySetInnerHTML is NOT used because the content is hand-written JSX, not stored HTML.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c "getPublishedPage\|listPublished\|getPostForPublic" src/app/\(site\)/about/page.tsx | grep -q '^0' && echo "no DB queries OK" || echo "FAIL: DB query found on static About page"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(site)/about/page.tsx exists and renders hard-coded TSX content
    - src/app/(site)/about/page.tsx does NOT call getPublishedPage, listPublished, or getPostForPublic (no DB queries)
    - src/app/(site)/about/page.tsx does NOT use dangerouslySetInnerHTML (content is literal JSX)
    - src/app/(site)/about/page.tsx has generateMetadata with 'use cache'
    - src/app/(site)/about/page.tsx contains a link to /contact
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>About page is a hard-coded TSX marketing page with no DB queries; metadata via cached generateMetadata; links to /contact.</done>
</task>

<task type="auto">
  <name>Task 2: T&C + Privacy pages rendering dashboard-managed content (SITE-11, Pitfall 8)</name>
  <files>src/app/(site)/terms-and-conditions/page.tsx, src/app/(site)/privacy-policy/page.tsx</files>
  <read_first>
    - src/app/(site)/preview/[token]/page.tsx (lines 96-134: the renderPostBody + dangerouslySetInnerHTML + prose pattern — the structural analog for rendering a stored body)
    - src/lib/post-render.ts (lines 39-45: renderPostBody = generateHTML then sanitizeBeforeRender — the security gate before dangerouslySetInnerHTML, Pitfall 8)
    - src/lib/queries/pages.ts (from Plan 06-01 — getPublishedPage(slug) returns published-only pages row or null)
    - src/lib/seo/metadata.ts (buildPageMetadata — uses pages row metaTitle/metaDescription)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (SITE-11: T&C + Privacy from dashboard-managed pages; Phase 4 D-17 seeded these rows)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (Pitfall 8 lines 709-712: raw dangerouslySetInnerHTML without renderPostBody is the XSS gate — reinforced)
  </read_first>
  <action>
src/app/(site)/terms-and-conditions/page.tsx — server component rendering the "terms-and-conditions" pages row.
1. generateMetadata: 'use cache' + getSeoSettings + buildPageMetadata with static title "Terms and Conditions". (The pages row's metaTitle/metaDescription can be used if present, with a static fallback.)
2. Page: call getPublishedPage("terms-and-conditions") from @/lib/queries/pages. If null (not found or draft), call notFound() (defense-in-depth — do NOT render a draft legal page). If found, render: an h1 with the page title, then the body via renderPostBody(page.body) before dangerouslySetInnerHTML (Pitfall 8 — this is the security boundary; NEVER raw dangerouslySetInnerHTML). Prose classes: className="prose prose-lg max-w-none dark:prose-invert". Include a "Last updated" date from page.updatedAt.

src/app/(site)/privacy-policy/page.tsx — same pattern as T&C. getPublishedPage("privacy-policy"). notFound() if null. renderPostBody before dangerouslySetInnerHTML. generateMetadata with title "Privacy Policy".

Both pages were seeded as pages rows in Phase 4 D-17 (slug "terms-and-conditions" and "privacy-policy"). If the operator has not yet published them, the notFound() path renders the styled 404 (from Plan 06-06 Task 3).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c "renderPostBody" src/app/\(site\)/terms-and-conditions/page.tsx src/app/\(site\)/privacy-policy/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(site)/terms-and-conditions/page.tsx calls getPublishedPage("terms-and-conditions")
    - src/app/(site)/terms-and-conditions/page.tsx calls notFound() when the page is missing or unpublished
    - src/app/(site)/terms-and-conditions/page.tsx imports renderPostBody from @/lib/post-render
    - src/app/(site)/terms-and-conditions/page.tsx does NOT use dangerouslySetInnerHTML without renderPostBody processing the body first
    - src/app/(site)/privacy-policy/page.tsx calls getPublishedPage("privacy-policy")
    - src/app/(site)/privacy-policy/page.tsx calls notFound() when the page is missing or unpublished
    - src/app/(site)/privacy-policy/page.tsx imports renderPostBody from @/lib/post-render
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>T&C and Privacy pages render dashboard-managed published pages rows via getPublishedPage + renderPostBody; notFound() on missing/unpublished; Pitfall 8 gate enforced (renderPostBody before every dangerouslySetInnerHTML).</done>
</task>

<task type="auto">
  <name>Task 3: 404 styling with SuggestedPosts (SITE-12, Pitfall 6) + preview verify (SITE-15)</name>
  <files>src/app/not-found.tsx, src/app/(site)/preview/[token]/page.tsx</files>
  <read_first>
    - src/app/not-found.tsx (FULL FILE — lines 1-133: the existing RedirectChecker in Suspense at line 92; the 404 UI at lines 96-130; the EXTENSION point for a second Suspense)
    - src/app/(site)/preview/[token]/page.tsx (FULL FILE — verify it works as-is; check for dark: classes, meta row, share buttons; polish only)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (Pitfall 6 lines 697-701: the 404 already streams a redirect-check — suggested posts must be in a SEPARATE Suspense; never inline DB read outside Suspense)
    - .planning/phases/06-public-frontend/06-PATTERNS.md (not-found.tsx section lines 578-603 — the second Suspense pattern)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (SITE-12: 404 styling with suggested posts + search; SITE-15: preview verify/polish, do NOT rebuild)
  </read_first>
  <action>
src/app/not-found.tsx — EXTEND, do not rewrite. Keep the existing RedirectChecker Suspense (lines 92-94) exactly as-is. Add:
1. A SuggestedPosts async server component (local function, same file): calls listPublished({ page: 1, pageSize: 3 }) from @/lib/queries/posts. Returns a simple list of post links (title linking to /blog/${slug}). Does NOT use PostCard (keeps the 404 lightweight — just a heading "Popular Posts" + 3 links).
2. A SECOND Suspense boundary wrapping SuggestedPosts with a null or minimal-skeleton fallback: `<Suspense fallback={null}><SuggestedPosts /></Suspense>`. Place this BELOW the existing 404 UI (the "Back to Home" button area). CRITICAL — Pitfall 6: this is a SEPARATE Suspense from the RedirectChecker Suspense. Do NOT inline the listPublished call in the static 404 shell (build error: uncached data outside Suspense).
3. A search prompt/link: a Link to /search ("Search the site") alongside the "Back to Home" button.
4. Polish the 404 copy and styling for a friendly reader experience (D-16: "friendly 404 suggesting popular posts + search box").

src/app/(site)/preview/[token]/page.tsx — POLISH ONLY. Do NOT rebuild (Phase 3 D-19, SITE-15). Verify:
- The route works (the token lookup, notFound, renderPostBody pattern is already correct from Phase 3).
- Add dark: classes if any visual elements are missing dark-mode support (the "Draft preview" banner, the prose div already has dark:prose-invert).
- Do NOT add view-count, related-posts, TOC, share, or reading-time to the preview — those are published-post features, not draft-preview features.
- The route stays a single-Suspense (token lookup) — do NOT split into two Suspense boundaries (the preview is per-request, not cached; the /blog/[slug] two-Suspense pattern does not apply here).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c "Suspense" src/app/not-found.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/not-found.tsx contains at least 2 Suspense boundaries (grep -c returns >= 2 — one for RedirectChecker, one for SuggestedPosts)
    - src/app/not-found.tsx contains listPublished (inside the SuggestedPosts component)
    - src/app/not-found.tsx contains a link to /search
    - The existing RedirectChecker Suspense is preserved (grep finds RedirectChecker)
    - src/app/(site)/preview/[token]/page.tsx still imports renderPostBody (unchanged from Phase 3)
    - src/app/(site)/preview/[token]/page.tsx does NOT import ViewCount or RelatedPosts (preview is verify-only)
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>404 page extended with a SuggestedPosts Suspense slot + search link; existing RedirectChecker Suspense preserved; preview route verified and polished (not rebuilt).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pages table (dashboard-managed) → renderPostBody → public display | Stored legal content rendered as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Tampering | T&C/Privacy body dangerouslySetInnerHTML | critical | mitigate | renderPostBody (generateHTML then sanitizeBeforeRender) runs BEFORE every dangerouslySetInnerHTML on pages-row bodies — Pitfall 8 / CLAUDE.md Pitfall #2 (same gate as post bodies) |
| T-06-16 | Tampering | SuggestedPosts DB read outside Suspense on 404 | high | mitigate | SuggestedPosts is wrapped in its own Suspense boundary — Pitfall 6 (uncached data access outside Suspense breaks the static 404 shell build) |
</threat_model>

<verification>
- pnpm tsc --noEmit passes
- pnpm build succeeds — the 404 page compiles with two Suspense boundaries
- grep confirms renderPostBody on T&C and Privacy pages
- grep confirms notFound() calls on T&C and Privacy pages
- grep confirms listPublished inside a Suspense on not-found.tsx
- grep confirms the existing RedirectChecker is preserved on not-found.tsx
</verification>

<success_criteria>
- About page is hard-coded TSX (no DB, no dangerouslySetInnerHTML) per SITE-09
- T&C and Privacy render dashboard-managed published pages rows via renderPostBody per SITE-11
- 404 has two Suspense boundaries (RedirectChecker + SuggestedPosts) + search link per SITE-12
- Preview route verified and polished (not rebuilt) per SITE-15 / Phase 3 D-19
</success_criteria>

<output>
Create `.planning/phases/06-public-frontend/06-06-SUMMARY.md` when done
</output>
