---
phase: 06-public-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/excerpt/index.ts
  - src/lib/storage/seed.ts
  - src/instrumentation.ts
  - src/lib/queries/posts.ts
  - src/lib/queries/taxonomy.ts
  - src/lib/queries/users.ts
  - src/lib/queries/pages.ts
  - src/lib/queries/archive.ts
  - src/lib/seo/jsonld.ts
  - src/lib/reading-time/index.ts
  - src/lib/toc/index.ts
  - src/lib/rate-limit/index.ts
  - src/lib/queries/__tests__/posts.test.ts
  - src/lib/queries/__tests__/users.test.ts
  - src/lib/queries/__tests__/search.test.ts
  - src/lib/reading-time/__tests__/reading-time.test.ts
  - src/lib/toc/__tests__/toc.test.ts
  - src/lib/rate-limit/__tests__/rate-limit.test.ts
autonomous: true
requirements:
  - SITE-07
  - SITE-08
  - SITE-13
  - SITE-17
user_setup: []

must_haves:
  truths:
    - "pnpm drizzle-kit generate creates a new migration containing posts.featured, posts.views, user.username, and a generated tsvector column with a GIN index"
    - "Every function in lib/queries/* returns ONLY rows where status='published' AND deletedAt IS NULL (drafts and soft-deleted posts never reach public reads)"
    - "incrementViewCount is an atomic UPDATE that sets views = views + 1 and returns the new count via RETURNING"
    - "searchPosts uses websearch_to_tsquery('simple', query) — the 'simple' config has no stemming so Bangla queries match (SEARCH-02 v2 caveat)"
    - "No function in lib/queries/* calls requireCan, assertOwnsPost, or any permission gate (published content is public; the ONE write is incrementViewCount which is unauthenticated by design per D-01)"
    - "getPostForPublic calls cacheTag with the exact tag strings that publishPost's revalidateTag already invalidates: post-${id}, author-${authorId}"
    - "deriveReadingTime uses Intl.Segmenter (NOT whitespace split) to count words — Bangla grapheme boundaries respected per D-15"
    - "buildToc extracts only heading nodes at level 2 and 3 from ProseMirror JSON"
    - "tryConsume returns false after the per-IP limit is exceeded within the window"
  artifacts:
    - "src/lib/queries/posts.ts exporting getPostForPublic, incrementViewCount, listPublished, listFeatured, listRelated, searchPosts"
    - "src/lib/queries/taxonomy.ts exporting getCategoryBySlug, getTagBySlug, listCategoriesWithCounts"
    - "src/lib/queries/users.ts exporting getUserByUsername, listAuthorPosts"
    - "src/lib/queries/pages.ts exporting getPublishedPage"
    - "src/lib/queries/archive.ts exporting listArchive"
    - "src/lib/reading-time/index.ts exporting deriveReadingTime"
    - "src/lib/toc/index.ts exporting buildToc"
    - "src/lib/rate-limit/index.ts exporting tryConsume"
    - "src/lib/seo/jsonld.ts extended with personJsonLd and breadcrumbListJsonLd"
    - "A new migration SQL file under db/migrations/ generated by pnpm drizzle-kit generate"
  key_links:
    - "cacheTag(`post-${id}`) inside getPostForPublic MUST match publishPost's existing revalidateTag(`post-${id}`, 'max') at src/actions/posts.ts line 363"
    - "cacheTag('posts-list') inside listPublished/listFeatured/listRelated MUST match publishPost's existing revalidateTag('posts-list', 'max') at src/actions/posts.ts line 368"
    - "seedPublicFrontendSettings() called in instrumentation.ts after seedSeoSettings()"
    - "collectText exported from src/lib/excerpt/index.ts and imported by deriveReadingTime"
---

## Phase Goal

**As a** blog reader, **I want to** browse home/feeds/archives, read single posts at maximum speed, search content, and view marketing/legal pages, **so that** I can discover and consume well-optimized blog content with the fastest possible experience on any device.

<objective>
Foundation slice: schema migration (posts.featured, posts.views, user.username, FTS tsvector+GIN) + published-only read-query module (lib/queries/*) + the view-count atomic write path + Bangla-aware reading-time + TOC builder + in-memory rate-limit + Person/BreadcrumbList JSON-LD builders + settings seeds + Wave 0 test scaffolds + [BLOCKING] drizzle-kit generate.

Purpose: This plan unblocks ALL other Phase 6 plans. Every public route consumes lib/queries/* for published-only reads. The single-post page (06-03) consumes getPostForPublic, incrementViewCount, listRelated. Browse routes (06-04) consume listPublished, listFeatured. Search (06-07) consumes searchPosts. Author page (06-07) consumes getUserByUsername. Contact (06-05) consumes tryConsume. The schema deltas (D-01/D-04/D-11) and FTS infrastructure (D-09) are prerequisites for every downstream plan.

Output: 5 query modules, 3 utility modules, 2 JSON-LD builders, schema migration SQL, settings seeds, 6 Wave 0 test files.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-public-frontend/06-CONTEXT.md
@.planning/phases/06-public-frontend/06-RESEARCH.md
@.planning/phases/06-public-frontend/06-PATTERNS.md

@src/db/schema.ts
@src/actions/posts.ts
@src/lib/excerpt/index.ts
@src/lib/seo/settings.ts
@src/lib/seo/jsonld.ts
@src/lib/storage/seed.ts
@src/instrumentation.ts
@src/app/sitemap.ts
@src/app/(site)/layout.tsx
</context>

## Artifacts this phase produces

**New modules:**
- `src/lib/queries/posts.ts` — getPostForPublic(slug), incrementViewCount(postId), listPublished(opts), listFeatured(limit), listRelated(postId, categoryId), searchPosts(query, filters)
- `src/lib/queries/taxonomy.ts` — getCategoryBySlug(slug), getTagBySlug(slug), listCategoriesWithCounts()
- `src/lib/queries/users.ts` — getUserByUsername(username), listAuthorPosts(username, page)
- `src/lib/queries/pages.ts` — getPublishedPage(slug)
- `src/lib/queries/archive.ts` — listArchive(filters, page)
- `src/lib/reading-time/index.ts` — deriveReadingTime(bodyJson, wpm?)
- `src/lib/toc/index.ts` — buildToc(bodyJson)
- `src/lib/rate-limit/index.ts` — tryConsume(ip, limit, windowMs)

**Extended modules:**
- `src/lib/seo/jsonld.ts` — personJsonLd(input), breadcrumbListJsonLd(input)
- `src/lib/excerpt/index.ts` — collectText now exported (was private)
- `src/lib/storage/seed.ts` — seedPublicFrontendSettings()
- `src/instrumentation.ts` — calls seedPublicFrontendSettings()
- `src/db/schema.ts` — posts.featured, posts.views, posts.searchVector + GIN index, user.username

**Migration:** new file under `db/migrations/` (generated by `pnpm drizzle-kit generate --name add_featured_views_username_fts`)

**Settings keys seeded:** `contact.recipient_email`, `analytics.script`, `analytics.umami_id`, `footer.social_twitter`, `footer.social_facebook`, `footer.social_linkedin`

<tasks>

<task type="auto">
  <name>Task 1: Schema deltas + collectText export + settings seeds + [BLOCKING] migration</name>
  <files>src/db/schema.ts, src/lib/excerpt/index.ts, src/lib/storage/seed.ts, src/instrumentation.ts</files>
  <read_first>
    - src/db/schema.ts (current posts table lines 47-66 — 2-arg pgTable form; user table lines 171-188 — no username field; postTags lines 99-108 — 3-arg pgTable form for the index pattern)
    - src/lib/excerpt/index.ts (line 25: collectText is a private function — add export keyword)
    - src/lib/storage/seed.ts (lines 79-96: seedSeoSettings pattern to mirror for seedPublicFrontendSettings)
    - src/instrumentation.ts (lines 48-55: the seed call sequence — add seedPublicFrontendSettings after seedSeoSettings)
    - .planning/phases/06-public-frontend/06-PATTERNS.md (schema.ts section lines 459-511 — exact extension points + vector export confirmed)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (FTS Schema Delta lines 770-796 — the vector + generatedAlwaysAs + GIN recipe)
  </read_first>
  <action>
Schema deltas per D-01/D-04/D-11/D-09:

In src/db/schema.ts — add to the posts table: `featured: boolean("featured").default(false).notNull()` (D-04 — manual editorial flag for the home hero); `views: integer("views").default(0).notNull()` (D-01 — simple counter column, no de-dupe); `searchVector: vector("search_vector").generatedAlwaysAs(sql\`to_tsvector('simple', coalesce(title, '') || ' ' || coalesce(excerpt, ''))\`)` (D-09 — generated tsvector from title + excerpt using 'simple' config for Bangla compatibility per SEARCH-02). Import `vector` from "drizzle-orm/pg-core" (PATTERNS.md confirmed the barrel chain exports it) and `sql` from "drizzle-orm". Switch the posts table from the current 2-arg pgTable form to the 3-arg form `(t) => ({ ... })` to add the GIN index: `searchIdx: index("posts_search_vector_idx").using("gin", t.searchVector)`. The 'simple' config has no stemming (no PG Bengali stemmer exists — accepted for v1 per SEARCH-02). Do NOT FTS against posts.body (it is jsonb — structural noise; Pitfall 4).

Add to the user table: `username: varchar("username", { length: 255 }).unique()` (D-11 — nullable, set via dashboard profile UI; user.id is a UUID unsuitable for public URLs; PATTERNS.md confirmed no existing slug field exists).

In src/lib/excerpt/index.ts — add the `export` keyword to `collectText` on line 25 (currently private; RESEARCH A4 confirmed this is a safe one-keyword refactor; deriveReadingTime in Task 3 imports it).

In src/lib/storage/seed.ts — add `seedPublicFrontendSettings()` mirroring the exact shape of `seedSeoSettings` (lines 79-96): idempotent `db.insert(schema.settings).values([...]).onConflictDoNothing()`. Seed keys: `contact.recipient_email` (empty string default — admin sets via settings), `analytics.script` (empty — Umami instance deploys Phase 7 per D-17), `analytics.umami_id` (empty), `footer.social_twitter`/`footer.social_facebook`/`footer.social_linkedin` (empty strings). Empty defaults mean the admin fills them without a redeploy.

In src/instrumentation.ts — add `await seedPublicFrontendSettings()` after the existing `await seedSeoSettings()` call (lines 48-55). Import it from the same destructured import.

[BLOCKING] SCHEMA GATE: After ALL schema.ts modifications are complete, run `pnpm drizzle-kit generate --name add_featured_views_username_fts` to produce the migration SQL file under db/migrations/. This step is MANDATORY — build and type checks pass without the migration (TS types come from schema.ts config, not the live DB), creating a false-positive verification state. The generated migration must be committed in the same PR.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm drizzle-kit generate --name add_featured_views_username_fts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.ts contains `featured: boolean("featured").default(false).notNull()`
    - src/db/schema.ts contains `views: integer("views").default(0).notNull()`
    - src/db/schema.ts contains `username: varchar("username"` and `.unique()` on the user table
    - src/db/schema.ts contains `vector("search_vector")` and `generatedAlwaysAs` and `"gin"`
    - src/db/schema.ts posts table uses the 3-arg pgTable form with a config callback `(t) =>`
    - src/lib/excerpt/index.ts line 25 contains `export function collectText` (the export keyword is present)
    - src/lib/storage/seed.ts contains `seedPublicFrontendSettings`
    - src/instrumentation.ts contains `seedPublicFrontendSettings`
    - A new .sql migration file exists under db/migrations/ (generated by drizzle-kit)
    - pnpm tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Schema migration generated with featured/views/username/searchVector columns + GIN index; collectText exported; settings seeds + instrumentation wired; TypeScript compiles cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Published-only read-query module + JSON-LD builders + Wave 0 tests</name>
  <files>src/lib/queries/posts.ts, src/lib/queries/taxonomy.ts, src/lib/queries/users.ts, src/lib/queries/pages.ts, src/lib/queries/archive.ts, src/lib/seo/jsonld.ts, src/lib/queries/__tests__/posts.test.ts, src/lib/queries/__tests__/users.test.ts, src/lib/queries/__tests__/search.test.ts</files>
  <read_first>
    - src/lib/seo/settings.ts (lines 60-83: the 'use cache' + cacheTag pattern to copy for cached reads)
    - src/app/sitemap.ts (lines 41-45: the published-only filter pattern — and(eq(status, "published"), isNull(deletedAt)))
    - src/actions/posts.ts (lines 325-375: publishPost — the EXACT revalidateTag tag strings these cacheTags must match: post-${id} line 363, author-${authorId} line 364, category-${categoryId} line 366, posts-list line 368)
    - src/actions/media.ts (lines 243-255: the Drizzle sql template-literal pattern for FTS and atomic increment)
    - src/actions/categories.ts (lines 46-56: listCategories — the query shape to adapt by dropping requireCan)
    - src/actions/pages.ts (lines 167-179: getPage — adapt to published-only + slug lookup)
    - src/lib/seo/jsonld.ts (lines 18-77: BlogPostingInput interface + blogPostingJsonLd builder — the exact shape to mirror for PersonJsonLdInput/personJsonLd and BreadcrumbListJsonLdInput/breadcrumbListJsonLd)
    - src/db/schema.ts (the full table shapes after Task 1 — posts, postSeo, user, categories, tags, postTags, pages)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (Pattern 1 lines 404-538: getPostForPublic + incrementViewCount shapes; Pattern 3 lines 566-596: searchPosts FTS shape)
  </read_first>
  <behavior>
    - getPostForPublic returns null for a non-existent or draft slug (never returns unpublished content)
    - getPostForPublic returns post + seo + author data for a published slug
    - incrementViewCount atomically increments views by 1 and returns the new count
    - listPublished returns only published, non-deleted posts ordered by publishedAt desc
    - listFeatured returns only published posts where featured=true
    - listRelated excludes the current post, prefers same category, fills with tag-sharing posts if under cap
    - searchPosts ranks by ts_rank desc, filters published-only, applies optional category/tag/author/date filters
    - searchPosts uses the 'simple' text-search config (no stemming)
    - getUserByUsername returns null for a missing username
    - listAuthorPosts returns only published posts by that username
    - getPublishedPage returns null for a draft or missing page slug
    - listArchive applies category/tag/author/date filters via where-clause accumulation
    - personJsonLd returns a valid schema.org Person object
    - breadcrumbListJsonLd returns a valid schema.org BreadcrumbList object
  </behavior>
  <action>
Create the public read-query module under src/lib/queries/. These are READ-ONLY for published content with NO permission checks (published content is public). The ONE write is incrementViewCount (unauthenticated by design per D-01).

src/lib/queries/posts.ts — six exports:
1. `getPostForPublic(slug: string)` — 'use cache' directive. Left-join posts + postSeo + user. Filter: and(eq(posts.slug, slug), eq(posts.status, "published"), isNull(posts.deletedAt)). After the query succeeds, call cacheTag(`post-${post.posts.id}`) and if authorId exists cacheTag(`author-${post.posts.authorId}`). Return null if not found. These tag strings MUST match publishPost's revalidateTag calls exactly (per D-02 / RESEARCH Q3).
2. `incrementViewCount(postId: number): Promise<number>` — atomic UPDATE: db.update(schema.posts).set({ views: sql\`${schema.posts.views} + 1\` }).where(eq(schema.posts.id, postId)).returning({ views: schema.posts.views }). Return the new count. NO 'use cache' (per-request per D-02).
3. `listPublished(opts: { page: number; pageSize?: number; categoryId?: number; tagId?: number; authorId?: string })` — 'use cache' + cacheLife("hours") + cacheTag("posts-list") + conditional cacheTag(`category-${categoryId}`) / cacheTag(`author-${authorId}`). Published-only filter. Order by desc(publishedAt). Limit/offset pagination.
4. `listFeatured(limit?: number)` — 'use cache' + cacheLife("hours") + cacheTag("posts-list"). Filter: featured=true AND published. Order by desc(publishedAt). D-04.
5. `listRelated(postId: number, categoryId: number | null, limit?: number)` — 'use cache' + cacheLife("hours") + cacheTag(`category-${categoryId}`) + cacheTag("posts-list"). Same-category posts first (latest-published); if fewer than limit, fill with posts sharing the most tags via postTags join. Always exclude the current postId. Cap at 3-4 (D-06).
6. `searchPosts(query: string, filters: SearchFilters)` — NO 'use cache' (searchParams make the route dynamic; results in Suspense). Build tsquery via sql\`websearch_to_tsquery('simple', ${query})\`. Filter: sql\`${searchVector} @@ ${tsquery}\` AND published AND not-deleted. Optional filter accumulation for categoryId/tagId/authorId/dateRange. Order by desc(sql\`ts_rank(${searchVector}, ${tsquery})\`). Limit 20. D-09. Use the Drizzle sql template (parameterized — no string concat; T-06-02 mitigation).

src/lib/queries/taxonomy.ts — `getCategoryBySlug(slug)`, `getTagBySlug(slug)`, `listCategoriesWithCounts()` (cached via 'use cache' + cacheTag("posts-list"); returns categories with published post counts). No requireCan (published content is public).

src/lib/queries/users.ts — `getUserByUsername(username: string)` (returns user or null; D-11), `listAuthorPosts(username: string, page: number)` (published-only posts by that username).

src/lib/queries/pages.ts — `getPublishedPage(slug: string)` (filter: status="published" AND deletedAt IS NULL; returns the page row or null). For T&C/Privacy/Contact rendering.

src/lib/queries/archive.ts — `listArchive(filters: ArchiveFilters, page: number)` where ArchiveFilters includes optional categoryId, tagId, authorId, dateFrom, dateTo. Where-clause accumulation pattern from src/actions/media.ts listMedia (lines 122-140). Published-only. Paginated. D-12.

src/lib/seo/jsonld.ts — add two sibling builders following the EXACT shape of blogPostingJsonLd (interface + function returning a plain schema.org object):
- `personJsonLd(i: PersonJsonLdInput)` — @type "Person" with name, url, jobTitle, description. Closes Phase 5 D-03 deferral for author pages.
- `breadcrumbListJsonLd(i: BreadcrumbListJsonLdInput)` — @type "BreadcrumbList" with itemListElement array (Home > Category/Tag). Closes Phase 5 D-03 deferral for taxonomy archives.

Wave 0 tests: create test scaffolds in __tests__/ subdirectories. Test the published-only filter invariant, the atomic increment, the cacheTag presence, FTS ranking with 'simple' config (include a Bangla sample to verify 'simple' config matching), and the JSON-LD builder output shapes. Mock the db module where needed (vitest vi.mock).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm test src/lib/queries src/lib/seo/__tests__/jsonld.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -rl "requireCan" src/lib/queries/ returns nothing (no permission gates in public reads)
    - src/lib/queries/posts.ts contains "use cache" in getPostForPublic, listPublished, listFeatured, listRelated
    - src/lib/queries/posts.ts does NOT contain "use cache" in incrementViewCount or searchPosts
    - src/lib/queries/posts.ts contains cacheTag(`post-${` (matching publishPost line 363)
    - src/lib/queries/posts.ts contains cacheTag("posts-list") (matching publishPost line 368)
    - src/lib/queries/posts.ts contains websearch_to_tsquery('simple' (the 'simple' config for Bangla)
    - src/lib/queries/posts.ts contains sql`${schema.posts.views} + 1` (atomic increment)
    - src/lib/seo/jsonld.ts contains personJsonLd and breadcrumbListJsonLd exports
    - pnpm test passes for all Wave 0 query + jsonld tests
  </acceptance_criteria>
  <done>All 5 query modules + 2 JSON-LD builders created and tested; published-only filter invariant proven; cacheTags match publishPost's revalidateTags; no permission gates in public reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Utilities — reading-time + TOC + rate-limit + Wave 0 tests</name>
  <files>src/lib/reading-time/index.ts, src/lib/toc/index.ts, src/lib/rate-limit/index.ts, src/lib/reading-time/__tests__/reading-time.test.ts, src/lib/toc/__tests__/toc.test.ts, src/lib/rate-limit/__tests__/rate-limit.test.ts</files>
  <read_first>
    - src/lib/excerpt/index.ts (lines 25-43: collectText walker — now exported after Task 1; the recursive ProseMirror text-collection pattern)
    - src/lib/excerpt/index.ts (lines 56-68: safeSlice — the Bangla-aware UTF-16 approach, model for Intl.Segmenter usage)
    - src/lib/slug/index.ts (validateSlug — Latin-only; DO NOT reuse for TOC heading IDs which may be Bangla)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (Code Examples lines 718-766: deriveReadingTime + buildToc shapes)
    - .planning/phases/06-public-frontend/06-PATTERNS.md (reading-time section lines 298-333, toc section lines 338-361, rate-limit section lines 366-370)
    - .claude/CLAUDE.md (Intl.Segmenter for Bangla grapheme/word boundaries — Node 20.19+ built-in)
  </read_first>
  <behavior>
    - deriveReadingTime returns a number >= 1 for any input
    - deriveReadingTime returns 1 for an empty or null body
    - deriveReadingTime counts Bangla words correctly via Intl.Segmenter (a Bangla body with 400 words returns ~2 at 200 WPM, not 1)
    - buildToc returns an empty array for a body with no H2/H3 headings
    - buildToc returns items with level 2 or 3 only (never 1, 4, 5, 6)
    - buildToc generates unique IDs (deduplication when two headings have the same text)
    - tryConsume returns true for the first N requests from the same IP within the window
    - tryConsume returns false for request N+1 from the same IP within the same window
    - tryConsume resets after the window elapses (returns true again)
  </behavior>
  <action>
src/lib/reading-time/index.ts — `deriveReadingTime(bodyJson: unknown, wpm = 200): number`. Import collectText from @/lib/excerpt. Walk the body JSON via collectText, join the text blocks, count words using `new Intl.Segmenter("en", { granularity: "word" })` (Node 20.19+ built-in — NOT whitespace split; D-15 Bangla-aware). Return Math.max(1, Math.round(words / wpm)). Return 1 for empty/null body. The WPM constant defaults to 200 (tunable for Bangla density — CONTEXT.md discretion). D-15.

src/lib/toc/index.ts — `buildToc(bodyJson: unknown): TocItem[]` where TocItem is { id: string; text: string; level: 2 | 3 }. Walk the ProseMirror JSON recursively. For each node where node.type === "heading" and (node.attrs?.level === 2 or 3), extract text from content text nodes, generate a URL-safe ID. The ID slugifier MUST handle arbitrary Unicode (Bangla headings): use `text.trim().toLowerCase().replace(/\s+/g, "-").replace(/[^\wঀ-৿-]/g, "")` (the ঀ-৿ range preserves Bengali script). DO NOT use validateSlug from src/lib/slug (it rejects non-Latin text — D-20). Add a dedupe counter suffix (-1, -2) when two headings produce the same ID. D-15. NOTE: the rendered HTML must produce matching id attributes on h2/h3 elements — this is handled in Plan 06-03 Task 3 (post-process renderPostBody output), not here.

src/lib/rate-limit/index.ts — `tryConsume(ip: string, limit: number, windowMs: number): boolean`. A module-level `Map<string, { count: number; resetAt: number }>` store. On each call: if the IP is not in the map OR Date.now() >= entry.resetAt, reset the entry to { count: 1, resetAt: Date.now() + windowMs }, return true. If count < limit, increment count, return true. Otherwise return false. Single-instance in-memory store (v1; v2 swaps for Redis per SCALE-01 per D-07). Export a `resetRateLimit()` for testing (clears the map).

Wave 0 tests: deriveReadingTime with a Bangla sample body JSON (verify > 0 and reasonable); empty body (verify returns 1). buildToc with a body containing H2/H3/H1/H4 (verify only H2/H3 extracted); empty body (verify empty array); duplicate heading text (verify unique IDs). tryConsume: under-limit returns true; over-limit returns false; after window elapses returns true again (use vi.useFakeTimers).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm test src/lib/reading-time src/lib/toc src/lib/rate-limit</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/reading-time/index.ts imports collectText from @/lib/excerpt
    - src/lib/reading-time/index.ts contains Intl.Segmenter
    - src/lib/toc/index.ts contains node.type === "heading"
    - src/lib/toc/index.ts does NOT import from @/lib/slug (Bangla-incompatible slugifier)
    - src/lib/rate-limit/index.ts contains a Map<string structure and tryConsume export
    - pnpm test passes for all three Wave 0 utility test suites
  </acceptance_criteria>
  <done>Reading-time (Bangla-aware via Intl.Segmenter), TOC (H2/H3 extraction with Unicode-safe IDs), and rate-limit (in-memory per-IP window) utilities created and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public reader → lib/queries/* | Unauthenticated reads of published content (no trust in caller) |
| Public reader → incrementViewCount | The ONE unauthenticated public write (view count +1) |
| /search searchParams → searchPosts | User-controlled query string enters a DB full-text query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | searchPosts FTS query | medium | mitigate | Drizzle sql template parameterizes the query value — websearch_to_tsquery('simple', ${query}) uses bound parameters, not string concat (V5/V8 ASVS) |
| T-06-02 | Information Disclosure | All lib/queries/* read functions | high | mitigate | Every query filters and(eq(status, "published"), isNull(deletedAt)) — drafts and soft-deleted posts NEVER reach public reads (V4 ASVS) |
| T-06-03 | Spoofing | incrementViewCount | low | accept | D-01 explicitly accepts minor view-count inflation from refreshes/crawlers (no de-dupe for v1). Low impact — vanity metric only. |
| T-06-04 | Tampering | listArchive filter params | medium | mitigate | URL searchParams parsed via Zod on the consuming route; filter values are integers/dates passed through Drizzle eq() (parameterized), never raw SQL |
</threat_model>

<verification>
- pnpm drizzle-kit generate --name add_featured_views_username_fts produces a migration file
- pnpm tsc --noEmit passes (zero type errors)
- pnpm test passes (all Wave 0 tests green)
- grep -rl "requireCan" src/lib/queries/ returns nothing (no permission gates in public reads)
- The migration SQL file contains ALTER TABLE posts ADD COLUMN featured, ADD COLUMN views, and the tsvector + GIN index DDL
</verification>

<success_criteria>
- Schema migration generated with posts.featured (D-04), posts.views (D-01), user.username (D-11), and searchVector generated tsvector + GIN index (D-09)
- All 5 lib/queries/* modules export published-only read functions + the atomic view-count write
- cacheTag strings in getPostForPublic/listPublished/listFeatured/listRelated match publishPost's existing revalidateTag calls
- searchPosts uses 'simple' config for Bangla-compatible FTS
- deriveReadingTime uses Intl.Segmenter (not whitespace split) for Bangla-aware word counting
- buildToc extracts H2/H3 with Unicode-safe IDs (not validateSlug)
- tryConsume implements per-IP in-memory rate limiting
- personJsonLd + breadcrumbListJsonLd added to jsonld.ts
- Settings seeds (contact.recipient_email, analytics.*, footer socials) wired into instrumentation.ts
- All Wave 0 tests pass
</success_criteria>

<output>
Create `.planning/phases/06-public-frontend/06-01-SUMMARY.md` when done
</output>
