---
phase: 06-public-frontend
plan: 07
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-03
files_modified:
  - src/components/site/SearchForm.tsx
  - src/app/(site)/search/page.tsx
  - src/app/(site)/author/[username]/page.tsx
autonomous: true
requirements:
  - SITE-06
  - SITE-08
user_setup: []

must_haves:
  truths:
    - "/search is a server-GET page: a plain HTML form (method GET) + server-rendered Postgres FTS ranked results (D-09)"
    - "No client autocomplete — zero client JS for search (D-09 explicitly rejected the header autocomplete box)"
    - "Search uses websearch_to_tsquery('simple', query) with ts_rank ordering (the 'simple' config has no stemming — Bangla queries match)"
    - "Search filters (category/tag/author/date) are applied via URL searchParams (D-09)"
    - "The FTS results query is wrapped in Suspense (uncached DB access under cacheComponents requires Suspense or 'use cache')"
    - "/author/[username] renders a full bio page (name + avatar + bio from AUTH-08) + their published posts (D-11)"
    - "The author page injects Person JSON-LD (closes Phase 5 D-03 deferral)"
    - "The single-post byline links to /author/[username] (D-11 — the username slug from the Plan 06-01 schema delta)"
    - "An author page for a missing username calls notFound()"
  artifacts:
    - "src/components/site/SearchForm.tsx (plain HTML GET form — progressive enhancement)",
    - "src/app/(site)/search/page.tsx (FTS + filters + ranked results in Suspense)",
    - "src/app/(site)/author/[username]/page.tsx (bio header + posts + Person JSON-LD)"
  key_links:
    - "searchPosts from @/lib/queries/posts (Plan 06-01 — the FTS query)"
    - "getUserByUsername + listAuthorPosts from @/lib/queries/users (Plan 06-01)",
    - "personJsonLd from @/lib/seo/jsonld (Plan 06-01 — closes Phase 5 D-03)",
    - "PostCard from @/components/site/PostCard (Plan 06-03)"
---

<objective>
Search (SITE-08) and Author page (SITE-06): /search is a dedicated page with a server GET form + Postgres FTS ranked results (websearch_to_tsquery, ts_rank, 'simple' config for Bangla) + URL searchParams filters — zero client JS per D-09. /author/[username] is a full bio page (name + avatar + bio from AUTH-08) + their published posts + Person JSON-LD (closes Phase 5 D-03 deferral per D-11).

Purpose: Search is the "find" job from D-03's discover-read-find triad. The author page closes the Phase 5 D-03 Person JSON-LD deferral and gives posts a byline target (the username slug from the Plan 06-01 schema delta).

Output: 1 search form component + 2 route pages.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-public-frontend/06-CONTEXT.md
@.planning/phases/06-public-frontend/06-RESEARCH.md
@.planning/phases/06-public-frontend/06-PATTERNS.md
@.planning/phases/06-public-frontend/06-01-SUMMARY.md
@.planning/phases/06-public-frontend/06-03-SUMMARY.md

@src/lib/seo/metadata.ts
@src/lib/seo/jsonld.ts
@src/app/sitemap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: /search page — server GET form + FTS ranked results (SITE-08, D-09)</name>
  <files>src/components/site/SearchForm.tsx, src/app/(site)/search/page.tsx</files>
  <read_first>
    - src/app/sitemap.ts (lines 41-45: the URL searchParams consumption pattern; the published-only filter)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (Pattern 3 lines 566-596: the FTS searchPosts recipe; Pitfall 4 lines 685-689: do NOT FTS against posts.body jsonb)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (D-09: page-only, server-GET, no client autocomplete)
    - src/lib/queries/posts.ts (from Plan 06-01 — searchPosts(query, filters) signature; uses 'simple' config)
    - src/lib/queries/taxonomy.ts (from Plan 06-01 — listCategoriesWithCounts for the category filter dropdown)
    - src/components/site/PostCard.tsx (from Plan 06-03 — for rendering search result cards)
    - src/lib/seo/metadata.ts (buildArchiveMetadata for the search page metadata)
  </read_first>
  <action>
src/components/site/SearchForm.tsx — a plain HTML server component (NO "use client"). Renders a `<form action="/search" method="GET">` with a text input named "q" (the search query), plus optional filter dropdowns: category (select named "category"), author (select named "author"), date range (two date inputs named "dateFrom" and "dateTo"). The form is progressive enhancement — it works without JavaScript (the browser submits a GET request to /search?q=...&category=...). Pre-fill the inputs with the current searchParams values (passed as props from the page). The category dropdown options come from listCategoriesWithCounts (cached). Include a submit button "Search". D-09: NO client autocomplete, NO API Route, NO client component. This is a plain HTML form.

src/app/(site)/search/page.tsx — server component.
1. generateMetadata: buildArchiveMetadata with title "Search". 'use cache' for settings-driven metadata.
2. Page({ searchParams }): await searchParams. Extract q (string), category (int), tag (int), author (string), dateFrom (ISO date), dateTo (ISO date), page (int). Validate via inline Zod parse.
3. Render the SearchForm with the current filter values pre-filled.
4. If q is empty or absent, render a prompt state: "Enter a search term to find posts" (D-16 empty state).
5. If q is present, wrap the results section in a `<Suspense fallback={<ResultsSkeleton />}>` boundary (the FTS query is uncached DB access — under cacheComponents it MUST be in Suspense or marked 'use cache'). Create a SearchResults async component (local or inline) that calls searchPosts(q, filters) from @/lib/queries/posts. Render the results as PostCards. Include result count ("N results for 'query'") and numbered pagination.
6. If no results, render: "No results for 'query' — try different terms or remove filters" (D-16).
7. Filters via URL searchParams: the filter dropdowns in SearchForm submit via the GET form, so changing a filter re-navigates to /search?q=...&category=... with the new params (no client JS).

The searchPosts function from Plan 06-01 already handles the FTS query: websearch_to_tsquery('simple', query), ts_rank ordering, published-only filter, and optional category/tag/author/date filter accumulation. This page just wires it into a route.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c 'method="GET"' src/components/site/SearchForm.tsx | grep -v '^0' && grep -c "searchPosts" src/app/\(site\)/search/page.tsx | grep -v '^0'</automated>
  </verify>
  <acceptance_criteria>
    - src/components/site/SearchForm.tsx contains a form with method="GET" and action="/search"
    - src/components/site/SearchForm.tsx does NOT contain "use client" (server-rendered GET form per D-09)
    - src/components/site/SearchForm.tsx does NOT import from @tanstack/react-query or use fetch/useMutation
    - src/app/(site)/search/page.tsx calls searchPosts from @/lib/queries/posts
    - src/app/(site)/search/page.tsx reads searchParams (q, category, author, etc.)
    - src/app/(site)/search/page.tsx wraps search results in a Suspense boundary
    - src/app/(site)/search/page.tsx has an empty state for no query and for no results
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>/search page with a plain GET form (no client JS) + server-rendered FTS ranked results in Suspense + URL searchParams filters + empty states complete per D-09.</done>
</task>

<task type="auto">
  <name>Task 2: /author/[username] bio page + Person JSON-LD (SITE-06, D-11)</name>
  <files>src/app/(site)/author/[username]/page.tsx</files>
  <read_first>
    - src/app/(site)/preview/[token]/page.tsx (lines 45-77: generateMetadata with params + getSeoSettings — the per-record metadata pattern to adapt for author)
    - src/lib/seo/jsonld.ts (from Plan 06-01 — personJsonLd builder signature; also the blogPostingJsonLd shape at lines 52-77 for the JSON-LD script injection pattern)
    - src/lib/queries/users.ts (from Plan 06-01 — getUserByUsername(username) and listAuthorPosts(username, page) signatures)
    - src/db/schema.ts (user table after Plan 06-01 Task 1 — has username varchar unique, name, bio, avatar columns)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (D-11: full bio page + username slug + Person JSON-LD)
    - src/components/site/PostCard.tsx (from Plan 06-03 — for rendering the author's posts)
    - src/lib/seo/metadata.ts (buildArchiveMetadata for the author page metadata)
  </read_first>
  <action>
src/app/(site)/author/[username]/page.tsx — server component.

1. generateMetadata({ params }): await params.username. Call getUserByUsername(username) from @/lib/queries/users. If null, return { title: "Not Found" }. If found, return buildArchiveMetadata with title `${user.name}` and description from user.bio (or a static fallback "Posts by ${user.name}").

2. Page({ params, searchParams }): await params.username. Call getUserByUsername(username). If null, call notFound() (no existence leak — the 404 is the same page for any missing username).

3. Render the bio header:
- Author name (h1)
- Avatar via next/image (if user.avatar exists — it's an R2 object key resolved by the image loader; NEVER raw img)
- Bio text from user.bio (AUTH-08 field, already in the schema)

4. Person JSON-LD: inject a `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(personJsonLd({ name: user.name, url: `/author/${username}`, description: user.bio, image: user.avatar })) }} />`. Closes Phase 5 D-03 deferral per D-11.

5. Author's posts: await searchParams for page number. Call listAuthorPosts(username, page) from @/lib/queries/users (published-only — the query handles the filter). Render the posts as PostCards in a grid. Numbered pagination preserving the /author/${username} base path.

6. Empty state: if the author has no published posts, render: "${user.name} hasn't published any posts yet" (D-16).

7. The single-post byline (from Plan 06-03 Task 1) links to /author/${author.username} when the username exists. If the author has no username set, the byline shows the name without a link.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c "getUserByUsername" src/app/\(site\)/author/\[username\]/page.tsx | grep -v '^0' && grep -c "personJsonLd" src/app/\(site\)/author/\[username\]/page.tsx | grep -v '^0'</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(site)/author/[username]/page.tsx calls getUserByUsername from @/lib/queries/users
    - src/app/(site)/author/[username]/page.tsx calls notFound() when the user does not exist
    - src/app/(site)/author/[username]/page.tsx calls listAuthorPosts for the author's posts
    - src/app/(site)/author/[username]/page.tsx injects personJsonLd via a script tag (closes Phase 5 D-03)
    - src/app/(site)/author/[username]/page.tsx uses next/image for the avatar (NOT raw img)
    - src/app/(site)/author/[username]/page.tsx renders PostCard for each post
    - src/app/(site)/author/[username]/page.tsx has an empty state for authors with no posts
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>/author/[username] renders the author bio (name + avatar + bio) + their published posts + Person JSON-LD; notFound() on missing username; byline links here (D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader searchParams → searchPosts FTS query | User-controlled query string enters a full-text search query |
| Reader → /author/[username] | Unauthenticated author profile view |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-17 | Tampering | searchPosts FTS query injection | medium | mitigate | Drizzle sql template parameterizes the query: websearch_to_tsquery('simple', ${query}) uses bound parameters. URL searchParams parsed via Zod before passing to searchPosts. (V5/V8 ASVS) |
| T-06-18 | Information Disclosure | Author profile exposing non-published data | low | mitigate | getUserByUsername returns only the public profile fields (name, bio, avatar, username) — NOT email, role, or session data. listAuthorPosts filters published-only internally. |
</threat_model>

<verification>
- pnpm tsc --noEmit passes
- pnpm build succeeds — /search and /author/[username] compile
- grep confirms method="GET" on SearchForm (D-09 — no client JS)
- grep confirms searchPosts call on /search
- grep confirms getUserByUsername + personJsonLd on /author/[username]
- grep confirms next/image (not raw img) for the author avatar
</verification>

<success_criteria>
- /search is a server-GET page with FTS ranked results + URL searchParams filters — zero client JS per D-09
- /author/[username] is a full bio page with Person JSON-LD (closes Phase 5 D-03) per D-11
- Both routes have meaningful empty states per D-16
- The FTS query uses 'simple' config for Bangla compatibility (SEARCH-02 v2 caveat)
</success_criteria>

<output>
Create `.planning/phases/06-public-frontend/06-07-SUMMARY.md` when done
</output>
