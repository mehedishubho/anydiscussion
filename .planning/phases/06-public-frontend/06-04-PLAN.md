---
phase: 06-public-frontend
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-03
files_modified:
  - src/app/(site)/page.tsx
  - src/app/(site)/blog/page.tsx
  - src/app/(site)/blog/page/[pageNumber]/page.tsx
  - src/app/(site)/archive/page.tsx
  - src/app/(site)/category/[slug]/page.tsx
  - src/app/(site)/tag/[slug]/page.tsx
  - src/components/site/ArchiveList.tsx
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
user_setup: []

must_haves:
  truths:
    - "Home is a magazine layout: featured hero (most-recently-published posts.featured=true) + latest grid + 1-2 category teasers (D-03/D-04)"
    - "Home hero is NOT identical to /blog first item — the featured flag gives editorial control per D-04"
    - "/blog is a full reverse-chronological paginated feed with URL-based page numbers (D-03)"
    - "/archive is a dense filterable list with a top filter bar (category/tag/author/date-range via URL searchParams) and numbered pagination (D-12)"
    - "/category/[slug] and /tag/[slug] reuse the ArchiveList component pre-filtered to one taxon (D-14)"
    - "All list routes use PostCard (from Plan 06-03) for consistent card rendering"
    - "All list routes use 'use cache' + cacheTag('posts-list') matching publishPost's revalidateTag('posts-list', 'max')"
    - "Pagination is classic URL-based page numbers everywhere (ISR/SEO-friendly, no client fetching per D-03)"
    - "BreadcrumbList JSON-LD is injected on /category/[slug] and /tag/[slug] (closes Phase 5 D-03 per D-14)"
    - "Every route has meaningful empty states (D-16: 'No posts found' / 'No posts in this category yet')"
  artifacts:
    - "src/app/(site)/page.tsx (REPLACED — magazine home)"
    - "src/app/(site)/blog/page.tsx (paginated feed page 1)"
    - "src/app/(site)/blog/page/[pageNumber]/page.tsx (paginated feed page N)"
    - "src/app/(site)/archive/page.tsx (filterable dense list)"
    - "src/app/(site)/category/[slug]/page.tsx (category archive reusing ArchiveList)"
    - "src/app/(site)/tag/[slug]/page.tsx (tag archive reusing ArchiveList)"
    - "src/components/site/ArchiveList.tsx (reusable filterable list component)"
  key_links:
    - "Home listFeatured + listPublished queries from @/lib/queries/posts (Plan 06-01)"
    - "/archive listArchive query from @/lib/queries/archive (Plan 06-01)"
    - "/category getCategoryBySlug + /tag getTagBySlug from @/lib/queries/taxonomy (Plan 06-01)"
    - "breadcrumbListJsonLd from @/lib/seo/jsonld (Plan 06-01)"
    - "PostCard from @/components/site/PostCard (Plan 06-03)"
---

<objective>
Browse + taxonomy routes: Home (magazine — featured hero + latest grid + category teasers, D-03/D-04), /blog (full reverse-chronological paginated feed, SITE-02), /archive (dense filterable list with category/tag/author/date filters, D-12), /category/[slug] and /tag/[slug] (reusing ArchiveList pre-filtered, D-14, with BreadcrumbList JSON-LD). All use PostCard, numbered pagination, and 'use cache' with cacheTag matching publishPost's revalidation.

Purpose: These are the browse and discovery surfaces — the three distinct jobs (discover → read-latest → find) per D-03. The taxonomy archives close the Phase 5 D-03 BreadcrumbList JSON-LD deferral.

Output: 6 route pages + 1 reusable ArchiveList component.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-public-frontend/06-CONTEXT.md
@.planning/phases/06-public-frontend/06-RESEARCH.md
@.planning/phases/06-public-frontend/06-PATTERNS.md
@.planning/phases/06-public-frontend/06-01-SUMMARY.md
@.planning/phases/06-public-frontend/06-03-SUMMARY.md

@src/app/(site)/page.tsx
@src/app/sitemap.ts
@src/lib/seo/metadata.ts
@src/lib/seo/jsonld.ts
@src/actions/posts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home magazine (SITE-01) + /blog paginated feed (SITE-02)</name>
  <files>src/app/(site)/page.tsx, src/app/(site)/blog/page.tsx, src/app/(site)/blog/page/[pageNumber]/page.tsx</files>
  <read_first>
    - src/app/(site)/page.tsx (current placeholder — lines 23-33: the static "coming soon" to REPLACE entirely; keep the generateMetadata 'use cache' pattern from lines 17-21)
    - src/app/sitemap.ts (line 85: confirms /blog/${slug} is the post URL — /blog is the blog section index)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (D-03: three distinct routes; D-04: featured = manual flag; D-16: skeletons + friendly empties)
    - .planning/phases/06-public-frontend/06-RESEARCH.md (Pattern 2 lines 541-562: the cached paginated list query shape)
    - src/lib/seo/metadata.ts (buildArchiveMetadata signature — for /blog metadata)
    - src/components/site/PostCard.tsx (from Plan 06-03 — the card component to reuse)
    - src/lib/queries/posts.ts (from Plan 06-01 — listPublished, listFeatured signatures)
    - src/lib/queries/taxonomy.ts (from Plan 06-01 — listCategoriesWithCounts)
  </read_first>
  <action>
src/app/(site)/page.tsx — REPLACE the placeholder entirely. Magazine layout per D-03/D-04:
1. generateMetadata: keep the existing 'use cache' + getSeoSettings + buildSiteMetadata pattern (lines 17-21 — already correct for the home route).
2. Hero section: call listFeatured(1) — if a featured post exists, render a large hero card (feature image via next/image, title linking to /blog/${slug}, excerpt, author, date). If no featured posts, fall back to the most recent published post (listPublished page 1, first item).
3. Latest grid: call listPublished({ page: 1, pageSize: 6 }) — render PostCards in a responsive grid (exclude the hero post if it's also in the latest list).
4. Category teasers: call listCategoriesWithCounts() — pick 1-2 categories with the most posts, render a section per category with the category name (linking to /category/${slug}) and its top 3-4 posts as smaller PostCards.
5. Empty states: if no published posts at all, render a friendly "No posts yet — check back soon" message (D-16).
6. All data reads are cached: listFeatured and listPublished use 'use cache' + cacheTag('posts-list') internally (from Plan 06-01). No additional 'use cache' needed on the page component (the queries handle caching).

src/app/(site)/blog/page.tsx — full reverse-chronological paginated feed (SITE-02). generateMetadata via buildArchiveMetadata with title "Blog". Call listPublished({ page: 1, pageSize: 9 }). Render PostCards in a grid. Numbered pagination at the bottom (links to /blog/page/2, /blog/page/3, etc. — D-03 classic URL-based). If page 1 and no posts, empty state.

src/app/(site)/blog/page/[pageNumber]/page.tsx — paginated feed for page N. await params.pageNumber, parse to integer. If < 1, redirect to /blog. Call listPublished({ page: N, pageSize: 9 }). If the page is beyond the last page (query returns empty and N > 1), call notFound(). generateMetadata via buildArchiveMetadata with title "Blog — Page N". Numbered pagination with prev/next links.

Pagination component: a simple numbered nav (1, 2, 3, ... N) with prev/next arrows. Links are plain anchor tags to /blog/page/${n}. This is server-rendered (no client JS) per D-03.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c "listFeatured" src/app/\(site\)/page.tsx | grep -v '^0' && grep -c "listPublished" src/app/\(site\)/blog/page.tsx | grep -v '^0'</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(site)/page.tsx does NOT contain "coming soon" (the placeholder is replaced)
    - src/app/(site)/page.tsx calls listFeatured (the D-04 hero)
    - src/app/(site)/page.tsx calls listPublished (the latest grid)
    - src/app/(site)/page.tsx calls listCategoriesWithCounts (the category teasers)
    - src/app/(site)/blog/page.tsx calls listPublished with page: 1
    - src/app/(site)/blog/page/[pageNumber]/page.tsx reads params.pageNumber and calls listPublished with the parsed page number
    - src/app/(site)/blog/page.tsx contains numbered pagination links to /blog/page/ (classic URL-based, not client fetching)
    - All three routes render PostCard for each post
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>Home magazine (featured hero + latest grid + category teasers) + /blog paginated feed (reverse-chronological, URL-based numbered pagination) replace the placeholder; all use cached queries and PostCard.</done>
</task>

<task type="auto">
  <name>Task 2: ArchiveList component + /archive filterable list (SITE-03, D-12)</name>
  <files>src/components/site/ArchiveList.tsx, src/app/(site)/archive/page.tsx</files>
  <read_first>
    - src/actions/media.ts (lines 122-140: listMedia filter-accumulation pattern — the where-clause accumulation model for listArchive)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (D-12: top filter bar + numbered pagination; D-16: skeletons + friendly empties)
    - .planning/phases/06-public-frontend/06-PATTERNS.md (ArchiveList row line 29: partial-match from TailAdmin tables)
    - src/lib/queries/archive.ts (from Plan 06-01 — listArchive signature with filters)
    - src/components/site/PostCard.tsx (from Plan 06-03)
    - src/lib/seo/metadata.ts (buildArchiveMetadata for the archive metadata)
  </read_first>
  <action>
src/components/site/ArchiveList.tsx — server component that renders a dense list of posts using PostCard. Props: posts array, pagination info (currentPage, totalPages), and optional filter-bar state (active filters). Renders:
1. A top filter bar: category dropdown (select), tag multi-select (a small progressive-enhancement client bit — a details/summary or a minimal "use client" component that toggles checkboxes), author dropdown (select), date-range inputs (from/to date). All filters are applied via URL searchParams — the filter bar is a GET form whose field names match the searchParams keys (category, tag, author, dateFrom, dateTo per D-12).
2. The post list: a dense vertical list of PostCards (smaller variant than home/blog — more compact for scanning).
3. Numbered pagination at the bottom (links preserving current filter searchParams + changing the page number).
4. Empty state when posts array is empty: "No posts found — try adjusting your filters" (D-16).

src/app/(site)/archive/page.tsx — the archive route. await searchParams. Parse filter values (q, category, tag, author, dateFrom, dateTo, page) — validate via Zod (integers for category/tag/author, ISO dates for date range, integer for page). Call listArchive(parsedFilters, page) from @/lib/queries/archive. If no results and no filters, show "No posts yet". If no results with filters, show the filter-adjustment empty state. generateMetadata via buildArchiveMetadata with title "Archive". Render ArchiveList with the results and filter state.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c "listArchive" src/app/\(site\)/archive/page.tsx | grep -v '^0'</automated>
  </verify>
  <acceptance_criteria>
    - src/components/site/ArchiveList.tsx renders PostCard components in a dense list
    - src/components/site/ArchiveList.tsx contains a filter bar with category/tag/author/date inputs
    - src/components/site/ArchiveList.tsx filter form uses method GET (URL searchParams drive filters)
    - src/app/(site)/archive/page.tsx calls listArchive with parsed searchParams filters
    - src/app/(site)/archive/page.tsx parses searchParams for category/tag/author/date/page
    - src/app/(site)/archive/page.tsx has numbered pagination preserving filter searchParams
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>ArchiveList (reusable dense list + filter bar + numbered pagination) and /archive route (filterable by category/tag/author/date-range via URL searchParams) complete per D-12.</done>
</task>

<task type="auto">
  <name>Task 3: /category/[slug] (SITE-04) + /tag/[slug] (SITE-05) + BreadcrumbList JSON-LD (D-14)</name>
  <files>src/app/(site)/category/[slug]/page.tsx, src/app/(site)/tag/[slug]/page.tsx</files>
  <read_first>
    - src/components/site/ArchiveList.tsx (from Task 2 — the reusable component to reuse here)
    - .planning/phases/06-public-frontend/06-CONTEXT.md (D-14: reuse archive template + BreadcrumbList)
    - src/lib/seo/jsonld.ts (from Plan 06-01 — breadcrumbListJsonLd builder)
    - src/lib/queries/taxonomy.ts (from Plan 06-01 — getCategoryBySlug, getTagBySlug)
    - src/lib/queries/archive.ts (from Plan 06-01 — listArchive with categoryId/tagId filters)
    - src/lib/seo/metadata.ts (buildArchiveMetadata for taxonomy metadata)
  </read_first>
  <action>
src/app/(site)/category/[slug]/page.tsx — category archive reusing ArchiveList per D-14.
1. generateMetadata({ params }): await params.slug. Call getCategoryBySlug(slug). If null, return { title: "Not Found" }. Call getSeoSettings, build metadata via buildArchiveMetadata with the category name as title.
2. Page({ params, searchParams }): await params.slug. Call getCategoryBySlug(slug). If null, notFound(). Await searchParams for page number. Call listArchive({ categoryId: category.id }, page). Render: a BreadcrumbList JSON-LD script (Home > Category) via breadcrumbListJsonLd from @/lib/seo/jsonld (closes Phase 5 D-03). A header with category name + description. The ArchiveList component pre-filtered (pass the results + hide the category filter since we're already scoped). Numbered pagination preserving the /category/${slug} base path.

src/app/(site)/tag/[slug]/page.tsx — tag archive, same pattern as category.
1. generateMetadata: getTagBySlug(slug). If null, not-found metadata.
2. Page: getTagBySlug(slug). If null, notFound(). listArchive({ tagId: tag.id }, page). BreadcrumbList JSON-LD (Home > Tag). Header with tag name. ArchiveList pre-filtered. Numbered pagination.

Both routes inject breadcrumbListJsonLd as a real `<script type="application/ld+json" dangerouslySetInnerHTML>` tag. The breadcrumb items are: Home (url: base) > Category/Tag name (url: current path).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && grep -c "breadcrumbListJsonLd" src/app/\(site\)/category/\[slug\]/page.tsx src/app/\(site\)/tag/\[slug\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(site)/category/[slug]/page.tsx calls getCategoryBySlug and listArchive with categoryId
    - src/app/(site)/category/[slug]/page.tsx injects breadcrumbListJsonLd via a script tag
    - src/app/(site)/category/[slug]/page.tsx calls notFound() when the category does not exist
    - src/app/(site)/tag/[slug]/page.tsx calls getTagBySlug and listArchive with tagId
    - src/app/(site)/tag/[slug]/page.tsx injects breadcrumbListJsonLd via a script tag
    - src/app/(site)/tag/[slug]/page.tsx calls notFound() when the tag does not exist
    - Both routes reuse the ArchiveList component (import from @/components/site/ArchiveList)
    - pnpm tsc --noEmit passes
  </acceptance_criteria>
  <done>/category/[slug] and /tag/[slug] reuse ArchiveList pre-filtered to one taxon; both inject BreadcrumbList JSON-LD (closing Phase 5 D-03); both call notFound() on missing taxon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader searchParams → listArchive filters | User-controlled filter values enter DB queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Tampering | /archive searchParams → listArchive | medium | mitigate | Filter values parsed via Zod (integers for IDs, ISO dates for range); passed through Drizzle eq() parameterized queries — never raw SQL string concat |
| T-06-11 | Information Disclosure | List queries returning drafts | high | mitigate | listPublished and listArchive internally filter status='published' AND deletedAt IS NULL — the consuming routes cannot bypass this (V4 ASVS) |
</threat_model>

<verification>
- pnpm tsc --noEmit passes
- pnpm build succeeds — all 6 routes compile
- Home page no longer shows "coming soon"
- grep confirms listFeatured on home, listPublished on /blog, listArchive on /archive
- grep confirms breadcrumbListJsonLd on /category and /tag routes
- grep confirms notFound() calls on /category and /tag routes
</verification>

<success_criteria>
- Home is a magazine (featured hero + latest grid + category teasers) per D-03/D-04
- /blog is a full reverse-chronological paginated feed per D-03
- /archive is a dense filterable list with top filter bar per D-12
- /category/[slug] and /tag/[slug] reuse ArchiveList + BreadcrumbList JSON-LD per D-14
- All routes use PostCard + numbered pagination + cached queries with cacheTag matching publishPost
- All routes have meaningful empty states per D-16
</success_criteria>

<output>
Create `.planning/phases/06-public-frontend/06-04-SUMMARY.md` when done
</output>
