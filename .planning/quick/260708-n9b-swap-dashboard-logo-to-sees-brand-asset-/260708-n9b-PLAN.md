---
phase: quick
plan: 260708-n9b
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layout/AppSidebar.tsx
  - src/layout/AppHeader.tsx
  - src/components/site/SiteHeader.tsx
  - src/layout/SidebarWidget.tsx
  - public/images/logo/sees-logo.png
autonomous: true
requirements: [QUICK-sees-branding]
user_setup: []

must_haves:
  truths:
    - The expanded dashboard sidebar renders the SEES wordmark (sees-logo.png) in both light and dark themes via a single next/image <Image> (no separate dark variant).
    - The collapsed dashboard sidebar still renders logo-icon.svg at 32x32, unchanged.
    - The dashboard mobile header (AppHeader) renders sees-logo.png via a single <Image> in both themes.
    - The public site header (SiteHeader) renders the SEES wordmark instead of falling back to site-title text.
    - The TailAdmin SidebarWidget promo box is gone from the sidebar, and its file is deleted with no remaining imports.
    - The existing [CITED: ...] header comments in AppSidebar/AppHeader are preserved.
  artifacts:
    - public/images/logo/sees-logo.png (committed to git — currently untracked)
    - src/layout/SidebarWidget.tsx (DELETED)
  key_links:
    - AppSidebar logo Link href="/dashboard" → expanded branch single SEES <Image>
    - AppHeader logo Link href="/" (lg:hidden) → single SEES <Image>
    - SiteHeader logo Link href="/" → SEES wordmark <Image> (text fallback when no logo)
---

<objective>
Swap the dashboard + public-site logos to the SEES brand asset (sees-logo.png) and remove the TailAdmin "Upgrade To Pro" promo widget (SidebarWidget) from the dashboard sidebar.

Purpose: Replace the placeholder TailAdmin logo + generic promo box with the real SEES brand wordmark across every chrome surface (sidebar, dashboard mobile header, public site header), and delete the leftover TailAdmin promo component that is no longer relevant to this CMS.

Output: Three edited chrome files (AppSidebar.tsx, AppHeader.tsx, SiteHeader.tsx), one deleted file (SidebarWidget.tsx), and the sees-logo.png asset committed to git.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@D:/Devsroom-Work/anydiscussion/CLAUDE.md
@D:/Devsroom-Work/anydiscussion/.claude/CLAUDE.md

# Source files (read each once, extract patterns, do not re-read)
@D:/Devsroom-Work/anydiscussion/src/layout/AppSidebar.tsx
@D:/Devsroom-Work/anydiscussion/src/layout/AppHeader.tsx
@D:/Devsroom-Work/anydiscussion/src/layout/SidebarWidget.tsx
@D:/Devsroom-Work/anydiscussion/src/components/site/SiteHeader.tsx
@D:/Devsroom-Work/anydiscussion/src/app/(full-width-pages)/(auth)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap all logos to sees-logo.png and delete the TailAdmin SidebarWidget promo</name>
  <files>src/layout/AppSidebar.tsx, src/layout/AppHeader.tsx, src/components/site/SiteHeader.tsx, src/layout/SidebarWidget.tsx, public/images/logo/sees-logo.png</files>
  <behavior>
    - AppSidebar expanded branch: exactly one <Image> pointing at /images/logo/sees-logo.png, no dark:hidden/hidden dark:block pair.
    - AppSidebar collapsed branch: still one <Image> pointing at /images/logo/logo-icon.svg (32x32) — byte-identical to before.
    - AppSidebar: no SidebarWidget import, no SidebarWidget render.
    - AppHeader: exactly one <Image> pointing at sees-logo.png, no light/dark pair.
    - SiteHeader: logoUrl is the SEES path; wordmark renders IN PLACE OF the text title when set.
    - SidebarWidget.tsx no longer exists; nothing imports it.
  </behavior>
  <action>
Implement per locked decisions QUICK-D01/D02/D03 (single SEES asset for expanded+header in both themes; keep logo-icon.svg for collapsed; remove SidebarWidget entirely).

The SEES wordmark aspect is 4.8125:1 (verified from the auth layout render at width=231 height=48). Size the sidebar/header <Image> props to preserve that ratio.

1) src/layout/AppSidebar.tsx — EXPANDED logo block (the `isExpanded || isHovered || isMobileOpen ? (...)` branch, currently two <Image> tags for logo.svg + logo-dark.svg at width=150 height=40): replace BOTH <Image> tags with a SINGLE `<Image src="/images/logo/sees-logo.png" alt="SEES" width={150} height={32} />` (150/32 = 4.6875 ≈ the wordmark ratio; fits the 290px sidebar). Drop the `dark:hidden` and `hidden dark:block` classNames — one asset serves both themes. Do NOT touch the collapsed `else` branch that renders `/images/logo/logo-icon.svg` at 32x32 (QUICK-D02 — keep it byte-identical).

2) src/layout/AppSidebar.tsx — REMOVE the SidebarWidget wiring: delete the `import SidebarWidget from "./SidebarWidget";` line (near the top of the imports) AND delete the render expression `{isExpanded || isHovered || isMobileOpen ? <SidebarWidget /> : null}` (near the end of the <aside>). Leave the surrounding structure intact.

3) src/layout/AppHeader.tsx — mobile header logo (the `<Link href="/" className="lg:hidden">` block, currently two <Image> tags for logo.svg + logo-dark.svg at width=154 height=32): replace BOTH with a SINGLE `<Image src="/images/logo/sees-logo.png" alt="SEES" width={154} height={32} />` (154/32 = 4.8125 — exactly the wordmark ratio). Drop `dark:hidden` / `hidden dark:block`. Preserve the surrounding Link and its className.

4) src/components/site/SiteHeader.tsx — line with `const logoUrl = null;`: change the value to `"/images/logo/sees-logo.png"`. PRESERVE the trailing TODO comment verbatim (it becomes settings-driven when the settings/general page ships). Then restructure the logo/title branch inside the `<Link href="/">` so the SEES wordmark renders IN PLACE OF (not alongside) the site-title text: when logoUrl is truthy, render only the wordmark `<Image src={logoUrl} alt={seo.siteTitle} width={150} height={32} priority />` (drop the old 32x32 square + `h-8 w-8` className — those were sized for an icon mark, not a wordmark); when logoUrl is falsy, render the `<span>{seo.siteTitle}</span>` text as the fallback. Keep the `<Link href="/">` and its existing flex/typography classes. Rationale: sees-logo.png is a wordmark (it already contains the site name visually), so showing it next to the title text would duplicate the name; "renders the SEES logo instead of falling back to site-title text" means logo-present replaces the text.

5) DELETE src/layout/SidebarWidget.tsx (it becomes unreferenced after step 2).

6) Commit the asset: `public/images/logo/sees-logo.png` is currently UNTRACKED in git — it MUST be included in the commit (the plan's git commit step adds it). Do not move or rename the file.

CONSTRAINTS (non-negotiable):
- Do NOT touch src/app/(full-width-pages)/(auth)/layout.tsx — it already uses sees-logo.png correctly.
- Do NOT touch the signin flow, signin/page.tsx, auth config, or .planning/REQUIREMENTS.md (user WIP).
- Do NOT change the collapsed-sidebar logo-icon.svg branch.
- PRESERVE every existing `[CITED: ...]` header comment in AppSidebar.tsx and AppHeader.tsx — edit ONLY the logo lines and the SidebarWidget import/render. Match surrounding code style (tabs/semicolons/className ordering).
- Use next/image `<Image>` only — never a raw <img> tag (CLAUDE.md hard rule).
- TypeScript strict: no `any`, no unused imports after removing SidebarWidget.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/anydiscussion" && pnpm lint && pnpm exec tsc --noEmit && echo "--- dangling-ref grep (expect empty) ---" && ! grep -rn "SidebarWidget" src/ && ! grep -rn "logo-dark.svg\|/images/logo/logo\.svg" src/layout/ src/components/site/ && echo "--- collapsed icon preserved (expect match) ---" && grep -n "logo-icon.svg" src/layout/AppSidebar.tsx</automated>
  </verify>
  <done>
    - Sidebar expanded branch renders a single sees-logo.png <Image> (no light/dark pair); collapsed branch still renders logo-icon.svg 32x32 unchanged.
    - AppHeader renders a single sees-logo.png <Image> (no light/dark pair).
    - SiteHeader renders the SEES wordmark in place of the title text (text is the fallback); TODO comment preserved.
    - SidebarWidget.tsx deleted; zero references to it anywhere under src/.
    - No remaining references to logo.svg or logo-dark.svg in the edited files; logo-icon.svg still present in AppSidebar collapsed branch.
    - `pnpm lint` clean and `pnpm exec tsc --noEmit` clean.
    - sees-logo.png committed to git.
    - [CITED: ...] header comments in AppSidebar.tsx / AppHeader.tsx intact.
  </done>
</task>

</tasks>

<verification>
- `pnpm lint` (eslint .) passes — confirms no unused SidebarWidget import and no lint regressions.
- `pnpm exec tsc --noEmit` passes — confirms types are sound after deleting SidebarWidget.tsx and restructuring SiteHeader's logo branch.
- Grep gates (in <verify>): zero `SidebarWidget` refs under src/; zero `logo-dark.svg` / `/images/logo/logo.svg` refs in the edited chrome files; `logo-icon.svg` still present in AppSidebar (collapsed branch preserved).
- Optional human eyeball (not blocking): `pnpm dev` → check dashboard sidebar (expanded + collapsed), dashboard mobile header, and public site header in both light and dark themes; wordmark should not be stretched (aspect ~4.8:1) and the sidebar promo box should be gone.
</verification>

<success_criteria>
All three chrome surfaces (dashboard sidebar expanded, dashboard mobile header, public site header) show the SEES wordmark via a single next/image <Image> in both light and dark themes; the collapsed sidebar still uses logo-icon.svg; the TailAdmin SidebarWidget promo box and its file are gone with no dangling imports; lint + typecheck are clean; the sees-logo.png asset is committed.
</success_criteria>

<output>
Create `.planning/quick/260708-n9b-swap-dashboard-logo-to-sees-brand-asset-/260708-n9b-SUMMARY.md` when done.
</output>
