---
status: complete
quick_id: 260824-36g
completed: 2026-08-24
---

# Quick Task 260824-36g: De-brand dashboard/auth surfaces to anydiscussion — Summary

Replaced every previous-operator brand remnant on dashboard/auth surfaces with the anydiscussion brand block (speech-bubble icon + lowercase wordmark) via a new shared BrandLogo component, and fixed the sign-in page metadata. Presentation only, zero functionality changes.

## Tasks Completed

| Task | Commit | What changed |
|------|--------|--------------|
| 1 — Shared BrandLogo + dashboard chrome swap | `4461ea2` | New `src/components/common/BrandLogo.tsx` (pure presentational, no "use client", no hooks; wordmark/icon-only variants; defaults byte-match the SiteHeader brand block incl. the SVG path). `AppSidebar.tsx`: expanded branch renders full brand block, collapsed 90px rail renders icon-only variant (`wordmark={false}`); unused `next/image` import removed. `AppHeader.tsx`: mobile `lg:hidden` logo swapped to default brand block; unused `next/image` import removed. |
| 2 — Auth panel de-brand + sign-in metadata + straggler sweep | `5e5d493` | Auth layout panel: brand block with `iconClassName="h-10 w-10 text-brand-400"` + `wordmarkClassName="text-2xl font-bold tracking-tight text-white"` (locked decision 5 — always-dark brand-950 surface); blurb replaced with "A home for curious minds — read, write, and discuss ideas worth sharing on anydiscussion.com."; unused `next/image` import removed. `signin/page.tsx`: title "Sign In \| Any Discussion" (pipe-title convention), description "Sign in to the anydiscussion dashboard."; stray double space in old title gone. Read-only confirmations: signup metadata already clean; the six lowercase-verb "sees" comment locations untouched. |

## Files Changed

- `src/components/common/BrandLogo.tsx` (new)
- `src/layout/AppSidebar.tsx`
- `src/layout/AppHeader.tsx`
- `src/app/(full-width-pages)/(auth)/layout.tsx`
- `src/app/(full-width-pages)/(auth)/signin/page.tsx`

Full diff vs base `79198ba` touches exactly these five files — nothing else (plan success criterion 1).

## Verification

- `pnpm build` (definitive gate): exit 0, "Compiled successfully", 49 static pages generated.
- Token gates (Task 2): `sees-logo` under `src/` = 0; whole-word `SEES` under `src/` = 0; `public/images/logo/sees-logo.png` still on disk; `git diff` touches nothing under `components/site` or `public/images`.
- `pnpm exec tsc --noEmit`: 12 errors, all pre-existing on base and all in files untouched by this diff (see Deviations). Zero errors in any of the five changed files — the new BrandLogo and all swapped surfaces type-check clean.

## Deviations

**1. [Environment — Rule 3] Worktree had no `node_modules`** — fresh worktree fork. Fixed with `pnpm install --frozen-lockfile --prefer-offline` (locked deps only). No repo changes.

**2. [Environment — Rule 3] Worktree lacked gitignored `.env.local`** — first `pnpm build` failed during prerender of DB-backed pages (`SASL: client password must be a string`). Copied `.env.local` from the main checkout (gitignored per `.gitignore:30`, never staged). Build then green. No repo changes.

**3. [Out-of-scope discovery — documented, not fixed] `pnpm exec tsc --noEmit` fails on 12 pre-existing errors.** All in files this plan does not touch (`src/actions/__tests__/storage-settings.test.ts`, `SignInForm`/`SignUpForm`/`ResetPasswordForm`, `form/date-picker.tsx`, and one byte-identical-to-base line in `AppSidebar.tsx:205`). Root cause: repo-wide `src/svg.d.ts` declares `*.svg` default exports as `string` while `@svgr/webpack` serves them as React components at build time, so `.svg`-icon usages with `className` fail under bare tsc; the test-file errors are strictness on possibly-undefined fields. Error set is identical before and after both tasks (no new errors introduced). Fixing these would modify unrelated files, violating the plan's frozen-files rule and "nothing else in the repo diff" success criterion — logged to `deferred-items.md` instead.

**4. [Implementation note — not a behavior deviation] `iconClassName`/`wordmarkClassName` use `override ?? default` rather than a literal `[default, override].join()`.** The plan's action text specifies "REPLACES the default classes entirely" / "full-replacement semantics"; a literal join would keep both class sets, and conflicting Tailwind utilities (`text-brand-500` vs `text-brand-400`) then resolve by stylesheet order, not attribute order — which would break the auth panel's locked-decision-5 brand-400 icon. `??` implements the specified replacement semantics deterministically; `className` still appends via the join pattern.

## Self-Check: PASSED

- All five files exist on disk and match the committed diff.
- Commits `4461ea2` and `5e5d493` present on `worktree-agent-af34253797f22b5bc`.
