---
phase: quick-260824-36g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/common/BrandLogo.tsx
  - src/layout/AppSidebar.tsx
  - src/layout/AppHeader.tsx
  - src/app/(full-width-pages)/(auth)/layout.tsx
  - src/app/(full-width-pages)/(auth)/signin/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dashboard sidebar shows the anydiscussion brand block when expanded/hovered/mobile-open (speech-bubble icon + lowercase anydiscussion wordmark) and the speech-bubble icon alone when collapsed to the 90px rail — adaptive light/dark colors, no previous-operator logo imagery left (locked decision 1)"
    - "Mobile dashboard header (< lg, beside the hamburger) shows the same brand block linking to / with adaptive colors (locked decisions 1 + 5)"
    - "Auth layout right panel (bg-brand-950, always-dark surface) shows the brand block with brand-400 icon + white wordmark and a neutral one-line anydiscussion blurb — the old company blurb is gone (locked decisions 2 + 5)"
    - "Sign-in browser tab reads 'Sign In | Any Discussion' with a dashboard-scoped description — same pipe-title convention as signup / forgot-password / reset-password and every dashboard page (locked decision 3)"
    - "public SiteHeader.tsx / SiteFooter.tsx, everything under public/images, and the old logo asset file itself are untouched (locked decision 4)"
    - "Zero occurrences of the old brand tokens under src/ (case-sensitive gates below) — the lowercase English verb 'sees' in unrelated comments is NOT branding and must NOT be edited (locked decision 1)"
  artifacts:
    - src/components/common/BrandLogo.tsx (new shared presentational brand block — icon+wordmark and icon-only variants; no hooks, no 'use client', safe in server AND client components)
    - src/layout/AppSidebar.tsx (both logo branches swapped; unused next/image import removed)
    - src/layout/AppHeader.tsx (mobile logo swapped; unused next/image import removed)
    - src/app/(full-width-pages)/(auth)/layout.tsx (panel logo + blurb swapped; unused next/image import removed)
    - src/app/(full-width-pages)/(auth)/signin/page.tsx (metadata title + description)
  key_links:
    - "BrandLogo → imported by AppSidebar (client component), AppHeader (client component), and the (auth) layout (server component) — one component, both rendering contexts; zero props are required for the default look"
    - "BrandLogo's SVG path is a byte-copy of the SiteHeader.tsx brand block path (lines 95-99) — SiteHeader itself is NOT modified (locked decision 4); the component's header comment must state the two copies are kept in sync"
    - "Existing link wrappers survive: AppSidebar Link href='/dashboard' and AppHeader / auth-layout Link href='/' keep their href and responsive classes — only the logo children inside them swap"
---

<objective>
Replace every remnant of the previous operator's branding on dashboard/auth surfaces with the anydiscussion brand block (speech-bubble icon + lowercase "anydiscussion" wordmark) that the public site header already ships, and fix the sign-in page metadata. Five files, presentation only, zero functionality changes.

Purpose: The dashboard still carries the previous organization's logo image and company blurb (introduced by quick task 260708-n9b); the product is now anydiscussion, and every surface a user or admin sees must say so — brand-consistent with the 260823-6je site header restyle.

Output: One new shared BrandLogo component plus four de-branded surfaces (sidebar, mobile header, auth panel, sign-in metadata).

Locked user decisions implemented (do NOT revisit):
- Decision 1: Remove ALL old-brand logo imagery and ALL old-brand text from dashboard/auth surfaces; replace with the anydiscussion brand block (speech-bubble icon + lowercase "anydiscussion" wordmark).
- Decision 2: The auth layout's company blurb is replaced with an anydiscussion-appropriate line — planner pick, static, simple (spec'd verbatim in Task 2; no settings/DB read).
- Decision 3: Metadata titles become "Sign In | Any Discussion" — the decision's example was illustrative ("e.g."), and its parenthetical instructs matching existing title conventions, which are uniformly "… | Any Discussion" (verified: signup, forgot-password, reset-password, all dashboard pages).
- Decision 4: Do NOT touch the public site header/footer, dashboard functionality, or delete the old logo asset file itself (other projects may reference it; just stop using it).
- Decision 5: Keep light/dark support; wordmark/icon colors adapt per surface (sidebar/header adaptive via dark: variants; auth panel is an always-dark brand-950 surface → white wordmark + brand-400 icon).

Key facts verified during planning (executors can trust these):
- Old-brand occurrences under src/ are EXACTLY four files: src/layout/AppSidebar.tsx lines 357-362 (expanded logo Image) and 364-370 (collapsed logo Image, /images/logo/logo-icon.svg), src/layout/AppHeader.tsx lines 87-92 (mobile lg:hidden logo Image), src/app/(full-width-pages)/(auth)/layout.tsx line 28 (panel logo Image) + line 33 (company blurb <p>), src/app/(full-width-pages)/(auth)/signin/page.tsx lines 5-6 (metadata title + description). All other grep hits for the lowercase verb "sees" are English prose in comments of unrelated files — leave them.
- src/app/(full-width-pages)/(auth)/signup/page.tsx metadata is ALREADY clean ("Create Admin Account | Any Discussion") — no edit needed there; Task 2 re-verifies it read-only.
- The brand block to copy lives in src/components/site/SiteHeader.tsx lines 86-105: Link > span.text-brand-500.dark:text-brand-400 > svg (h-8 w-8, viewBox="0 0 24 24", fill="currentColor", aria-hidden, one <path fillRule="evenodd" clipRule="evenodd" d="M6 4H18A3 3 0 0 1 21 7V14A3 3 0 0 1 18 17H11.5L8 20.3V17H6A3 3 0 0 1 3 14V7A3 3 0 0 1 6 4ZM7.5 9.1A1.4 1.4 0 1 0 7.5 11.9A1.4 1.4 0 1 0 7.5 9.1ZM12 9.1A1.4 1.4 0 1 0 12 11.9A1.4 1.4 0 1 0 12 9.1ZM16.5 9.1A1.4 1.4 0 1 0 16.5 11.9A1.4 1.4 0 1 0 16.5 9.1Z" />) + sibling span.text-xl.font-bold.tracking-tight.text-gray-900.dark:text-white containing the literal lowercase word "anydiscussion".
- Image-import safety (verified by grep): AppSidebar has exactly two <Image usages (both being replaced → remove the `import Image from "next/image"` at line 29); AppHeader has exactly one (line 87 → remove its import at line 6); the (auth) layout has exactly one (line 25 → remove its import at line 5).
- /images/logo/logo-icon.svg is referenced ONLY by AppSidebar line 365 (TailAdmin's generic template icon, not project branding) — swapping the collapsed branch to the brand icon orphans the asset but nothing else breaks. The asset file is NOT deleted (same no-delete rule as decision 4).
- The brand color tokens (brand-400/brand-500/brand-950) already exist in the Tailwind theme — the auth panel itself uses bg-brand-950 today; no CSS/theme edits belong in this task.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Source of the brand block to reuse (read the lines 80-110 region; do not edit this file):
@src/components/site/SiteHeader.tsx

Surfaces being edited:
@src/layout/AppSidebar.tsx
@src/layout/AppHeader.tsx
@src/app/(full-width-pages)/(auth)/layout.tsx
@src/app/(full-width-pages)/(auth)/signin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared BrandLogo component + dashboard chrome swap (sidebar + mobile header)</name>
  <files>src/components/common/BrandLogo.tsx, src/layout/AppSidebar.tsx, src/layout/AppHeader.tsx</files>
  <action>
  A. Create src/components/common/BrandLogo.tsx — a pure presentational server-and-client-safe component (NO "use client" directive, no hooks). Props contract: wordmark?: boolean (default true; false renders the icon alone), iconClassName?: string (REPLACES the icon wrapper's default classes entirely), wordmarkClassName?: string (REPLACES the wordmark span's default classes entirely), className?: string (appended to the outer span). Defaults must reproduce the SiteHeader brand block exactly: outer span "flex items-center gap-2"; icon wrapper span "h-8 w-8 text-brand-500 dark:text-brand-400" containing the speech-bubble svg with className "h-full w-full", viewBox "0 0 24 24", fill "currentColor", aria-hidden="true", and the exact single <path> copied byte-for-byte from src/components/site/SiteHeader.tsx line 98 (path quoted verbatim in this plan's objective); wordmark span "text-xl font-bold tracking-tight text-gray-900 dark:text-white" containing the literal lowercase text anydiscussion. Merge optional props with the simple [default, override].filter(Boolean).join(" ") pattern (full-replacement semantics for iconClassName/wordmarkClassName; className appends). Component header comment (1-3 lines): states this duplicates the SiteHeader.tsx brand block on purpose because SiteHeader is frozen for this task (260824-36g locked decision 4) and the two copies must be kept in sync. Comment discipline: comments in this file must NOT contain the old-brand tokens used by Task 2's verify gates — say "brand swap" / "260824-36g", never name the old asset or the previous operator.

  B. src/layout/AppSidebar.tsx — inside the existing Link href="/dashboard" (line 355): replace the expanded-branch Image element (lines 356-362) with a bare BrandLogo default render; replace the collapsed-branch Image element (lines 363-370, the generic template icon) with the icon-only variant (wordmark=false) — planner call per locked decision 1: the collapsed 90px rail gets the same brand mark so the sidebar is coherent in both states; the 32px default icon fits the rail as-is. Keep the ternary structure, the justify-center collapsed layout, and all surrounding classes untouched. Remove the now-unused "import Image from "next/image"" (line 29) — verified this file has no other Image usage.

  C. src/layout/AppHeader.tsx — inside the existing Link href="/" className="lg:hidden" (line 86): replace only the Image element (lines 87-92) with a bare BrandLogo default render. Keep the Link, its lg:hidden class, and sibling buttons untouched. Remove the now-unused next/image import (line 6) — verified sole usage. The header bar is white/dark adaptive, so the component defaults (gray-900/white wordmark, brand-500/400 icon) satisfy locked decision 5 with no overrides.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && { grep -rn "sees-logo" src/layout | wc -l; } </automated>
  </verify>
  <done>tsc clean; the two layout files contain zero references to the old logo path; sidebar renders brand block expanded + brand icon collapsed; mobile header renders brand block; both next/image imports removed; no comment in the three files contains a Task 2 gate token.</done>
</task>

<task type="auto">
  <name>Task 2: Auth panel de-brand (logo + blurb) + sign-in metadata + straggler sweep</name>
  <files>src/app/(full-width-pages)/(auth)/layout.tsx, src/app/(full-width-pages)/(auth)/signin/page.tsx</files>
  <action>
  A. src/app/(full-width-pages)/(auth)/layout.tsx — inside the existing Link href="/" (line 24): replace the Image element (lines 25-30) with BrandLogo using iconClassName "h-10 w-10 text-brand-400" and wordmarkClassName "text-2xl font-bold tracking-tight text-white". Rationale (locked decision 5): this panel is bg-brand-950 dark:bg-white/5 — an always-dark brand surface, so the icon uses the lighter brand-400 tone and the wordmark is white in both themes; the slightly larger sizing (40px icon, text-2xl) replaces the previous 231x48 image's visual weight. Replace the company blurb <p> (line 33) text with exactly: A home for curious minds — read, write, and discuss ideas worth sharing on anydiscussion.com. (locked decision 2 — planner pick, static, simple; keep the existing text-center text-gray-400 dark:text-white/60 classes and the surrounding max-w-xs container). Remove the now-unused next/image import (line 5) — verified sole usage in this file. GridShape, ThemeTogglerTwo, ThemeProvider wiring all stay untouched.

  B. src/app/(full-width-pages)/(auth)/signin/page.tsx — set metadata title to "Sign In | Any Discussion" and description to "Sign in to the anydiscussion dashboard." (locked decision 3: "Any Discussion" spelling per the codebase-wide pipe-title convention — signup, forgot-password, reset-password, and every dashboard page use it; lowercase brand spelling in the description matches the wordmark). Note the current title also has a stray double space — the rewrite removes it.

  C. Read-only confirmations + straggler sweep (no edits): signup/page.tsx metadata is already "Create Admin Account | Any Discussion" (verified during planning — leave it). Do NOT edit the lowercase English verb "sees" in comments of unrelated files (src/lib/auth/index.ts line 31, src/app/(admin)/layout.tsx line 22, src/app/api/auth/google/callback/route.ts lines 18+72, src/actions/pages.ts line 150, src/actions/contact.ts line 93, src/components/site/ContactForm.tsx line 86) — prose, not branding. Do NOT touch anything under src/components/site/ or public/images/ and do NOT delete the old logo asset (locked decision 4). Then run the gates below; if any hit, fix the hit file only.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && test -f public/images/logo/sees-logo.png && [ "$(grep -rn "sees-logo" src/ | wc -l)" -eq 0 ] && [ "$(grep -rnw "SEES" src/ | wc -l)" -eq 0 ] && [ "$(git diff --name-only | grep -cE "components/site|public/images")" -eq 0 ] && pnpm build</automated>
  </verify>
  <done>Build green; auth panel shows brand block (brand-400 icon + white wordmark) + neutral blurb; sign-in tab reads "Sign In | Any Discussion"; zero case-sensitive old-brand token occurrences under src/; old logo asset still on disk; git diff touches no site component and no public asset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational markup swap in already-auth-gated surfaces — no input handling, no data flow, no auth logic touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260824-36g-01 | Tampering | src/components/common/BrandLogo.tsx (new file) | low | mitigate | New component contains only static JSX copied from the reviewed SiteHeader block — no props reach HTML injection surfaces (all props are className strings applied via template join, never dangerouslySetInnerHTML); code review at commit covers drift |
| T-260824-36g-02 | Information disclosure | signin/page.tsx metadata | low | accept | Static title/description only — no user or site data disclosed |
</threat_model>

<verification>
- pnpm build passes (definitive gate: the swapped components compile in client context — AppSidebar/AppHeader — and server context — auth layout).
- All Task 2 automated gates hold repo-wide: no old-logo path under src/, no case-sensitive old-brand token under src/, asset file intact, no site-component or public-image diffs.
- Visual spot-check (optional, manual): dashboard at desktop width (expanded + collapsed sidebar), at mobile width (header logo), and /signin (panel + tab title) show the anydiscussion brand block with working light/dark toggle.
</verification>

<success_criteria>
- All five files_modified changed as specified and nothing else in the repo diff (apart from the plan/summary docs).
- Dashboard sidebar (both states), mobile dashboard header, and auth panel all render the anydiscussion speech-bubble brand block; no previous-operator imagery or company text remains anywhere under src/.
- Sign-in metadata follows the "… | Any Discussion" convention.
- Public site header/footer and the old logo asset are byte-untouched; pnpm build is green.
</success_criteria>

<output>
Create `.planning/quick/260824-36g-de-brand-dashboard-from-sees-to-anydiscu/260824-36g-SUMMARY.md` when done
</output>
