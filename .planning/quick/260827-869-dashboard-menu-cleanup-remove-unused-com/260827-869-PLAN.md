---
quick_id: 260827-869
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layout/AppSidebar.tsx
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/AdminShell.tsx
  - src/layout/AppHeader.tsx
  - src/components/header/UserDropdown.tsx
  - src/app/(admin)/dashboard/profile/ProfileForm.tsx
  - src/app/(admin)/dashboard/profile/page.tsx
  - src/app/(admin)/dashboard/profile/PasswordForm.tsx
  - src/actions/users-schema.ts
  - src/actions/users.ts
  - src/actions/__tests__/users.test.ts
files_deleted:
  - src/app/(admin)/(ui-elements)/            # all 7 showcase pages — only entry point was the removed sidebar group
  - src/app/(admin)/dashboard/calendar/page.tsx
  - src/components/calendar/                   # Calendar.tsx — imported only by the deleted calendar page
  - src/components/example/                    # ModalExample/* — imported only by the deleted modals page
  - src/components/ui/video/                   # VideosExample.tsx — imported only by the deleted videos page
  - src/components/ui/images/                  # ResponsiveImage/TwoColumn/ThreeColumn grids — only the deleted images page
  - src/components/common/ComponentCard.tsx    # only consumers were the showcase pages + demos above
autonomous: true
must_haves:
  truths:
    - Dashboard sidebar shows NO "Components" group and NO "Calendar" entry
    - Sidebar has exactly ONE Settings section (the second group, label renamed) containing Storage, SEO, Backup, Newsletter (admin-gated) and Profile (all roles)
    - Header user thumb, display name, and dropdown identity show the signed-in user's real avatar/name/email from the session — not TailAdmin demo data
    - Saving the profile form refreshes the header so a new name/avatar appears immediately
    - The profile page has a Change password card; changing requires the current password, and a wrong current password shows a friendly error (production flight-safe via digest)
  artifacts:
    - src/layout/AppSidebar.tsx (restructured nav data)
    - src/components/header/UserDropdown.tsx (props-driven identity)
    - src/app/(admin)/dashboard/profile/PasswordForm.tsx (new)
    - src/actions/users.ts changeOwnPassword action
    - src/actions/users-schema.ts changePasswordSchema + digests
  key_links:
    - (admin)/layout.tsx AuthGate → AdminShell → AppHeader → UserDropdown prop chain (user identity)
    - PasswordForm → changeOwnPassword Server Action → auth.api.changePassword (verified present in installed better-auth 1.6.23, dist/api/routes/update-user.mjs:75)
---

<objective>
Dashboard menu cleanup + profile/header identity sync + self-service password change.

Purpose: The TailAdmin-derived sidebar still ships demo navigation (Components showcase, Calendar), splits settings across two groups ("Menu" holds the Settings parent, a leftover group labeled for reference material sits below), the header shows hardcoded demo identity (portrait, "Musharof", "randomuser@pimjo.com") instead of the signed-in user, and the profile page cannot change the account password.

Output: A focused CMS sidebar with a single Settings section (settings routes + Profile), demo routes/components deleted, header identity driven by session data, and a tested self-service password-change flow.

Owner decision note: This request supersedes 04-CONTEXT D-02's "preserve the (ui-elements) living reference" stance — the owner is now confident in the TailAdmin kit and explicitly asked for removal. Update the stale CITED comments in AppSidebar.tsx accordingly (D-05 UX-only role filter and server-side RBAC notes stay valid).
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/CLAUDE.md
@.planning/STATE.md

Key source files (read before editing):
- src/layout/AppSidebar.tsx — navItems + othersItems arrays, group labels, hasRole UX-only filter, auto-open submenu effect
- src/app/(admin)/layout.tsx — AuthGate (server, has session) currently passes only `role` to AdminShell
- src/app/(admin)/AdminShell.tsx — client shell wiring AppSidebar/AppHeader
- src/components/header/UserDropdown.tsx — hardcoded identity to replace
- src/app/(admin)/dashboard/profile/page.tsx + ProfileForm.tsx — session-driven profile form (pattern for identity shape + additionalFields cast)
- src/actions/users.ts + users-schema.ts — action conventions: getSessionOrThrow/requireCan first, Zod gate, digest-carrying errors, headers-forwarding rule (260824-qtu)
- src/actions/__tests__/users.test.ts — vi.hoisted mock harness to extend
- node_modules/better-auth/dist/api/routes/update-user.mjs (changePassword endpoint, verified): body { currentPassword (required, verified against credential hash), newPassword (min/max length enforced — default min 8 since src/lib/auth/index.ts sets no custom password config), revokeOtherSessions?: boolean } — with revokeOtherSessions:true it deletes all user sessions, creates a fresh one and sets the session cookie. Gated by sensitiveSessionMiddleware (session resolved from request headers → MUST forward headers).

Verified usage facts (grep-proven, do not re-litigate):
- /dashboard/calendar is referenced ONLY by the sidebar entry; src/components/calendar/Calendar.tsx is imported ONLY by the calendar page.
- The (ui-elements) routes (/alerts, /avatars, /badge, /buttons, /images, /modals, /videos) are referenced ONLY from AppSidebar.tsx othersItems.
- After deleting those pages, ComponentCard, ModalExample/*, VideosExample, and the ui/images grids have zero remaining importers (grep-verified) — delete them. KEEP components/ui/{alert,avatar,badge,button,modal,dropdown,...} (used by real features, e.g. Badge in UsersTable, Modal in ConfirmDialog).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar restructure — remove demo groups, unify Settings section, delete orphaned demo routes/components</name>
  <files>src/layout/AppSidebar.tsx; delete src/app/(admin)/(ui-elements)/, src/app/(admin)/dashboard/calendar/page.tsx, src/components/calendar/, src/components/example/, src/components/ui/video/, src/components/ui/images/, src/components/common/ComponentCard.tsx</files>
  <action>
Edit src/layout/AppSidebar.tsx (keep the component mechanics — submenu render machinery, hasRole filter, auto-open effect — intact; only the data and labels change):

1. navItems (main "Menu" group) becomes exactly: Posts, Categories, Tags, Media, Pages, Users (requiredRole admin), Subscribers (requiredRole admin). REMOVE the Settings parent-with-subItems entry, the Profile entry, and the Calendar entry from navItems.
2. Replace othersItems with a flat settingsItems array (rename the variable and the visibleOthersItems derivation consistently): direct-link items — Storage → /dashboard/settings/storage, SEO → /dashboard/settings/seo, Backup → /dashboard/settings/backup, Newsletter → /dashboard/settings/newsletter (all four requiredRole "admin"), then Profile → /dashboard/profile (NO role gate — every role manages their own profile, matching D-09 self-service). Carry over the per-item history comments (Plan 04-05 Storage, 05-03 SEO, 08-04 Backup, 260824-3l2 Newsletter) so provenance survives the move. Give each item an icon already available in the existing import set; drop icon imports that become unused (the calendar icon and the table icon used by the old showcase group; keep PlugInIcon only if reused).
3. Change the second group's rendered heading label (the uppercase text next to the "Menu" heading, currently the leftover showcase label) to "Settings". The collapsed-state icon behavior stays as-is.
4. Update the file's header comment block: the showcase/reference and Calendar notes are now obsolete (superseded by this owner request); keep the D-05 UX-only role-filter note and the D-01 /dashboard/* prefix note accurate for the new structure.

Deletions (grep first per constraint — references already verified in planning context): delete the seven showcase page folders under src/app/(admin)/(ui-elements)/, the calendar page, and the now-orphaned components: src/components/calendar/, src/components/example/, src/components/ui/video/, src/components/ui/images/, src/components/common/ComponentCard.tsx. Do NOT touch src/components/ui/{alert,avatar,badge,button,modal}, src/components/common/PageBreadCrumb.tsx, or any shared component — they have live consumers.

pnpm only for any command.
  </action>
  <verify>
    <automated>cd /d/Devsroom-Work/anydiscussion &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; bash -c 'grep -c "CalenderIcon\|TableIcon" src/layout/AppSidebar.tsx | grep -qx "0" ; grep -q "Settings" src/layout/AppSidebar.tsx &amp;&amp; ! grep -q "Reference" src/layout/AppSidebar.tsx &amp;&amp; grep -q "settingsItems" src/layout/AppSidebar.tsx &amp;&amp; ! grep -rq "components/calendar\|ComponentCard\|ModalExample\|VideosExample\|ui/images\|(ui-elements)" src --include="*.tsx" --include="*.ts"'</automated>
  </verify>
  <done>
    Sidebar renders one Menu group (Posts/Categories/Tags/Media/Pages/Users/Subscribers) and one Settings group (Storage/SEO/Backup/Newsletter admin-gated + Profile for all roles); no Components group; no Calendar entry; showcase + calendar routes and their orphaned demo components are gone; tsc clean; no dangling imports of deleted files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Header identity sync — session user data drives the header thumb, name, and dropdown; profile save refreshes the header</name>
  <files>src/app/(admin)/layout.tsx, src/app/(admin)/AdminShell.tsx, src/layout/AppHeader.tsx, src/components/header/UserDropdown.tsx, src/app/(admin)/dashboard/profile/ProfileForm.tsx</files>
  <action>
Thread the signed-in user's identity from the server auth gate down to the dropdown (prop chain — all serializable):

1. src/app/(admin)/layout.tsx AuthGate already holds the session. Derive a headerUser object { name: string; email: string; avatar: string | null } exactly the way profile/page.tsx does (avatar via the additionalFields cast on session.user) and pass it to AdminShell alongside the existing role prop.
2. src/app/(admin)/AdminShell.tsx: accept the user prop and forward it to AppHeader.
3. src/layout/AppHeader.tsx: accept the user prop and forward it to UserDropdown.
4. src/components/header/UserDropdown.tsx: replace ALL hardcoded identity (the TailAdmin demo portrait path, the first-name label, and the demo full-name/email lines in the dropdown header) with props. Avatar rendering: when avatar is a non-empty string, render it with next/image (same 44x44 rounded-full treatment; avatar values follow the imageUrlSchema contract — root-relative /api/media or absolute CDN URLs — already rendered via next/image by ProfileForm, so no loader/config change needed). When avatar is null/empty, render a rounded-full div with the user's initials (first letters of the first two name words, uppercased) on a brand-toned background — never a stock photo of a different person. Collapse the three demo entries that all point at the profile route into a single "Edit profile" DropdownItem linking /dashboard/profile. Leave the Sign out link exactly as-is (pre-existing behavior, explicitly out of scope for this task).
5. src/app/(admin)/dashboard/profile/ProfileForm.tsx: import useRouter from next/navigation and call router.refresh() inside the mutation's onSuccess (alongside the existing queryClient.invalidateQueries) so the server-rendered (admin) layout re-fetches the session and the header thumb/name update immediately after a profile save.

Both the profile page and the header now read from the same session source of truth — the "sync" the owner asked for. Keep pnpm-only commands.
  </action>
  <verify>
    <automated>cd /d/Devsroom-Work/anydiscussion &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; bash -c '! grep -q "owner.jpg\|Musharof\|randomuser" src/components/header/UserDropdown.tsx &amp;&amp; grep -q "router.refresh" "src/app/(admin)/dashboard/profile/ProfileForm.tsx"'</automated>
  </verify>
  <done>
    Header thumb + display name + dropdown identity render the signed-in user's real avatar/name/email; users without an avatar get an initials fallback; saving the profile updates the header without a manual reload; no demo identity strings remain in UserDropdown; tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Self-service password change — Zod schema, digest-mapped Server Action, profile page card, regression tests</name>
  <files>src/actions/users-schema.ts, src/actions/users.ts, src/app/(admin)/dashboard/profile/PasswordForm.tsx (new), src/app/(admin)/dashboard/profile/page.tsx, src/actions/__tests__/users.test.ts</files>
  <behavior>
    - Test: no session → getSessionOrThrow rejects and auth.api.changePassword is NEVER reached (mock it to throw MUST_NOT_BE_REACHED).
    - Test: newPassword shorter than 8 chars (or empty currentPassword) → action throws INVALID_INPUT before auth.api.changePassword is reached.
    - Test: happy path → auth.api.changePassword called with forwarded headers (260824-qtu header-forwarding rule) and body { currentPassword, newPassword, revokeOtherSessions: true }.
    - Test: endpoint rejects with body.code INVALID_PASSWORD (wrong current password) → action rethrows an Error whose digest === "INVALID_PASSWORD".
  </behavior>
  <action>
1. src/actions/users-schema.ts — add (pure sibling module, follows the existing digest pattern):
   - changePasswordSchema: currentPassword z.string().min(1).max(128), newPassword z.string().min(8).max(128) — min 8 mirrors Better Auth's default minPasswordLength (src/lib/auth/index.ts sets no custom password config; the endpoint enforces its own bounds too — the Zod gate is the early client+server contract).
   - CHANGE_PASSWORD_DIGESTS = { INVALID_PASSWORD: "INVALID_PASSWORD", CHANGE_FAILED: "CHANGE_FAILED" } and CHANGE_PASSWORD_ERROR_MESSAGES map: INVALID_PASSWORD → "Your current password is incorrect.", CHANGE_FAILED → "Failed to update password — please try again." (CR-02: thrown .message never reaches the production client; digests do).
2. src/actions/users.ts — add exported async changeOwnPassword(input: { currentPassword: string; newPassword: string }):
   a. const session = await getSessionOrThrow() FIRST — self-service for any signed-in role; NO requireCan (this is the user's own credential, not an admin-gated resource; there is no userId parameter so it can never target another user).
   b. safeParse input against changePasswordSchema → throw Error("INVALID_INPUT") on failure.
   c. Call auth.api.changePassword({ headers: await headers(), body: { currentPassword, newPassword, revokeOtherSessions: true } }) — headers forwarded per the 260824-qtu middleware rule (endpoint resolves the session from request headers); revokeOtherSessions: true is the standard post-change hardening (other devices signed out; the endpoint sets a fresh session cookie for the current one).
   d. Wrap in try/catch: extract the Better Auth error code (err.body?.code), map INVALID_PASSWORD → throw a digest-carrying Error via a local helper mirroring deleteUserGuardError; anything else → CHANGE_FAILED. Log per T-Q-04 convention (log.info "password changed" only after the endpoint resolves; log.error on failure) — never rethrow the raw APIError.
3. src/actions/__tests__/users.test.ts — extend the vi.hoisted block with changePasswordMock and register it on the mocked auth.api (flat, like the others); add the four behavior tests above following the file's existing mock/reset patterns.
4. src/app/(admin)/dashboard/profile/PasswordForm.tsx — NEW client component, modeled on ProfileForm.tsx: "use client"; RHF + zodResolver + TanStack useMutation (non-optimistic); fields currentPassword / newPassword / confirmPassword (all type="password", autoComplete attributes current-password / new-password); local form schema = changePasswordSchema fields + confirmPassword with superRefine (confirm must equal newPassword; newPassword must differ from currentPassword); submit calls changeOwnPassword; on mutation.error map err.digest via CHANGE_PASSWORD_ERROR_MESSAGES with a CHANGE_FAILED fallback message (same digest-mapping pattern UsersTable uses); success banner "Password updated."; reuse ProfileForm's INPUT_CLASS styling and error/success banner markup so the two cards look identical.
5. src/app/(admin)/dashboard/profile/page.tsx — render a second card below the profile card (same rounded-2xl card wrapper + heading treatment): title "Change password", one-line hint that other signed-in devices will be signed out, then <PasswordForm />. No props needed — the action self-resolves the session.

pnpm only. RED first: add the tests, run them against the not-yet-existing action (fail), then implement schema+action+UI to green.
  </action>
  <verify>
    <automated>cd /d/Devsroom-Work/anydiscussion &amp;&amp; pnpm exec vitest run src/actions/__tests__/users.test.ts &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    Profile page shows a Change password card; the action verifies the session, validates input with the shared Zod schema, forwards request headers, requires the current password (verified by Better Auth against the credential hash), revokes other sessions, and maps wrong-current-password to a production-safe friendly digest error; all new tests pass; existing 600+ suite stays green; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → changeOwnPassword Server Action | Untrusted input (passwords) crosses here; self-service, any role |
| Server Action → Better Auth /change-password | Sensitive: current-password verification + session rewrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-869-01 | Spoofing | changeOwnPassword | high | mitigate | getSessionOrThrow first; no userId param (cannot target others); Better Auth verifies currentPassword against the credential hash server-side; action never trusts client-side auth state |
| T-Q-869-02 | Brute force | currentPassword guessing | medium | mitigate | Session required (not anonymous); Better Auth rate limit (Redis-backed, Plan 07-02) covers auth endpoints; Zod length bounds |
| T-Q-869-03 | Information disclosure | error mapping | medium | mitigate | Digest-only client contract (CR-02 pattern) — no hash internals or raw APIError text cross the flight boundary; INVALID_INPUT generic |
| T-Q-869-04 | Session tampering / stale access | post-change sessions | medium | mitigate | revokeOtherSessions: true — other devices signed out; fresh cookie set by the endpoint |
| T-Q-869-05 | Elevation of privilege | sidebar flatten | low | accept | UX-only reorganization; authoritative RBAC unchanged server-side in every mutating action (D-05 note preserved); settings actions all re-check requireRole admin |
| T-Q-869-06 | Repudiation | password-change audit | low | accept | log.info fires only after endpoint resolution (T-Q-04 convention); no separate audit table in scope |
</threat_model>

<verification>
1. pnpm exec vitest run src/actions/__tests__/users.test.ts — all pass (4 new + existing).
2. pnpm exec tsc --noEmit — clean.
3. Full suite spot-check: pnpm exec vitest run — stays green (no import breakage from deletions).
4. Manual smoke (if dev server running, owner UAT): sidebar shows Menu + Settings only; header thumb/name reflect the signed-in user; profile save updates header; wrong current password shows the friendly error; correct change succeeds and other sessions are revoked.
</verification>

<success_criteria>
- All three user-request items shipped: (1) unused menus removed with their orphaned pages/components, (2) one unified Settings section containing the settings routes and Profile with header identity synced to profile data, (3) working password update on the profile page.
- No dangling imports; tsc clean; vitest green including the four new password-action tests.
- Security conventions held: session check first, shared Zod schema client+server, current-password verification kept (Better Auth default), digest-mapped production errors, headers forwarded, pnpm only.
</success_criteria>

<output>
Create `.planning/quick/260827-869-dashboard-menu-cleanup-remove-unused-com/260827-869-SUMMARY.md` when done
</output>
