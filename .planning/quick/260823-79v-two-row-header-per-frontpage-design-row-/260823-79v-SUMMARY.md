---
phase: quick-260823-79v
plan: 01
subsystem: public-site
tags: [site-header, sticky-header, categories, cache-components, pure-helpers, social-links, css-sticky]
requires:
  - "listCategoriesWithCounts (src/lib/queries/taxonomy) — cached, cacheTag('posts-list'); consumed UNCHANGED by both the row-1 dropdown and row 2"
  - "getSeoSettings (src/lib/seo/settings) — cached, cacheTag('seo-settings')"
  - "pickSocialLinks + FooterCategoryLite (src/lib/footer-links, from 260823-6je) — reused as-is"
  - "260823-6je header content (brand block, 5 nav entries, circular search button, ThemeToggle) — carried into row 1 untouched"
provides:
  - "src/lib/queries/social-links.ts — shared cached readSocialLinks ('use cache' + cacheTag seo-settings) + SocialLinkSettings interface; consumed by BOTH footer and header row 2"
  - "SOCIAL_ICON_PATHS exported from src/lib/footer-links.ts — single source for footer + header social SVG paths"
  - "src/lib/header-bar.ts — pure generic boundHeaderBarCategories (bounded ~10, postCount desc, name-asc tie-break) + 6 node-env tests"
  - "SiteHeader two-row structure — non-sticky white row 1 + pure-CSS sticky gray category bar row 2 with configured-only social circles"
affects:
  - "Every (site) route — header is shared layout chrome (visual/structure only; no route logic changed)"
  - "SiteFooter — import sources for readSocialLinks/SOCIAL_ICON_PATHS changed; rendered output byte-identical"
tech-stack:
  added: []
  patterns:
    - "pure-CSS sticky second header row via two-sibling fragment under the layout's full-height flex column — zero client JS (no scroll listeners / IntersectionObserver)"
    - "shared cached-query module for a settings read consumed by two chrome components (extract, do not duplicate)"
    - "generic pure bound-helper <T extends CategoryLite> preserving DB row fields (id for React keys) through copy-sort-slice"
key-files:
  created:
    - src/lib/queries/social-links.ts
    - src/lib/header-bar.ts
    - src/lib/__tests__/header-bar.test.ts
  modified:
    - src/components/site/SiteHeader.tsx
    - src/components/site/SiteFooter.tsx
    - src/lib/footer-links.ts
decisions:
  - "Row 1 keeps every 260823-6je element but drops ALL positioning (no sticky/z-40/backdrop-blur/white-95) — solid white/gray-900, scrolls away (locked decision 1)"
  - "Row 2 renders conditionally (only when barCategories or socialLinks are non-empty) — graceful zero-state for fresh DBs; sticky top-0 z-40 exactly (the value the old single header had), so the row-1 dropdown's z-50 panel still paints above it"
  - "Row-2 roster = boundHeaderBarCategories(listCategoriesWithCounts(), 10) — most-published first; horizontal overflow (overflow-x-auto + whitespace-nowrap, hidden scrollbars) is the degrade path for long Bangla names; never truncate or wrap"
  - "Social circles on row 2 reuse pickSocialLinks + SOCIAL_ICON_PATHS verbatim — configured-only (footer.social_* keys), target=_blank + rel=noopener noreferrer, hidden below sm (mobile keeps the category strip as its only navigation)"
  - "boundHeaderBarCategories made generic so listCategoriesWithCounts rows keep their id through the pipeline — the plan locks key={cat.id} while FooterCategoryLite has no id"
metrics:
  duration: 14 min
  completed: 2026-08-23
status: complete
---

# Quick Task 260823-79v: Two-row header per frontpage design Summary

**One-liner:** Public SiteHeader restructured into a non-sticky white nav row + a pure-CSS sticky gray category bar (bounded ~10 most-published DB categories left, configured-only social circles right), with readSocialLinks/SOCIAL_ICON_PATHS extracted into shared modules consumed by both header and footer.

## What Was Built

### Task 1: Shared socials modules (extraction, not duplication) — 2f9c2ef
- `src/lib/queries/social-links.ts` — `readSocialLinks` moved verbatim from SiteFooter.tsx (same `'use cache'` + `cacheTag("seo-settings")`, same single `Promise.all` round-trip, same explicit `{ twitter, facebook, linkedin }` shape now also exported as `SocialLinkSettings`). Private `SOCIAL_KEYS` + the why-cache-required rationale carried over, updated to name BOTH consumers (footer + header row 2). Comment hygiene: no client-directive literal, no async-signature phrase in comments.
- `src/lib/footer-links.ts` — gains exported `SOCIAL_ICON_PATHS` (three SVG path strings verbatim, `Record<"twitter" | "facebook" | "linkedin", string>`, keys match `FooterSocialLink["key"]`). Existing exports and tests untouched.
- `src/components/site/SiteFooter.tsx` — imports-only rewiring: local `readSocialLinks`/`SOCIAL_KEYS`/`SOCIAL_ICON_PATHS` declarations and the now-unused `db`/`eq` imports deleted; new imports from the shared modules. Markup, classes, and its own `'use cache'` boundary with BOTH `cacheTag("seo-settings")` + `cacheTag("posts-list")` byte-identical. One CITED line notes the extraction.

### Task 2: Pure boundHeaderBarCategories helper (TDD) — a50edc3 (RED) + fa7c563 (GREEN)
- `src/lib/__tests__/header-bar.test.ts` written first — 6 node-env vitest cases mirroring the footer-links test style: empty input, default limit 10 (11 in → 10 highest out), custom limit 3-of-5, postCount-desc + name-asc localeCompare tie-break, non-mutation, extra-field rows surviving intact.
- `src/lib/header-bar.ts` — pure module (no db/react/next imports): copy → sort (`b.postCount - a.postCount || a.name.localeCompare(b.name)`, same comparator semantics as `boundFooterCategories`) → slice(limit). RED confirmed against the missing module, then GREEN.
- Follow-up c4577f3 (see Deviations): generic `<T extends FooterCategoryLite>` signature.

### Task 3: SiteHeader two-row restructure — 9deadfb
- Fragment of TWO SIBLINGS (no wrapper div) — both direct children of the layout's `div.flex.min-h-screen.flex-col`, the same ancestor chain the old sticky header proved.
- `Promise.all` extended to three cached reads: `getSeoSettings()` + `listCategoriesWithCounts()` + `readSocialLinks()`; derives `barCategories = boundHeaderBarCategories(categories)` and `socialLinks = pickSocialLinks(socials)`. Component remains a non-cache-boundary pure server component.
- ROW 1 `<header>`: `w-full border-b border-gray-200 bg-white dark:border-gray-800 dark:bg-gray-900` — every positioning class dropped. Interior untouched: brand block (speech-bubble SVG + wordmark, `title={seo.siteTitle}`), all 5 nav entries incl. Categories hover-dropdown (group-relative invisible/opacity + z-50 panel kept exactly), circular outlined search link, `<ThemeToggle />`; nav keeps `hidden md:flex`; no overflow-hidden.
- ROW 2 `<nav aria-label="Categories">`: rendered only when `barCategories.length > 0 || socialLinks.length > 0`; `sticky top-0 z-40 w-full border-b border-gray-200 bg-gray-100/95 backdrop-blur dark:border-gray-800 dark:bg-gray-900/95`, inner `mx-auto flex h-11 max-w-6xl items-center justify-between gap-4 px-4`.
  - LEFT: `flex min-w-0 flex-1 items-center gap-5 overflow-x-auto whitespace-nowrap py-0.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden` — Links `/category/{slug}`, `key={cat.id}`, name-only labels (no counts), `text-xs sm:text-sm` gray-600/300 with brand-safe hovers.
  - RIGHT: `hidden shrink-0 items-center gap-2.5 sm:flex` — anchors (target=_blank, rel="noopener noreferrer", aria-label) styled as h-7 w-7 subdued circles with brand hover, each wrapping an `h-3.5 w-3.5` SVG from `SOCIAL_ICON_PATHS[social.key]`. Zero circles when nothing configured.
- Pure-CSS sticky only — no scroll listeners, no IntersectionObserver, no effects. Zero `use client` tokens anywhere in the file (the old comment quoting the directive literal was rewritten).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] boundHeaderBarCategories return type dropped `id`, blocking the locked `key={cat.id}`**
- **Found during:** Task 3 (SiteHeader implementation)
- **Issue:** The plan locks row-2 links to `key={cat.id}`, but the declared return type `FooterCategoryLite[]` (no `id`) would make `cat.id` a TypeScript error even though the runtime rows carry it.
- **Fix:** Made the helper generic — `<T extends FooterCategoryLite>(categories: readonly T[], limit = 10): T[]` — so `listCategoriesWithCounts` rows keep `id`/`description` through the copy-sort-slice pipeline. Runtime behavior unchanged; committed separately for atomicity.
- **Files modified:** src/lib/header-bar.ts
- **Verification:** All 6 header-bar tests re-run green; full suite green; build exit 0.
- **Committed in:** c4577f3

**2. [Rule 3 - Blocking] Dev database unreachable — Docker engine down**
- **Found during:** Task 1 verification
- **Issue:** `pnpm build` failed with `ECONNREFUSED` — the docker-compose dev Postgres (host port 5435) was down because Docker Desktop's engine was stopped (it had been running during the previous task's worktree). Worktree setup itself (pnpm install + gitignored `.env.local` copy, verified via `git check-ignore`) followed the previous task's precedent.
- **Fix:** Started Docker Desktop (engine auto-resumed the main checkout's `anydiscussion-postgres-1` on 5435, healthy); removed the stray compose project/volume my initial `docker compose up` attempt created in the worktree (port already allocated by the real DB); re-ran the build green. No repo files changed.
- **Files modified:** none (local environment only)
- **Verification:** `pnpm build` exit 0 against the real dev DB.
- **Committed in:** n/a (environment)

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes unblocked verification without scope creep; rendered output, cache contracts, and locked decisions all intact.

## Known Stubs

| File | Location | Reason |
|------|----------|--------|
| src/components/site/SiteFooter.tsx | Newsletter column (input + Subscribe button) | Pre-existing intentional stub from 260823-6je locked decision 2 (inert visuals, no form/Server Action). Untouched by this task — imports-only rewiring. Backend wiring explicitly deferred to a later task. |

## Verification Results

- `pnpm test`: 54 files / 537 tests green (531 pre-existing + 6 new header-bar cases; footer-links tests untouched and green — proof the extraction changed no behavior).
- `pnpm build`: production build exit 0 — Cache Components compliant (header awaits only cached reads; footer boundary unchanged), both rows compile as server components.
- Task 1 grep gates: `queries/social-links` in SiteFooter = 1 (import only); `cacheTag("posts-list")` = 1 and `cacheTag("seo-settings")` = 1 on the footer's own boundary; `SOCIAL_ICON_PATHS` in footer-links = 2 (declaration + comment).
- Task 3 grep gates: `<header` lines containing `sticky` = 0; `sticky top-0 z-40` = 1 (exactly one band); `use client` tokens = 0 file-wide; `boundHeaderBarCategories` = 4; `overflow-x-auto` = 2; `<ThemeToggle />` = 1.
- Scope check: `git diff --name-only` across all task commits = exactly the 6 planned files. SearchForm.tsx, "frontpage design.png" (git-tracked in the base tree despite the plan calling it untracked — untouched either way), ThemeToggle, dashboard files, and page backgrounds all untouched; no file deletions in any commit.
- Threat model: T-79v-01 mitigated (row-2 anchors keep target=_blank + rel="noopener noreferrer"; URLs render as href values only; no dangerouslySetInnerHTML introduced); T-79v-02/03/SC accepted as documented; no new threat surface beyond the plan's register.

## Self-Check: PASSED

- Files exist: src/lib/queries/social-links.ts, src/lib/header-bar.ts, src/lib/__tests__/header-bar.test.ts, src/components/site/SiteHeader.tsx, src/components/site/SiteFooter.tsx, src/lib/footer-links.ts — all FOUND.
- Commits exist: 2f9c2ef, a50edc3, fa7c563, c4577f3, 9deadfb — all FOUND on worktree-agent-a12c239ea1ee0093f.

---
*Quick task: 260823-79v*
*Completed: 2026-08-23*
