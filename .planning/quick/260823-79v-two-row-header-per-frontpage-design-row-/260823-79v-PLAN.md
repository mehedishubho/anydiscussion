---
phase: quick-260823-79v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries/social-links.ts
  - src/lib/header-bar.ts
  - src/lib/__tests__/header-bar.test.ts
  - src/lib/footer-links.ts
  - src/components/site/SiteFooter.tsx
  - src/components/site/SiteHeader.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Header ROW 1 keeps every current element — speech-bubble logo + wordmark, all 5 nav entries (Home, Blog, Categories hover-dropdown fed by listCategoriesWithCounts, About, Contact), circular outlined search button, ThemeToggle — on a white bg that is NO LONGER sticky: it scrolls away with the page (locked decision 1)"
    - "Header ROW 2 is a gray band (gray-100 light / gray-900 dark + border-b, backdrop-blur) that pins to the viewport top via pure CSS sticky when row 1 scrolls off — no scroll listeners, no IntersectionObserver, no client JS of any kind (locked decisions 2 + 3)"
    - "Row 2 lists REAL DB categories, bounded ~10, most-published first, degrading to horizontal scroll (overflow, no wrap) when names are long — plus small subtle social circles on the right that render ONLY for configured settings keys (footer.social_twitter / footer.social_facebook / footer.social_linkedin): no Instagram, no dead links (locked decision 2)"
    - "SiteHeader remains a pure server component; ThemeToggle stays the only client island; the header's caching posture is unchanged — it awaits cached functions (getSeoSettings, listCategoriesWithCounts, and the extracted readSocialLinks) without itself being a cache boundary (locked decisions 3 + 5)"
    - "SiteFooter's rendered output and its own 'use cache' boundary (BOTH tags: seo-settings AND posts-list) are byte-identical — the only footer change is WHERE readSocialLinks and the social icon paths are imported from (locked decision 4 + constraints: extract, do not duplicate)"
    - "Untouched: src/components/site/SearchForm.tsx (carries the user's uncommitted edits — never modify, stage, or revert it), 'frontpage design.png', every dashboard/(admin) file, and the mobile nav posture (row-1 nav keeps hidden md:flex) (locked decision 4)"
    - "Outfit font, brand palette, light/dark support all preserved — row 2 uses the existing gray + brand token system, no page background changes (locked decision 5)"
  artifacts:
    - src/lib/queries/social-links.ts (readSocialLinks moved verbatim from SiteFooter.tsx — 'use cache' + cacheTag seo-settings)
    - src/lib/header-bar.ts (pure boundHeaderBarCategories — no db/react/next imports)
    - src/lib/__tests__/header-bar.test.ts (node-env vitest)
    - src/lib/footer-links.ts (gains the SOCIAL_ICON_PATHS export moved from SiteFooter.tsx; existing exports unchanged)
    - src/components/site/SiteFooter.tsx (imports-only rewiring — zero markup/logic changes)
    - src/components/site/SiteHeader.tsx (restructured into two sibling rows)
  key_links:
    - "readSocialLinks (@/lib/queries/social-links, cacheTag seo-settings) → pickSocialLinks (@/lib/footer-links, reused as-is) → row-2 social anchors (target=_blank + rel=noopener noreferrer)"
    - "listCategoriesWithCounts (UNCHANGED, cacheTag posts-list) → boundHeaderBarCategories(…, 10) → row-2 /category/{slug} links"
    - "row-2 sticky top-0 z-40 ↔ parent div.flex.min-h-screen.flex-col in src/app/(site)/layout.tsx — the sticky context is PROVEN: the current single-row header already sticks inside this exact ancestor chain"
    - "SiteFooter own boundary cacheTag('seo-settings') + cacheTag('posts-list') ↔ revalidateTag calls in src/actions/settings.ts + src/actions/categories.ts — unchanged by this task"
---

<objective>
Restructure the public SiteHeader into the two-row pattern from "frontpage design.png": row 1 white and non-sticky (logo, 5 nav links with Categories dropdown, circular search button, ThemeToggle — all current content), row 2 a sticky-on-scroll gray category bar (dynamic DB categories left, configured social icons right). Pure CSS sticky only — zero client JS.

Purpose: Match the frontpage design's two-tier header while giving readers a persistent category navigation, building directly on quick task 260823-6je's restyled header without redoing any of it.

Output: Shared socials modules (extracted from SiteFooter, not duplicated), pure header-bar helper + tests, two-row SiteHeader.

Locked user decisions implemented (do NOT revisit):
- Decision 1 (Row 1): keep ALL 5 nav links (Home, Blog, Categories hover-dropdown fed by listCategoriesWithCounts(), About, Contact) + speech-bubble logo + circular search button + ThemeToggle. White bg, NOT sticky — scrolls away. Only the positioning changes; content is exactly what 260823-6je shipped.
- Decision 2 (Row 2 sticky): horizontal bar of real DB categories (dynamic — reuse/extend the existing listCategoriesWithCounts feed; same caching posture the header has today) + configured social icons right (same settings keys and configured-only rule as the footer: footer.social_twitter / footer.social_facebook / footer.social_linkedin; NO Instagram, no dead links; circles small and subtle). sticky top-0 with a z-index that pins it above content when row 1 scrolls off; bg gray-100 light / gray-900 dark + border-b; backdrop-blur acceptable. Category links degrade gracefully when many or long (overflow-x-auto, no wrap).
- Decision 3: sticky mechanics introduce NO client JS — pure CSS sticky; ThemeToggle remains the only client island.
- Decision 4 (untouched): SearchForm.tsx, "frontpage design.png", SiteFooter (rendered output — the readSocialLinks/icon-path extraction below is the sanctioned exception: same behavior, new import source), dashboard, mobile posture (nav hidden md:flex as today; planner call: the category bar itself stays visible on mobile as a horizontal scroller — it is the ONLY navigation mobile users get — while the social cluster hides below sm).
- Decision 5: keep Outfit font, brand palette, light/dark support, existing 'use cache'/cacheTag patterns.

Key facts verified during planning (executors can trust these):
- Current SiteHeader.tsx (164 lines): single <header> with sticky top-0 z-40 + bg-white/95 backdrop-blur, h-16 max-w-6xl inner row; awaits Promise.all([getSeoSettings(), listCategoriesWithCounts()]) — both cached inside themselves, so the header component is NOT a cache boundary and that posture builds green under cacheComponents. Adding one more cached read (readSocialLinks) keeps the same proven shape.
- Sticky context is PROVEN: src/app/(site)/layout.tsx renders <SiteHeader /> as a direct child of <div className="flex min-h-screen flex-col bg-white dark:bg-gray-900"> — the current sticky header works inside exactly this ancestor chain (no overflow-hidden ancestors). A fragment returning TWO siblings preserves that chain; a sticky second child sticks within the full-height parent, i.e. for the whole page scroll.
- Stacking order works: row 1 becomes a static (unpositioned) header, so the Categories dropdown's absolute z-50 panel participates in the root stacking context and paints ABOVE row 2's sticky z-50-beating value (50 > 40) when opened at scroll-top. Give row 2 exactly z-40 (the value the old header had).
- SiteFooter.tsx holds readSocialLinks (private, 'use cache' + cacheTag("seo-settings"), returns { twitter, facebook, linkedin } each string | null — single Promise.all fetch, already cleaned in 6je) and SOCIAL_ICON_PATHS (Record keyed "twitter" | "facebook" | "linkedin" → path strings). src/lib/footer-links.ts already exports pickSocialLinks + FooterSocialLink + FooterCategoryLite (pure, 9 node-env tests green) — REUSE pickSocialLinks as-is for row 2; do not fork it.
- listCategoriesWithCounts() (src/lib/queries/taxonomy.ts L56-81) returns {id, name, slug, description, postCount} ordered name-asc, counting only published non-deleted posts, cached with cacheLife("hours") + cacheTag("posts-list"). DO NOT modify it — the bar re-sorts client-side-of-the-server (in the component) via the new pure helper.
- Design measurements (from pixel analysis — no need to re-open the image): row 2 band = rgb(241,242,243) ≈ Tailwind gray-100; small text (~13-14px); row height ~44px vs row 1's 64px (h-11 vs h-16); ~8-10 category links left, 4 small social icons far right.
- src/components/site/SearchForm.tsx currently carries UNCOMMITTED user edits (git status M) — it is read-only for this task.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@./.claude/CLAUDE.md
@.planning/quick/260823-6je-restyle-public-siteheader-sitefooter-to-/260823-6je-SUMMARY.md

Design reference (OPTIONAL — layout measurements are already embedded in the objective):
- "frontpage design.png" (repo root — READ-ONLY, never modify or commit it)

Core source files (read all before editing):
- src/components/site/SiteHeader.tsx — the component being restructured (sticky single header, all row-1 content already correct from 6je)
- src/components/site/SiteFooter.tsx — source of readSocialLinks + SOCIAL_ICON_PATHS to extract; its own cache boundary (L105-107) must survive untouched
- src/lib/footer-links.ts — pure module to extend with SOCIAL_ICON_PATHS; pickSocialLinks/FooterCategoryLite reused as-is
- src/lib/queries/taxonomy.ts — cached-query-in-lib precedent + the listCategoriesWithCounts contract (read-only)
- src/lib/__tests__/footer-links.test.ts — the node-env test style to mirror for header-bar tests
- src/app/(site)/layout.tsx — the sticky ancestor chain (read-only; confirms fragment-sibling approach)
- src/app/globals.css — brand ramp tokens (grep brand-500 / font-outfit)

Hard DO-NOT-TOUCH list (locked decision 4):
- src/components/site/SearchForm.tsx (uncommitted user edits — never modify, stage, or revert)
- "frontpage design.png" (untracked at repo root)
- Any dashboard/(admin) file
- src/components/site/ThemeToggle.tsx (rendered as-is)
- The row-1 nav's hidden md:flex mobile posture
- Page backgrounds anywhere — the gray band is the bar element only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared socials modules (readSocialLinks → lib/queries, icon paths → footer-links)</name>
  <files>src/lib/queries/social-links.ts, src/lib/footer-links.ts, src/components/site/SiteFooter.tsx</files>
  <action>
  Extraction, not duplication (per this task's constraints). SiteFooter's rendered output and cache contract stay byte-identical — the ONLY footer diff is two deletions plus two imports.

  1. Create src/lib/queries/social-links.ts — mirror the header-comment + doc posture of src/lib/queries/taxonomy.ts, citing this task. Move the readSocialLinks function VERBATIM from SiteFooter.tsx: same Promise.all single-round-trip body, same 'use cache' directive, same cacheTag("seo-settings") call, same explicit return shape { twitter: string | null; facebook: string | null; linkedin: string | null }. Export it, and export that shape as a named interface (e.g. SocialLinkSettings) so the header can annotate. Keep the private SOCIAL_KEYS const (the three footer.social_* settings keys) inside this module — carry over its explanatory comment about why the cache tag exists (seo-settings invalidation via saveSeoSettings). Keep the "why 'use cache' is required under cacheComponents" rationale comment, updated to note BOTH consumers (footer + header row 2).
  2. Extend src/lib/footer-links.ts: add an exported SOCIAL_ICON_PATHS constant — the three Twitter/Facebook/LinkedIn SVG path strings moved verbatim from SiteFooter.tsx, typed Record<"twitter" | "facebook" | "linkedin", string>, with a short doc note that the keys match FooterSocialLink["key"] and that header row 2 and the footer both render from this single source. Do NOT touch the existing pickSocialLinks / boundFooterCategories / types or their tests.
  3. Rewire src/components/site/SiteFooter.tsx minimally: delete its local readSocialLinks definition (the whole function with its doc comment) and its local SOCIAL_KEYS + SOCIAL_ICON_PATHS declarations; import readSocialLinks from "@/lib/queries/social-links" and SOCIAL_ICON_PATHS from "@/lib/footer-links" (pickSocialLinks + boundFooterCategories already import from there). NOTHING else in the footer changes — markup, classes, its own 'use cache' boundary with BOTH cacheTag calls, the Promise.all fetch shape, all stay exactly as-is. Update the footer's [CITED] header comments to note the extraction (one line).
  4. Hygiene for later gates: when commenting, never quote any client directive literal and never write an async-signature phrase for readSocialLinks into a comment — later automated greps count those tokens.

  Atomic commit: refactor(260823-79v): extract readSocialLinks + social icon paths to shared modules
  </action>
  <verify>
    <automated>pnpm test && pnpm build && [ "$(grep -c 'queries/social-links' src/components/site/SiteFooter.tsx)" = "1" ] && [ "$(grep -c 'cacheTag("posts-list")' src/components/site/SiteFooter.tsx)" -ge "1" ] && [ "$(grep -c 'cacheTag("seo-settings")' src/components/site/SiteFooter.tsx)" -ge "1" ] && [ "$(grep -c 'SOCIAL_ICON_PATHS' src/lib/footer-links.ts)" -ge "1" ]</automated>
  </verify>
  <done>
  readSocialLinks lives in src/lib/queries/social-links.ts with its cache directive + tag intact; SOCIAL_ICON_PATHS exported from the pure module src/lib/footer-links.ts; SiteFooter compiles with imports-only changes and still declares BOTH cache tags on its own boundary; full test suite + production build green (footer behavior provably unchanged).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure boundHeaderBarCategories helper + tests (TDD)</name>
  <files>src/lib/header-bar.ts, src/lib/__tests__/header-bar.test.ts</files>
  <behavior>
    - boundHeaderBarCategories: empty input returns an empty array.
    - Default limit is 10 — 11 categories in, 10 out, and the survivors are the 10 highest postCounts.
    - Custom limit honored (e.g. limit 3 of 5 in → 3 out, most-published first).
    - Sorts by postCount descending; equal postCounts tie-break name ascending via localeCompare (same comparator semantics as boundFooterCategories — deliberate consistency).
    - Does not mutate the input array (original order and length preserved after the call).
    - Accepts rows carrying extra fields (listCategoriesWithCounts rows structurally satisfy the category-lite shape).
  </behavior>
  <action>
  Create src/lib/header-bar.ts — a PURE module (no db, no react, no next imports — same posture as src/lib/footer-links.ts so node-env vitest can import it). Reuse FooterCategoryLite from "@/lib/footer-links" as the parameter/return element type (pure→pure import is fine); document that rows from listCategoriesWithCounts structurally satisfy it. Export function boundHeaderBarCategories(categories: readonly FooterCategoryLite[], limit = 10): FooterCategoryLite[] — non-mutating: copy, sort postCount desc with name-asc (localeCompare) tie-break BEFORE slicing, then slice to limit. This is locked decision 2's bounded roster: most-published categories fill the bar; zero-count ones only appear when the roster is short; horizontal overflow (Task 3) is the degrade path for long names, so no truncation logic here.

  Write src/lib/__tests__/header-bar.test.ts FIRST (mirror src/lib/__tests__/footer-links.test.ts style — node env, describe/it/expect) covering every behavior-block case above (~6-7 focused cases). Run to see it fail (RED), then implement header-bar.ts to green.

  Atomic commit: test(260823-79v): header bar category-bound helper (TDD) — then, after GREEN, fold any implementation tweaks in as: feat(260823-79v): header-bar pure helper
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
  boundHeaderBarCategories exported from a pure module with zero server imports; all new vitest cases green; full existing suite still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: SiteHeader two-row restructure — non-sticky white row 1 + sticky gray category bar row 2</name>
  <files>src/components/site/SiteHeader.tsx</files>
  <action>
  Locked decisions 1-5. The component returns a fragment of TWO SIBLING elements (no wrapper div — both become direct children of the layout's full-height flex column, the same ancestor chain the current sticky header already sticks in):

  0. Data: extend the existing Promise.all to THREE cached reads — getSeoSettings(), listCategoriesWithCounts(), readSocialLinks() (now from "@/lib/queries/social-links"). Derive barCategories = boundHeaderBarCategories(categories) and socialLinks = pickSocialLinks(socials) (both imports — pickSocialLinks + SOCIAL_ICON_PATHS from "@/lib/footer-links", boundHeaderBarCategories from "@/lib/header-bar"). The component stays a pure server component with no client directive anywhere in the file — and rewrite the existing file-header comment that currently quotes that directive phrase, stating "pure server component — zero client directives" WITHOUT quoting the literal (the automated gate greps the whole file for it). seo keeps feeding the logo Link's title attribute exactly as today.

  1. ROW 1 (locked decision 1): the current <header> element loses ALL positioning — drop the sticky, top-0, z-40, and backdrop-blur classes and the /95 opacity; it becomes className "w-full border-b border-gray-200 bg-white dark:border-gray-800 dark:bg-gray-900" (solid white / gray-900). EVERYTHING inside is untouched: brand block (speech-bubble SVG + lowercase bold wordmark, Link title={seo.siteTitle}), all 5 nav entries incl. the Categories hover-dropdown with chevron + counts (keep the group-relative invisible/opacity mechanism and its z-50 panel exactly), circular outlined search Link, ThemeToggle. Inner container classes (mx-auto flex h-16 max-w-6xl items-center justify-between gap-4 px-4) unchanged. The nav keeps its hidden md:flex posture (locked decision 4). No overflow-hidden on the header element (the dropdown must escape it).

  2. ROW 2 (locked decision 2): render conditionally — only when barCategories.length > 0 OR socialLinks.length > 0 (a fully empty bar renders nothing; graceful zero-state for fresh DBs). Element: a <nav aria-label="Categories"> with className "sticky top-0 z-40 w-full border-b border-gray-200 bg-gray-100/95 backdrop-blur dark:border-gray-800 dark:bg-gray-900/95". Inner: "mx-auto flex h-11 max-w-6xl items-center justify-between gap-4 px-4".
     a. LEFT — category links: a cluster div "flex min-w-0 flex-1 items-center gap-5 overflow-x-auto whitespace-nowrap py-0.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden" mapping barCategories to Links href /category/{cat.slug}, key cat.id, label cat.name only (no counts on the bar), classes "whitespace-nowrap text-xs font-medium text-gray-600 transition-colors hover:text-gray-900 sm:text-sm dark:text-gray-300 dark:hover:text-white". The overflow-x-auto + whitespace-nowrap IS the degrade path for many/long (Bangla) names — never truncate or wrap.
     b. RIGHT — social circles (locked decision 2's configured-only rule): a cluster div "hidden shrink-0 items-center gap-2.5 sm:flex" mapping socialLinks to anchors (href social.url, target _blank, rel noopener noreferrer, aria-label social.label) styled "flex h-7 w-7 items-center justify-center rounded-full border border-gray-300 text-gray-500 transition-colors hover:border-brand-500 hover:text-brand-600 dark:border-gray-600 dark:text-gray-400 dark:hover:border-brand-400 dark:hover:text-brand-400" — smaller and more subdued than the footer's h-9 circles, per "small and subtle". Inside each, an svg className "h-3.5 w-3.5" fill currentColor viewBox 0 0 24 24 aria-hidden with path d = SOCIAL_ICON_PATHS[social.key]. Zero circles when nothing is configured — never a placeholder link, never Instagram. The sm: hide is the planner's mobile call: mobile keeps the scrolling category strip (its only navigation) and sheds the redundant icons.

  3. Sticky mechanics (locked decision 3): NOTHING but the Tailwind sticky classes above — no scroll listener, no IntersectionObserver, no effect, no client directive. ThemeToggle remains the file's only interactive piece.

  4. Update the [CITED] header comments to reference this task's locked decisions 1-5 and the two-row structure; drop comment text describing the old single-row sticky posture. Keep comment hygiene: no client-directive literal anywhere in the file.

  Atomic commit: feat(260823-79v): two-row header — non-sticky white nav row + sticky category bar
  </action>
  <verify>
    <automated>pnpm test && pnpm build && [ "$(grep -- '<header' src/components/site/SiteHeader.tsx | grep -c sticky)" = "0" ] && [ "$(grep -c 'sticky top-0 z-40' src/components/site/SiteHeader.tsx)" -ge "1" ] && [ "$(grep -c 'use client' src/components/site/SiteHeader.tsx)" = "0" ] && [ "$(grep -c 'boundHeaderBarCategories' src/components/site/SiteHeader.tsx)" -ge "2" ] && [ "$(grep -c 'overflow-x-auto' src/components/site/SiteHeader.tsx)" -ge "1" ] && [ "$(grep -c '<ThemeToggle />' src/components/site/SiteHeader.tsx)" = "1" ]</automated>
  </verify>
  <done>
  Row 1 renders all current header content on solid white with zero positioning classes and scrolls away; row 2 pins top-0 z-40 as a gray bordered band (gray-100/gray-900 + blur) listing ~10 most-published DB categories that scroll horizontally when long, with subtle configured-only social circles right (hidden below sm); pure CSS sticky — the file has no client directive token anywhere; dropdown still opens above the band at scroll-top; tests + production build green (Cache Components compliant, server component).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings DB rows → public markup | Social URLs (admin-configured) render as external anchors in row 2 |
| categories DB rows → public links | Category names/slugs render as in-app links in row 2 |
| viewport scroll → overlay | The sticky band floats over page content while scrolled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79v-01 | Tampering | Row-2 social anchors (settings-sourced URLs) | low | mitigate | Same mitigation as the footer (T-6je-01): URLs writable only via admin-gated saveSeoSettings; anchors keep target=_blank + rel="noopener noreferrer"; URLs render as href attribute values only — no dangerouslySetInnerHTML anywhere in this task |
| T-79v-02 | Spoofing | Row-2 category links | low | accept | Slugs render into template-built /category/{slug} paths via next/link — same trust level as the existing header dropdown and footer column; no raw HTML from DB |
| T-79v-03 | Tampering | Sticky overlay covering content | low | accept | Pure CSS sticky at z-40 (the value the old single-row header already used); the row-1 dropdown's z-50 panel still paints above it; no JS scroll handlers introduced, so no scroll-jacking surface |
| T-79v-SC | Tampering | package installs | n/a | accept | No package installs in this task — Tailwind utility classes + inline SVG only |
</threat_model>

<verification>
- pnpm test — full vitest suite green including the new header-bar tests; footer-links tests untouched and green (proof the extraction changed no behavior).
- pnpm build — production build green: Cache Components compliant (header awaits only cached reads; footer boundary unchanged), type-safe, both header rows compile as server components.
- Grep gates (Task 1 + Task 3): shared-module imports wired; footer keeps both cache tags; header opening tag carries no sticky classes; exactly one sticky band; zero client-directive tokens in SiteHeader.tsx (comments included); bound helper used; overflow degrade present; ThemeToggle still rendered once.
- Scope check via git status before each commit: src/components/site/SearchForm.tsx must appear only with its PRE-EXISTING user modification (never staged by this task), "frontpage design.png" untouched/untracked, no dashboard files, ThemeToggle untouched.
- Manual spot-check (optional, local dev): light + dark — row 1 scrolls away, row 2 pins to top with content sliding beneath the blurred band; long/many categories scroll horizontally without wrapping; social circles appear only for configured keys; Categories dropdown in row 1 opens ABOVE the pinned band; mobile: category strip scrolls, no social icons below sm.
</verification>

<success_criteria>
- Locked decisions 1-5 fully implemented with no scope drift: all 5 nav entries intact, pure-CSS sticky only, SiteFooter output byte-identical, SearchForm.tsx / design.png / dashboard / mobile-nav posture untouched.
- Zero client JS added to the public site — SiteHeader remains a server component; ThemeToggle stays the only client island.
- Socials and categories flow through shared/pure modules (readSocialLinks, SOCIAL_ICON_PATHS, pickSocialLinks, boundHeaderBarCategories) — nothing duplicated between header and footer.
- Three atomic commits, one per task.
</success_criteria>

<output>
Create `.planning/quick/260823-79v-two-row-header-per-frontpage-design-row-/260823-79v-SUMMARY.md` when done
</output>
