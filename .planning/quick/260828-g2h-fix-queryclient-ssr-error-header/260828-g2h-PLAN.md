---
quick_id: 260828-g2h
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Task 1 — QueryProvider hoist (RED/GREEN, one task)
  - src/app/(admin)/__tests__/AdminShell.test.tsx   # NEW — regression + probe test
  - src/app/(admin)/AdminShell.tsx                  # hoist QueryProvider over AppHeader + page content; D-28 comment updates
  - src/app/(admin)/QueryProvider.tsx               # comment-only update (no code change)
autonomous: true
must_haves:
  truths:
    - A signed-in dashboard page renders with the header search input and the notification bell present and NO "No QueryClient set" recoverable-error banner / fallback-to-client-rendering (the regression introduced by 260827-se8's header islands)
    - Dashboard page content still resolves TanStack queries after the hoist — children remain inside the provider (proven by a useQueryClient probe child)
    - TanStack JS still never reaches the public bundle — QueryProvider remains imported ONLY by AdminShell (D-28 / PERF-02); root layout and (site) untouched
  artifacts:
    - src/app/(admin)/__tests__/AdminShell.test.tsx — (a) regression test rendering AdminShell with NO outer QueryClientProvider and asserting both header islands render, (b) probe test proving {children} stay inside the provider
    - src/app/(admin)/AdminShell.tsx — QueryProvider wraps AppHeader AND the p-4 page-content div; stale "wraps {children}" D-28 comments updated
    - src/app/(admin)/QueryProvider.tsx — comment-only scope note (AdminShell mounts it around header + page content since this task); zero code change
  key_links:
    - "AdminShell render tree after the fix: flex-1 main-content div → QueryProvider → { AppHeader (GlobalSearch useQuery + NotificationDropdown useQueryClient/useQuery) + p-4 page-content div → {children} }; AppSidebar, Backdrop, and Toaster stay OUTSIDE the provider, exactly where they are today"
    - "D-28 static guard unchanged: src/app/(admin)/AdminShell.tsx remains the ONLY file matching `import QueryProvider` (grep gate); ESLint no-restricted-imports (site)↔(admin) path rules untouched — the hoist moves where the provider renders, never who imports it"
---

<objective>
Fix the SSR "No QueryClient set" error on every dashboard page. Since 260827-se8 put live TanStack Query islands into the header (GlobalSearch, NotificationDropdown), the dashboard shell renders its header OUTSIDE the QueryProvider: AdminShell.tsx mounts AppHeader (L73) as a sibling ABOVE the provider that wraps only {children} (L74-77). The islands' useQuery/useQueryClient calls throw during server rendering, so the page falls back to client rendering with a recoverable-error banner (user-observed on the live dev server).

Purpose: the header is part of the shell on EVERY dashboard route — the error banner degrades all of them; and the fix must not silently erode D-28 (admin)-only bundle isolation, the Phase 7 PERF-02 budget's foundation.

Output: One task, RED/GREEN. A regression test pinning "AdminShell supplies the QueryClient to its own header without any outer provider"; the provider hoisted to wrap AppHeader + page content (import site unchanged, so D-28 static and dynamic boundaries hold); the two stale D-28 comments corrected.

Executor constraints (from orchestrator): pnpm only; do NOT run `pnpm dev` (the orchestrator's dev server owns :3000); no build required (no new modules, no config touched — tsc + vitest cover the change); `pnpm exec tsc --noEmit` exits non-zero at the documented 8-error TailAdmin scaffold baseline (table in .planning/quick/260827-se8-dashboard-functional-gaps-make-dashboard/deferred-items.md) — the gate is NO NEW errors, not a zero exit code.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/CLAUDE.md
@.planning/STATE.md

Key source files (read before editing — all verified this session):

- src/app/(admin)/AdminShell.tsx — the bug site. Render tree L63-84: AppSidebar + Backdrop, then the flex-1 main-content div containing AppHeader (L73) and the p-4 page-content div whose ONLY content is QueryProvider wrapping {children} (L74-77). Toaster + sidebar stay put. Stale comments: D-28 block L32-36 says "wraps {children} with QueryProvider"; inline comment L74 says "Page Content — QueryProvider scoped to (admin) only".
- src/app/(admin)/QueryProvider.tsx — the single (admin) QueryClient (useState lazy init, staleTime 30s, refetchOnWindowFocus false, dev-only ReactQueryDevtools at L43). Scope comment L12-17: "imported ONLY by `(admin)/AdminShell.tsx`" — still true after the hoist; only its "wraps children … dashboard page/components" wording needs the header added.
- src/layout/AppHeader.tsx — mounts GlobalSearch + NotificationDropdown (imports L3-4); receives `user` (HeaderUser) from AdminShell.
- src/components/header/GlobalSearch.tsx — useQuery(["global-search", debounced]) L102-106, enabled only at ≥2 chars (so it never fires in the test); throws at render without a provider ancestor.
- src/components/header/NotificationDropdown.tsx — useQueryClient() at L79 and useQuery L81-94 run at MOUNT (unread count query fires immediately); this is the component that throws first today.
- src/app/(admin)/layout.tsx — server AuthGate invoking AdminShell with role + user props (the test passes the same prop shapes).
- Test pattern to follow: src/app/(admin)/dashboard/settings/backup/__tests__/BackupSettingsForm.test.tsx — `// @vitest-environment jsdom` pragma, vi.hoisted action spies + vi.mock so "use server" modules (and their drizzle imports) never load in jsdom, @testing-library/react render/screen.

Verified facts (do not re-derive):

- vitest.config.ts: default environment is node; component tests opt into jsdom per file via the `// @vitest-environment jsdom` pragma. No setup file exists.
- The whole AdminShell subtree needs exactly TWO context wrappers in jsdom: SidebarProvider (used by AdminShell, AppHeader, AppSidebar, Backdrop — throws "useSidebar must be used within a SidebarProvider") and ThemeProvider (used by AppHeader's ThemeToggleButton — throws "useTheme must be used within a ThemeProvider"). Both only touch window.innerWidth/localStorage, which jsdom provides; NO matchMedia anywhere in this tree.
- ReactQueryDevtools renders only when NODE_ENV === "development" (QueryProvider.tsx L43); vitest runs NODE_ENV=test, so devtools never mounts in the test — no mock needed.
- An avatar of null makes UserDropdown render the initials fallback — no next/image load in the test.
- D-28's ESLint guard (eslint.config.mjs) is path-based no-restricted-imports ((site)↔(admin) cross-group imports) — a render-tree move inside AdminShell cannot affect it.
- The word "QueryProvider" appears in island COMMENTS too (GlobalSearch.tsx L9, NotificationDropdown.tsx L7), so the isolation gate must grep `import QueryProvider`, not the bare component name.
- tsc baseline: exactly 8 pre-existing errors, all in TailAdmin scaffold files (deferred-items.md table). Gate = count stays ≤ 8.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hoist QueryProvider over AppHeader + page content — regression test RED, hoist GREEN, D-28 comments refreshed</name>
  <files>src/app/(admin)/__tests__/AdminShell.test.tsx, src/app/(admin)/AdminShell.tsx, src/app/(admin)/QueryProvider.tsx</files>
  <behavior>
    - Regression (RED today): rendering AdminShell with NO outer QueryClientProvider (wrapped only in SidebarProvider + ThemeProvider) throws "No QueryClient set" — after the hoist the same render succeeds and shows both header islands: the search input (placeholder "Search or type command...") and the bell button (accessible name beginning "Notifications")
    - Page content still renders inside the shell in the same test (the children text node appears in the document)
    - Probe (pins the refactor against overcorrection): a child component calling useQueryClient() inside {children} renders its data-testid="query-client-probe" marker instead of throwing — children must remain inside the provider after the hoist
    - Bell mount-time query never crosses the client/server boundary in the test: countUnreadNotifications is mocked to resolve 0 (the unread useQuery fires at mount); globalSearch/listNotifications/markNotificationsRead are mocked for safety even though the test never triggers them
  </behavior>
  <action>
    RED first — create src/app/(admin)/__tests__/AdminShell.test.tsx following the BackupSettingsForm.test.tsx conventions: `// @vitest-environment jsdom` pragma; vi.hoisted spies + vi.mock for BOTH @/actions/search (globalSearch → resolves empty groups: posts/users/categories/tags all empty arrays) and @/actions/notifications (countUnreadNotifications → resolves 0, listNotifications → resolves [], markNotificationsRead → resolves ok), so no "use server"/drizzle module ever loads in jsdom. Test user prop: name "Shell Test User", email "shell.test@example.com", avatar null (initials fallback, no next/image). Render helper wraps AdminShell (role "admin") in ThemeProvider then SidebarProvider from @/context — and deliberately NO QueryClientProvider anywhere (that absence IS the regression). Three tests: (1) renders the header islands — assert screen.getByPlaceholderText("Search or type command...") and screen.getByRole("button", { name: /^Notifications/i }) both exist, plus the children text ("shell page content") is visible; (2) the useQueryClient probe child inside {children} renders its data-testid="query-client-probe" marker; (3) whatever factoring of helpers the file needs stays local (no shared exports). Run the targeted vitest command — test (1) MUST fail with the thrown "No QueryClient set" error (that is the captured regression; test (2) passes today and guards the hoist). Commit `test(260828-g2h): AdminShell regression RED — header islands throw without provider`.

    GREEN — in src/app/(admin)/AdminShell.tsx move the QueryProvider tags so the provider, inside the flex-1 main-content div, wraps BOTH AppHeader AND the p-4 page-content div; {children} now sits directly inside the p-4 div (no provider nesting at that level anymore). AppSidebar, Backdrop, the mainContentMargin logic, every className, and the Toaster (props + position, outside the provider) stay byte-identical. Keep ONE provider instance — do not double-wrap.

    Comment updates (same GREEN commit): (a) AdminShell.tsx D-28 block (L32-36) — replace the "wraps {children}" wording with: the provider wraps the header AND page content so the 260827-se8 header islands (GlobalSearch, NotificationDropdown) get the QueryClient during SSR — the outside-the-provider placement threw at server render and forced the client-render fallback; this task is 260828-g2h; the D-28 guarantee itself is unchanged (provider still INSIDE AdminShell, never root layout, never (site)). (b) The inline comment at the old provider site (L74) — update to say the provider wraps header + page content, (admin)-scoped only. (c) src/app/(admin)/QueryProvider.tsx header comment — code untouched; keep the "imported ONLY by `(admin)/AdminShell.tsx`" scope statement verbatim, and extend the one wrapping-description sentence to note AdminShell mounts it around AppHeader + page content since 260828-g2h (header islands need the client at SSR).

    Do NOT touch: (site) files, src/app/layout.tsx, eslint.config.mjs, AppHeader, either header island, vitest.config.ts. No new packages. Commit `fix(260828-g2h): hoist QueryProvider over AppHeader + page content (GREEN)`.
  </action>
  <verify>
    <automated>pnpm exec vitest run "src/app/(admin)/__tests__/AdminShell.test.tsx" && pnpm test && grep -rln "import QueryProvider" src --include="*.tsx" --include="*.ts" | grep -q "AdminShell.tsx" && [ "$(grep -rln 'import QueryProvider' src --include='*.tsx' --include='*.ts' | wc -l)" -eq 1 ] && [ "$(pnpm exec tsc --noEmit 2>&1 | grep -c 'error TS')" -le 8 ] && echo ALL_GATES_PASS</automated>
  </verify>
  <done>All three tests green; the regression test renders AdminShell with no outer QueryClientProvider and both header islands appear; the probe proves children stay inside the provider; QueryProvider wraps AppHeader + the p-4 children container while sidebar/Backdrop/Toaster sit outside it unchanged; only AdminShell.tsx contains an `import QueryProvider` statement (D-28 intact); AdminShell.tsx and QueryProvider.tsx comments describe the actual wrap; full suite green; tsc reports no more than the 8 documented scaffold baseline errors; no pnpm dev, no build, no new packages.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (admin) bundle boundary (D-28 / PERF-02) | TanStack Query JS must not cross into the public (site) bundle — enforced by import discipline plus ESLint no-restricted-imports path rules |
| Client islands → Server Action | GlobalSearch/NotificationDropdown query actions unchanged by this fix — no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-g2h-01 | Information disclosure | QueryProvider bundle scope (D-28) | medium | mitigate | The hoist moves only WHERE the provider renders — still inside AdminShell. Grep gate in <automated> proves AdminShell.tsx is the sole importer; root layout and (site) untouched, ESLint cross-group rules unchanged |
| T-Q-g2h-02 | DoS | provider re-mount churn from the move | low | accept | Provider stays inside the same main-content div and mounts once per shell mount exactly as before; the move adds no remount path (QueryClient useState lazy init unchanged) |
</threat_model>

<verification>
- Targeted: pnpm exec vitest run "src/app/(admin)/__tests__/AdminShell.test.tsx" — 3/3 green (RED proven first at commit time: test 1 failed with the provider error before the hoist)
- Full suite: pnpm test — prior baseline 764 green + the 3 new tests, zero regressions
- Type check: pnpm exec tsc --noEmit — at most the 8 documented pre-existing scaffold errors (deferred-items.md table); no NEW errors
- D-28: exactly one file in src/ matches `import QueryProvider` and it is AdminShell.tsx (encoded in the <automated> gate)
- Owner observation (optional, no action needed from the executor): on the already-running dev server, a signed-in dashboard page reload shows the header without the recoverable-error banner — HMR picks the fix up automatically; do NOT start a second dev server
</verification>

<success_criteria>
- The 260827-se8 regression is gone and pinned: AdminShell supplies the QueryClient to its own header — regression test renders the shell with no outer provider and both islands appear
- Children remain inside the provider (probe test) — the hoist widened coverage, nothing lost
- D-28 intact: same single import site, same (admin)-only scope, (site)/root-layout untouched
- Both stale "wraps {children}" comments now describe reality
- Full suite green; tsc at baseline; two atomic commits (RED, GREEN)
</success_criteria>

<output>
Create `.planning/quick/260828-g2h-fix-queryclient-ssr-error-header/260828-g2h-SUMMARY.md` when done
</output>
