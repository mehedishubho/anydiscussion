# Deferred Items — quick task 260824-qtu

Out-of-scope discoveries logged during execution (per executor scope boundary — not fixed here).

## Pre-existing tsc errors in storage-settings.test.ts (found 2026-08-24)

- **File:** `src/actions/__tests__/storage-settings.test.ts` lines 318, 319, 321, 322
- **Errors:** TS18048 `'result.cloudinary' is possibly 'undefined'` / `'result.r2' is possibly 'undefined'` (4 total)
- **Evidence pre-existing:** `pnpm exec tsc --noEmit` on the MAIN checkout (no task changes) produces the identical 4 errors. The 260824-qtu worktree error set after `next typegen` is byte-identical to main's — this task introduced zero new type errors.
- **Why not fixed:** unrelated test file; touching it is outside this task's scope (users.ts auth call forwarding).
- **Suggested fix for a future quick task:** non-null assertions or narrowing after the schema/objectContaining shape that produces the optional fields.

## Worktree-environment note (not a code issue — for future executors)

A fresh git worktree lacks `next-env.d.ts` (gitignored, generated by `next dev`/`next build`). Without its `/// <reference types="next/image-types/global" />` reference, Next's `any`-typed `declare module '*.svg'` ambient declaration is absent, and `src/svg.d.ts`'s string-typed default export alone governs svg imports — producing ~16 bogus `TS2322 className does not exist on IntrinsicAttributes` errors in TailAdmin icon usages (SignInForm, SignUpForm, ResetPasswordForm, date-picker, AppSidebar). Fix in any fresh worktree: `pnpm exec next typegen` (output is gitignored; no commit needed). Discovered via a byte-diff of tsc error sets between main and the worktree.
