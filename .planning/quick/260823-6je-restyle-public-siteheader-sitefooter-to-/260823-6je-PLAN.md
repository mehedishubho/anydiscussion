---
phase: quick-260823-6je
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/footer-links.ts
  - src/lib/__tests__/footer-links.test.ts
  - src/components/site/SiteHeader.tsx
  - src/components/site/SiteFooter.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Site header shows an inline speech-bubble SVG icon in brand color + lowercase bold 'anydiscussion' wordmark linking to / — the SEES PNG logo no longer appears anywhere in the site header (locked decision 4); dashboard header untouched"
    - "All 5 nav entries survive, restyled to the design (Home, Blog, Categories hover-dropdown fed by listCategoriesWithCounts, About, Contact) — Blog stays one click away (locked decision 1)"
    - "Search is a circular OUTLINED icon button (rounded-full border, brand hover accent) with the ThemeToggle beside it; sticky + backdrop-blur behavior unchanged (locked decision 5)"
    - "Footer is always dark — gray-900 slab in light mode, gray-950 in dark mode, white/10 borders — with 4 columns: brand+blurb+social circles / Quick Links / dynamic Categories / Newsletter (locked decision 6 + design)"
    - "Social circles render ONLY for configured settings keys (footer.social_twitter / footer.social_facebook / footer.social_linkedin) — no Instagram, no dead '#' links (locked decision 3)"
    - "Newsletter column is frontend-only visuals: email input + Subscribe button with NO submission path of any kind, no Server Action, zero client JS on the public site (locked decision 2)"
    - "Footer Categories column lists real DB categories bounded to ~6 and the footer cache carries BOTH tags — seo-settings AND posts-list — so category mutations re-render it (locked decision 7)"
    - "Nav remains hidden on mobile exactly as today; no mobile-menu changes (locked decision 8)"
  artifacts:
    - src/lib/footer-links.ts (pure helpers: pickSocialLinks, boundFooterCategories — no db/react/next imports)
    - src/lib/__tests__/footer-links.test.ts
    - src/components/site/SiteHeader.tsx (restyled per design)
    - src/components/site/SiteFooter.tsx (always-dark 4-column restyle)
  key_links:
    - "SiteFooter cacheTag('posts-list') ↔ revalidateTag('posts-list','max') in src/actions/categories.ts (L57/L115/L144) — the tag category mutations actually revalidate"
    - "listCategoriesWithCounts() (cached, cacheTag posts-list) → boundFooterCategories(…, 6) → footer links /category/{slug}"
    - "readSocialLinks() settings rows → pickSocialLinks → conditional social-circle anchors (target=_blank rel=noopener noreferrer)"
---

<objective>
Restyle the public SiteHeader and SiteFooter to match "frontpage design.png" (repo root, read-only reference): speech-bubble logo + wordmark header with circular outlined search button, and an always-dark 4-column footer with configured-only social circles, dynamic bounded Categories, and an inert Newsletter column.

Purpose: Complete the frontpage redesign started in quick task 260823-4yc (homepage + PostCard) by upgrading the remaining public chrome, without touching the dashboard, search form, or mobile behavior.

Output: Pure footer helpers + tests, restyled SiteHeader.tsx, restyled SiteFooter.tsx.

Locked user decisions implemented (do NOT revisit):
- Decision 1: KEEP all 5 nav entries (Home, Blog, Categories dropdown, About, Contact) — design only shows 3 but Blog stays one click away. Restyle only.
- Decision 2: Newsletter column is frontend-only — email input + Subscribe button as visuals; button is inert (type="button", no Server Action, no client JS). Backend comes later.
- Decision 3: Social circles render ONLY when the URL is configured in settings (footer.social_twitter / footer.social_facebook / footer.social_linkedin). NO Instagram, no dead "#" links.
- Decision 4: Header logo = design's speech-bubble icon (inline SVG, brand color) + "anydiscussion" wordmark — replaces the SEES PNG logo in the SITE header only (dashboard header untouched).
- Decision 5: Keep sticky header, backdrop-blur, ThemeToggle next to the circular search button, Categories hover-dropdown fed by listCategoriesWithCounts(), Outfit font, brand palette (--color-brand-500 #465fff), light/dark support.
- Decision 6: Footer is ALWAYS dark: gray-900 in light mode, gray-950 in dark mode, white/10 borders.
- Decision 7: Footer Categories column is dynamic from DB, bounded ~5-6, cached consistently with the footer's existing 'use cache' pattern — the tag category mutations revalidate (posts-list) must be added to the footer.
- Decision 8: Do NOT touch src/components/site/SearchForm.tsx (uncommitted user edits), the untracked "frontpage design.png", or the dashboard header. No mobile-menu changes (nav stays hidden on mobile).

Key facts verified during planning (executors can trust these):
- listCategoriesWithCounts() (src/lib/queries/taxonomy.ts L56-81) is cached with 'use cache' + cacheLife("hours") + cacheTag("posts-list") and returns {id, name, slug, description, postCount} ordered by name asc, counting only published non-deleted posts. DO NOT modify it.
- src/actions/categories.ts calls revalidateTag("posts-list", "max") on create/update/delete (L57, L115, L144). THEREFORE the footer component itself must call cacheTag("posts-list") in addition to cacheTag("seo-settings"): the footer's own cached render output is only invalidated by tags declared on the footer's 'use cache' boundary — refreshing the nested query's cache entry alone would NOT re-render the footer. This is the exact requirement of locked decision 7.
- getSeoSettings() (src/lib/seo/settings.ts) returns siteTitle (default "Any Discussion") and siteDescription (may be empty string — footer keeps its existing fallback blurb text).
- SiteFooter already reads the three social keys via its internal readSocialLinks() ('use cache' + cacheTag seo-settings) returning {twitter, facebook, linkedin} each string|null. That helper contains dead code (a redundant first single-key query whose result is discarded via void) — clean it up while restructuring: keep only the Promise.all fetch.
- Pure-helper precedent: src/lib/post-card.ts + src/lib/__tests__/post-card.test.ts (node-env vitest, no DB mocks, no react imports) — mirror that pattern. SiteFooter.tsx itself imports next/cache + @/lib/db, so testable helpers MUST live in a separate pure module (src/lib/footer-links.ts).
- Brand tokens exist in src/app/globals.css: --color-brand-500 #465fff (+ full 25-950 ramp), font-outfit global. Because the footer slab is always dark, footer-internal classes need NO dark: variants — one white/gray palette set only.
- The header's Categories dropdown is a CSS hover dropdown (group relative + invisible/opacity pattern) — keep that mechanism exactly; only restyle surfaces.
- Current header nav styling (text-sm font-medium, gray-600/dark:gray-300 links, hover:text-gray-900/dark:hover:text-white) already matches the design's "medium-gray links with dark hover" — keep it; the header work is the logo swap, the outlined search button, and [CITED] comment updates.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@./.claude/CLAUDE.md

Design reference (read it — layout/shape only, colors/fonts from the existing Outfit + brand system):
- "frontpage design.png" (repo root — READ-ONLY, never modify or commit it)

Core source files (read all before editing):
- src/components/site/SiteHeader.tsx — component being restyled (logo const on L35, nav L59-123, search link L127-146)
- src/components/site/SiteFooter.tsx — component being restyled ('use cache' boundary L79-80, readSocialLinks L34-64, social SVG paths to REUSE L166-193)
- src/lib/queries/taxonomy.ts — listCategoriesWithCounts contract (read-only; do not modify)
- src/lib/seo/settings.ts — SeoSettings shape (siteTitle/siteDescription)
- src/actions/categories.ts — proof that posts-list is the category-mutation revalidation tag
- src/lib/post-card.ts + src/lib/__tests__/post-card.test.ts — pure-helper + node-env test precedents
- src/components/site/__tests__/pagination.test.ts — second test-style precedent
- src/app/globals.css — brand ramp + font tokens (grep brand-500 / font-outfit)

Hard DO-NOT-TOUCH list (locked decision 8):
- src/components/site/SearchForm.tsx (user has uncommitted edits)
- "frontpage design.png" (untracked at repo root)
- Any dashboard/(admin) file — the dashboard header keeps its current logo
- Mobile nav behavior — the nav element keeps its hidden md:flex posture
- src/components/site/ThemeToggle.tsx (rendered as-is)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure footer helpers (pickSocialLinks, boundFooterCategories) + tests</name>
  <files>src/lib/footer-links.ts, src/lib/__tests__/footer-links.test.ts</files>
  <behavior>
    - pickSocialLinks: all-null input returns an empty array; whitespace-only strings count as unset; only-configured keys appear; declared order twitter then facebook then linkedin is preserved; returned URLs are trimmed; each entry carries its key, an aria-friendly label ("Twitter / X", "Facebook", "LinkedIn"), and the URL.
    - boundFooterCategories: sorts by postCount descending with name-ascending (localeCompare) tie-break BEFORE slicing; default limit 6 (7 categories in → 6 out, the most-posted ones); custom limit honored (e.g. 3); empty input returns empty; does not mutate the input array.
  </behavior>
  <action>
  Create src/lib/footer-links.ts — a PURE module (no db, no react, no next imports — same posture as src/lib/post-card.ts so node-env vitest can import it):

  1. Export interface SocialLinkInput with three optional nullable string fields: twitter, facebook, linkedin (matches readSocialLinks' return shape).
  2. Export interface FooterSocialLink with fields: key ("twitter" | "facebook" | "linkedin"), label (string), url (string).
  3. Export function pickSocialLinks(input: SocialLinkInput): FooterSocialLink[] — iterate the three keys in declared order, include an entry only when the value is non-null AND non-empty after trimming, and return the trimmed URL. Use the existing aria-labels from the current footer as the label values ("Twitter / X", "Facebook", "LinkedIn") so helper output and rendered markup stay in sync.
  4. Export interface FooterCategoryLite with the minimal shape the footer renders: name (string), slug (string), postCount (number). listCategoriesWithCounts rows structurally satisfy it (they carry extra fields — fine).
  5. Export function boundFooterCategories(categories: readonly FooterCategoryLite[], limit = 6): FooterCategoryLite[] — non-mutating stable sort by postCount desc, tie-break name asc via localeCompare, then slice to limit. This is locked decision 7's "bounded ~5-6": most-published categories fill the column, zero-count ones only appear when the roster is short.

  6. Create src/lib/__tests__/footer-links.test.ts (node env, mirror post-card.test.ts style) covering every behavior-block case above — aim for ~8 focused assertions across both describe blocks.

  Atomic commit: test(260823-6je): footer social-pick + category-bound helpers (TDD)
  Then, after GREEN, if implementation tweaks were needed, fold them into the same commit as: feat(260823-6je): footer-link pure helpers
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
  Both helpers exported from a pure module with zero server imports; all new vitest assertions green; full existing suite still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: SiteHeader restyle — speech-bubble logo + wordmark, circular outlined search button</name>
  <files>src/components/site/SiteHeader.tsx</files>
  <action>
  Locked decisions 1, 4, 5. Both data fetches (getSeoSettings + listCategoriesWithCounts via Promise.all) and the component's server-component posture stay exactly as-is. Header keeps: sticky top-0, z-40, border-b, bg-white/95 + backdrop-blur (dark variants), h-16, max-w-6xl container, ThemeToggle rendered by the existing right cluster.

  1. Logo block (locked decision 4): DELETE the hardcoded SEES PNG logo constant (currently L35) and the now-unused next/image import — the old PNG filename must not survive anywhere in the file, including comments. Replace the logo Link content with: an inline speech-bubble chat icon (inline SVG, viewBox 0 0 24 24, fill currentColor, classic rounded message-bubble shape with a tail — match the bubble icon in "frontpage design.png"), sized h-8 w-8, aria-hidden, wrapped in a span with brand color classes (text-brand-500 dark:text-brand-400); followed by the wordmark span: the literal lowercase text "anydiscussion" with classes text-xl font-bold tracking-tight text-gray-900 dark:text-white. The Link keeps href="/" and its flex items-center gap-2 layout. The wordmark is a fixed brand literal per decision 4 — do not source it from settings.

  2. Nav (locked decision 1): keep ALL FIVE entries and their hrefs — Home (/), Blog (/blog), Categories dropdown, About (/about), Contact (/contact) — and keep the current color scheme, which already matches the design (text-sm font-medium, gray-600 dark:gray-300, hover to gray-900/white). Optionally add transition-colors to the links. The Categories dropdown keeps its hover mechanism (group relative, invisible→visible panel), its cached listCategoriesWithCounts feed, the rotate chevron, and the per-category published counts. Do NOT touch the hidden md:flex posture (decision 8).

  3. Search button (locked decision 5): the existing search Link (aria-label Search, h-9 w-9) becomes a circular OUTLINED button per the design: keep flex items-center justify-center h-9 w-9 rounded-full and the magnifier SVG, but replace the hover-background classes with an outline treatment — border border-gray-300 dark:border-gray-600 on rest, hover:border-brand-500 hover:text-brand-600 (dark: hover:border-brand-400 hover:text-brand-400) plus transition-colors. ThemeToggle stays immediately beside it, unchanged.

  4. Update the [CITED] header comments to reference this quick task's locked decisions 1/4/5 and drop any comment text that describes the old image logo.

  Atomic commit: feat(260823-6je): site header speech-bubble logo + outlined search button
  </action>
  <verify>
    <automated>pnpm build && [ "$(grep -v '^\s*//' src/components/site/SiteHeader.tsx | grep -c 'sees-logo')" = "0" ] && [ "$(grep -c 'anydiscussion' src/components/site/SiteHeader.tsx)" -ge "1" ]</automated>
  </verify>
  <done>
  Header renders brand speech-bubble icon + lowercase bold wordmark linking to / (no PNG, no next/image import); all 5 nav entries present with the dropdown intact and fed by the cached query; search is a rounded-full bordered button with brand hover accent beside the ThemeToggle; sticky/backdrop-blur kept; production build green (type-safe, server component).
  </done>
</task>

<task type="auto">
  <name>Task 3: SiteFooter restyle — always-dark 4-column layout, cached dynamic Categories, inert Newsletter</name>
  <files>src/components/site/SiteFooter.tsx</files>
  <action>
  Locked decisions 2, 3, 6, 7. The footer KEEPS its Cache Component contract: 'use cache' at the top of the component body with cacheTag — but now TWO tags: the existing seo-settings PLUS posts-list (locked decision 7; category mutations revalidate posts-list in src/actions/categories.ts, and only tags on the footer's own cache boundary re-render the footer). Keep new Date().getFullYear() inside the cache boundary exactly as today (documented, accepted behavior). Fetch three things in parallel via Promise.all: getSeoSettings(), readSocialLinks(), listCategoriesWithCounts() (import from @/lib/queries/taxonomy).

  1. readSocialLinks cleanup: delete the redundant first single-key query and its void silencer — keep only the Promise.all fetch of the three keys. Its 'use cache' + cacheTag("seo-settings") stay.

  2. Structure — always-dark slab (locked decision 6): outer footer classes bg-gray-900 dark:bg-gray-950 with the existing mt-16; inner container mx-auto max-w-6xl px-4 py-12. Because the slab is always dark, interior classes use ONE palette set with no dark: variants: headings text-white, body text-gray-400, borders border-white/10 (or white/15 for the social circles), muted bottom-bar text-gray-500.

  3. Four columns (grid grid-cols-1 gap-10 sm:grid-cols-2 lg:grid-cols-4):
     a. Brand column: the same speech-bubble icon + lowercase bold "anydiscussion" wordmark as the header but in white/gray-100 tones (icon text-brand-400, wordmark text-white text-xl font-bold tracking-tight); below it the blurb paragraph (seo.siteDescription with the existing fallback string) in text-sm text-gray-400; below that the social row — ONLY the entries pickSocialLinks returns (locked decision 3): each is an anchor (target _blank, rel noopener noreferrer, aria-label from the entry's label) styled as a circular bordered icon button flex h-9 w-9 items-center justify-center rounded-full border border-white/15 text-gray-400 with hover:border-brand-400 hover:text-white transition-colors, reusing the EXISTING Twitter/Facebook/LinkedIn SVG paths from the current file. When the helper returns an empty array, render no row at all — never a placeholder link. NO Instagram.
     b. Quick Links column: nav with h3 "Quick Links" (text-base font-semibold text-white, mb-3/4), then ul space-y-2 text-sm text-gray-400 links with hover:text-white. Entries: Home (/), Blog (/blog), About (/about), Contact (/contact), Terms and Conditions (/terms-and-conditions), Privacy Policy (/privacy-policy) — the two legal links currently live in their own column; the design has no fifth column, so fold them here rather than dropping live navigation.
     c. Categories column (locked decision 7): h3 "Categories"; map boundFooterCategories(categories, 6) (import from @/lib/footer-links) to ul space-y-2/3 text-sm links: /category/ plus slug, text-gray-400 hover:text-white, key by slug, label = name only (no counts in the footer). Renders zero links when the blog has no categories.
     d. Newsletter column (locked decision 2): h3 "Newsletter"; one short gray-400 text-sm sentence (e.g. subscribe for the latest posts); then the visual pair — an email input (type email, name email, placeholder text, aria-label, classes w-full rounded-lg border border-white/10 bg-white/5 px-4 py-2.5 text-sm text-white placeholder:text-gray-500 focus:border-brand-400 focus:outline-none) and a Subscribe button (text-white bg-brand-500 hover:bg-brand-600 rounded-lg px-4 py-2.5 text-sm font-semibold transition-colors) laid out as a responsive row (input flex-1 min-w-0, button shrink-0). INERTNESS IS THE CONTRACT: the button element carries type="button"; there is NO wrapping form element, no Server Action import, no form-related attribute, no client directive — the file stays a pure server component, so the public site ships zero JS for this column. Backend wiring comes in a later task.
  4. Bottom bar: mt-8 (or mt-10) border-t border-white/10 pt-6 text-center text-xs text-gray-500 — keep the existing copyright line verbatim (year + siteTitle + ". All rights reserved.") inside the cache boundary.

  5. Update the [CITED] header comments to reference locked decisions 2/3/6/7 of this task and the two-tag cache contract.

  Atomic commit: feat(260823-6je): always-dark 4-column footer with cached categories + inert newsletter
  </action>
  <verify>
    <automated>pnpm test && pnpm build && [ "$(grep -c 'posts-list' src/components/site/SiteFooter.tsx)" -ge "1" ] && [ "$(grep -v '^\s*//' src/components/site/SiteFooter.tsx | grep -c '<form')" = "0" ] && [ "$(grep -c 'type="button"' src/components/site/SiteFooter.tsx)" -ge "1" ]</automated>
  </verify>
  <done>
  Footer renders as a dark slab in BOTH modes (gray-900/gray-950, white/10 borders) with 4 columns: brand+blurb+configured-only social circles / Quick Links incl. legal / 6 bounded DB categories / inert newsletter visuals; both cache tags declared on the footer boundary; no submission path of any kind for the newsletter (inert button type, no form element, no action import); copyright bar centered; tests + production build green (Cache Components compliant — no uncached reads outside the boundary).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings DB rows → public markup | Social URLs (admin-configured) render as external anchors |
| categories DB rows → public links | Category names/slugs render as in-app links |
| newsletter input → nothing | Inert input — no data leaves the browser (no submission path exists) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6je-01 | Tampering | Social circle anchors (settings-sourced URLs) | low | mitigate | URLs come from admin-gated saveSeoSettings only; anchors keep target=_blank + rel="noopener noreferrer" from the current code; URLs render as href values only — no HTML injection surface, no dangerouslySetInnerHTML anywhere in this task |
| T-6je-02 | Information Disclosure | Newsletter email input | low | accept | Purely visual — no form element, inert button, no handler, no network call; nothing is transmitted or stored |
| T-6je-03 | Spoofing | categories column links | low | accept | Slugs render into template-built /category/ paths via Link — same trust level as the existing header dropdown; no raw HTML from DB |
| T-6je-SC | Tampering | package installs | n/a | accept | No package installs in this task (all styling is Tailwind utility classes; icons are inline SVG) |
</threat_model>

<verification>
- pnpm test — full vitest suite green including the new footer-links tests.
- pnpm build — production build passes: Cache Components compliance (footer's uncached-in-practice reads stay inside its 'use cache' boundary), no type errors, both restyled components compile as server components.
- Grep gates: old PNG filename absent from SiteHeader code; wordmark present; posts-list tag present in SiteFooter; no form element in SiteFooter code; inert button type present.
- Manual spot-check (optional, local dev): light + dark mode — header logo/icon + outlined circular search + 5 nav entries with working hover dropdown; footer dark in BOTH modes, social circles only for configured keys, category links resolve, newsletter button click does nothing.
</verification>

<success_criteria>
- Locked decisions 1-8 fully implemented with no scope drift: no mobile changes, no dashboard changes, SearchForm.tsx and "frontpage design.png" untouched (verify via git status before committing).
- Footer re-renders correctly on both SEO-settings saves (seo-settings tag) and category mutations (posts-list tag).
- Zero client JS added to the public site — both files remain server components; the only client island in the header stays ThemeToggle.
- Three atomic commits, one per task.
</success_criteria>

<output>
Create `.planning/quick/260823-6je-restyle-public-siteheader-sitefooter-to-/260823-6je-SUMMARY.md` when done
</output>
