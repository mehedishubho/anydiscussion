---
phase: quick-260824-u1b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/dashboard/users/ConfirmDialog.tsx
  - src/app/(admin)/dashboard/users/UsersTable.tsx
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "Clicking any of the four row actions (Unban / Ban / Revoke sessions / Delete) on /dashboard/users opens the Modal-based ConfirmDialog with the exact prior wording — no native browser confirm popup appears anywhere on the users dashboard"
    - "Confirming dispatches the pre-existing mutation for that kind and closes the dialog; optimistic row flips, rollback, pendingAction, and the shared error alert behave exactly as before (mutation logic byte-identical)"
    - "Ban and Delete confirms render a solid red confirm button (bg-error-500 hover:bg-error-600); Unban and Revoke confirms render a neutral solid confirm button; Cancel, Escape, and backdrop-click all close without acting"
    - "Both dialog buttons disable with a Working… state on the confirm button while the corresponding mutation isPending"
    - "Full vitest suite stays 573/573 green; pnpm exec tsc --noEmit introduces zero new errors; UsersTable.tsx contains zero occurrences of the removed browser confirm API token"
  artifacts:
    - "src/app/(admin)/dashboard/users/ConfirmDialog.tsx — new client component wrapping Modal with props { isOpen, onClose, title, description, confirmLabel, onConfirm, pending?, danger? }, max-w-md, p-6 content, Cancel-left/Confirm-right footer"
    - "src/app/(admin)/dashboard/users/UsersTable.tsx — four confirm-wrapped onClicks replaced by setConfirmTarget({ kind, user }), module-scope CONFIRM_CONTENT map with the exact prior wording, single ConfirmDialog render, stale comment rewritten"
  key_links:
    - "UsersTable confirmTarget state → CONFIRM_CONTENT[kind] (title/description/confirmLabel/danger) → ConfirmDialog props → CONFIRM_MUTATIONS[kind].mutate(user.id) → existing banUser/unbanUser/revokeSessions/deleteUser actions — action file untouched"
    - "ConfirmDialog → Modal (@/components/ui/modal) → Escape handler + body overflow lock + backdrop click close (all inherited from the shared shell, same as UserDrawer)"
---

<objective>
Owner feedback during live UAT on /dashboard/users: the browser-native confirm popup is wrong for this dashboard. Replace it with the application's TailAdmin modal system.

All FOUR row-action confirms in src/app/(admin)/dashboard/users/UsersTable.tsx (~lines 240, 253, 266, 285) currently gate their mutation behind the native browser confirm builtin: Unban, Ban, Revoke sessions, Delete. Build one reusable ConfirmDialog on the shared Modal shell (the same shell UserDrawer uses) and drive it from a single confirmTarget state, preserving the exact existing wording and ALL mutation logic byte-identically.

Purpose: destructive row actions must confirm through the dashboard's own UI language, not the browser chrome.
Output: new ConfirmDialog.tsx + rewired UsersTable.tsx. Pure UI-layer swap — src/actions/users.ts, the Modal component, UserDrawer.tsx, useModal.ts, CategoriesTable/TagsTable, and all test files are untouched.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/(admin)/dashboard/users/UsersTable.tsx
@src/app/(admin)/dashboard/users/UserDrawer.tsx
@src/app/(admin)/dashboard/users/page.tsx
@src/components/ui/modal/index.tsx
@src/hooks/useModal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfirmDialog component + UsersTable modal-confirm wiring (single atomic swap)</name>
  <files>src/app/(admin)/dashboard/users/ConfirmDialog.tsx, src/app/(admin)/dashboard/users/UsersTable.tsx</files>
  <action>
PART A — create src/app/(admin)/dashboard/users/ConfirmDialog.tsx (new file):

1. "use client" directive; house-style file-header comment citing 260824-u1b-PLAN.md (row-action confirms moved from the native browser confirm popup into this TailAdmin Modal wrapper per owner UAT feedback; sibling of UserDrawer on the same Modal shell). Per the comment-discipline note in Part B item 8, do not name the removed browser API anywhere in this file either.
2. Default-export component ConfirmDialog with props interface EXACTLY: isOpen: boolean; onClose: () => void; title: string; description: string; confirmLabel: string; onConfirm: () => void; pending?: boolean; danger?: boolean.
3. Render Modal from @/components/ui/modal with className="max-w-md" (compact — UserDrawer's drawer uses max-w-xl). Inner content div p-6 (no scroll wrapper needed — fixed small content).
4. Title as h3 with UserDrawer's heading idiom (line ~145 of UserDrawer.tsx): mb-2 text-lg font-semibold text-gray-800 dark:text-white/90. Description as p with text-sm text-gray-600 dark:text-gray-400.
5. Footer div mt-6 flex justify-end gap-3 (Cancel left, Confirm right). Cancel button: type="button", onClick={onClose}, disabled={pending}, text "Cancel", className copied verbatim from UserDrawer's cancel button (line ~306: inline-flex items-center justify-center rounded-lg bg-white px-5 py-2.5 text-sm font-medium text-gray-700 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 dark:bg-gray-800 dark:text-gray-400 dark:ring-gray-700) plus disabled:cursor-not-allowed disabled:opacity-50.
6. Confirm button: type="button", onClick={onConfirm}, disabled={pending}, label pending ? "Working…" : confirmLabel. Base classes: inline-flex items-center justify-center rounded-lg px-5 py-2.5 text-sm font-medium shadow-theme-xs disabled:cursor-not-allowed disabled:opacity-50. When danger: bg-error-500 text-white hover:bg-error-600 (matches the existing Delete row-button palette in UsersTable). When not danger: neutral solid — bg-gray-800 text-white hover:bg-gray-700 dark:bg-gray-700 dark:hover:bg-gray-600.

PART B — rewire src/app/(admin)/dashboard/users/UsersTable.tsx:

1. File-header comment: add a [CITED: 260824-u1b-PLAN.md — …] line noting the four row-action confirms now flow through the ConfirmDialog modal.
2. Import the default export from "./ConfirmDialog" (next to the UserDrawer import).
3. Module scope, near ROLE_BADGE: define type ConfirmKind = "delete" | "ban" | "unban" | "revoke" and a const CONFIRM_CONTENT map keyed by ConfirmKind, each entry { title, description, confirmLabel, danger } where title is a (name: string) => string template. EXACT wording, preserved verbatim from the current popup messages (title = the question, description = the consequence sentence):
   - unban: title → `Unban ${name}?`; description → "They will be able to sign in again."; confirmLabel "Unban"; danger false
   - ban: title → `Ban ${name}?`; description → "They will be signed out and cannot sign in until unbanned."; confirmLabel "Ban"; danger true
   - revoke: title → `Revoke all sessions for ${name}?`; description → "They will be signed out of every device."; confirmLabel "Revoke sessions"; danger false
   - delete: title → `Delete ${name}?`; description → "This permanently deletes this user and cannot be undone."; confirmLabel "Delete"; danger true
4. Component state, next to the other useState calls: const [confirmTarget, setConfirmTarget] = useState&lt;{ kind: ConfirmKind; user: UserRow } | null&gt;(null). UserRow is already imported from ./page. Deliberately NOT using src/hooks/useModal.ts — that hook is boolean-only, while this dialog state must carry a payload (kind + user); the nullable state doubles as the open flag. Do not modify useModal.ts.
5. After the four useMutation definitions, build a plain kind→mutation lookup WITHOUT an explicit Record annotation (the four mutations have different generics; an annotated Record may not unify): const CONFIRM_MUTATIONS = { unban: unbanMutation, ban: banMutation, revoke: revokeMutation, delete: deleteMutation }. Only .mutate(user.id) and .isPending are consumed from it.
6. Replace each of the four confirm-wrapped onClick handlers with a single setConfirmTarget call: Unban → setConfirmTarget({ kind: "unban", user }); Ban (the isBanned ternary's else branch) → kind "ban"; Revoke sessions → kind "revoke"; Delete → kind "delete". Everything else on those four buttons stays byte-identical: type="button", the existing disabled={...Mutation.isPending} attributes, all classNames, the isBanned ternary structure, and the user.id !== sessionUserId Delete render guard.
7. Render ONE ConfirmDialog at the bottom of the component (after the UserDrawer render), conditionally mounted on confirmTarget being non-null: isOpen, onClose={() => setConfirmTarget(null)}, title/description/confirmLabel/danger derived from CONFIRM_CONTENT[confirmTarget.kind] with confirmTarget.user.name, pending={CONFIRM_MUTATIONS[confirmTarget.kind].isPending}, and onConfirm that calls CONFIRM_MUTATIONS[confirmTarget.kind].mutate(confirmTarget.user.id) then setConfirmTarget(null). The conditional mount guarantees the props are always derived from a non-null target — no fallbacks needed.
8. Rewrite the stale delete-mutation comment (~lines 123-128): its final sentence credits the removed browser popup as the UX-friction layer — replace that sentence so it names the ConfirmDialog modal as the friction layer instead ("The server guards are authoritative; the ConfirmDialog modal is UX friction, not the security boundary."), keeping the rest of the comment intact. COMMENT DISCIPLINE (gate-critical): the completion gate greps UsersTable.tsx for the removed browser confirm API token and must find ZERO occurrences — when writing or rewriting ANY comment in this file, refer to the old flow as "the native browser confirm popup"; never name the removed API token, or the gate self-invalidates.
9. Mutation logic stays byte-identical: do NOT touch the four useMutation bodies (onMutate/onError/onSuccess/onSettled), pendingAction, the shared error-alert block, onDrawerSuccess, or the UserDrawer render props. Explicitly out of scope: src/actions/users.ts, CategoriesTable/TagsTable, the Modal component, UserDrawer.tsx, useModal.ts, any test file.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm exec tsc --noEmit && pnpm lint && [ "$(grep -c "window.confirm" "src/app/(admin)/dashboard/users/UsersTable.tsx")" = "0" ] && grep -q "ConfirmDialog" "src/app/(admin)/dashboard/users/UsersTable.tsx" && grep -q "confirmLabel" "src/app/(admin)/dashboard/users/ConfirmDialog.tsx"</automated>
  </verify>
  <done>Full vitest suite 573/573 green; tsc --noEmit and eslint clean; UsersTable.tsx contains zero occurrences of the removed browser confirm API token (4 call sites + 1 comment all rewritten); ConfirmDialog.tsx exists with the exact props interface; each of the four row actions opens the modal with its exact prior wording; Confirm dispatches the existing mutation and closes the dialog; Ban/Delete confirms are solid red, Unban/Revoke neutral; Cancel/Escape/backdrop close without acting; both buttons disable with a Working… label while pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard client → Server Actions | UNCHANGED by this plan — same four mutations (banUser/unbanUser/revokeSessions/deleteUser), same session-cookie credential, same server-side requireCan gates |

No package-manager installs occur in this plan (existing local deps only) — the package-legitimacy / supply-chain checkpoint does not apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q1-01 | Tampering (accidental double-fire) | ConfirmDialog onConfirm | low | mitigate | Dialog closes immediately on confirm (setConfirmTarget(null) right after .mutate), both buttons disabled while pending, and the row buttons keep their existing isPending disables |
| T-Q1-02 | Elevation of Privilege | row-action mutations | low | accept | Client-UI-only swap — src/actions/users.ts untouched; every action still re-checks requireCan server-side (Phase 2 primitives); UI hiding/confirmation was never the security boundary |
| T-Q1-03 | Denial of Service (destructive misclick) | confirm copy + styling | low | mitigate | Exact prior wording preserved incl. the "permanently deletes … cannot be undone" sentence; solid-red danger styling on Ban/Delete distinguishes destructive confirms from neutral ones; Cancel, Escape, and backdrop-click all close without acting |
</threat_model>

<verification>
- pnpm vitest run — full suite stays 573/573 green (no test files touched)
- pnpm exec tsc --noEmit — zero new errors
- pnpm lint — clean
- grep count of the removed browser confirm API token in UsersTable.tsx is exactly 0 (call sites AND the rewritten comment)
- ConfirmDialog referenced in UsersTable.tsx; confirmLabel prop present in ConfirmDialog.tsx
- Manual (optional, next dev server run): on /dashboard/users click each of the four actions — modal opens with the same wording as before, Escape/backdrop/Cancel dismiss without acting, Confirm executes and the row updates optimistically exactly as before
</verification>

<success_criteria>
- All four row-action confirms flow through the TailAdmin Modal-based ConfirmDialog; the native browser popup is gone from the users dashboard
- Exact prior wording preserved (title = question, description = consequence sentence); danger=true on delete and ban, false on unban and revoke
- Confirm dispatches the EXISTING mutation for its kind and closes; pending shows a disabled Working… state; Cancel/Escape/backdrop are no-ops on data
- Mutation logic, error alert, and pendingAction behavior byte-identical; out-of-scope files untouched
- 573/573 tests green, tsc and eslint clean, zero gate-token occurrences in UsersTable.tsx
</success_criteria>

<output>
Create `.planning/quick/260824-u1b-users-confirm-dialog-modal/260824-u1b-SUMMARY.md` when done
</output>
