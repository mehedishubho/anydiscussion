---
status: complete
quick_id: 260824-3l2
completed: 2026-08-24
---

# 260824-3l2: Functional frontend newsletter with dashboard configuration

Functional single-opt-in footer newsletter: public subscribe Server Action (honeypot + Redis per-IP rate limit) storing subscribers in Postgres, dashboard enable/texts configuration via settings keys with seo-settings tag revalidation, admin-gated subscriber list/delete/CSV export — zero new packages.

## Tasks Completed

| # | Task | Commit(s) | What changed |
|---|------|-----------|--------------|
| 1 | subscribers table + status enum (D-01/D-04) | `359ed9f` | `db/schema.ts`: `subscriber_status` enum + `subscribers` table (email unique, token varchar unique, no IP column); `0006_add_subscribers` migration via `pnpm drizzle-kit generate`; `scripts/test-migrations.mjs` expects 13 tables |
| 2 | settings keys + reader + admin save (D-02) | RED `3315418` → GREEN `9d057f7` | `newsletter-schema.ts` (pure Zod module — "use server" files export only async functions), `src/lib/queries/newsletter-settings.ts` (`use cache` + `cacheTag("seo-settings")`, only exact `"false"` disables), `saveNewsletterSettings` with requireRole FIRST + 2-arg `revalidateTag("seo-settings","max")` + `revalidatePath("/","layout")` only |
| 3 | dashboard newsletter settings page (D-02) | `af61530` | `settings/newsletter/page.tsx` + RHF/zodResolver client form; sidebar Settings → Newsletter |
| 4 | public subscribe action + rate limiter (D-01/D-05) | RED `0590516` → GREEN `5bfbfce` | `subscribeNewsletter` (useActionState signature, returned error states, honeypot silent-success, `newsletterLimiter` slidingWindow 5/1h prefix `ratelimit:newsletter`, single-statement `onConflictDoUpdate` upsert reactivating unsubscribed rows); second Ratelimit instance in rate-limit lib |
| 5 | footer island wired to settings (D-02/D-06) | `d654399` | `NewsletterForm.tsx` client island (React.useActionState, only string props cross the cache boundary, honeypot input, inline success/error states); SiteFooter renders it conditionally inside its `use cache` boundary |
| 6 | admin subscribers page + hard delete (D-03) | RED `a870773` → GREEN `a0f0e22` | `listSubscribers` (token column NEVER selected) / `countSubscribers` / `deleteSubscriber` (hard delete, requireRole FIRST each); first searchParams-paginated dashboard page (Next 16 await); SubscribersTable with SSR-hydrated useQuery + optimistic delete; admin-only sidebar item |
| 7 | CSV export route + export link (D-03) | RED `9932f21` → GREEN `a555499` | `export/route.ts` GET (in-handler requireRole → 403 Response, token never selected, no-store) + pure `toSubscribersCsv` (UTF-8 BOM `\uFEFF`, RFC 4180 all-fields quoting with doubled quotes, CRLF, ISO-8601, apostrophe formula guard on `= + - @` tab); Export CSV anchor in table header |

Pre-task infra fix: `47858eb` (see Deviations 1).

## Files Changed

**New (11):**
- `src/actions/newsletter-schema.ts` — pure Zod schemas + SubscribeState + zodResolver re-export
- `src/actions/newsletter.ts` — "use server": save/subscribe/list/count/delete
- `src/actions/__tests__/newsletter.test.ts` — 15 tests (MUST_NOT_BE_REACHED admin-gate proofs)
- `src/lib/queries/newsletter-settings.ts` — cached settings reader
- `src/app/(admin)/dashboard/settings/newsletter/page.tsx` + `NewsletterSettingsForm.tsx`
- `src/app/(admin)/dashboard/subscribers/page.tsx` + `SubscribersTable.tsx`
- `src/app/(admin)/dashboard/subscribers/export/route.ts` (+ pure helper)
- `src/app/(admin)/dashboard/subscribers/__tests__/export.test.ts` — 5 helper tests
- `src/components/site/NewsletterForm.tsx` — client island
- `src/db/migrations/0006_add_subscribers.sql` + `meta/` snapshots

**Modified (7):**
- `src/db/schema.ts`, `src/db/migrations/meta/_journal.json`, `scripts/test-migrations.mjs`
- `src/lib/rate-limit/upstash-ioredis-adapter.ts` + `index.ts`
- `src/components/site/SiteFooter.tsx`, `src/layout/AppSidebar.tsx`

## Verification

- Full suite `pnpm test`: **56 files / 557 tests passed, exit 0** (includes the 20 new newsletter/export tests)
- `pnpm test:migrations`: **PASSED** — clean-room DB, 13 expected tables incl. `subscribers`
- `pnpm build`: **exit 0, zero errors** (final run + per-task runs)
- requireRole gate: `grep -v '^\s*//' src/actions/newsletter.ts \| grep -c 'await requireRole("admin")'` = **exactly 4**
- Exclusion gate: `src/app/(site)/newsletter` does **not** exist; `sendEmail` usage in newsletter.ts = **0**; `git diff --stat package.json` empty (**zero packages added**)
- Footer-island prerender gate (Task 5 MEDIUM flag): build exit 0 with no `next-request-in-use-cache` / uncached-access errors — the island pattern (client child importing the action directly, string-only props) is prerender-safe
- CSV smoke (Task 7 MEDIUM flag): dev server, `curl` GET `/dashboard/subscribers/export` with **no cookie → 403 "Forbidden"** (the authoritative in-handler gate; plan PASS = 403 or 3xx). Note: the dev server took port **3002** (3000 was occupied by an unrelated process; the first curl against 3000 hit the wrong app and returned 404 — re-run against 3002)
- `tsc --noEmit`: no NEW errors — 4 pre-existing errors confined to untouched `src/actions/__tests__/storage-settings.test.ts` (known repo-wide issue; `pnpm build` is authoritative)

## Deviations

1. **[Rule 3] docker-compose postgres-test port fix (`47858eb`)** — pre-existing breakage blocking Task 1's migration gate: `postgres-test` mapped `5438:5436` (host 5438 → container 5436, where postgres listens on 5432 — nothing listens on 5436). Restored `5436:5432` (host 5436 verified free), recreated the container.
2. **[Rule 3] dev-DB migration apply via journal sync + inline migrator instead of `pnpm setup`** — `pnpm setup` could not run (its redis step conflicts with sibling projects on host 6379, and its default creds mismatch compose). The dev DB journal was behind the migration files (migrator compares only the last journal row's `created_at`), so a sync row for 0005 was inserted and the same inline node migrator `scripts/setup.mjs` uses applied 0006 cleanly; verified `\d subscribers` in the dev DB.
3. **Denied cleanup left behind** — `anydiscussion-redis-1` container sits in "Created" (never running) state: `docker rm` was denied by the permission classifier because it predates this task. Harmless; safe to remove manually.
4. **[Rule 3] Task 7: dropped `export const dynamic = "force-dynamic"`** — Turbopack rejects the segment config under `nextConfig.cacheComponents` ("Route segment config 'dynamic' is not compatible"). Per-request behavior is guaranteed the repo-standard way: `requireRole`'s `headers()` access opts the GET handler into dynamic execution (verified: build output registers the route as `ƒ` dynamic; live smoke returned a per-request 403). `Cache-Control: no-store` still set on the response.
5. **cwd-adapted verify commands** — the plan's `cd D:/Devsroom-Work/anydiscussion && …` gates were run from the worktree root instead (files identical on this branch).

## Known Stubs

None — every surface is wired end-to-end (footer form → action → DB; settings form → action → settings rows → cached reader → footer; list/delete/export → DB). No email sending and no public unsubscribe route by explicit scope (D-04: token column only).
