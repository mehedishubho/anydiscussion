---
quick_id: 260824-3l2
description: >
  Functional frontend newsletter with dashboard configuration — real subscribe
  flow on the footer newsletter column (Postgres storage), dashboard config
  (enable toggle + heading/description/success texts), admin-gated subscriber
  management page (list/delete/CSV export).
status: ready-for-execution
type: execute
autonomous: true
waves: 4
tasks: 7
estimated_context: 40%
files_created:
  - src/db/migrations/0006_*.sql            # generated by drizzle-kit
  - src/db/migrations/meta/_journal.json    # generated (appended)
  - src/actions/newsletter-schema.ts
  - src/actions/newsletter.ts
  - src/actions/__tests__/newsletter.test.ts
  - src/lib/queries/newsletter-settings.ts
  - src/components/site/NewsletterForm.tsx
  - src/app/(admin)/dashboard/settings/newsletter/page.tsx
  - src/app/(admin)/dashboard/settings/newsletter/NewsletterSettingsForm.tsx
  - src/app/(admin)/dashboard/subscribers/page.tsx
  - src/app/(admin)/dashboard/subscribers/SubscribersTable.tsx
  - src/app/(admin)/dashboard/subscribers/export/route.ts
  - src/app/(admin)/dashboard/subscribers/__tests__/export.test.ts
files_modified:
  - src/db/schema.ts
  - src/components/site/SiteFooter.tsx
  - src/lib/rate-limit/upstash-ioredis-adapter.ts
  - src/lib/rate-limit/index.ts
  - src/layout/AppSidebar.tsx
  - scripts/test-migrations.mjs             # add "subscribers" to expected tables
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]
must_haves:
  truths:
    - "D-01: Submitting a valid email in the footer stores a subscriber row with status active — no confirmation email, no email sending of any kind"
    - "D-01: Re-submitting an already-active email returns success without error; re-submitting a previously unsubscribed email flips it back to active (single idempotent upsert)"
    - "D-02: newsletter.enabled=false removes the entire newsletter column from the public footer; heading/description/success texts come from settings with built-in defaults when rows are absent"
    - "D-02: Saving newsletter settings from /dashboard/settings/newsletter refreshes the cached footer without a rebuild (revalidateTag seo-settings max)"
    - "D-03: Only admin can list subscribers, delete a subscriber, and download the CSV; non-admin and unauthenticated callers never reach a DB write (requireRole admin is the first line of every gated action/handler)"
    - "D-05: A filled honeypot field returns success and inserts nothing; more than 5 subscribes per IP per hour returns a rate-limit error and inserts nothing"
    - "Unauthenticated GET /dashboard/subscribers/export returns 403 with a body built by an RFC 4180-quoting + BOM CSV helper"
  artifacts:
    - "src/db/schema.ts carries subscriberStatusEnum + subscribers (email unique, token unique notNull, status enum, createdAt, updatedAt)"
    - "0006 migration generated via drizzle-kit (never hand-written) and applied to the dev DB"
    - "src/actions/newsletter.ts exports subscribeNewsletter, saveNewsletterSettings, listSubscribers, countSubscribers, deleteSubscriber — and NOTHING else (upsertSetting stays private)"
    - "src/components/site/NewsletterForm.tsx is a 'use client' island rendering inside the cached SiteFooter"
    - "CSV export Route Handler at /dashboard/subscribers/export with requireRole inside the handler"
  key_links:
    - "NewsletterForm imports subscribeNewsletter directly from src/actions/newsletter.ts — only string props (heading, description, successMessage) cross the footer cache boundary; no function props, no headers() inside 'use cache'"
    - "saveNewsletterSettings -> revalidateTag('seo-settings','max') -> SiteFooter's own cache tag -> footer re-renders with new texts/visibility"
    - "subscribeNewsletter -> db.insert(subscribers).values({email, token: crypto.randomUUID()}).onConflictDoUpdate({target: email, set: {status: 'active', updatedAt: new Date()}}) — one statement covers all three D-01 branches"
    - "AppSidebar 'Subscribers' item (admin-only) -> /dashboard/subscribers -> listSubscribers (requireRole first)"
    - "SubscribersTable Export anchor -> GET /dashboard/subscribers/export -> requireRole try/catch -> 403 or CSV download"
---

<objective>
Make the footer newsletter column functional end to end per the six locked decisions in 260824-3l2-CONTEXT.md:

- A public subscribe Server Action storing subscribers in Postgres — single opt-in, idempotent (D-01), honeypot + Redis per-IP rate limit (D-05), Server Action as the only mutation path with a small useActionState client island inside the cached footer (D-06).
- Dashboard configuration: enable toggle plus heading/description/success texts stored as settings keys; disabled removes the column entirely; saves revalidate the existing seo-settings cache tag (D-02).
- Admin-gated subscriber management: paginated list, delete, CSV export (D-03).
- Schema carries a unique unsubscribe token column now, but NO public unsubscribe route and NO email sending in this task (D-04 / D-01 deferrals).

Purpose: The footer Newsletter column is currently inert visuals (260823-6je decision 2 deferred backend wiring to exactly this task). This plan delivers the working subscribe loop with zero new packages and zero new cache machinery.
Output: 13 new files (including a drizzle-generated migration) + 6 modified files, all following existing codebase patterns cited in RESEARCH.md.

Exclusions (do NOT build): public unsubscribe route (D-04 explicitly defers — nothing can carry the link until email sending exists), email sending / confirmation flow (D-01), subscriber create/edit/bulk actions (D-03 — "list hygiene surface, not a CRUD editor").

Deviations from RESEARCH.md file map (both ADDITIVE, no contradictions):
1. scripts/test-migrations.mjs is modified to add "subscribers" to the expected-tables list — keeps the clean-room drift gate covering the new table (the check is subset-based, so this is hygiene, not a fix).
2. src/app/(admin)/dashboard/subscribers/__tests__/export.test.ts added — unit-tests the pure CSV helper exported from the route file (same precedent as rss.xml route helpers tested in src/lib/seo/__tests__/rss.test.ts; vitest include pattern src/**/__tests__/**/*.test.ts covers it).
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260824-3l2-functional-frontend-newsletter-with-dash/260824-3l2-CONTEXT.md
@.planning/quick/260824-3l2-functional-frontend-newsletter-with-dash/260824-3l2-RESEARCH.md
@.planning/STATE.md

# Templates to copy from (read each once before its task; do NOT re-read):
#   Wave 1: src/db/schema.ts (style reference), scripts/test-migrations.mjs
#   Wave 2: src/actions/settings.ts (saveSeoSettings + private upsertSetting),
#           src/actions/seo-settings-schema.ts, src/lib/queries/social-links.ts,
#           src/actions/__tests__/seo-settings.test.ts,
#           src/app/(admin)/dashboard/settings/seo/page.tsx + SeoSettingsForm.tsx
#   Wave 3: src/actions/contact.ts (public-action template), src/actions/contact-schema.ts,
#           src/lib/rate-limit/upstash-ioredis-adapter.ts + index.ts,
#           src/components/auth/SignUpForm.tsx (useActionState precedent),
#           src/components/site/SiteFooter.tsx
#   Wave 4: src/app/(admin)/dashboard/categories/page.tsx + CategoriesTable.tsx,
#           src/lib/queries/users.ts (PAGE_SIZE pagination shape),
#           src/app/rss.xml/route.ts (pure helpers exported from a route file),
#           src/layout/AppSidebar.tsx (navItems)

Global conventions: pnpm only. Zod schemas live beside their feature in src/actions/. TypeScript strict, no unexplained any. Comments in this codebase cite decisions/plans in header blocks — keep that style (see any file above). Stack locks: Next 16 (revalidateTag is 2-arg), Drizzle ^0.45.2 (never 1.x), Zod 4. Zero package installs.
</context>

<tasks>

### Wave 1 — Storage foundation (schema + migration)

<task type="auto">
  <name>Task 1: subscribers table + subscriber_status enum + generated migration (D-01, D-04)</name>
  <files>src/db/schema.ts, src/db/migrations/0006_*.sql (generated), src/db/migrations/meta/_journal.json (generated), scripts/test-migrations.mjs</files>
  <action>
Add to src/db/schema.ts (mirroring the exact shape given in 260824-3l2-RESEARCH.md Priority 3 "Pattern to follow"):
- subscriberStatusEnum = pgEnum("subscriber_status", ["active", "unsubscribed"]) — pgEnum, NOT a text column + check (matches postStatusEnum precedent).
- subscribers = pgTable("subscribers", ...): id serial PK; email text notNull unique (lowercase-normalized before insert — normalization lives in Zod, no citext); status subscriberStatusEnum default "active" notNull; token varchar(255) notNull unique — crypto.randomUUID() generated at insert time inside the Server Action, NEVER inside the footer component (D-04 token-ready for the future unsubscribe route; unlike posts.previewToken it is notNull because every row needs it from birth); createdAt timestamp defaultNow notNull (this is the "subscribed at" shown in the dashboard); updatedAt timestamp defaultNow with $onUpdate(() => new Date()) notNull (re-subscribe flips status per D-01).
- Extend the schema header comment (D-08 section): subscribers is a hard-delete utility table — NO deletedAt. A soft-deleted email's unique row would be resurrected by onConflictDoUpdate with stale token/createdAt semantics; "unsubscribed" IS the soft state via the status enum.
- Do NOT add an ip column — IP is used transiently for the rate limit only, never stored (research A3: no PII retention; contact action persists no request data either).

Then generate and apply:
1. Run `pnpm drizzle-kit generate --name add_subscribers` (repo convention from 06-01; NEVER hand-write the SQL, NEVER drizzle-kit push). It produces 0006_*.sql + a journal entry under src/db/migrations/meta/.
2. Edit scripts/test-migrations.mjs: add "subscribers" to the expected tables array (after "settings"/"pages" — anywhere in the array). This keeps the clean-room drift gate covering the new table.
3. Apply to the dev DB with `pnpm setup` — the established apply path (docker compose up + drizzle-orm/node-postgres migrator against the dev DB on :5435 + MinIO bucket confirm; idempotent — the migrator skips already-applied files). Docker Desktop must be running.
4. Run `pnpm test:migrations` — replays ALL migrations against the clean-room scratch DB on :5436 and asserts the expected tables (now including subscribers).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && pnpm test:migrations && ls src/db/migrations/0006_*.sql && grep -c "subscriber_status" src/db/migrations/0006_*.sql</automated>
  </verify>
  <done>
Migration 0006 generated by drizzle-kit (CREATE TYPE subscriber_status + CREATE TABLE subscribers with unique email + unique notNull token), applied to the dev DB via pnpm setup, clean-room migration test passes with subscribers in the expected list, tsc clean.
  </done>
  <commit>feat(260824-3l2): subscribers table + status enum via drizzle-kit (D-01/D-04)</commit>
</task>

### Wave 2 — Newsletter configuration (settings keys + reader + save action + dashboard UI)

<task type="auto" tdd="true">
  <name>Task 2: newsletter schema module + settings reader + saveNewsletterSettings action + admin-gate tests (D-02)</name>
  <files>src/actions/newsletter-schema.ts, src/lib/queries/newsletter-settings.ts, src/actions/newsletter.ts, src/actions/__tests__/newsletter.test.ts</files>
  <behavior>
    - Test: non-admin calling saveNewsletterSettings rejects with FORBIDDEN and NEVER reaches db.update/db.insert (MUST_NOT_BE_REACHED, mirroring seo-settings.test.ts)
    - Test: admin save persists all 4 keys (4 upsert calls) and calls revalidateTag with exactly ("seo-settings", "max") — 2-arg form
    - Test: admin save calls revalidatePath("/", "layout")
    - Test: Zod rejects a heading longer than 100 chars before any db write
    - Test: enabled=false persists the string "false" (booleans stored as "true"/"false" strings — settings.value is text)
  </behavior>
  <action>
Create src/actions/newsletter-schema.ts — pure Zod module, NO "use server"/"use client" directive (a "use server" file can only export async functions; mirrors seo-settings-schema.ts / contact-schema.ts). Contains BOTH schemas (the subscribe one is consumed by Wave 3 — create the whole module now so the file is touched once):
- newsletterSettingsSchema: enabled z.boolean() required; heading z.string().max(100).optional() defaulting to ""; description z.string().max(500).optional() defaulting to ""; successMessage z.string().max(200).optional() defaulting to "". Export type NewsletterSettingsInput. Empty strings are VALID — an empty stored value means "fall back to the built-in default" at read time.
- subscribeSchema: email — z.string().trim().toLowerCase().email(...) with max(255), matching the email-validation style already used in src/actions/contact-schema.ts (Zod does the normalization: trim + lowercase, no citext extension); website — optional string, the honeypot field (bots auto-fill fields named website). Export type SubscribeInput.
- Export type SubscribeState = { status: "idle" } | { status: "success" } | { status: "error"; message: string } — consumed by the Wave 3 island.
- Re-export zodResolver from @hookform/resolvers/zod for the single-import-surface convention (contact-schema.ts precedent; the dashboard form uses RHF, the public island does not).

Create src/lib/queries/newsletter-settings.ts — readNewsletterSettings() mirroring readSocialLinks() in src/lib/queries/social-links.ts line for line: "use cache" + cacheTag("seo-settings") (multi-consumer shape — footer AND settings page read it; the tag lets the save action invalidate it), Promise.all of per-key selects on schema.settings. Keys: newsletter.enabled, newsletter.heading, newsletter.description, newsletter.success_message. Return type NewsletterSettingsSettings { enabled, heading, description, successMessage } with DEFAULTS APPLIED — null or whitespace-empty stored value falls back: enabled defaults TRUE (absent = enabled per D-02), heading defaults "Newsletter", description defaults "Subscribe for the latest posts delivered straight to your inbox.", successMessage defaults "Thanks for subscribing!". enabled parses stored strings: only the exact string "false" is false; absent/anything else is true. NO seed migration — defaults-without-seed is the established getSeoSettings pattern.

Create src/actions/newsletter.ts ("use server") containing saveNewsletterSettings ONLY (subscribe/list/delete are added in Waves 3/4). Copy saveSeoSettings from src/actions/settings.ts as the template:
1. requireRole("admin") is the FIRST line — before Zod parse, before any DB write (CLAUDE.md non-negotiable; proven by MUST_NOT_BE_REACHED test).
2. Parse input through newsletterSettingsSchema.
3. PRIVATE upsertSetting(key, value) helper duplicated inside this file (copy the update-then-insert-onConflictDoNothing shape from settings.ts). NEVER export it — a "use server" file's exports are all publicly invocable endpoints; a generic upsertSetting would let any unauthenticated caller write arbitrary settings keys.
4. Promise.all the four writes; enabled stored as String(data.enabled) ("true"/"false").
5. Revalidation: revalidateTag("seo-settings", "max") — 2-arg form is mandatory in Next 16.2.x — plus revalidatePath("/", "layout"). Do NOT revalidate sitemap/robots/rss (reasoned delta from saveSeoSettings: no SEO route reads newsletter keys).
6. log.info("newsletter settings saved", ...) via @/lib/log; return { ok: true }.

Create src/actions/__tests__/newsletter.test.ts mirroring src/actions/__tests__/seo-settings.test.ts: vi.hoisted mocks for @/lib/permissions, @/lib/db, next/cache, @/lib/log; implement the behavior cases above. Extend this file in later waves — it is the single test module for the action file.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm vitest run src/actions/__tests__/newsletter.test.ts && pnpm tsc --noEmit && grep -v '^\s*//' src/actions/newsletter.ts | grep -c 'await requireRole("admin")'</automated>
  </verify>
  <done>
Newsletter test suite green (admin gate MUST_NOT_BE_REACHED, 4-key write, 2-arg revalidateTag + layout revalidatePath, Zod caps enforced). requireRole count in code lines is 1 at this stage. Schema module exports both schemas + SubscribeState. Reader applies defaults with no seeding.
  </done>
  <commit>feat(260824-3l2): newsletter settings keys + reader + admin save action (D-02)</commit>
</task>

<task type="auto">
  <name>Task 3: dashboard Newsletter settings page + form + sidebar link (D-02)</name>
  <files>src/app/(admin)/dashboard/settings/newsletter/page.tsx, src/app/(admin)/dashboard/settings/newsletter/NewsletterSettingsForm.tsx, src/layout/AppSidebar.tsx</files>
  <action>
Create the settings folder mirroring the seo folder 1:1:
- page.tsx (Server Component, no "use client"): metadata title "Newsletter Settings — Dashboard"; try/catch around readNewsletterSettings(); loadError banner on failure (copy the seo page's error banner classes); on success pass initial (the defaults-applied snapshot: enabled boolean, three strings — no null-coalescing needed, the reader already applied defaults) into NewsletterSettingsForm. Header copy explains: disabled removes the newsletter column from the public footer entirely; empty text fields fall back to built-in defaults.
- NewsletterSettingsForm.tsx ("use client"): mirror SeoSettingsForm.tsx — react-hook-form + zodResolver(newsletterSettingsSchema) imported from @/actions/newsletter-schema (client components importing the pure schema module is established — ContactForm does it with contact-schema) + TanStack useMutation calling saveNewsletterSettings, NOT optimistic (settings are server-confirm, same D-27 reasoning as SeoSettingsForm). Fields: an enabled checkbox (label "Show newsletter column in the footer", styled like a standard dashboard form checkbox), heading input, description textarea (rows 3), success-message input — each with a one-line help text noting the built-in default it falls back to. Save button with isSaving state, error banner on mutation.error, success banner "Newsletter settings saved." Local INPUT_CLASS/LABEL_CLASS constants copied from SeoSettingsForm (NOT imported across folders).
- src/layout/AppSidebar.tsx: add { name: "Newsletter", path: "/dashboard/settings/newsletter" } to the Settings subItems array (beside Storage and Backup). The Settings group already carries requiredRole "admin" — no per-item role needed.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && pnpm build && grep -c "readNewsletterSettings" src/app/(admin)/dashboard/settings/newsletter/page.tsx && grep -c "saveNewsletterSettings" src/app/(admin)/dashboard/settings/newsletter/NewsletterSettingsForm.tsx</automated>
  </verify>
  <done>
pnpm build passes with the new admin route. Page reads via the cached reader; form saves via the admin-gated action; sidebar Settings group shows Newsletter. An admin can toggle enabled and edit all three texts; saving revalidates the seo-settings tag so the footer refreshes without a rebuild.
  </done>
  <commit>feat(260824-3l2): dashboard newsletter settings page + sidebar link (D-02)</commit>
</task>

### Wave 3 — Public subscribe flow (action + rate limit + footer island)

<task type="auto" tdd="true">
  <name>Task 4: newsletterLimiter + subscribeNewsletter action + tests (D-01, D-05, D-06)</name>
  <files>src/lib/rate-limit/upstash-ioredis-adapter.ts, src/lib/rate-limit/index.ts, src/actions/newsletter.ts, src/actions/__tests__/newsletter.test.ts</files>
  <behavior>
    - Test: honeypot filled ("website" non-empty) returns { status: "success" } and db.insert is NEVER called (silent success — bots that see errors retry)
    - Test: limiter returns success=false → returns { status: "error", message: "RATE_LIMITED" } and db.insert NEVER called
    - Test: invalid email → { status: "error", message: "INVALID_EMAIL" } and db.insert NEVER called
    - Test: valid mixed-case email → db.insert called once with the LOWERCASE email and a non-empty token, and onConflictDoUpdate is configured against the email column with set { status: "active" } plus an explicit updatedAt Date
    - Test: subscribeNewsletter has NO permission gate by design (public action — mirrors contact.ts; no requireRole mock expectation)
  </behavior>
  <action>
Rate limiter (D-05 — reuse the existing Redis infra; the in-memory fallback was deliberately removed in 07-02, do not reintroduce it):
- In src/lib/rate-limit/upstash-ioredis-adapter.ts add newsletterLimiter beside contactFormLimiter: new Ratelimit over a new IoredisAdapter(), limiter Ratelimit.slidingWindow(5, "1 h") — same policy as contact (research A2), prefix exactly "ratelimit:newsletter", analytics false, same @ts-expect-error note shape as the existing instance.
- In src/lib/rate-limit/index.ts re-export newsletterLimiter.

Add subscribeNewsletter to src/actions/newsletter.ts — copy src/actions/contact.ts as the template (the codebase's public-unauthenticated-action precedent) and adapt:
- Signature: export async function subscribeNewsletter(prev: SubscribeState, formData: FormData): Promise<SubscribeState> — the useActionState signature directly (SignUpForm precedent), no local wrapper.
- Order of operations, mirroring contact.ts steps 1-3: (1) gather email and website from formData and parse through subscribeSchema — on Zod failure return { status: "error", message: "INVALID_EMAIL" } rather than throwing (a thrown error from a useActionState action escapes to the error boundary instead of rendering inline — returned error states keep the public footer resilient); (2) honeypot: parsed website non-empty after trim → return { status: "success" } WITHOUT inserting (D-05 silent-success); (3) rate limit: await headers(), read x-forwarded-for first value with "unknown" fallback (exactly contact.ts's extraction — do not invent a second style), await newsletterLimiter.limit(ip), on failure return { status: "error", message: "RATE_LIMITED" }.
- Then the D-01 upsert in a single statement — no read-check-write race: db.insert(schema.subscribers).values({ email: parsed.email, token: crypto.randomUUID() }).onConflictDoUpdate({ target: schema.subscribers.email, set: { status: "active", updatedAt: new Date() } }). The explicit updatedAt in set is REQUIRED — Drizzle's $onUpdate does not reliably fire on the conflict path. One statement covers all three D-01 branches: first subscribe inserts active; existing active is an idempotent no-op success; previously unsubscribed flips back to active. The action returns { status: "success" } on every success path — duplicate emails are NEVER an error, and the response never leaks whether the email already existed beyond that (uniform success).
- Wrap the DB write in try/catch: on unexpected error, log.error via @/lib/log and return { status: "error", message: "UNKNOWN" }. Do NOT add "use cache" to this file — cached Server Actions silently no-op after the first submission (contact.ts header Pitfall 7); mutations are never cached. Do NOT add requireRole here — public by design; the honeypot + rate limit are the controls.
- Header comment: cite D-01/D-05/D-06, the contact.ts precedent, and note the second public write surface beside submitContact.

Extend src/actions/__tests__/newsletter.test.ts with the behavior cases above (mock next/headers, @/lib/rate-limit with a controllable newsletterLimiter mock, extend the @/lib/db mock's insert chain with values().onConflictDoUpdate() capture).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm vitest run src/actions/__tests__/newsletter.test.ts && pnpm tsc --noEmit && grep -c 'prefix: "ratelimit:newsletter"' src/lib/rate-limit/upstash-ioredis-adapter.ts</automated>
  </verify>
  <done>
All newsletter tests green including honeypot no-insert, rate-limit no-insert, and the upsert conflict configuration. newsletterLimiter live beside contactFormLimiter with the ratelimit:newsletter prefix. Subscribe action is public, normalizes email via Zod, generates the token at request time, and returns error STATES (never throws) for invalid/rate-limited/unknown failures.
  </done>
  <commit>feat(260824-3l2): public subscribe action + redis rate limiter (D-01/D-05/D-06)</commit>
</task>

<task type="auto">
  <name>Task 5: NewsletterForm client island + SiteFooter wiring (D-02, D-06)</name>
  <files>src/components/site/NewsletterForm.tsx, src/components/site/SiteFooter.tsx</files>
  <action>
Create src/components/site/NewsletterForm.tsx ("use client", sibling of ThemeToggle.tsx):
- Props: heading, description, successMessage — all plain strings (trivially serializable; only these cross the footer cache boundary). The island renders the ENTIRE column content: the h3 heading, the description paragraph, and the form — copying the current footer column's always-dark classes verbatim from SiteFooter's existing newsletter markup (white/10 borders, gray-400 text, brand-500 button, bg-white/5 input).
- Form mechanics: import subscribeNewsletter directly from @/actions/newsletter.ts and SubscribeState from @/actions/newsletter-schema (the standard client-component-to-"use server"-module import path — NO function prop ever crosses the cache boundary; verified supported by the Next 16 use-cache docs, research Priority 6). const [state, formAction, pending] = React.useActionState(subscribeNewsletter, { status: "idle" }). Render <form action={formAction}> containing: the email input (type email, name "email", required, aria-label, same styling as today's inert input), the honeypot input (name "website", no label, hidden from humans and assistive tech — absolutely positioned off-screen, tabIndex -1, aria-hidden, autocomplete off), and the submit button (type "submit" now, not the old inert type button; disabled while pending; label "Subscribe" / "Subscribing…" while pending).
- States: state.status "success" → replace the form with the successMessage paragraph (aria-live polite) — the text comes from the settings prop, NOT from the action payload (keeps the action payload constant and the text cache-controlled). state.status "error" → render an inline error line below the form: map the sentinel message "RATE_LIMITED" to "Too many subscriptions — please try again later.", map everything else (INVALID_EMAIL, UNKNOWN) to "Something went wrong. Please try again." Non-JS users degrade to the previous inert visuals (progressive enhancement accepted per D-06, not a gate).

Rewire src/components/site/SiteFooter.tsx (the footer's own "use cache" boundary with cacheTag seo-settings + posts-list stays exactly as is):
1. Add readNewsletterSettings() as a fourth entry in the existing Promise.all (the read runs at cache-fill time and its result is captured in the cached output — revalidation via the footer's own seo-settings tag refreshes it).
2. Replace the entire inert newsletter column block (the comment + div with h3, p, input, type="button" button) with: when newsletter.enabled is false render NOTHING for the column (D-02 — no column at all, not a disabled form; the lg grid then lays out 3 children in 4 tracks, an accepted cosmetic per research A4 — do NOT add grid-class switching), otherwise render <NewsletterForm heading={...} description={...} successMessage={...} />.
3. Update the file header comments: the 260823-6je decision-2 "inert visuals" citation is superseded by this task (cite 260824-3l2 D-06) — the column is now a client island inside the cache boundary.
4. Do NOT add headers()/cookies() usage anywhere in the footer (forbidden inside "use cache") and do NOT pass any action/function prop into the footer itself — the footer keeps taking no props.

MEDIUM-FLAG GATE (research: first component-level "use cache" in this codebase to render a client child): pnpm build MUST pass with the footer prerendering — no "next-request-in-use-cache" or "Uncached data outside Suspense" errors. The island-imports-action design is the documented-safe shape; a build failure here means the composition is wrong (e.g. someone passed a function prop), not a framework gap.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm tsc --noEmit && pnpm build && grep -c '"use client"' src/components/site/NewsletterForm.tsx && grep -c 'useActionState' src/components/site/NewsletterForm.tsx && grep -c 'NewsletterForm' src/components/site/SiteFooter.tsx</automated>
  </verify>
  <done>
pnpm build exits 0 with the (site) routes prerendering (MEDIUM flag cleared). Footer renders the island with settings-driven texts; enabled=false removes the column entirely; the cache boundary structure and tags are unchanged. Honeypot field present but invisible; pending/success/error states render inline.
  </done>
  <commit>feat(260824-3l2): functional footer newsletter island wired to settings (D-02/D-06)</commit>
</task>

### Wave 4 — Admin subscriber management (list + delete + CSV export)

<task type="auto" tdd="true">
  <name>Task 6: list/count/delete actions + subscribers page + table + sidebar (D-03)</name>
  <files>src/actions/newsletter.ts, src/app/(admin)/dashboard/subscribers/page.tsx, src/app/(admin)/dashboard/subscribers/SubscribersTable.tsx, src/layout/AppSidebar.tsx, src/actions/__tests__/newsletter.test.ts</files>
  <behavior>
    - Test: non-admin calling deleteSubscriber rejects FORBIDDEN and NEVER reaches db.delete (MUST_NOT_BE_REACHED)
    - Test: non-admin calling listSubscribers rejects FORBIDDEN before any db.select
    - Test: admin deleteSubscriber performs a hard delete (db.delete where id) and returns ok
    - Test: deleteSubscriber rejects a non-positive/non-integer id via Zod before any DB call
  </behavior>
  <action>
Add three admin actions to src/actions/newsletter.ts — EACH one starts with await requireRole("admin") as its FIRST line, before Zod parse and before any DB access (D-03: admin role only — the precise gate for an admin-only surface, mirroring saveSeoSettings; repo-root middleware.ts covers /dashboard/:path* for UX only, forged cookies pass it, the requireRole re-check is the real gate):
- listSubscribers(page: number): clamp page with Math.max(1, page) (users.ts pagination shape: PAGE_SIZE const = 20, offset = (page - 1) * PAGE_SIZE); select EXPLICIT columns id, email, status, createdAt — never select the token column (it is an unsubscribe credential; it does not belong in dashboard payloads or logs); orderBy desc(createdAt); offset; limit PAGE_SIZE. NOT cached — dashboard read, no "use cache" anywhere in (admin).
- countSubscribers(): select count (drizzle sql count or the established count pattern in the codebase) returning a number — the page computes pageCount = Math.max(1, Math.ceil(count / 20)).
- deleteSubscriber(id: number): Zod-validate id as a positive integer first (after the role gate), then a HARD delete (db.delete where eq id) — no soft-delete on this utility table. NO revalidation needed: no public cached surface reads subscribers; the table refreshes via TanStack invalidation.

Create the page at src/app/(admin)/dashboard/subscribers/page.tsx following src/app/(admin)/dashboard/categories/page.tsx as the shell (PageBreadcrumb from @/components/common/PageBreadCrumb, metadata, try/catch action reads, loadError banner) with the pagination delta (first dashboard page to paginate via searchParams — Next 16 async form: { searchParams }: { searchParams: Promise<{ page?: string }> }, await it, parse int with fallback 1):
- Await listSubscribers(page) + countSubscribers() (Promise.all), pass rows + currentPage + pageCount into SubscribersTable; render server-side prev/next Links with ?page=N plus a "Page X of Y" line below the table card (Link-based server pagination — the public Pagination component is the visual analog; keep it simple).

Create SubscribersTable.tsx ("use client") following CategoriesTable.tsx verbatim as the CRUD analog, minus create/edit (D-03: no create — the public form does that; no edit — invites typos into a field the subscriber owns):
- SSR-hydrated useQuery (initialData = rows, staleTime 30s) keyed ["subscribers", currentPage]; deleteMutation with the full optimistic pattern (onMutate cancelQueries + snapshot + optimistic remove, onError rollback + error banner, onSettled invalidateQueries on the ["subscribers"] prefix).
- Delete confirmation via window.confirm (CategoriesTable precedent) with hard-delete wording: deleting permanently removes the row.
- Table from @/components/ui/table (Table/TableHeader/TableBody/TableRow/TableCell). Columns: Email, Status, Subscribed At, Actions. Status as a small badge — active uses the dashboard success palette (success-50/700 etc. as seen in SeoSettingsForm banners), unsubscribed uses a neutral gray. Subscribed At formatted as a readable local datetime string (dashboard-only surface; the CSV export keeps ISO-8601).
- Empty state: the dashed empty-box pattern with copy "No subscribers yet. Subscriptions from the public footer form will appear here."
- Do NOT add the Export link in this task — Task 7 adds it alongside the route it points to.

Sidebar (src/layout/AppSidebar.tsx): add a top-level nav item { icon: <ListIcon />, name: "Subscribers", path: "/dashboard/subscribers", requiredRole: "admin" } — placed after the Users item (admin surfaces grouped); ListIcon is already imported (no new icon import).

Extend src/actions/__tests__/newsletter.test.ts with the behavior cases above (add db.delete chain + select-count shape to the db mock).
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm vitest run src/actions/__tests__/newsletter.test.ts && pnpm tsc --noEmit && grep -v '^\s*//' src/actions/newsletter.ts | grep -c 'await requireRole("admin")'</automated>
  </verify>
  <done>
requireRole("admin") count in code lines is exactly 4 (save + list + count + delete) — grep gate proves no gated action lost its check. Admin sees a paginated subscriber table with delete working optimistically; non-admin tests prove FORBIDDEN before any DB reach; sidebar shows Subscribers to admin only.
  </done>
  <commit>feat(260824-3l2): admin subscribers page + hard delete (D-03)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 7: CSV export Route Handler + export link (D-03)</name>
  <files>src/app/(admin)/dashboard/subscribers/export/route.ts, src/app/(admin)/dashboard/subscribers/__tests__/export.test.ts, src/app/(admin)/dashboard/subscribers/SubscribersTable.tsx</files>
  <behavior>
    - Test (pure helper, DB-free): output starts with the UTF-8 BOM (first char code 0xfeff)
    - Test: header row is exactly email,status,subscribed_at (quoted)
    - Test: a field containing a comma, a double quote, or a newline is wrapped in double quotes with internal quotes doubled (RFC 4180)
    - Test: a field starting with = or + or - or @ or a tab is prefixed with an apostrophe (formula-injection guard, research A1)
    - Test: rows are joined with CRLF; timestamps emitted as ISO-8601 (toISOString)
  </behavior>
  <action>
Create src/app/(admin)/dashboard/subscribers/export/route.ts as a GET Route Handler (research MEDIUM flag — first HTTP download surface; D-03 permits route or action, the route gives a direct browser download with zero client JS and the simplest auth story; async params pattern not needed — no params):
- export const dynamic = "force-dynamic" — the handler reads the session, it must never be prerendered or cached.
- GET handler body: FIRST await requireRole("admin") wrapped in try/catch — on throw return new Response("Forbidden", { status: 403 }). The in-handler requireRole is non-negotiable: the middleware matcher is a UX gate only, forged cookies pass it (research Priority 5 pitfall). A thrown FORBIDDEN must become a 403 Response — a Route Handler returns statuses, it does not propagate thrown errors.
- On success: query ALL subscribers selecting email, status, createdAt only (again never the token), orderBy desc(createdAt); build the CSV via a PURE exported helper toSubscribersCsv(rows) defined in this same file and exported beside GET (the rss.xml route precedent — pure helpers exported from route files for DB-free unit testing: RSS_LIMIT/buildRssItem/escapeXml do exactly this).
- toSubscribersCsv contract: header row email,status,subscribed_at; every field wrapped in double quotes with internal double quotes doubled (RFC 4180 — emails can legally contain quotes/commas; hand-rolled unquoted concatenation is the classic broken-CSV bug); rows joined with CRLF; the body prefixed with the UTF-8 BOM written in JS source as the backslash-u escape for U+FEFF so Excel opens UTF-8/Bangla correctly; formula-injection guard — any field whose first character is one of = + - @ or a tab gets an apostrophe prefixed (research A1, cheap hardening); createdAt values emitted via toISOString().
- Response: Content-Type text/csv; charset=utf-8; Content-Disposition attachment with filename subscribers-YYYY-MM-DD.csv (date from new Date().toISOString().slice(0, 10)); Cache-Control no-store.
- Return new Response(csvBody, { headers }) — a string body is fine.

Create src/app/(admin)/dashboard/subscribers/__tests__/export.test.ts importing toSubscribersCsv from the route file (vitest include pattern src/**/__tests__/**/*.test.ts discovers it; node environment; no mocks needed — the helper is pure).

Add the Export trigger to SubscribersTable.tsx header row (next to where CategoriesTable puts its action button): a plain anchor with href="/dashboard/subscribers/export" styled like the + New Category button (secondary styling) — native browser download, zero client JS.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/anydiscussion && pnpm vitest run src/app/(admin)/dashboard/subscribers/__tests__/export.test.ts && pnpm tsc --noEmit && pnpm build && grep -v '^\s*//' src/app/(admin)/dashboard/subscribers/export/route.ts | grep -c 'requireRole("admin")'</automated>
  </verify>
  <verify>
    MEDIUM-FLAG smoke (executor-run, no human needed — the requireRole gate on the first download surface): start the dev server in the background (pnpm dev), wait for ready, then run curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/dashboard/subscribers/export with NO session cookie — it MUST print 403; then stop the server. A 200/307 here means the in-handler requireRole was skipped or bypassed — fix before commit. (The authenticated 200 + text/csv path is covered by the unit-tested helper plus the admin-session action tests.)
  </verify>
  <done>
CSV helper tests green (BOM, RFC 4180 quoting, CRLF, ISO timestamps, formula guard). Build passes with the route compiled. Unauthenticated export returns 403; admin export downloads subscribers-YYYY-MM-DD.csv; the subscribers table header links to it.
  </done>
  <commit>feat(260824-3l2): subscribers CSV export route + export link (D-03)</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → subscribeNewsletter | Unauthenticated Server Action invocation; arbitrary FormData crosses here |
| Public internet → GET /dashboard/subscribers/export | Unauthenticated HTTP GET against an admin download surface |
| Authenticated dashboard user (any role) → save/list/count/delete actions | Session holder below admin role attempting admin operations |
| Admin browser → CSV file opened in Excel/Sheets | Exported data interpreted as formulas by the spreadsheet app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3l2-01 | Tampering/DoS | subscribeNewsletter (public) | high | mitigate | Honeypot "website" silent-success (D-05) + Redis slidingWindow 5/1h per IP, prefix ratelimit:newsletter (Task 4) |
| T-3l2-02 | Elevation of privilege | saveNewsletterSettings + settings writes | critical | mitigate | requireRole("admin") FIRST line (Task 2); upsertSetting kept private — never exported from the "use server" file (exported upsert = arbitrary settings write for anyone) |
| T-3l2-03 | Elevation of privilege | list/count/delete actions | critical | mitigate | requireRole("admin") first in each; proven by MUST_NOT_BE_REACHED tests (Tasks 2/6) |
| T-3l2-04 | Info disclosure / enumeration | subscribeNewsletter responses | medium | mitigate | D-01 uniform success for new/duplicate/re-activated emails — response never reveals subscriber existence; error states only for invalid input/rate limit |
| T-3l2-05 | Info disclosure | CSV export route | high | mitigate | In-handler requireRole → 403 (middleware is UX-only); Cache-Control no-store; token column never selected into list or CSV payloads (Tasks 6/7) |
| T-3l2-06 | Tampering (formula injection) | CSV opened in Excel/Sheets | medium | mitigate | RFC 4180 quote-wrapping + apostrophe prefix on = + - @ tab-leading fields (Task 7 helper, unit-tested) |
| T-3l2-07 | Repudiation | subscribe writes | low | accept | No IP/UA retention by design (research A3 — privacy over auditability for a v1 single-opt-in list); server logs via @/lib/log cover incident forensics |
| T-3l2-SC | Tampering | package installs | n/a | accept | Zero packages installed in this task (research: "No new packages are required") — no [ASSUMED]/[SUS] legitimacy checkpoints needed |
</threat_model>

<verification>
After all waves (whole-task gate, run in order):
1. pnpm test — full vitest suite green (newsletter + export tests plus all 40+ existing tests — no regressions in settings/contact/rate-limit surfaces).
2. pnpm test:migrations — clean-room replay includes subscribers.
3. pnpm build — zero errors; both MEDIUM flags cleared (footer island prerenders; export route compiles).
4. Permission grep gate: grep -v '^\s*//' src/actions/newsletter.ts | grep -c 'await requireRole("admin")' equals exactly 4.
5. Exclusion gate (D-04 deferral honored): test ! -d "src/app/(site)/newsletter" — no public unsubscribe route exists; and grep for sendEmail usage inside src/actions/newsletter.ts returns nothing (no email sending per D-01).
6. No new entries in package.json dependencies (zero-install task): git diff --stat package.json is empty.
</verification>

<success_criteria>
- Submitting a valid email in the running footer inserts an active subscriber row; submitting the same email again shows the configured success message with no error (D-01 idempotency).
- Toggling the dashboard enable switch to off and saving makes the public footer render with NO newsletter column; new heading/description/success texts appear on the footer after save without a rebuild (D-02).
- A bot-style submit with the honeypot filled shows success and inserts nothing; a 6th subscribe from one IP within an hour shows the rate-limit message (D-05).
- /dashboard/subscribers (admin only) lists email/status/subscribed-at with working delete and a CSV download whose first bytes are the BOM and whose fields are quote-wrapped (D-03).
- The unsubscribe token column exists and is populated on every row, but no public route consumes it and no admin surface displays it (D-04).
- All automated gates in <verification> pass; each task's commit landed with the 260824-3l2 scope tag.
</success_criteria>

<output>
Create `.planning/quick/260824-3l2-functional-frontend-newsletter-with-dash/260824-3l2-SUMMARY.md` when done (what shipped, files, verification results, deviations).
</output>
