---
phase: quick-260823-4yc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries/posts.ts
  - src/lib/queries/archive.ts
  - src/lib/queries/users.ts
  - src/lib/post-card.ts
  - src/lib/__tests__/post-card.test.ts
  - src/lib/queries/__tests__/posts.test.ts
  - src/components/site/PostCard.tsx
  - src/components/site/Pagination.tsx
  - src/components/site/__tests__/pagination.test.ts
  - src/components/site/HomeFeed.tsx
  - src/app/(site)/page.tsx
  - "src/app/(site)/page/[pageNumber]/page.tsx"
  - src/app/(site)/blog/page.tsx
  - "src/app/(site)/blog/page/[pageNumber]/page.tsx"
  - "src/app/(site)/category/[slug]/page.tsx"
  - "src/app/(site)/tag/[slug]/page.tsx"
  - src/app/(site)/archive/page.tsx
  - "src/app/(site)/author/[username]/page.tsx"
  - src/components/site/RelatedPosts.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Homepage shows only a Featured horizontal card (image left ~45%, category tag, large title, avatar+name+date+read-time meta row, excerpt, Read More link) and a Latest Posts 3-column grid of 12 PostCards with numbered pagination — no per-category teasers, no Trending, no Newsletter (locked decision 1)"
    - "\"/page/2\" renders homepage grid page 2; \"/page/1\" and \"/page/0\" redirect to \"/\"; a page beyond the last 404s (locked decision 2)"
    - "PostCards on /blog, /blog/page/N, /category/[slug], /tag/[slug], /author/[username], /archive, and RelatedPosts show a category tag (when set), an author avatar image or initial-letter fallback, and an 'N min read' label (locked decision 3)"
    - "Pagination on the homepage links page 1 to \"/\" and page N to \"/page/N\" — never a protocol-relative double-slash URL"
    - "The featured/hero post never appears twice across homepage pages (excluded from the paginated feed on every page)"
    - "Consumers that cannot supply the new props (notably /search) compile and render exactly as before — all new PostCard props are optional"
    - "Read time is computed from posts.body Tiptap JSON via the existing Bangla-aware deriveReadingTime (200 WPM, minimum 1)"
  artifacts:
    - src/lib/post-card.ts (shared row-to-PostCardProps mapper)
    - src/components/site/HomeFeed.tsx (shared home feed: Featured card + Latest grid + Pagination)
    - "src/app/(site)/page/[pageNumber]/page.tsx (homepage pagination route)"
    - src/lib/__tests__/post-card.test.ts
    - src/components/site/__tests__/pagination.test.ts
  key_links:
    - "listPublished/listArchive/listAuthorPosts/listRelated leftJoin categories (+ user where missing) → toPostCardProps → PostCard categoryName/categorySlug/authorAvatar/readTime"
    - "posts.body jsonb → deriveReadingTime → 'N min read' label"
    - "Pagination buildPageHref('/') normalization → /page/[pageNumber] route"
    - "listPublished/countPublished excludeIds → hero excluded consistently across all homepage pages"
---

<objective>
Implement the frontpage design: homepage rebuilt as Featured horizontal card + Latest Posts 3-column grid (12/page, real pagination), plus a site-wide PostCard upgrade (category tag, author avatar with initial fallback, read time) applied to every PostCard consumer.

Purpose: Match the approved "frontpage design.png" mockup (layout structure only — colors/fonts come from the existing Outfit/brand system) and give every post card richer context (category, author, read time).

Output: Rewritten homepage + new /page/[pageNumber] route + upgraded PostCard + extended query layer feeding it, with unit tests.

Locked user decisions implemented (do NOT revisit):
- Decision 1: Homepage = Featured horizontal card (image left ~45%, content right: category tag, large title, avatar+name+date+read-time meta row, excerpt, Read More link) → Latest 3-col grid of PostCards, 12/page → Pagination → footer. NO Trending/Newsletter sections. Category teaser sections removed.
- Decision 2: Real homepage pagination — /page/[pageNumber] under (site) mirroring /blog/page/[pageNumber] (page 1 = "/", page N = "/page/N", /page/1 redirects to "/").
- Decision 3: PostCard optional props — category tag (small uppercase brand label → /category/[slug]), author avatar (user.avatar via next/image, initial-letter fallback when unset), read time ("N min read", ~200 words/min from post content) — applied to ALL consumers: /blog, /blog/page/N, /category/[slug], /tag/[slug], /author/[username], /archive, RelatedPosts.

Key facts verified during planning (executors can trust these):
- The read-time computation ALREADY EXISTS: src/lib/reading-time/index.ts exports deriveReadingTime(bodyJson, wpm=200) — Bangla-aware (Intl.Segmenter), returns minimum 1, handles null/invalid input. Reuse it; do NOT write a new word counter. Display format "{n} min read" matches the single-post page.
- Post content lives in posts.body (jsonb, Tiptap JSON, nullable) — there is no posts.content column.
- Avatar rendering precedent: src/app/(site)/author/[username]/page.tsx renders user.avatar via next/image with an initial-letter circle fallback — mirror that pattern at card scale.
- user.avatar is a URL/path already resolvable by next/image (the CI/CD loader in src/lib/image-loader.ts) — the author page proves it works.
- The (site)/layout.tsx already renders SiteHeader/SiteFooter around every route, so "then footer" needs no footer work.
- SiteHeader nav, SiteFooter column layout, and /search result cards are OUT OF SCOPE (not in the locked decisions; optional props keep /search compiling unchanged).
- The mockup's extra horizontal card under "Latest Posts" is NOT built — decision 1 defines Latest as a uniform 3-column grid.
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@./.claude/CLAUDE.md

Design reference (read it — layout structure only, colors/fonts from existing system):
- "frontpage design.png" (repo root)

Core source files (read all before editing):
- src/app/(site)/page.tsx — current homepage being rewritten (keep: hero featured→fallback logic, D-16 empty state, generateMetadata; remove: category teasers, listCategoriesWithCounts usage)
- src/app/(site)/blog/page.tsx — BLOG_PAGE_SIZE export pattern + PostCard grid + Pagination usage
- "src/app/(site)/blog/page/[pageNumber]/page.tsx" — THE route pattern to mirror for /page/[pageNumber] (parse, redirect, notFound, Suspense + PostCardGridSkeleton, generateMetadata shape)
- src/components/site/PostCard.tsx — component being upgraded (pure server component, keep it that way)
- src/components/site/Pagination.tsx — buildPageHref needs root-basePath normalization (currently basePath "/" would produce a protocol-relative "//page/N" URL — a real bug this task fixes)
- src/components/site/ArchiveList.tsx — takes PostCardProps[]; no changes needed (posts prop just gets richer)
- src/components/site/RelatedPosts.tsx — currently passes null author (listRelated has no user join); becomes fully wired
- src/lib/queries/posts.ts — listPublished / listFeatured / listRelated / countPublished; cacheTag('posts-list') + cacheLife('hours') contract MUST be preserved
- src/lib/queries/archive.ts — listArchive (feeds /archive, /category, /tag)
- src/lib/queries/users.ts — listAuthorPosts (innerJoins user already)
- src/lib/reading-time/index.ts — deriveReadingTime (reuse as-is)
- "src/app/(site)/author/[username]/page.tsx" — avatar + initial-fallback markup to mirror
- src/lib/queries/__tests__/posts.test.ts — chainable Drizzle mock (see Task 1 for the one mock addition needed)

Conventions that apply (from CLAUDE.md / .claude/CLAUDE.md):
- pnpm only. Server Components by default; NO "use client" on anything here. next/image only (never raw img). Drizzle joins, no raw SQL. Next 16 async params (await params). cacheComponents: query-layer 'use cache' + cacheTag stays; page components themselves carry no 'use cache' when all called functions are cached (current homepage already works this way). TypeScript strict, no new `any`. Update [CITED] header comments where intent changes.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PostCard upgrade (category tag / avatar / read time) + query joins + shared mapper</name>
  <files>src/lib/queries/posts.ts, src/lib/queries/archive.ts, src/lib/queries/users.ts, src/lib/post-card.ts, src/components/site/PostCard.tsx, src/lib/__tests__/post-card.test.ts, src/lib/queries/__tests__/posts.test.ts</files>
  <behavior>
    - Test (new src/lib/__tests__/post-card.test.ts): toPostCardProps maps a joined row {posts, user, categories} to PostCardProps including categoryName/categorySlug (null when categories is null), authorAvatar (null when user null), authorName/authorUsername, and readTime computed from posts.body (a Tiptap doc whose text totals ~400 word-like segments → readTime 2; a null body → readTime 1, never null).
    - Test (extend src/lib/queries/__tests__/posts.test.ts): listPublished({page:1, excludeIds:[7]}) still cacheTags "posts-list" and returns rows; countPublished({excludeIds:[7]}) returns Number(value) from the count row.
  </behavior>
  <action>
  Query layer — add the data the upgraded cards need (locked decision 3). Preserve every existing 'use cache' + cacheLife + cacheTag call exactly.

  1. src/lib/queries/posts.ts:
     - listPublished: add `.leftJoin(schema.categories, eq(schema.categories.id, schema.posts.categoryId))` to BOTH branches (tag innerJoin branch and plain branch, after the existing user leftJoin). Add optional `excludeIds?: number[]` to opts — when set and non-empty, push `not(inArray(schema.posts.id, opts.excludeIds))` onto conditions (both `not` and `inArray` are already imported). This powers consistent hero exclusion across homepage pages.
     - countPublished: add the same optional `excludeIds` filter (same condition, both branches) so homepage totalPages match the excluded feed.
     - listFeatured: add the same categories leftJoin (row gains a categories key).
     - listRelated: add `.leftJoin(schema.user, eq(schema.user.id, schema.posts.authorId))` AND the categories leftJoin to BOTH the same-category query and the tag-fallback query — every returned row now has posts + user + categories keys (the tag branch also keeps postTags). Update the dedupe/normalization code and header comment accordingly (the "posts" in row check still works; RelatedPosts simplification happens in Task 3).
  2. src/lib/queries/archive.ts — listArchive: add the same categories leftJoin to both branches (feeds /archive, /category/[slug], /tag/[slug]).
  3. src/lib/queries/users.ts — listAuthorPosts: add the categories leftJoin after the existing user innerJoin.

  4. Create src/lib/post-card.ts — the ONE shared row-to-props mapper (replaces the inline mapping currently duplicated across 7 consumers):
     - Export interface PostCardJoinedRow with the fields the mapper reads: posts { id, title, slug, excerpt, featureImage, publishedAt, body (unknown — jsonb) }, user { name, username, avatar } | null, categories { name, slug } | null.
     - Export function toPostCardProps(row: PostCardJoinedRow): PostCardProps returning the current fields PLUS authorAvatar (row.user?.avatar ?? null), categoryName/categorySlug (null-safe from categories), and readTime: deriveReadingTime(row.posts.body). Type-only import of PostCardProps from "@/components/site/PostCard"; import deriveReadingTime from "@/lib/reading-time". Pure module — no db, no react.
  5. src/components/site/PostCard.tsx — add three OPTIONAL, nullable props to PostCardProps (locked decision 3 — optional so consumers that cannot supply them, notably /search, compile and render unchanged): categoryName?: string | null, categorySlug?: string | null, authorAvatar?: string | null, readTime?: number | null. Reorder the card body to match the design mockup: (image) → category tag → title → meta row → excerpt:
     - Category tag: when BOTH categoryName and categorySlug are set, render above the title a small uppercase brand-colored label: a Link to /category/ plus the slug, styled text-xs font-semibold uppercase tracking-wider in brand-600 (dark brand-400) with hover:underline.
     - Meta row: avatar first — when authorAvatar is set, next/image width 24 height 24 rounded-full object-cover with alt of the author name plus "'s avatar"; else when authorName is set, a 24px rounded-full initial-letter circle (first character of authorName uppercased) mirroring the author page fallback markup at small scale (gray-100 bg / gray-500 text, dark variants); then author name (existing link-or-plain logic), published date, and readTime as a span rendering the number followed by " min read" (same format as the single-post page), separated by the existing bullet separators. Render nothing for a piece when its value is absent.
     - Excerpt moves below the meta row (line-clamp-3 kept).
     - Keep it a pure server component with plain Links.
  6. Tests: create src/lib/__tests__/post-card.test.ts per the behavior block (node env — deriveReadingTime is pure and already tested, so no db mock needed). In src/lib/queries/__tests__/posts.test.ts: import countPublished alongside the existing imports; make the chainable mock awaitable by giving the builder self a `then` that resolves to the value of selectMock() (one line — countPublished awaits the builder directly instead of calling .limit); add the two excludeIds tests per the behavior block.

  Atomic commit: feat(260823-4yc): PostCard category/avatar/read-time props + query joins + shared mapper
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
  All list/feed queries (listPublished, listFeatured, listRelated, listArchive, listAuthorPosts) return rows carrying categories name+slug and user avatar; countPublished/listPublished accept excludeIds; toPostCardProps exists and computes readTime from body; PostCard renders category tag, avatar-or-initial, and min-read label from optional props; existing consumers compile untouched (props optional); full vitest suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Homepage rebuild — Featured horizontal card, 12-post Latest grid, /page/[pageNumber] route, Pagination root fix</name>
  <files>src/components/site/Pagination.tsx, src/components/site/__tests__/pagination.test.ts, src/components/site/HomeFeed.tsx, src/app/(site)/page.tsx, "src/app/(site)/page/[pageNumber]/page.tsx"</files>
  <action>
  Locked decisions 1 + 2. Match "frontpage design.png" structure only (colors/fonts/typography from the existing Outfit + brand-500 system, light+dark variants on every new class).

  1. src/components/site/Pagination.tsx — fix buildPageHref for a root base path and export it for testing (named export buildPageHref). Normalize the base by stripping trailing slashes, then: page 1 resolves to the normalized base OR "/" when it became empty, and page N greater than 1 resolves to normalized base + "/page/" + N. Behavior for every existing caller ("/blog", "/archive", category/tag paths) must be byte-identical; the new "/" caller now yields "/" and "/page/N" instead of the previously-produced protocol-relative double-slash path.
  2. Create src/components/site/__tests__/pagination.test.ts (node env) importing only buildPageHref: assert root basePath page 1 → "/", page 2 → "/page/2"; "/blog" page 1 → "/blog", page 2 → "/blog/page/2"; and the searchParams mode with basePath "/archive" and an incoming page param of "2" at page 3 → "/archive?page=3" (unchanged behavior). If importing the module (it imports next/link) fails under the node environment, move buildPageHref into a new tiny module src/components/site/page-href.ts, re-export it from Pagination.tsx, and import the tiny module in the test instead.
  3. Create src/components/site/HomeFeed.tsx — the shared homepage content (async server component; NO "use cache" on it — its queries carry the cache directives, same posture as the current homepage). Export const HOME_PAGE_SIZE = 12. Props: page number.
     - Data: featured = await listFeatured(1); heroRow = featured[0] or, when empty, the first row of listPublished({page:1, pageSize:1}) — KEEP this fallback (a fresh blog still gets a hero). excludeIds = hero id present ? array of that one id : undefined. Then in parallel: gridRows = listPublished({page, pageSize: HOME_PAGE_SIZE, excludeIds}) and total = countPublished({excludeIds}); totalPages = max(1, ceil(total / HOME_PAGE_SIZE)). Excluding the hero on EVERY page (not just page 1) prevents the same post appearing twice across pages.
     - Beyond-last-page guard: when page is greater than 1 AND page exceeds totalPages AND gridRows is empty, call notFound().
     - Empty state: when there is no hero row and the grid is empty, render the existing D-16 friendly "No posts yet" markup (copy it from the current page.tsx).
     - Render (container: mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8):
       a. Featured section — ONLY when page is 1 and a hero row exists: an h2 "Featured" heading, then FeaturedCard.
       b. FeaturedCard (local function component in the same file): a rounded-2xl bordered card laid out flex-col on small screens and flex-row from lg up — image left approximately 45% (a relative container: full width with a 16/10 aspect on mobile, lg:aspect-auto lg:w-[45%] shrink-0; next/image fill object-cover priority, wrapped in a Link to /blog/ plus slug with tabIndex -1 and aria-hidden, mirroring the existing hero image pattern; omitted entirely when no feature image). Right column (flex-1, padded p-6 sm:p-8): category tag (same small uppercase brand label markup as PostCard, when the hero has a category), the post title as an h1 (text-2xl sm:text-3xl lg:text-4xl font-bold, linking to /blog/ plus slug), meta row (32px avatar — next/image or initial-letter fallback, author name linking /author/ plus username when set, formatted date, readTime as number + " min read"), excerpt (line-clamp-3), and a "Read More" link with a right arrow in brand color to /blog/ plus slug. Derive all card props by mapping the hero row through toPostCardProps from src/lib/post-card (Task 1) so category/avatar/readTime come from the same mapper.
       c. Latest section — when grid rows exist: an h2 "Latest Posts" heading, then a 1/2/3-column responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3, gap-6) of PostCard via toPostCardProps, then Pagination with the current page, totalPages, and basePath "/" (page 1 renders no nav thanks to the totalPages guard inside Pagination).
     - NO Trending section, NO Newsletter section, NO per-category teasers (locked decision 1), and no "View all" link.
  4. Rewrite src/app/(site)/page.tsx: keep generateMetadata exactly as-is ('use cache' + getSeoSettings + buildSiteMetadata) and the updated header comments; the default export becomes just HomeFeed with page 1 inside the site layout (SiteFooter already wraps it — no footer work). Delete the old hero/grid/teaser code, the local JoinedPostRow/toPostCardProps/dateFormatter helpers, and the taxonomy import.
  5. Create "src/app/(site)/page/[pageNumber]/page.tsx" mirroring "src/app/(site)/blog/page/[pageNumber]/page.tsx" (locked decision 2):
     - generateMetadata: await params; getSeoSettings; buildArchiveMetadata with name "Home — Page " + pageNumber, kind "category", path "/page/" + pageNumber (byte-similar to the blog route's metadata shape).
     - Default export wraps the content in Suspense with PostCardGridSkeleton (PPR pattern). Content component: await params, parseInt the segment; when NaN, less than 1, OR exactly 1 → redirect("/") (canonical page 1); otherwise render HomeFeed with that page (HomeFeed owns the beyond-last 404). Import nothing page-size-related here — HomeFeed owns HOME_PAGE_SIZE.
  Update [CITED] header comments in both route files to cite this quick task's locked decisions.

  Atomic commit: feat(260823-4yc): homepage featured card + 12-post Latest grid + /page/N pagination
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <done>
  "/" renders Featured (horizontal, image ~45% left) + Latest Posts 3-col grid of 12 + numbered pagination; teasers/Trending/Newsletter gone; "/page/2" renders grid page 2 without the hero duplicated; "/page/1" and "/page/0" redirect to "/"; a page beyond the last 404s; homepage Pagination links are "/" and "/page/N" (never double-slash); "/blog" pagination URLs unchanged; build passes (route + type safety).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire every remaining PostCard consumer to the upgraded props</name>
  <files>src/app/(site)/blog/page.tsx, "src/app/(site)/blog/page/[pageNumber]/page.tsx", "src/app/(site)/category/[slug]/page.tsx", "src/app/(site)/tag/[slug]/page.tsx", src/app/(site)/archive/page.tsx, "src/app/(site)/author/[username]/page.tsx", src/components/site/RelatedPosts.tsx</files>
  <action>
  Locked decision 3 rollout: replace each consumer's inline row-mapping block with the shared mapper so every card gets category tag, avatar, and read time.

  1. In src/app/(site)/blog/page.tsx and "src/app/(site)/blog/page/[pageNumber]/page.tsx": replace the local row-shape cast + field-by-field object building with rows.map over toPostCardProps (import from "@/lib/post-card"; cast each row with `as PostCardJoinedRow` following the existing cast convention). Delete the now-dead local JoinedPostRow types.
  2. Same replacement in "src/app/(site)/category/[slug]/page.tsx", "src/app/(site)/tag/[slug]/page.tsx", and src/app/(site)/archive/page.tsx (their rows come from listArchive, which now joins categories — the cards on category/tag routes will show their own category tag, which is correct and harmless).
  3. src/app/(site)/author/[username]/page.tsx: replace the inline mapping (listAuthorPosts rows now carry categories + user.avatar) with toPostCardProps; keep the bio header, Person JSON-LD, and Prev/Next author pagination untouched.
  4. src/components/site/RelatedPosts.tsx: every listRelated branch now returns rows with posts + user + categories keys, so drop the old union normalization ("posts" in row check) and the null-author placeholder comments — map every row directly through toPostCardProps. Update the header comment that called the user join a future enhancement (it has landed).
  5. Do NOT touch src/app/(site)/search/page.tsx — searchPosts returns flat rows without user/category/body, and the new PostCard props are optional, so it compiles and renders exactly as before. Leave a one-line note in its header comment only if a comment there would otherwise read as stale.
  Update [CITED] header comments in touched files to reference this quick task.

  Atomic commit: feat(260823-4yc): wire all PostCard consumers to category/avatar/read-time props
  </action>
  <verify>
    <automated>pnpm test && pnpm build && grep -rL "toPostCardProps" "src/app/(site)/blog/page.tsx" "src/app/(site)/blog/page/[pageNumber]/page.tsx" "src/app/(site)/category/[slug]/page.tsx" "src/app/(site)/tag/[slug]/page.tsx" "src/app/(site)/archive/page.tsx" "src/app/(site)/author/[username]/page.tsx" src/components/site/RelatedPosts.tsx</automated>
  </verify>
  <done>
  grep -rL output is empty (every listed consumer uses the shared mapper); cards on /blog, /blog/page/N, /category, /tag, /archive, /author, and RelatedPosts show category tag + avatar-or-initial + min-read; /search untouched and compiling; full test suite + production build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → public pages | Cached published-only reads (status='published' AND deletedAt IS NULL) feed public cards — filters unchanged by this task |
| DB values → rendered markup | New rendered values: category name/slug, user avatar URL, derived read time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4yc-01 | Information Disclosure | joined queries (user columns) | low | accept | Full-row selects already returned all user columns before this task (user join pre-existed); the mapper reads only public fields (name/username/avatar) and rows never cross to the client — server components render strings only |
| T-4yc-02 | Tampering | PostCard/FeaturedCard avatar + category hrefs (DB-sourced) | low | mitigate | Avatar renders only via next/image (rejects non-http(s) srcs; CI/CD loader resolves); category hrefs are template-built /category/ + slug — never raw HTML injection; no dangerouslySetInnerHTML anywhere in this task |
| T-4yc-03 | Denial of Service | read-time derivation on list rows | low | accept | deriveReadingTime runs once per cached row render inside 'use cache' query results (cacheLife hours); bodies are bounded by editor content |
| T-4yc-SC | Tampering | package installs | n/a | accept | No package installs in this task (all dependencies already in package.json) |
</threat_model>

<verification>
- pnpm test — full vitest suite green (new mapper + pagination-href + excludeIds tests included).
- pnpm build — production build passes: new /page/[pageNumber] route compiles, no type errors from the optional props, existing routes unaffected.
- Manual spot-check (optional, local dev): "/" shows Featured horizontal card + 12-card Latest grid + pagination; "/page/1" redirects to "/"; a PostCard with no category and no avatar still renders cleanly (initial fallback only when authorName exists).
</verification>

<success_criteria>
- Locked decisions 1, 2, and 3 fully implemented with no scope additions (no Trending/Newsletter/teasers, no header/footer redesign, /search untouched).
- All must_haves truths observable on the running site.
- Zero regressions: /blog pagination URLs, ArchiveList filters, author page bio/JSON-LD, and search results render exactly as before.
- Three atomic commits, one per task.
</success_criteria>

<output>
Create `.planning/quick/260823-4yc-implement-frontpage-design-homepage-site/260823-4yc-SUMMARY.md` when done
</output>
