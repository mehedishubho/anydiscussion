---
quick_id: 260827-se8
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Task 1 — notifications data layer
  - src/db/schema.ts
  - src/db/migrations/            # generated via pnpm db:generate (never hand-written)
  - src/lib/notifications.ts      # NEW — notifyUsers fan-out helper (server-only module)
  - src/actions/notifications.ts  # NEW — countUnreadNotifications / listNotifications / markNotificationsRead
  - src/actions/newsletter.ts     # subscriber event hook
  - src/actions/__tests__/notifications.test.ts   # NEW
  - src/actions/__tests__/newsletter.test.ts
  # Task 2 — shared list-filter infrastructure
  - src/lib/list-filters.ts                         # NEW — firstValue/bounded/clampPage + DASHBOARD_PAGE_SIZE
  - src/lib/__tests__/list-filters.test.ts          # NEW
  - src/components/dashboard/lists/ListFilterBar.tsx # NEW — generic URL-writing client filter bar
  # Task 3 — storage form
  - src/app/(admin)/dashboard/settings/storage/StorageSettingsForm.tsx
  - src/app/(admin)/dashboard/settings/storage/__tests__/StorageSettingsForm.test.tsx  # NEW
  # Task 4 — posts workstream
  - src/actions/posts.ts
  - src/actions/posts-schema.ts
  - src/actions/__tests__/posts.test.ts
  - src/app/(admin)/dashboard/posts/page.tsx
  - src/app/(admin)/dashboard/posts/components/PostRowActions.tsx
  # Task 5 — users list
  - src/actions/users.ts
  - src/actions/users-schema.ts
  - src/actions/__tests__/users.test.ts
  - src/app/(admin)/dashboard/users/page.tsx
  # Task 6 — categories list
  - src/actions/categories.ts
  - src/actions/taxonomy-schema.ts
  - src/actions/__tests__/taxonomy.test.ts
  - src/app/(admin)/dashboard/categories/page.tsx
  # Task 7 — media list
  - src/actions/media.ts
  - src/actions/media-schema.ts
  - src/actions/__tests__/media.test.ts
  - src/app/(admin)/dashboard/media/page.tsx
  # Task 8 — header functionalization
  - src/actions/search.ts                           # NEW — globalSearch
  - src/actions/__tests__/search.test.ts            # NEW
  - src/components/header/GlobalSearch.tsx          # NEW
  - src/components/header/NotificationDropdown.tsx  # rewrite as functional bell island
  - src/layout/AppHeader.tsx
autonomous: true
must_haves:
  truths:
    - Typing at least 2 characters in the dashboard header search shows a grouped live-results dropdown (Posts / Users / Categories / Tags); clicking a post result opens its edit page; non-admins never receive user results; authors only receive their own posts in the posts group (no draft leakage from other authors)
    - The header bell shows the signed-in user's unread notification count; opening it lists recent notifications and marks them read; the count clears
    - Submitting a post for review notifies every editor/admin except the actor; publishing or returning a post notifies its author except the actor; a first-time (or re-subscribe-after-unsubscribe) newsletter signup notifies admins — and a notification-insert failure NEVER fails the parent mutation
    - /dashboard/posts, /dashboard/users, /dashboard/categories, /dashboard/media each support URL-driven search + filters + pagination — deep-linkable, back-button correct, 20 rows/page, count computed server-side over the same WHERE
    - Editor/admin see a working Return action on pending_review posts that sends the post back to draft and notifies the author
    - /dashboard/settings/storage renders all four provider credential sections simultaneously with the active provider visually distinguished; each non-active section remains editable and testable in place
  artifacts:
    - src/db/schema.ts notifications table + one drizzle-kit-generated migration under src/db/migrations/
    - src/lib/notifications.ts notifyUsers helper; src/actions/notifications.ts three session-scoped read actions
    - src/lib/list-filters.ts coercion helpers + src/components/dashboard/lists/ListFilterBar.tsx
    - src/actions/posts.ts returnForRevision action + notify hooks in submitForReview/publishPost
    - src/actions/search.ts globalSearch action; src/components/header/GlobalSearch.tsx + rewritten NotificationDropdown.tsx mounted in src/layout/AppHeader.tsx
    - Extended listPosts/listUsers/listCategories/listMedia + countPosts/countUsers/countCategories/countMedia + the four wired list pages
    - StorageSettingsForm.tsx rendering all four provider sections unconditionally + render test
  key_links:
    - "List pages: searchParams (awaited Promise, Next 16) → list-filters parse → extended list* action (permission check FIRST → WHERE/count/limit-offset) → Link-based site Pagination (buildPageHref preserves filter params) — the URL is the single source of truth; client components only write URLs"
    - "Bell: useQuery([\"notifications-unread\"]) → countUnreadNotifications (session-scoped) → markNotificationsRead on open → invalidate both notification query keys"
    - "Event sources (submitForReview / publishPost / returnForRevision / subscribeNewsletter) → notifyUsers(recipients minus actor, type, payload) → awaited try/catch + log.error, swallowed after logging (never void fire-and-forget in a Server Action)"
    - "GlobalSearch: useQuery([\"global-search\", q], 300ms debounced, enabled at 2 chars) → globalSearch (getSessionOrThrow FIRST; role-scoped legs derived from the session, never client input) → click-through routes"
---

<objective>
Close the six dashboard functional gaps: functional header notification bell + global search dropdown; server-side URL-driven filter/search/pagination on the four dashboard list pages; storage settings showing all four provider sections at once; plus the missing return-for-revision action the notification design requires.

Purpose: The dashboard chrome is real but inert in three spots — the header search/bell are TailAdmin demo markup, the list pages render unfilterable first-N rows, and the storage form hides three of four provider sections behind the active-provider selector. The notifications decision also surfaces a genuine codebase gap: TRANSITIONS legalizes editor/admin pending_review→draft but no action wraps it, so the "notify author on return" event has no trigger.

Output: One plan, 8 atomic tasks in 2 dependency waves. Wave 1 (Tasks 1-3): notifications data layer + migration, shared list-filter infrastructure, storage form. Wave 2 (Tasks 4-8, file-disjoint, safe to run in parallel): posts, users, categories, media list mechanics + header functionalization.

Task dependency order (Task 4 needs Task 1 + 2 artifacts; Tasks 5-7 need Task 2; Task 8 needs Task 1):

```
Wave 1:  T1 notifications-data-layer   T2 list-infrastructure   T3 storage-form
Wave 2:  T4 posts (dep T1,T2)   T5 users (dep T2)   T6 categories (dep T2)
         T7 media (dep T2)      T8 header search+bell (dep T1)
```

Research is authoritative (260827-se8-RESEARCH.md, HIGH confidence, verified against installed next@16.3.3 / drizzle-orm 0.45.2 / the source files) — do not re-litigate. Discretion items resolved by this plan: 20 rows/page everywhere; 300ms debounce for both header search and filter-bar q inputs; 2-char minimum for header search; bell refetchInterval 60s + refetchOnWindowFocus; author filter on posts is a free-text input matching author name/email via join; pagination control = the existing Link-based site Pagination rendered server-side on all four pages (uniform, zero client JS, buildPageHref already unit-tested — TailAdmin chip styling deliberately traded for URL-first correctness); the bell dropdown's demo "View All Notifications" footer link is dropped (no notifications page exists and none is requested).
</objective>

<execution_context>
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/workflows/execute-plan.md
@D:/Devsroom-Work/anydiscussion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/CLAUDE.md
@.planning/STATE.md
@.planning/quick/260827-se8-dashboard-functional-gaps-make-dashboard/260827-se8-RESEARCH.md

Key source files (read before editing — all verified this session):
- src/actions/{posts,users,categories,media,newsletter}.ts + {posts,users,taxonomy,media}-schema.ts — action conventions: permission check FIRST, Zod gate after, mock-db test harness in src/actions/__tests__/
- src/app/(admin)/dashboard/{posts,users,categories,media}/page.tsx — the four list shells (all already export `instant = false`; keep it, add NO `connection()` — awaiting searchParams is itself the dynamic access, per RESEARCH Finding 2)
- src/app/(site)/search/page.tsx L41-95 — parseSearch/firstValue/bounded/clampPage pattern to extract (manual V5/V8 coercion, rationale documented in-file)
- src/components/site/Pagination.tsx — Link-based Pagination + exported buildPageHref (preserves filter params in ?page=N)
- src/layout/AppHeader.tsx — inline inert search form (L114-146, with ⌘K ref) + NotificationDropdown mount (L158); QueryProvider is already mounted inside AdminShell (AdminShell.tsx L76), so TanStack Query works in any header island
- src/components/header/NotificationDropdown.tsx — hardcoded demo content to rewrite
- src/lib/permissions/index.ts — getSessionOrThrow / requireCan / requireRole / assertOwnsPost signatures
- src/lib/permissions/post-transitions.ts — TRANSITIONS table; editor/admin pending_review→draft is legal (L30-37)
- src/db/schema.ts — 13 tables, session_userId_idx index pattern (L290) to mirror for notifications
- src/app/(admin)/dashboard/settings/storage/StorageSettingsForm.tsx — the four conditional provider blocks at L169 (cloudinary), L192 (r2), L216 (push-cdn), L245 (local)
- src/app/(admin)/dashboard/posts/components/PostRowActions.tsx — non-optimistic mutation + toast + invalidate(["posts"]) pattern to extend

Verified facts (do not re-derive):
- searchParams is a Promise in Next 16 — `await` it, type `Promise<Record<string, string | string[] | undefined>>` (installed next@16.3.3 docs; in-repo precedent src/app/(site)/search/page.tsx:120-144).
- drizzle-orm 0.45.2 exports ilike / count / or / and (runtime-verified).
- drizzle.config.ts: schema ./src/db/schema.ts, out ./src/db/migrations (forward-only, D-11).
- @testing-library/react 16 + jsdom 29 are installed; vitest runs node env with per-file jsdom opt-in via `// @vitest-environment jsdom` pragma (vitest.config.ts).
- /dashboard/tags exists (tags results click through to it); listTags lives in src/actions/tags.ts.
- The users-leg admin gate mirrors listUsers' admin-only requireCan({user:["read"]}); categories/tags reads are ungated dashboard reads (listCategories has no permission check today — keep that).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Notifications data layer — table, migration, fan-out helper, session-scoped actions, subscriber hook</name>
  <files>src/db/schema.ts, src/db/migrations/ (generated), src/lib/notifications.ts, src/actions/notifications.ts, src/actions/newsletter.ts, src/actions/__tests__/notifications.test.ts, src/actions/__tests__/newsletter.test.ts</files>
  <behavior>
    - notifyUsers(recipients, type, payload): performs ONE db.insert carrying all recipient rows; when the recipients array is empty, performs no insert at all; when db.insert rejects, log.error fires with the error and the helper resolves normally (swallow — proven by mock returning rejection while the caller still succeeds)
    - countUnreadNotifications: getSessionOrThrow FIRST (UNAUTHORIZED thrown before any db call); counts rows filtered by the session user's id with unread semantics (readAt null)
    - listNotifications(page): session-scoped, newest-first, 20 per page, page clamped to ≥1
    - markNotificationsRead: session-scoped update touching ONLY the caller's unread rows (readAt set to now); no userId parameter exists anywhere in the file
    - subscribeNewsletter: on a successful upsert where NO existing ACTIVE subscription row was found by the pre-read, notifyUsers fires for all admins; when the pre-read found an active row (idempotent duplicate), no notify call happens at all; a notifyUsers rejection still returns status "success"
  </behavior>
  <action>
    RED first: write src/actions/__tests__/notifications.test.ts + extend newsletter.test.ts using the vi.hoisted mock-db harness from src/actions/__tests__/users.test.ts (mock @/lib/db, @/lib/permissions, @/lib/log; for newsletter also keep the existing rate-limit/Redis mocks). Commit `test(260827-se8): notifications data layer RED`.

    1. Schema + migration: add a `notifications` table to src/db/schema.ts following existing conventions — id serial PK; userId text NOT NULL .references(() => user.id, { onDelete: "cascade" }); type text NOT NULL; payload jsonb (holds { postId?, postTitle?, subscriberEmail? }); readAt timestamp (NULL = unread); createdAt timestamp .defaultNow().notNull(); plus an index on (userId, readAt) mirroring the session_userId_idx pattern (schema.ts L290). Notification is non-authoritative display data — NO deletedAt (mark-as-read is the only state change; nothing deletes rows in this task). Type strings: "post_submitted", "post_published", "post_returned", "new_subscriber". Then run `pnpm db:generate` (drizzle-kit — NEVER hand-write SQL) and apply to the local dev DB with `pnpm exec drizzle-kit migrate` per drizzle.config.ts (out: ./src/db/migrations).

    2. src/lib/notifications.ts (NEW, plain server-only module — NO "use server", it is imported BY action files): export async function notifyUsers(recipients: string[], type: string, payload: Record<string, unknown>): Promise<void>. Empty array → immediate return. Otherwise ONE awaited db.insert(schema.notifications).values(recipients.map(...)). The ENTIRE insert sits inside try/catch → log.error(...err) → swallow. Cite the createUser verification-email rationale (users.ts L138-154): awaited, never void fire-and-forget (void makes the catch dead code + unhandled rejection inside the Server Action runtime), and a notify failure must never fail the parent mutation.

    3. src/actions/notifications.ts (NEW, "use server"): three actions, each with getSessionOrThrow() as the literal FIRST statement, each filtering by session.user.id — NEVER accepting a userId parameter (T-Q-869-01 self-service precedent): countUnreadNotifications() → number; listNotifications(page = 1) → newest-first page of 20 rows (id, type, payload, readAt, createdAt); markNotificationsRead() → update set readAt new Date() where own + unread.

    4. src/actions/newsletter.ts — subscriber event hook inside subscribeNewsletter (the new-subscriber → admins decision): BEFORE the D-01 upsert, pre-read the subscriber row (select status where email); AFTER the upsert succeeds, only when the pre-read found NO existing row with status "active" (first subscribe or re-subscribe-after-unsubscribe — idempotent duplicates must not re-notify), select all admin ids (role = "admin") and await notifyUsers(adminIds, "new_subscriber", { subscriberEmail }). notifyUsers swallows internally, so the action still returns status "success" when the notify insert fails. Cite research A1 (tiny pre-read/upsert race acceptable — display-only data; rate limiter already bounds volume).

    GREEN: implement until the new tests pass. Commit `feat(260827-se8): notifications data layer GREEN`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/actions/__tests__/notifications.test.ts src/actions/__tests__/newsletter.test.ts && pnpm exec tsc --noEmit && git status --porcelain src/db/migrations | grep -q . && echo MIGRATION_GENERATED</automated>
  </verify>
  <done>All behavior cases green; notifications table exists in schema.ts with the (userId, readAt) index; one drizzle-kit-generated migration file committed under src/db/migrations/; no hand-written SQL; notifyUsers swallows insert failures; all three notification actions are session-scoped with no userId parameter; subscriber notify fires only for non-active pre-read states and never fails the subscribe.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared list-filter infrastructure — coercion helpers + generic URL-writing filter bar</name>
  <files>src/lib/list-filters.ts, src/lib/__tests__/list-filters.test.ts, src/components/dashboard/lists/ListFilterBar.tsx</files>
  <behavior>
    - firstValue(["a","b"]) → "a"; firstValue("a") → "a"; firstValue(undefined) → undefined
    - bounded("  x  ", 200) → "x"; bounded(long-string, 200) → exactly 200 chars; bounded(undefined, n) → ""
    - clampPage(undefined) → 1; clampPage("0") → 1; clampPage("-5") → 1; clampPage("abc") → 1; clampPage("9999") → 1000; clampPage("42") → 42
    - DASHBOARD_PAGE_SIZE === 20
  </behavior>
  <action>
    RED first: src/lib/__tests__/list-filters.test.ts for the pure helpers. Commit `test(260827-se8): list-filter helpers RED`.

    1. src/lib/list-filters.ts (NEW, pure module — no server imports): export firstValue(v: string | string[] | undefined): string | undefined; bounded(v, max): string (trim + slice); clampPage(v): number (parseInt, ≥1, ≤1000 — bounds prevent offset abuse); DASHBOARD_PAGE_SIZE = 20. Extract verbatim from the (site)/search/page.tsx L41-95 implementations and carry over their V5/V8 input-validation rationale comment (tampered string[] URLs, length caps, manual-not-Zod coercion reasoning) with a CITED note.

    2. src/components/dashboard/lists/ListFilterBar.tsx (NEW, "use client"): generic filter bar that ONLY writes URLs (the Server Component re-queries — list-mechanics decision). Props: basePath: string; q: string; selects?: Array<{ name; label; value; options: Array<{ value: string; label: string }> }>; textField?: { name; label; value; placeholder? }. Behavior: builds a fresh URLSearchParams from ALL current prop values (non-empty only — deterministic without reading the URL, sidestepping the useSearchParams CSR-bailout class per research Pitfall 8), always DROPS the page param (any filter change resets to page 1), and pushes via useRouter().push(`${basePath}?${qs}`). q input debounced ~300ms (discretion) and applied immediately on Enter; selects and the optional text field apply on change. TailAdmin form styling (rounded-lg border, dark-mode classes) matching StorageSettingsForm's INPUT_CLASS look. The q param name is "q"; select/text params use their configured names verbatim.

    3. Pagination choice (applies to Tasks 4-7): the pages render the existing Link-based default export from src/components/site/Pagination.tsx directly — server-rendered links, zero client JS, buildPageHref already preserves filter params in ?page=N and is unit-tested. No new pagination component is created.

    GREEN + commit `feat(260827-se8): list-filter helpers + filter bar GREEN`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/__tests__/list-filters.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Helpers extracted with rationale comments and green tests; ListFilterBar compiles, is prop-driven (no useSearchParams), writes URLs only, resets page to 1 on every filter change.
  </done>
</task>

<task type="auto">
  <name>Task 3: Storage settings — render all four provider sections simultaneously with active highlight</name>
  <files>src/app/(admin)/dashboard/settings/storage/StorageSettingsForm.tsx, src/app/(admin)/dashboard/settings/storage/__tests__/StorageSettingsForm.test.tsx</files>
  <action>
    Form-only change (verified: schema, saveStorageSettings, per-provider testStorageConnection, and boot registration already handle all four providers — no backend change).

    1. In StorageSettingsForm.tsx, remove the four conditional-render wrappers that hide inactive provider sections (the watched-activeProvider equality checks at L169 cloudinary / L192 r2 / L216 push-cdn / L245 local) so all four ProviderSections render unconditionally, one after another. Add a visual "Active" indicator (small badge + brand border ring, e.g. border-brand-500) on the section whose provider matches watch("activeProvider") — ProviderSection gains an optional isActive prop. Everything else stays byte-identical: the selector keeps its semantics (uploads route through the selected provider), secret fields still default "" and are never pre-filled (Pitfall 7 / D-25 redaction), per-section Test connection buttons keep working for any provider. Update the L168 comment that says only the active provider is visible.

    2. NEW render test src/app/(admin)/dashboard/settings/storage/__tests__/StorageSettingsForm.test.tsx with the `// @vitest-environment jsdom` pragma + @testing-library/react (installed; per-file jsdom opt-in is the documented vitest.config.ts convention). Mock the server action imports (saveStorageSettings/testStorageConnection from the actions module). Assert: with initial activeProvider "local", the section headings for Cloudinary, Cloudflare R2, Push-CDN, AND Local are all present in the same render (the exact regression this task fixes), and the Local section carries the active indicator. Commit style: `test(260827-se8)` RED then `feat(260827-se8)` GREEN, or one `feat` commit with the test — follow the repo's quick-task TDD convention.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/app/(admin)/dashboard/settings/storage/__tests__/StorageSettingsForm.test.tsx && ! grep -F 'activeProvider === ' src/app/(admin)/dashboard/settings/storage/StorageSettingsForm.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>All four provider sections mount in one render regardless of selector value; active section visually distinguished; secrets still never pre-filled; render test green; no remaining conditional provider-section wrappers in the file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Posts workstream — URL-driven list, returnForRevision action, notify hooks, Return row button</name>
  <files>src/actions/posts.ts, src/actions/posts-schema.ts, src/actions/__tests__/posts.test.ts, src/app/(admin)/dashboard/posts/page.tsx, src/app/(admin)/dashboard/posts/components/PostRowActions.tsx</files>
  <behavior>
    - listPosts(opts): requireCan({post:["read"]}) still FIRST (non-privileged call throws before any db select); applies q as ilike on title OR slug, status equality, categoryId equality, author as ilike on the joined user's name OR email; deterministic desc(updatedAt) ordering; limit/pageSize (default 20, cap 100) + offset
    - countPosts(opts): same permission gate + same WHERE, sql-count(*) shape — never counts by materializing rows
    - returnForRevision(postId): assertOwnsPost FIRST → transitionPost(postId, "draft") → notifies the post's authorId (skipped when authorId is null or equals the actor); a notifyUsers rejection does NOT fail the action (still returns ok); revalidateTag uses the 2-arg form
    - submitForReview: after the transition, notifyUsers fires for every editor+admin id EXCLUDING the actor; notify failure leaves the action succeeding
    - publishPost: after the transition, notifyUsers fires for the post's authorId unless it equals the actor; notify failure leaves publish succeeding (revalidation untouched)
  </behavior>
  <action>
    Depends on Task 1 (notifyUsers, notifications table) and Task 2 (list-filters, ListFilterBar). RED first in posts.test.ts (extend the existing harness). Commit `test(260827-se8): posts list + return-for-revision RED`.

    1. src/actions/posts-schema.ts: add postListSchema — q (string, max 200, optional), status (post status enum, optional), categoryId (positive int, optional), author (string, max 200, optional), page (int ≥1, default 1), pageSize (int 1-100, default 20). Shared Zod, client-agnostic.

    2. src/actions/posts.ts — listPosts: keep requireCan first, then postListSchema.parse; build WHERE with and()/or()/ilike() from drizzle-orm (NOT posts.searchVector FTS — searchPosts is published-only and wrong for the dashboard where drafts/pending must be findable; admin tables are small, ILIKE is the verified decision); author filter needs leftJoin user on posts.authorId; add orderBy desc(posts.updatedAt); limit/offset from parsed page. NEW countPosts(opts): identical gate + WHERE, `select({ n: sql`count(*)` })` shape (newsletter.ts countSubscribers precedent). Keep returning the plain rows array.

    3. returnForRevision(postId) in posts.ts: assertOwnsPost(postId) FIRST (editor/admin bypass; authors can only affect own posts — the server stays correct even though the button is editor/admin-only UI); await transitionPost(postId, "draft") (TRANSITIONS already legalizes pending_review→draft for editor/admin — this is the verified missing wrapper); then fetch the post (title, slug, authorId), then await notifyUsers(filter recipient, "post_returned", { postId, postTitle }) with actor exclusion; then revalidateTag("posts-list", "max") ONLY, with an inline comment noting a pending_review post has no public cache surface (public feeds filter published) and the 2-arg form is mandatory (Pitfall 5). Return { ok: true }.

    4. Notify hooks: submitForReview — capture the session from assertOwnsPost's return; after the transition + a title fetch, select user ids where role in (editor, admin), filter out the actor, await notifyUsers(ids, "post_submitted", { postId, postTitle }). publishPost — add title to the existing post select (L342-353); after step 5's revalidation, await notifyUsers([authorId minus actor], "post_published", { postId, postTitle }) with the same null-author guard. Both cite the swallow contract.

    5. src/app/(admin)/dashboard/posts/page.tsx: keep `export const instant = false` (awaiting searchParams is itself the dynamic access — NO connection() per research Finding 2); add searchParams: Promise<Record<string, string | string[] | undefined>> to props and await it; build a small local parse (parsePostsFilters) on Task 2's firstValue/bounded/clampPage (the (site)/search/page.tsx parseSearch idiom); Promise.all([listPosts({...}), countPosts({...})]) inside the existing try/catch banner; render ListFilterBar with q + a status select (draft/pending_review/published) + a category select (options from a bare listCategories() call — full list, see Task 6 back-compat note) + the author free-text field; render the site Pagination with currentPage, totalPages = max(1, ceil(total/20)), basePath "/dashboard/posts", searchParams=sp; keep the table + PostRowActions + error banner exactly as-is.

    6. PostRowActions.tsx: add a returnMutation (same non-optimistic useMutation + toast + invalidate(["posts"]) pattern); render a second link-button "Return" visible ONLY for editor/admin when status is "pending_review" (UX-only gating — the action re-checks authority); keep existing Publish/Submit logic untouched; per-mutation pending labels.

    GREEN. Commit `feat(260827-se8): posts list mechanics + return-for-revision + notify hooks GREEN`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/actions/__tests__/posts.test.ts && pnpm exec tsc --noEmit && grep -c 'export const instant = false' "src/app/(admin)/dashboard/posts/page.tsx" | grep -qx 1 && grep -c 'returnForRevision' src/app/(admin)/dashboard/posts/components/PostRowActions.tsx | grep -qv 0</automated>
  </verify>
  <done>Posts page is fully URL-driven (deep-linkable q/status/category/author/page, 20/page, server count); returnForRevision exists with permission-first ordering and 2-arg revalidation; Return button appears for editor/admin on pending_review rows; submit/publish/return all notify the right recipients with actor exclusion and swallow notify failures; instant export intact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Users list — URL-driven q/role/banned/verified search, filters, pagination</name>
  <files>src/actions/users.ts, src/actions/users-schema.ts, src/actions/__tests__/users.test.ts, src/app/(admin)/dashboard/users/page.tsx</files>
  <behavior>
    - listUsers(opts): requireCan({user:["read"]}) still FIRST (MUST_NOT_BE_REACHED style proof that a forbidden call never reaches db); q applies as ilike on name OR email; role equality when provided; banned/verified tri-state ("" = any, "true"/"false" → boolean equality on banned / emailVerified); deterministic desc(createdAt) ordering; limit/pageSize default 20 cap 100 + offset
    - countUsers(opts): same gate + WHERE, count(*) shape
  </behavior>
  <action>
    Depends on Task 2. RED first in users.test.ts. Commit `test(260827-se8): users list filters RED`.

    1. users-schema.ts: add userListSchema — q (max 200, optional), role (enum admin/editor/author, optional), banned ("true"|"false" string enum, optional), verified ("true"|"false", optional), page/pageSize as in Task 4. The boolean filters stay string enums in the URL layer and are coerced to booleans inside the action after the permission gate (documented coercion — mirrors the manual-parse ethos).

    2. users.ts: extend listUsers(opts) keeping requireCan FIRST and the existing column projection (no passwordHash — unchanged); apply and/or/ilike per behavior; add countUsers(opts) with the countSubscribers shape. No other action in the file changes.

    3. users/page.tsx: keep `export const instant = false`; await searchParams; local parse via Task 2 helpers; Promise.all inside the existing try/catch; render ListFilterBar (q + Role select + Status select for banned: All/Banned/Active + Verified select: All/Verified/Unverified) and the site Pagination (basePath "/dashboard/users"); UsersTable still receives its page of rows as props (its optimistic mutations + ["posts"]-style invalidations are unaffected — rows just arrive pre-filtered). Keep the sessionUserId logic for delete suppression.

    GREEN. Commit `feat(260827-se8): users list mechanics GREEN`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/actions/__tests__/users.test.ts && pnpm exec tsc --noEmit && grep -c 'export const instant = false' "src/app/(admin)/dashboard/users/page.tsx" | grep -qx 1</automated>
  </verify>
  <done>/dashboard/users supports deep-linkable q/role/banned/verified/page at 20/page with a server-side count; permission-first ordering proven by tests; UsersTable behavior untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Categories list — URL-driven search + pagination (back-compat: bare calls stay unpaginated)</name>
  <files>src/actions/categories.ts, src/actions/taxonomy-schema.ts, src/actions/__tests__/taxonomy.test.ts, src/app/(admin)/dashboard/categories/page.tsx</files>
  <behavior>
    - listCategories(opts): q applies as ilike on name (or slug); keeps isNull(deletedAt) + asc(name); pagination applies ONLY when page/pageSize provided — a bare listCategories() call with no opts returns the FULL list exactly as today (post-editor pickers and the posts-page options fetch depend on this)
    - countCategories(opts): same WHERE (including the deletedAt filter), count(*) shape
  </behavior>
  <action>
    Depends on Task 2. RED first in taxonomy.test.ts. Commit `test(260827-se8): categories list filters RED`.

    1. taxonomy-schema.ts: add categoryListSchema — q (max 200, optional), page (optional — its absence signals "no pagination"), pageSize (1-100, default 20, optional).

    2. categories.ts: extend listCategories(opts?) — it stays an ungated dashboard read (mirrors the existing comment; the proxy + route-group gates are the boundary here); ilike + optional limit/offset per behavior. Add countCategories(opts) (also ungated, same WHERE). No CRUD action changes.

    3. categories/page.tsx: keep `export const instant = false`; await searchParams; local parse; Promise.all inside the existing try/catch; render ListFilterBar (q only — the per-page discretion) + site Pagination (basePath "/dashboard/categories"); CategoriesTable receives its page of rows (optimistic CRUD unaffected).

    GREEN. Commit `feat(260827-se8): categories list mechanics GREEN`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/actions/__tests__/taxonomy.test.ts && pnpm exec tsc --noEmit && grep -c 'export const instant = false' "src/app/(admin)/dashboard/categories/page.tsx" | grep -qx 1</automated>
  </verify>
  <done>/dashboard/categories supports deep-linkable q/page; bare listCategories() callers (CategoryPicker, posts-page options) return the full list unchanged; soft-delete filter preserved in list AND count.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Media list — URL-driven q/kind search + pagination</name>
  <files>src/actions/media.ts, src/actions/media-schema.ts, src/actions/__tests__/media.test.ts, src/app/(admin)/dashboard/media/page.tsx</files>
  <behavior>
    - listMedia(opts): requireCan({media:["read"]}) still FIRST; q applies as ilike on altText OR providerKey; kind applies as a mimeType PREFIX match (like 'image/%') since the column stores full types — replaces nothing, the existing exact mimeType filter stays available; keeps isNull(deletedAt) + desc(createdAt); converts to page/pageSize (default 20, cap 100) computed to limit/offset
    - countMedia(opts): same gate + WHERE, count(*) shape
  </behavior>
  <action>
    Depends on Task 2. RED first in media.test.ts (extend the existing mock harness — the requireCan-before-db proof pattern already lives there). Commit `test(260827-se8): media list filters RED`.

    1. media-schema.ts: extend mediaListSchema to the uniform shape — q (max 200, optional), kind (enum image/video/audio/application, optional), page (int ≥1, default 1), pageSize (int 1-100, default 20). Keep mimeType (exact) optional for compatibility; drop the raw limit/offset fields in favor of page/pageSize (the only caller is the media page, updated in this task; tests updated accordingly).

    2. media.ts: listMedia parses the new schema after the permission gate; kind → like(media.mimeType, `${kind}/%`); q → or(ilike altText, ilike providerKey); offset = (page-1)*pageSize. NEW countMedia(opts): identical gate + WHERE + count(*) shape.

    3. media/page.tsx: keep `export const instant = false`; await searchParams; local parse; Promise.all inside the existing try/catch (replacing the hardcoded listMedia({limit:100}) call); render ListFilterBar (q + Kind select: All/Image/Video/Audio/Application) + site Pagination (basePath "/dashboard/media"); MediaGrid receives its page of rows — its upload/delete flows and optimistic invalidations are untouched (new uploads surface through its existing refresh mechanism).

    GREEN. Commit `feat(260827-se8): media list mechanics GREEN`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/actions/__tests__/media.test.ts && pnpm exec tsc --noEmit && grep -c 'export const instant = false' "src/app/(admin)/dashboard/media/page.tsx" | grep -qx 1</automated>
  </verify>
  <done>/dashboard/media supports deep-linkable q/kind/page at 20/page with a server-side count; permission-first ordering still proven; soft-delete filter preserved; MediaGrid behavior untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Header functionalization — global search dropdown + notification bell</name>
  <files>src/actions/search.ts, src/actions/__tests__/search.test.ts, src/components/header/GlobalSearch.tsx, src/components/header/NotificationDropdown.tsx, src/layout/AppHeader.tsx</files>
  <behavior>
    - globalSearch(q): getSessionOrThrow FIRST (unauthenticated → UNAUTHORIZED before any db access); q length-bounded to 100
    - Posts leg: gated by requireCan({post:["read"]}) AND role-scoped from the session (never client input) — author sees ONLY own posts (authorId equality); editor/admin see any status (drafts findable — dashboard semantics); each leg capped at 5 rows via limit
    - Users leg: runs ONLY when session.user.role is admin — for any other role the user-table select is never invoked (MUST_NOT_BE_REACHED proof) and the returned users group is empty
    - Categories + tags legs: open to all dashboard roles; tags exclude soft-deleted; ilike name; capped at 5
    - Returned shape: { posts: [{id,title,status}], users: [{id,name,email}], categories: [{id,name,slug}], tags: [{id,name,slug}] } — minimal columns only
  </behavior>
  <action>
    Depends on Task 1 (notification actions + table). RED first in search.test.ts. Commit `test(260827-se8): globalSearch RED`.

    1. src/actions/search.ts (NEW, "use server"): export async function globalSearch(q: string). getSessionOrThrow() FIRST; bounded(q, 100) (Task 2 helper); under 2 characters → return empty groups without touching the DB. Role-scoping derived from the session (the constraint: no draft leakage to non-privileged roles). Author scoping note (research A3): the posts list page itself does NOT author-scope (existing dashboard behavior, out of scope) — globalSearch is deliberately the safer direction; document the asymmetry in a comment. All queries are parameterized Drizzle ilike templates (no string concat).

    2. src/components/header/GlobalSearch.tsx (NEW, "use client"): self-contained island — owns the search input (same TailAdmin input markup/placeholder as today), the ⌘K/Ctrl+K focus effect (move it here from AppHeader), ~300ms debounce, minimum 2 characters, useQuery({ queryKey: ["global-search", q], enabled: q.length >= 2 }) (keyed queries discard stale responses; QueryProvider already wraps the shell). Renders an absolutely-positioned grouped dropdown below the input: Posts / Users / Categories / Tags section headers, up to 5 rows each, empty groups hidden. Click-through (verified routes): post row → `/dashboard/posts/${id}/edit`; user row → `/dashboard/users?q=${encodeURIComponent(email)}`; category row → `/dashboard/categories?q=${encodeURIComponent(name)}` (free synergy with the new URL-driven lists); tag row → `/dashboard/tags`. Escape and click-outside close the dropdown.

    3. src/components/header/NotificationDropdown.tsx — full rewrite keeping the export name (AppHeader imports it): self-contained bell island per research Finding 6 — do NOT thread an unread count through the AuthGate prop chain (the layout does not re-render on sibling page navigations; the count would go stale). useQuery({ queryKey: ["notifications-unread"] }) → countUnreadNotifications with refetchOnWindowFocus + refetchInterval 60s (discretion); unread badge on the bell (hidden at 0). Open → useQuery(["notifications"]) → listNotifications(1) + a useMutation calling markNotificationsRead() on open, then invalidateQueries on both keys. Rows render a type→friendly-copy map local to the component ("submitted a post for review" / "published your post" / "returned your post for revision" / "new subscriber") with payload postTitle / subscriberEmail + relative createdAt; rows carrying payload.postId link to the post's edit page. Drop the demo "View All Notifications" footer link (no such page exists — research OQ2). Demo/hardcoded user content is replaced entirely.

    4. src/layout/AppHeader.tsx: replace the inert inline form block (search input + its inputRef/effect) with `<GlobalSearch />` inside the existing hidden/lg:block container; keep the NotificationDropdown mount point as-is (the component behind it is now functional); the user prop chain from 260827-869 stays untouched.

    GREEN. Commit `feat(260827-se8): functional header search + notification bell GREEN`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/actions/__tests__/search.test.ts && pnpm exec tsc --noEmit && grep -c 'GlobalSearch' src/layout/AppHeader.tsx | grep -qv 0 && grep -c 'countUnreadNotifications' src/components/header/NotificationDropdown.tsx | grep -qv 0</automated>
  </verify>
  <done>Header search shows grouped live results on every dashboard page with role-safe scoping (authors never see others' drafts; non-admins never see user results); bell shows a live unread count, lists notifications, marks them read on open; AppHeader identity chain untouched; both islands ride the existing QueryProvider.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → Server Component | searchParams (q/status/role/page/…) are user-tamperable input crossing into Drizzle queries |
| Client island → Server Action | header search q and notification queries arrive from arbitrary dashboard clients |
| Session → others' data | globalSearch/bell must scope by the server-side session, never client claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-se8-01 | Information disclosure | globalSearch posts leg | high | mitigate | Role-scoped query derived from the session: author → own posts only (authorId equality); drafts of other authors never returned (Task 8 behavior test) |
| T-Q-se8-02 | Information disclosure | globalSearch users leg | high | mitigate | Users leg executes only when session.user.role is admin (mirrors listUsers' admin-only gate); MUST_NOT_BE_REACHED test for the user select on non-admin sessions |
| T-Q-se8-03 | Tampering | URL searchParams → list actions | medium | mitigate | firstValue/bounded/clampPage coercion (string[] flattening, length caps, page ≤1000) + Zod list schemas with pageSize cap 100 + parameterized Drizzle templates (no concat) |
| T-Q-se8-04 | Elevation of privilege | extended list*/count* actions | medium | mitigate | Every extended action keeps requireCan/getSessionOrThrow as the literal FIRST statement; existing MUST_NOT_BE_REACHED orderings re-proven by the extended tests |
| T-Q-se8-05 | Tampering | returnForRevision workflow bypass | medium | mitigate | assertOwnsPost FIRST + transitionPost TRANSITIONS enforcement (pending_review→draft legal only for editor/admin on others' posts); row button is UX-only |
| T-Q-se8-06 | Spoofing / cross-user access | notifications read/mark actions | high | mitigate | Session-scoped filters only (session.user.id), NO userId parameter exists (T-Q-869-01 precedent) — a client can never read or mark another user's notifications |
| T-Q-se8-07 | Repudiation / DoS | notification fan-out vs parent mutations | medium | mitigate | notifyUsers: single awaited try/catch + log.error, swallowed after logging (never void fire-and-forget — dead-catch + unhandled-rejection rationale, users.ts L138-154); notifications are non-authoritative display data |
| T-Q-se8-08 | DoS | header search query volume | low | mitigate | 100-char bound server-side, 2-char minimum + 300ms debounce client-side, limit(5) per group; session required |
</threat_model>

<verification>
- pnpm test (full suite — prior baseline 676 green; every new/extended test file included)
- pnpm exec tsc --noEmit (project-wide type-check, the 260826-oif bar)
- pnpm build — NOTE: rm -rf .next first (warm-build stale 'use cache' output gotcha from project memory) and ensure no leftover next-start PID on Windows
- One drizzle-kit-generated migration under src/db/migrations covering exactly the notifications table (no other schema change anywhere in this task — media q/kind are action-level Zod additions, not columns)
- grep sanity: all four list pages still export `const instant = false`; no `connection()` added to any list page; no `activeProvider ===` conditional wrappers remain in StorageSettingsForm
- Owner UAT (manual, dev server — research "Manual-only" list): bell open marks read + count clears; ⌘K focuses header search; dropdown click-throughs land on the right pages; filter/page URLs survive refresh + back button
</verification>

<success_criteria>
- All four CONTEXT locked decisions delivered: grouped live global search dropdown; content-event notifications with DB table + bell; server-side URL-driven list mechanics on all four pages; all-4-providers storage form
- returnForRevision gap closed (action + editor/admin row button + author notification)
- Permission-first ordering proven by tests in every new/extended action; notify failures provably cannot fail parent mutations
- Full suite green, tsc clean, build clean, migration generated (never hand-written)
</success_criteria>

<output>
Create `.planning/quick/260827-se8-dashboard-functional-gaps-make-dashboard/260827-se8-SUMMARY.md` when done
</output>
